[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter api... build"
# PR preview DB workflow resets from production and deploys via `railway redeploy --from-source` when API or DB schema changes.
watchPatterns = [
  "apps/api/**",
  "packages/db/**",
  "packages/types/**",
  "packages/utils/**",
  "packages/validators/**",
  "packages/telemetry/**",
  "package.json",
  "pnpm-lock.yaml",
  "turbo.json",
  "drizzle.config.ts"
]

[deploy]
preDeployCommand = ["pnpm db:migrate"]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.multiRegionConfig]
[deploy.multiRegionConfig.us-east4-eqdc4a]
numReplicas = 1
