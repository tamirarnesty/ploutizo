[build]
builder = "RAILPACK"
buildCommand = "pnpm --filter api... build"
# PR preview DB workflow deploys via `railway redeploy --from-source` when watch paths skip API.
watchPatterns = [
  "apps/api/**",
  "packages/db/**",
  "packages/types/**",
  "packages/utils/**",
  "packages/validators/**",
  "package.json",
  "pnpm-lock.yaml",
  "railpack.json",
  "turbo.json",
  "drizzle.config.ts"
]

[deploy]
preDeployCommand = ["pnpm db:migrate"]
startCommand = "node apps/api/dist/index.js"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[deploy.multiRegionConfig]
[deploy.multiRegionConfig.us-east4-eqdc4a]
numReplicas = 1
