---
phase: 03.4.3-fix-shared-account-owners-not-saved-or-displayed
verified: 2026-04-27T10:45:00Z
status: human_needed
score: 4/5 must-haves verified
overrides_applied: 0
overrides:
  - must_have: "pnpm turbo typecheck passes with zero errors after all three changes"
    reason: "pnpm turbo typecheck exits non-zero due to pre-existing errors in apps/api (webhooks.ts, index.ts, errors.test.ts, transactions.test.ts, merchant-rules.test.ts, categories.ts, households.ts — confirmed by git diff: none of these files appear in the 4 phase commits). The 5 files modified by this phase (packages/types/src/index.ts, apps/api/src/lib/queries/accounts.ts, apps/api/src/services/accounts.ts, apps/api/src/__tests__/accounts.test.ts, apps/web/src/components/accounts/AccountsTable.tsx) introduce zero new typecheck errors. web package typechecks clean."
    accepted_by: "tarnesty"
    accepted_at: "2026-04-27T10:45:00Z"
human_verification:
  - test: "Open the Accounts page in a browser, create or open a shared account with two members assigned, then view the Accounts table"
    expected: "The Owners column shows both members' display names as a comma-separated string (e.g., 'Alice, Bob'). Personal accounts show '—' in the Owners column."
    why_human: "DOM rendering and real API data require a browser session. Programmatic checks confirm all three code layers are correct and wired, but the end-to-end render path (TanStack Query fetch -> Account.owners -> table cell) can only be confirmed by visual inspection against a real or seeded database."
---

# Phase 03.4.3: Fix Shared Account Owners Not Saved or Displayed — Verification Report

**Phase Goal:** Fix shared account co-owners not appearing in the Accounts table — API list path returns owners array, table renders display names.
**Verified:** 2026-04-27T10:45:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | GET /api/accounts returns owners: AccountOwner[] on every account row (empty array for personal accounts) | VERIFIED | `listAccounts` in `apps/api/src/services/accounts.ts` calls `listAccountMemberDetails` and maps `owners: byAccount.get(r.id) ?? []` onto every row. Two tests assert `owners: [{ id: 'mem_1', displayName: 'Alice' }]` for shared and `owners: []` for personal. |
| 2 | Shared accounts with co-owners show their display names (comma-joined) in the Accounts table Owners column | NEEDS HUMAN | Cell implementation confirmed (`row.original.owners.map((o) => o.displayName).join(', ')`) but browser rendering not verified. See Human Verification section. |
| 3 | Personal accounts render '—' in the Owners column | VERIFIED (partial — logic confirmed, browser unverified) | `{names \|\| '—'}` in cell — when `owners` is `[]`, `names` is `''`, fallback renders `'—'`. Test asserts `owners: []` for personal accounts. |
| 4 | pnpm --filter api test passes (all existing + 2 new owner tests) | VERIFIED | `73/73 passed` — 71 pre-existing + 2 new owner assertion tests confirmed green. |
| 5 | pnpm turbo typecheck passes with zero errors after all three changes | PASSED (override) | Override: pre-existing api errors in webhooks.ts, index.ts, errors.test.ts, transactions.test.ts, merchant-rules.test.ts, categories.ts, households.ts — none in the 5 files modified by this phase. git diff 4dd3249~1..9c0a3d2 --name-only confirms no overlap. web package typechecks clean. |

**Score:** 4/5 truths verified (5th pending human browser check)

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/types/src/index.ts` | AccountOwner interface + owners field on Account interface | VERIFIED | `AccountOwner { id, displayName }` at line 12; `owners: AccountOwner[]` at line 28 |
| `apps/api/src/lib/queries/accounts.ts` | listAccountMemberDetails export | VERIFIED | Exported at line 94; full implementation with inArray guard and innerJoin |
| `apps/api/src/services/accounts.ts` | listAccounts updated to call listAccountMemberDetails and merge owners | VERIFIED | Lines 17-29: two-query merge with Map<string, []> grouping |
| `apps/api/src/__tests__/accounts.test.ts` | tests asserting owners field in GET response | VERIFIED | Tests at lines 130-167 and 170-205 assert correct owners values |
| `apps/web/src/components/accounts/AccountsTable.tsx` | Owners column cell wired to row.original.owners | VERIFIED | `row.original.owners` at line 114; `{names \|\| '—'}` at line 117 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| apps/api/src/services/accounts.ts:listAccounts | apps/api/src/lib/queries/accounts.ts:listAccountMemberDetails | sequential call after listAccountsQuery | WIRED | `listAccountMemberDetails(rows.map((r) => r.id))` called at line 20 |
| apps/web/src/components/accounts/AccountsTable.tsx | Account.owners (packages/types) | row.original.owners TypeScript access | WIRED | `row.original.owners.map((o) => o.displayName)` at line 114; Account type imported from `@ploutizo/types` at line 19 |
| apps/api/src/routes/accounts.ts:GET / | apps/api/src/services/accounts.ts:listAccounts | service import | WIRED | `listAccounts` imported and called at route line 7/22 |
| apps/web/src/lib/data-access/accounts/useGetAccounts.ts | Account[] (packages/types) | Promise<Account[]> return type | WIRED | `apiFetch<{ data: Account[] }>` at line 8; type propagates owners field automatically |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| AccountsTable.tsx | row.original.owners | useGetAccounts → apiFetch → GET /api/accounts | Yes — db.select with innerJoin on accountMembers/orgMembers; Map merge in service | FLOWING |
| apps/api/src/services/accounts.ts:listAccounts | memberRows | listAccountMemberDetails(accountIds) | Yes — Drizzle innerJoin query with inArray guard | FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| API test suite passes all 73 tests | `pnpm --filter api test` | 73/73 passed | PASS |
| owners mock returns correct shape for shared account | accounts.test.ts line 167 | `[{ id: 'mem_1', displayName: 'Alice' }]` | PASS |
| owners mock returns [] for personal account | accounts.test.ts line 204 | `[]` | PASS |
| AccountsTable cell accessor reads owners | `grep "row.original.owners" AccountsTable.tsx` | Line 114 found | PASS |
| Typecheck (web package) | `pnpm turbo typecheck` (web subset) | 0 errors in modified files | PASS |

---

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|----------|
| §2-OWNERS-DISPLAY | 03.4.3-01-PLAN.md | Account ownership many-to-many via account_members — owners must be returned by API and rendered in table | SATISFIED | All three layers implemented: type (AccountOwner + Account.owners), API (listAccountMemberDetails + service merge), table (cell reads row.original.owners) |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| apps/web/src/components/accounts/AccountsTable.tsx | 116 | `truncate` on `<Text as="span">` (inline element) — `overflow: hidden; text-overflow: ellipsis` have no effect on `display: inline`. Long owner-name lists will overflow the cell. | Warning | Visual: names show correctly but won't truncate with ellipsis on overflow. Identified in REVIEW.md WR-01. Fix: add `block` class to activate truncation. |
| apps/api/src/lib/queries/accounts.ts | 93 | Comment references "Pitfall 4" — opaque outside planning context | Info | Documentation quality only; no functional impact |
| apps/api/src/__tests__/accounts.test.ts | 244 | Archive test `expect([200, 404]).toContain(res.status)` — permissive assertion provides no signal | Info | Test coverage quality; pre-existing pattern, not introduced by this phase |

---

### Human Verification Required

#### 1. Owners column renders in browser with real data

**Test:** Open the Accounts page. Create a shared account and assign two org members as co-owners. Save, then view the Accounts table.
**Expected:** The Owners column shows both members' display names comma-separated (e.g., "Alice, Bob"). For a personal account (no co-owners assigned), the Owners column shows "—".
**Why human:** The full render path (TanStack Query fetch to real API to actual database rows to table cell) requires a running dev environment and real or seeded data. Programmatic checks confirm all three code layers are correctly implemented and wired, but end-to-end browser validation is the only way to confirm the complete data flow.

---

### Gaps Summary

No automated gaps. All five artifacts exist, are substantive, are wired, and data flows through the pipeline. The single outstanding item is browser confirmation of the end-to-end render path (human verification item above). The typecheck failure is confirmed pre-existing and override-accepted — none of the 7 files with errors appear in this phase's change set.

Note for follow-up: the `truncate` class on the inline `<span>` (WR-01) is a CSS no-op. The feature works — names display — but truncation will not activate. A one-word fix (`block` added to className) can be applied in a polish pass.

---

_Verified: 2026-04-27T10:45:00Z_
_Verifier: Claude (gsd-verifier)_
