---
phase: 03.4.3
plan: 01
subsystem: accounts
tags: [bug-fix, api, types, ui, tdd]
dependency-graph:
  requires: []
  provides: [Account.owners type, listAccountMemberDetails query, listAccounts owner enrichment, AccountsTable owners cell]
  affects: [apps/api, packages/types, apps/web]
tech-stack:
  added: []
  patterns: [two-query merge with Map grouping, TDD red-green]
key-files:
  created: []
  modified:
    - packages/types/src/index.ts
    - apps/api/src/lib/queries/accounts.ts
    - apps/api/src/services/accounts.ts
    - apps/api/src/__tests__/accounts.test.ts
    - apps/web/src/components/accounts/AccountsTable.tsx
decisions:
  - Two-query merge (listAccountsQuery then listAccountMemberDetails) preferred over SQL join to keep Drizzle query functions simple
  - Map<string, []> for O(1) grouping per js-perf skill rule
  - Comma-joined displayNames in single Text span with min-w-0 truncate per web-design-guidelines
metrics:
  duration: ~8 minutes
  completed: 2026-04-27T14:35:00Z
  tasks-completed: 3
  files-modified: 5
---

# Phase 03.4.3 Plan 01: Fix Shared Account Owners Not Saved or Displayed Summary

**One-liner:** Two-query owner enrichment with Map grouping wires co-owner display names from account_members/org_members join into the Accounts table Owners column.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Extend Account type — add AccountOwner interface and owners field | 4dd3249 | packages/types/src/index.ts |
| 2 (RED) | Add failing tests for owners enrichment | a30cfd6 | apps/api/src/__tests__/accounts.test.ts |
| 2 (GREEN) | Enrich listAccounts with owner data | c8f0597 | apps/api/src/lib/queries/accounts.ts, apps/api/src/services/accounts.ts |
| 3 | Wire AccountsTable Owners cell to row.original.owners | 9c0a3d2 | apps/web/src/components/accounts/AccountsTable.tsx |

## What Was Built

Fixed the display path for shared account co-owners. The write path (form submit, API handlers, DB inserts into account_members) was verified correct and not modified.

Changes:
- `packages/types/src/index.ts`: Added `AccountOwner { id, displayName }` interface and `owners: AccountOwner[]` field on `Account`
- `apps/api/src/lib/queries/accounts.ts`: Added `listAccountMemberDetails(accountIds)` — batch-fetches member names via `accountMembers → orgMembers` innerJoin with empty-array guard
- `apps/api/src/services/accounts.ts`: Replaced single-query `listAccounts` with two-query merge using `Map<string, []>` for O(1) grouping
- `apps/api/src/__tests__/accounts.test.ts`: Added `orgMembers: {}` to schema mock; added 2 new owner assertion tests via `vi.mocked(db).select.mockReturnValueOnce`
- `apps/web/src/components/accounts/AccountsTable.tsx`: Replaced hardcoded `—` Owners cell with accessor that renders `owners.map(o => o.displayName).join(', ')` or `—`

## Verification

- `pnpm --filter api test`: 73/73 passing (71 existing + 2 new owner tests)
- `pnpm turbo typecheck` (web package): clean — 0 errors in modified files
- Phase gate checks: all grep assertions confirmed

## Deviations from Plan

### Auto-adjusted Issues

**1. [Rule 2 - Missing] orgMembers: {} needed in schema mock**
- **Found during:** Task 2 RED phase planning
- **Issue:** Schema mock had `{ accounts: {}, accountMembers: {} }` but `listAccountMemberDetails` references `orgMembers` fields — would throw TypeError at runtime in tests
- **Fix:** Added `orgMembers: {}` to `vi.mock('@ploutizo/db/schema', ...)` factory
- **Files modified:** apps/api/src/__tests__/accounts.test.ts
- **Commit:** a30cfd6

**2. [Rule 1 - Pattern] Used vi.mocked(db).select.mockReturnValueOnce per-test instead of factory rewrite**
- **Found during:** Task 2 RED phase — plan's proposed `const selectMock = vi.fn()` approach would hit vi.mock hoisting issues
- **Fix:** Used `vi.mocked(db).select.mockReturnValueOnce()` chain per test, imported `db` at top of test file
- **Files modified:** apps/api/src/__tests__/accounts.test.ts
- **Commit:** a30cfd6

### Pre-existing Typecheck Failures (Out of Scope)

`pnpm turbo typecheck` exits non-zero due to pre-existing errors in:
- `apps/api/src/__tests__/errors.test.ts` — StatusCode overload mismatch
- `apps/api/src/__tests__/transactions.test.ts` — unused imports, unknown type
- `apps/api/src/__tests__/merchant-rules.test.ts` — StatusCode overload mismatch
- `apps/api/src/services/webhooks.ts` — OrganizationJSON type issues
- `apps/api/src/lib/queries/categories.ts` — updatedAt field
- `apps/api/src/routes/households.ts` — overload mismatch

None are in files touched by this plan. The `web` package typechecks clean (zero errors). Logged to deferred-items for future attention.

## TDD Gate Compliance

- RED gate: `test(03.4.3-01): add failing tests...` at a30cfd6 — 2 new tests failed as expected
- GREEN gate: `feat(03.4.3-01): enrich listAccounts...` at c8f0597 — all 73 tests passed

## Known Stubs

None. `row.original.owners` is wired to real API data. The `—` fallback is correct behavior for personal accounts (empty owners array), not a stub.

## Threat Flags

No new trust boundaries introduced. `listAccountMemberDetails` only receives account IDs already scoped by `listAccountsQuery(orgId, ...)` — org isolation is preserved (T-03.4.3-01 mitigated as designed).

## Self-Check: PASSED

Files exist:
- packages/types/src/index.ts: FOUND
- apps/api/src/lib/queries/accounts.ts: FOUND
- apps/api/src/services/accounts.ts: FOUND
- apps/api/src/__tests__/accounts.test.ts: FOUND
- apps/web/src/components/accounts/AccountsTable.tsx: FOUND

Commits exist:
- 4dd3249: FOUND
- a30cfd6: FOUND
- c8f0597: FOUND
- 9c0a3d2: FOUND
