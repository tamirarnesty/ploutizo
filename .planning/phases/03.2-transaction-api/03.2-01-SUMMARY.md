---
phase: "03.2"
plan: "01"
subsystem: "api/transactions"
tags: ["testing", "vitest", "mocks", "scaffold"]

dependency_graph:
  requires:
    - "03.1 — transaction schema (transactions, transactionAssignees, transactionTags tables)"
    - "apps/api/src/__tests__/accounts.test.ts — mock pattern reference"
  provides:
    - "apps/api/src/__tests__/transactions.test.ts — test scaffold for all transaction route behaviors"
  affects: []

tech_stack:
  added: []
  patterns:
    - "Vitest it.todo stubs for Wave 0 Nyquist compliance"
    - "db.select mock chain supporting both leftJoin (base query) and innerJoin (sub-query) paths"
    - "mockTxRow exported constant for reuse in Wave 1/2 test expansion"

key_files:
  created:
    - path: "apps/api/src/__tests__/transactions.test.ts"
      role: "Vitest test scaffold — 12 it.todo stubs + full vi.mock setup"
  modified: []

decisions:
  - "Single flexible db.select mock chain using mockReturnThis() for leftJoin/innerJoin — avoids needing separate mock per call sequence; Wave 1 can override with mockReturnValueOnce when testing specific query responses"
  - "mockTxRow exported so Wave 1/Wave 2 plans can import it directly when expanding stubs into real test bodies"

metrics:
  duration_seconds: 84
  completed_date: "2026-04-10"
  tasks_completed: 1
  tasks_total: 1
  files_created: 1
  files_modified: 0
---

# Phase 03.2 Plan 01: Transaction Test Scaffold Summary

Vitest test scaffold for all 12 TXN-* transaction route behaviors, stubbed as `it.todo()` for Wave 1/Wave 2 implementation.

## Tasks Completed

| # | Task | Commit | Files |
|---|------|--------|-------|
| 1 | Write transaction test scaffold (12 TXN-* stubs) | e427a67 | apps/api/src/__tests__/transactions.test.ts |

## Decisions Made

- **Single flexible select mock chain:** `leftJoin` and `innerJoin` both call `mockReturnThis()` on the same chain object. This handles the base list query (leftJoin×2 → where → orderBy → limit → offset) and sub-queries (innerJoin → where → resolves). Wave 1 will use `mockReturnValueOnce` to differentiate responses when testing specific behaviors.

- **mockTxRow exported:** Defined at module scope and exported so Wave 1/Wave 2 plans can `import { mockTxRow }` when filling in stub bodies — avoids duplicating the constant across test files.

## Deviations from Plan

None — plan executed exactly as written.

Note: `tenantGuard.test.ts` has a pre-existing failure unrelated to this plan (`mockOnConflictDoNothing` not called). Confirmed present in main repo before this plan's changes — out of scope per deviation rules, logged to deferred-items.

## Known Stubs

All 12 `it.todo` stubs are intentional — this is a Wave 0 scaffold plan. They will be expanded in:
- Wave 1 (Plans 02 + 03): TXN-POST-01..05, TXN-GET-01..03
- Wave 2 (Plan 03): TXN-PATCH-01..02, TXN-DELETE-01..02

The stubs do not prevent the plan's goal (Nyquist compliance — test file exists before production code).

## Threat Flags

None — Wave 0 contains no production code or real credentials. Mocks are file-local constants.

## Self-Check: PASSED

- [x] `apps/api/src/__tests__/transactions.test.ts` exists
- [x] commit e427a67 exists in git log
- [x] `grep -c "it.todo"` returns 12
- [x] All three vi.mock calls present
- [x] mockTxRow defined and exported
