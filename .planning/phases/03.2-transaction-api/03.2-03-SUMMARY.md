---
phase: "03.2"
plan: "03"
subsystem: "api"
tags: ["transactions", "hono", "drizzle", "service-layer", "query-layer"]
dependency_graph:
  requires: ["03.2-02"]
  provides: ["complete-crud-transaction-api"]
  affects: ["03.3-transaction-list-ui"]
tech_stack:
  added: []
  patterns: ["three-layer route/service/query separation", "replace-all D-03", "soft-delete D-15", "atomic db.transaction with query-layer helpers"]
key_files:
  created: []
  modified:
    - "apps/api/src/services/transactions.ts"
    - "apps/api/src/routes/transactions.ts"
    - "apps/api/src/index.ts"
    - "apps/api/src/__tests__/transactions.test.ts"
decisions:
  - "DrizzleTransaction cast via `tx as unknown as DrizzleTransaction` — db.transaction() callback receives a Neon-typed tx; double cast avoids exposing Neon internals in service layer while remaining type-safe at the query layer boundary"
  - "Split sum validation (D-11) throws Error in service layer; route catches and returns badRequest() — keeps service free of HTTP concerns"
metrics:
  duration_minutes: 15
  completed_date: "2026-04-10"
  tasks_completed: 2
  tasks_total: 2
  files_modified: 4
---

# Phase 03.2 Plan 03: PATCH + DELETE transaction API Summary

Complete transaction CRUD by adding updateTransaction()/deleteTransaction() to service layer, PATCH/DELETE handlers to routes, and registering /api/transactions in index.ts — all 12 TXN-* tests green.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | updateTransaction() + deleteTransaction() in service layer | 08ecd4c | apps/api/src/services/transactions.ts |
| 2 | PATCH + DELETE routes + fill stubs + register router | b9397fe | apps/api/src/routes/transactions.ts, apps/api/src/index.ts, apps/api/src/__tests__/transactions.test.ts |

## What Was Built

**Task 1 — Service layer:**
- `updateTransaction(id, orgId, data)`: wraps scalar UPDATE + replace-all assignees/tags in `db.transaction()`. D-11 sum validation fires only when both `assignees` and `amount` present. All SQL delegated to query layer — zero drizzle-orm imports in services/transactions.ts.
- `deleteTransaction(id, orgId)`: thin delegate to `softDeleteTransactionQuery()`. Returns `{id}` or null.

**Task 2 — Routes + registration:**
- `PATCH /:id`: validates body with `updateTransactionSchema`, catches split sum Error from service, returns 404 on null (Pitfall 7 — no 403 leakage).
- `DELETE /:id`: path-param only, returns `{data: {id}}` or 404.
- Registered `transactionsRouter` at `/api/transactions` in `index.ts`; CORS → Clerk → tenantGuard middleware order unchanged.
- Filled 4 remaining `it.todo` stubs: TXN-PATCH-01, TXN-PATCH-02, TXN-DELETE-01, TXN-DELETE-02.

## Verification

```
pnpm --filter api test
Tests: 39 passed | 1 pre-existing failure (tenantGuard.test.ts — unrelated to this plan)
All 12 TXN-* tests pass (0 todos)
```

Pre-existing failure: `tenantGuard.test.ts > upserts the org row before calling next()` — confirmed failing before this plan's changes. Out of scope.

## Deviations from Plan

**1. [Rule 1 - Bug] DrizzleTransaction cast required in updateTransaction()**
- **Found during:** Task 1
- **Issue:** `db.transaction()` callback infers the tx type from Drizzle internals; direct assignment to `DrizzleTransaction` typed parameters causes a TS type mismatch at the call site.
- **Fix:** Used `tx as unknown as DrizzleTransaction` at the two call sites (`updateTransactionScalarsQuery`, `replaceAssignees`, `replaceTags`). The query layer still enforces the correct type contract.
- **Files modified:** apps/api/src/services/transactions.ts
- **Commit:** 08ecd4c

## Known Stubs

None — all stubs filled, all endpoints wired to service layer.

## Threat Flags

No new security surface introduced beyond what the plan's threat model covers (T-03.2-08 through T-03.2-15). All mitigations confirmed present:
- PATCH WHERE includes `isNull(deletedAt)` + orgId check in query layer (T-03.2-11, T-03.2-08)
- DELETE WHERE includes `isNull(deletedAt)` + orgId check in query layer (T-03.2-12, T-03.2-09)
- `type` field excluded from `updateTransactionSchema` (T-03.2-13)
- Replace-all inside `db.transaction()` — atomic rollback on error (T-03.2-14)

## Self-Check: PASSED

All key files exist. Both task commits verified in git log.
