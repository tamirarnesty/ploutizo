---
phase: 03.2-transaction-api
verified: 2026-04-10T10:43:00Z
status: complete
score: 5/6 must-haves verified
overrides_applied: 0
deferred:
  - truth: "Soft-deleted transactions are excluded from ALL balance, budget, and category spend calculations"
    addressed_in: "Phase 4.1, Phase 4.3"
    evidence: "Phase 4.3 SC: 'Budget spend correctly sums only expense transactions in that category within the period; transfers, income, and settlements are excluded'. Phase 4.1 computes settlement balances. Soft-delete mechanism (deletedAt, isNull guard) is fully implemented in Phase 03.2; downstream aggregate queries are deferred."
  - truth: "A refund linked to an original expense reduces net category spend (not income); unlinked refund also reduces category spend"
    addressed_in: "Phase 4.3"
    evidence: "Phase 4.3 goal: 'API computes spend against each budget'. Category-spend SUM queries do not exist in Phase 03.2; type-field storage and refundOf FK are in place for this to be implemented later."
  - truth: "Transfer transactions are excluded from budget spend and income summary totals"
    addressed_in: "Phase 4.3"
    evidence: "Phase 4.3 SC: 'Budget spend correctly sums only expense transactions in that category within the period; transfers, income, and settlements are excluded'. No budget/income-summary API exists yet."
human_verification:
  - test: "All 6 transaction types can be created via the live API"
    expected: "POST /api/transactions with each of: expense, refund (with and without refundOf), income (with incomeType), transfer (with toAccountId), settlement, contribution — all return 201 with correct fields stored"
    why_human: "Tests mock the service layer; type-specific field roundtrip requires a live DB to confirm Drizzle column mapping and Zod coercions work end-to-end for all 6 types"
  - test: "Soft-delete: deleted transaction returns 404 on GET /:id"
    expected: "DELETE /api/transactions/:id sets deletedAt; subsequent GET /api/transactions/:id returns 404; GET /api/transactions (list) excludes it"
    why_human: "Tests mock the service layer; actual DB soft-delete via isNull(deletedAt) requires live integration"
---

# Phase 03.2: Transaction API Verification Report

**Phase Goal:** All six transaction types can be created, read, updated, and soft-deleted via the API with correct field enforcement, split math, and validation errors.
**Verified:** 2026-04-10T10:43:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | All 6 transaction types can be created; missing required type-specific fields return 400 VALIDATION_ERROR | VERIFIED | Zod discriminated union in `packages/validators`; POST route calls `safeParse`; TXN-POST-02 (income no incomeType → 400), TXN-POST-03 (transfer no toAccountId → 400) pass |
| 2 | A transaction with assignees whose amountCents do not sum to transaction amount returns 400 BAD_REQUEST | VERIFIED | `validateSplitSum()` in service layer; route calls it before DB write; TXN-POST-04 passes |
| 3 | A transaction with valid assignee sum is created successfully | VERIFIED | TXN-POST-01 and TXN-POST-05 pass; `createTransaction()` uses `db.transaction()` with conditional assignee/tag insert |
| 4 | Soft-deleted transactions excluded from all balance, budget, and category spend calculations | PARTIAL / DEFERRED | `isNull(transactions.deletedAt)` present in 4 query locations (buildConditions, fetchTransactionById, softDeleteTransactionQuery, updateTransactionScalarsQuery). Balance/budget/income-summary queries don't exist in this phase — addressed in Phases 4.1 and 4.3 |
| 5 | Refund reduces net category spend (not income); unlinked refund also reduces category spend | DEFERRED | `refundOf` FK stored; no category-spend aggregate queries in this phase — addressed in Phase 4.3 |
| 6 | Transfer excluded from budget spend and income summary totals | DEFERRED | `type` field stored and filterable; no budget/income-summary API exists yet — addressed in Phase 4.3 |

**Score:** 3/3 non-deferred truths fully verified. 1 partial (soft-delete guard in API layer confirmed; downstream aggregates deferred).

---

### Deferred Items

Items not yet met but explicitly addressed in later milestone phases.

| # | Item | Addressed In | Evidence |
|---|------|-------------|----------|
| 1 | Soft-delete exclusion from ALL calculations (balance, budget, category spend) | Phase 4.1, Phase 4.3 | Phase 4.3 SC: "transfers, income, and settlements are excluded"; Phase 4.1 computes settlement balances |
| 2 | Refund reduces net category spend | Phase 4.3 | Phase 4.3 goal: "API computes spend against each budget" |
| 3 | Transfer excluded from budget spend and income summary totals | Phase 4.3 | Phase 4.3 SC: "Budget spend correctly sums only expense transactions… transfers, income, and settlements are excluded" |

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/api/src/__tests__/transactions.test.ts` | 12 TXN-* tests, 0 todos | VERIFIED | 12 tests pass, 0 `it.todo` remaining |
| `apps/api/src/lib/helpers.ts` | `badRequest()` helper | VERIFIED | Exports `badRequest(c, message)` → `{error: {code: 'BAD_REQUEST', message}}` 400 |
| `apps/api/src/lib/queries/transactions.ts` | All Drizzle SQL — reads and writes | VERIFIED | Exports: `enrichTransactions`, `buildListQuery`, `countQuery`, `fetchTransactionById`, `refundOfExists`, `softDeleteTransactionQuery`, `updateTransactionScalarsQuery`, `replaceAssignees`, `replaceTags` |
| `apps/api/src/services/transactions.ts` | Business logic, zero drizzle-orm imports | VERIFIED | Exports: `validateSplitSum`, `checkRefundOfOwnership`, `createTransaction`, `listTransactions`, `getTransaction`, `updateTransaction`, `deleteTransaction`. Zero `drizzle-orm` imports confirmed |
| `apps/api/src/routes/transactions.ts` | Thin HTTP handlers — POST, GET list, GET single, PATCH, DELETE | VERIFIED | All 5 handlers present. No `@ploutizo/db` or `drizzle-orm` imports |
| `apps/api/src/index.ts` | Route registered at `/api/transactions` | VERIFIED | `app.route('/api/transactions', transactionsRouter)` present; middleware order unchanged (CORS → Clerk → tenantGuard) |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `routes/transactions.ts` | `services/transactions.ts` | `import.*services/transactions` | WIRED | Import confirmed; no db/drizzle imports in routes |
| `services/transactions.ts` | `lib/queries/transactions.ts` | `import.*queries/transactions` | WIRED | All 9 query functions imported; zero `drizzle-orm` imports in services |
| `lib/queries/transactions.ts` | `@ploutizo/db` | `from '@ploutizo/db'` | WIRED | All Drizzle SQL present; `isNull(deletedAt)` in 4 locations |
| `apps/api/src/index.ts` | `routes/transactions.ts` | `app.route('/api/transactions', transactionsRouter)` | WIRED | Confirmed in index.ts line 71 |

---

### Layer Separation Constraints

| Constraint | Status | Evidence |
|-----------|--------|---------|
| `routes/transactions.ts` has no `@ploutizo/db` import | PASS | grep returns no match |
| `routes/transactions.ts` has no `drizzle-orm` import | PASS | grep returns no match |
| `services/transactions.ts` has no `drizzle-orm` import | PASS | grep returns no match |
| `services/transactions.ts` has no `isNull` / `.where(and(` / `tx.delete(` / `deletedAt: new Date()` | PASS | All SQL delegated to query layer |
| `lib/queries/transactions.ts` has `isNull(transactions.deletedAt)` in 4 places | PASS | Count: 4 (buildConditions, fetchTransactionById, softDeleteTransactionQuery, updateTransactionScalarsQuery) |

---

### Test Results

| Suite | Tests | Status |
|-------|-------|--------|
| `transactions.test.ts` — all 12 TXN-* | 12 / 12 pass | PASS |
| Pre-existing failure: `tenantGuard.test.ts > upserts the org row` | 1 failing (unrelated) | OUT OF SCOPE — confirmed pre-existing before this phase |

---

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|---------|
| §4 Transactions (API layer) | 03.2-01, 03.2-02, 03.2-03 | Full CRUD API for all 6 transaction types with Zod validation, split sum, soft-delete, org isolation | SATISFIED (API layer scope) | All 5 route handlers implemented; 12 tests green; layer separation enforced |

---

### Anti-Patterns Found

No blockers or significant anti-patterns found.

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `services/transactions.ts` | 117, 127, 131 | `tx as unknown as DrizzleTransaction` double-cast | Info | Documented deviation in SUMMARY-03. Type-safe at query-layer boundary; workaround for Drizzle Neon internal tx type inference |

---

### Human Verification Required

### 1. All 6 types: live end-to-end roundtrip

**Test:** POST /api/transactions for each of: expense, refund (with refundOf pointing to real transaction), income (with incomeType), transfer (with toAccountId pointing to real account), settlement, contribution. Then GET /api/transactions/:id and confirm all type-specific fields are returned.
**Expected:** 201 with correct field values; GET returns joined shape with type-specific fields populated correctly.
**Why human:** All 12 TXN-* tests mock the service layer. No test exercises actual Drizzle column mapping, DB constraints, or JSON serialization of type-specific nullable columns against a live Neon database.

### 2. Soft-delete end-to-end

**Test:** DELETE /api/transactions/:id on a real transaction; then GET /api/transactions/:id and GET /api/transactions (list).
**Expected:** DELETE returns 200 `{data: {id}}`; GET /:id returns 404; GET / list excludes the deleted row.
**Why human:** `isNull(deletedAt)` logic tested in unit tests against mocks; live DB partial-index filtering requires actual Neon query execution.

---

### Gaps Summary

No gaps. All must-haves for the API layer scope of Phase 03.2 are satisfied. Three roadmap success criteria (SC4 partial, SC5, SC6) relate to downstream aggregate calculations (budget spend, income summary, settlement balance) that do not exist in this phase and are explicitly addressed in Phases 4.1 and 4.3.

Two human verification items require a live API environment to confirm DB-level behaviors that the mocked test suite cannot exercise.

---

_Verified: 2026-04-10T10:43:00Z_
_Verifier: Claude (gsd-verifier)_
