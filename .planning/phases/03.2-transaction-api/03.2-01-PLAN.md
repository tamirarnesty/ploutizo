---
phase: 03.2
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/api/src/__tests__/transactions.test.ts
autonomous: true
requirements:
  - §4 Transactions (API layer)
must_haves:
  truths:
    - "Test file exists with stubs for all 12 TXN-* test cases"
    - "pnpm --filter api test runs to completion (all stubs todo/skipped)"
    - "Mock pattern matches accounts.test.ts exactly — db.select returns chained builder, db.transaction wraps a real fn call"
  artifacts:
    - path: "apps/api/src/__tests__/transactions.test.ts"
      provides: "Vitest test scaffold for all transaction route behaviors"
      min_lines: 80
  key_links:
    - from: "apps/api/src/__tests__/transactions.test.ts"
      to: "apps/api/src/routes/transactions.ts"
      via: "Hono app.route() wiring"
      pattern: "transactionsRouter"
---

<objective>
Create the Vitest test scaffold for Phase 03.2 transaction routes. All 12 test cases from VALIDATION.md are stubbed as `it.todo()` so the Wave 1 implementation can fill them in without restructuring.

Purpose: Nyquist rule compliance — automated verify exists before implementation starts.
Output: `apps/api/src/__tests__/transactions.test.ts` with mocks + 12 stubs.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03.2-transaction-api/03.2-CONTEXT.md
@.planning/phases/03.2-transaction-api/03.2-RESEARCH.md
@apps/api/src/__tests__/accounts.test.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write transaction test scaffold</name>
  <files>apps/api/src/__tests__/transactions.test.ts</files>

  <read_first>
    - apps/api/src/__tests__/accounts.test.ts — exact mock pattern to replicate (vi.mock('@ploutizo/db'), vi.mock('@hono/clerk-auth'), vi.mock('@ploutizo/db/schema'), db.transaction mock with real fn call)
    - apps/api/src/routes/accounts.ts — how accountsRouter is imported and wired in tests
    - .planning/phases/03.2-transaction-api/03.2-VALIDATION.md — full list of 12 TXN-* test IDs and their descriptions
    - .planning/phases/03.2-transaction-api/03.2-RESEARCH.md — Pitfall 6 (inArray empty guard), Pitfall 5 (tagIds parsing), mock complexity note for parallel db.select calls
  </read_first>

  <action>
Create `apps/api/src/__tests__/transactions.test.ts` following the exact mock structure from `accounts.test.ts`.

**Mocks to replicate:**

```typescript
vi.mock('@hono/clerk-auth', () => ({
  getAuth: vi.fn(() => ({ orgId: 'org_test123' })),
}))
```

```typescript
vi.mock('@ploutizo/db', () => ({
  db: {
    select: vi.fn().mockReturnValue({
      from: vi.fn().mockReturnValue({
        leftJoin: vi.fn().mockReturnValue({
          where: vi.fn().mockReturnValue({
            orderBy: vi.fn().mockReturnValue({
              limit: vi.fn().mockReturnValue({
                offset: vi.fn().mockResolvedValue([]),
              }),
            }),
          }),
        }),
        innerJoin: vi.fn().mockReturnValue({
          where: vi.fn().mockResolvedValue([]),
        }),
        where: vi.fn().mockReturnValue({
          limit: vi.fn().mockResolvedValue([]),
          orderBy: vi.fn().mockReturnValue({
            limit: vi.fn().mockReturnValue({
              offset: vi.fn().mockResolvedValue([]),
            }),
          }),
        }),
      }),
    }),
    insert: vi.fn().mockReturnValue({
      values: vi.fn().mockReturnValue({
        returning: vi.fn().mockResolvedValue([{
          id: 'txn_1', orgId: 'org_test123', type: 'expense',
          amount: 5000, date: '2026-01-15', accountId: 'acct_1',
          description: null, merchant: null, categoryId: null,
          refundOf: null, incomeType: null, incomeSource: null,
          toAccountId: null, settledAccountId: null, investmentType: null,
          importBatchId: null, recurringTemplateId: null,
          deletedAt: null, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(),
        }]),
      }),
    }),
    update: vi.fn().mockReturnValue({
      set: vi.fn().mockReturnValue({
        where: vi.fn().mockReturnValue({
          returning: vi.fn().mockResolvedValue([{
            id: 'txn_1', orgId: 'org_test123', type: 'expense',
            amount: 5000, date: '2026-01-15', accountId: 'acct_1',
            description: 'Updated', merchant: null, categoryId: null,
            refundOf: null, incomeType: null, incomeSource: null,
            toAccountId: null, settledAccountId: null, investmentType: null,
            importBatchId: null, recurringTemplateId: null,
            deletedAt: null, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(),
          }]),
        }),
      }),
    }),
    delete: vi.fn().mockReturnValue({
      where: vi.fn().mockResolvedValue(undefined),
    }),
    transaction: vi.fn(async (fn: (tx: Record<string, ReturnType<typeof vi.fn>>) => Promise<unknown>) => {
      const txMock = {
        insert: vi.fn().mockReturnValue({
          values: vi.fn().mockReturnValue({
            returning: vi.fn().mockResolvedValue([{
              id: 'txn_1', orgId: 'org_test123', type: 'expense',
              amount: 5000, date: '2026-01-15', accountId: 'acct_1',
              description: null, merchant: null, categoryId: null,
              refundOf: null, incomeType: null, incomeSource: null,
              toAccountId: null, settledAccountId: null, investmentType: null,
              importBatchId: null, recurringTemplateId: null,
              deletedAt: null, createdAt: new Date().toISOString(), updatedAt: new Date().toISOString(),
            }]),
          }),
        }),
        delete: vi.fn().mockReturnValue({ where: vi.fn().mockResolvedValue(undefined) }),
        update: vi.fn().mockReturnValue({
          set: vi.fn().mockReturnValue({
            where: vi.fn().mockReturnValue({
              returning: vi.fn().mockResolvedValue([]),
            }),
          }),
        }),
      }
      return fn(txMock)
    }),
  },
}))

vi.mock('@ploutizo/db/schema', () => ({
  transactions: {},
  transactionAssignees: {},
  transactionTags: {},
  categories: {},
  accounts: {},
  tags: {},
  orgMembers: {},
}))
```

**Test stubs — 12 cases, matching VALIDATION.md IDs exactly:**

```typescript
const app = new Hono()
// Note: transactionsRouter not yet created — stubs are placeholders
// app.route('/', transactionsRouter)  ← uncomment in Wave 1

describe('POST /api/transactions', () => {
  it.todo('TXN-POST-01: POST creates expense with valid payload → 201')
  it.todo('TXN-POST-02: POST income with missing incomeType → 400 VALIDATION_ERROR')
  it.todo('TXN-POST-03: POST transfer with missing toAccountId → 400 VALIDATION_ERROR')
  it.todo('TXN-POST-04: POST with assignees sum mismatch → 400 BAD_REQUEST')
  it.todo('TXN-POST-05: POST with assignees omitted → 201 no assignee rows')
})

describe('GET /api/transactions', () => {
  it.todo('TXN-GET-01: GET / returns {data, total, page, limit} envelope')
})

describe('GET /api/transactions/:id', () => {
  it.todo('TXN-GET-02: GET /:id returns joined transaction shape with assignees and tags')
  it.todo('TXN-GET-03: GET /:id on soft-deleted transaction → 404')
})

describe('PATCH /api/transactions/:id', () => {
  it.todo('TXN-PATCH-01: PATCH updates fields; assignees replace-all')
  it.todo('TXN-PATCH-02: PATCH wrong orgId or missing → 404')
})

describe('DELETE /api/transactions/:id', () => {
  it.todo('TXN-DELETE-01: DELETE /:id sets deletedAt → 200 with {id}')
  it.todo('TXN-DELETE-02: DELETE already-deleted → 404')
})
```

The `transactionsRouter` import line is commented out (file doesn't exist yet in Wave 0). The app setup uses a bare `new Hono()` without mounting a route — all tests are `it.todo()` so they are skipped, not failing. Wave 1 implementation plan will uncomment the route mount and fill in the test bodies.
  </action>

  <verify>
    <automated>pnpm --filter api test</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `apps/api/src/__tests__/transactions.test.ts`
    - `grep -c "it.todo" apps/api/src/__tests__/transactions.test.ts` returns 12
    - `grep "vi.mock('@ploutizo/db'" apps/api/src/__tests__/transactions.test.ts` matches
    - `grep "vi.mock('@hono/clerk-auth'" apps/api/src/__tests__/transactions.test.ts` matches
    - `grep "vi.mock('@ploutizo/db/schema'" apps/api/src/__tests__/transactions.test.ts` matches
    - `pnpm --filter api test` exits 0 (all stubs are skipped, not failed)
  </acceptance_criteria>

  <done>Test scaffold committed. pnpm --filter api test exits 0. 12 it.todo stubs present covering all TXN-* test IDs.</done>
</task>

</tasks>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Test mocks accidentally exposing real DB credentials | LOW | Mocks replace @ploutizo/db entirely — no real DB connection possible in test environment |
</threat_model>

<verification>
`pnpm --filter api test` exits 0 with 12 todo stubs reported as skipped.
</verification>

<success_criteria>
- `apps/api/src/__tests__/transactions.test.ts` exists
- All 12 TXN-* test stubs are present as `it.todo()`
- Mock pattern is identical to `accounts.test.ts` structure
- `pnpm --filter api test` exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-transaction-api/03.2-01-SUMMARY.md`
</output>
