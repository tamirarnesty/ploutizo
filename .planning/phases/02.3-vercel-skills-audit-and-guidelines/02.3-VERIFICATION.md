---
phase: 02.3-vercel-skills-audit-and-guidelines
verified: 2026-04-07T23:30:00Z
status: gaps_found
score: 8/18 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 11/13
  gaps_closed:
    - "CategoriesSettings: setLocalCategories called during render body (useRef guard restored by Plan 06)"
    - "CategoriesSettings: min-w-0 truncate missing from category name span (restored by Plan 06)"
  gaps_remaining:
    - "AccountForm waterfall re-introduced (Plan 06 commit bb38c03 reverted Plan 05/02 parallel-fetch fix)"
    - "AccountForm: autoComplete attributes missing (reverted by Plan 06)"
    - "AccountForm: && conditional renders not converted to ternary (reverted by Plan 06)"
    - "CategoriesSettings: motion-safe:animate-pulse missing (Plan 06 reverted Plan 05 fix)"
    - "CategoriesSettings: && conditionals at lines 216/269 not converted to ternary (Plan 06 reverted Plan 05)"
    - "CategoryForm: autoComplete='off' missing, no ternary, no autoFocus justification (reverted by Plan 06)"
    - "RuleForm: autoComplete='off' missing on both inputs, && conditional not converted (reverted by Plan 06)"
    - "HouseholdSettingsForm: autoComplete='off' missing, && conditionals not converted (reverted by Plan 06)"
    - "LucideIconPicker: no autoFocus justification comment, straight ellipsis placeholder (reverted by Plan 06)"
    - "queryClient.ts: client-swr-dedup comment missing (reverted by Plan 06)"
  regressions:
    - "Plan 06 commit bb38c03 (fix: restore useRef guard) was initialized from the wrong base and touched 22 files. The commit message says it only targets CategoriesSettings.tsx, but the diff shows it reverted all Plan 05 changes across AccountForm, CategoryForm, RuleForm, HouseholdSettingsForm, LucideIconPicker, queryClient.ts, and also deleted CLAUDE.md and SKILL.md. The cleanup commit c894583 restored the documentation files but did NOT restore the source code changes."
gaps:
  - truth: "AccountForm fires both useGetAccountMembers and useGetOrgMembers simultaneously on mount, with no sequential waterfall between them"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted the parallel-fetch fix. The current AccountForm outer component calls only useGetAccountMembers (line 63). useGetOrgMembers is called inside AccountFormInner (line 78), creating the sequential waterfall. The AccountFormInnerProps interface no longer includes orgMembers."
    artifacts:
      - path: "apps/web/src/components/accounts/AccountForm.tsx"
        issue: "Lines 62-74: AccountForm outer component calls only useGetAccountMembers. Line 78: useGetOrgMembers called inside AccountFormInner. Single isLoading early return blocks AccountFormInner (and therefore useGetOrgMembers) until useGetAccountMembers resolves."
    missing:
      - "Move useGetOrgMembers call into AccountForm outer component (alongside useGetAccountMembers)"
      - "Combine loading gate: if (membersLoading || orgLoading)"
      - "Pass orgMembers as prop to AccountFormInner"
      - "Remove useGetOrgMembers call from AccountFormInner"
      - "Add OrgMember type import from @ploutizo/types"

  - truth: "All text inputs in CategoryForm, RuleForm, HouseholdSettingsForm have autoComplete='off'"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted all autoComplete additions from Plan 05. None of the three form files contain autoComplete attributes."
    artifacts:
      - path: "apps/web/src/components/settings/CategoryForm.tsx"
        issue: "Line 74-81: Input id='category-name' has no autoComplete attribute"
      - path: "apps/web/src/components/settings/RuleForm.tsx"
        issue: "Line 138-148: Pattern Input has no autoComplete attribute. Line 164-168: renameTo Input has no autoComplete attribute."
      - path: "apps/web/src/components/settings/HouseholdSettingsForm.tsx"
        issue: "Line 49-58: Input id='settlement-threshold' has no autoComplete attribute"
    missing:
      - "CategoryForm: add autoComplete=\"off\" to Input id='category-name'"
      - "RuleForm: add autoComplete=\"off\" to pattern Input"
      - "RuleForm: add autoComplete=\"off\" to renameTo Input"
      - "HouseholdSettingsForm: add autoComplete=\"off\" to Input id='settlement-threshold'"

  - truth: "AccountForm name/institution/lastFour inputs have autocomplete attributes"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted autoComplete attributes added in Plan 03. Current AccountForm has no autoComplete on any inputs."
    artifacts:
      - path: "apps/web/src/components/accounts/AccountForm.tsx"
        issue: "Lines 151-158: account-name Input has no autoComplete. Lines 198-205: account-institution Input has no autoComplete. Lines 217-225: account-last-four Input has no autoComplete."
    missing:
      - "AccountForm: add autoComplete=\"off\" and name=\"account-name\" to account-name Input"
      - "AccountForm: add autoComplete=\"organization\" and name=\"account-institution\" to account-institution Input"
      - "AccountForm: add autoComplete=\"off\" and name=\"account-last-four\" to account-last-four Input"

  - truth: "Both skeleton loader divs in CategoriesSettings use motion-safe:animate-pulse"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted the motion-safe: prefix added by Plan 05. Both skeleton divs use bare animate-pulse without prefers-reduced-motion guard."
    artifacts:
      - path: "apps/web/src/components/settings/CategoriesSettings.tsx"
        issue: "Line 93: className='h-10 animate-pulse rounded bg-muted' — missing motion-safe: prefix. Line 232: className='h-8 animate-pulse rounded bg-muted' — missing motion-safe: prefix."
    missing:
      - "CategoriesSettings line 93: change 'animate-pulse' to 'motion-safe:animate-pulse'"
      - "CategoriesSettings line 232: change 'animate-pulse' to 'motion-safe:animate-pulse'"

  - truth: "No && conditional JSX renders remain in AccountForm, CategoryForm, RuleForm, CategoriesSettings, or HouseholdSettingsForm"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted the ternary conversions applied by Plan 05. Multiple && conditional JSX renders remain across all five files."
    artifacts:
      - path: "apps/web/src/components/accounts/AccountForm.tsx"
        issue: "Line 159: errors.length > 0 && FieldError. Line 327: isSubmitting && Spinner."
      - path: "apps/web/src/components/settings/CategoryForm.tsx"
        issue: "Line 82: errors.length > 0 && FieldError."
      - path: "apps/web/src/components/settings/RuleForm.tsx"
        issue: "Line 149: errors.length > 0 && FieldError."
      - path: "apps/web/src/components/settings/CategoriesSettings.tsx"
        issue: "Lines 216-223: tagInputValue && !tags.some(...) && ComboboxItem. Line 269: dialogCategory !== false && CategoryDialog."
      - path: "apps/web/src/components/settings/HouseholdSettingsForm.tsx"
        issue: "Line 60: errors.length > 0 && p. Line 69: isSubmitting && Spinner."
    missing:
      - "Convert all 9 remaining && conditional JSX renders to ternary (? : null) across 5 files"

  - truth: "autoFocus on LucideIconPicker search input removed or justified with role-only attribution"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted the autoFocus justification comment and the placeholder typography fix applied by Plan 03. autoFocus is present without a comment, and placeholder uses straight ellipsis '...' instead of Unicode '…'."
    artifacts:
      - path: "apps/web/src/components/categories/LucideIconPicker.tsx"
        issue: "Line 163: autoFocus without justification comment. Line 166: placeholder='Search icons...' uses straight ellipsis instead of Unicode ellipsis '…'."
    missing:
      - "Add justification comment to autoFocus: // Picker opened by user action — autoFocus on search is intentional and expected"
      - "Fix placeholder to use Unicode ellipsis: placeholder='Search icons…'"

  - truth: "QueryClient has staleTime configured with client-swr-dedup documented"
    status: failed
    reason: "Plan 06 commit bb38c03 reverted the client-swr-dedup comment added by Plan 02. queryClient.ts has staleTime: 1000 * 60 but the comment only says '// 1 minute' without the rule reference or explanation."
    artifacts:
      - path: "apps/web/src/lib/queryClient.ts"
        issue: "Line 16: staleTime: 1000 * 60, // 1 minute — missing client-swr-dedup rule reference and TanStack Query deduplication semantics comment"
    missing:
      - "Update staleTime comment to reference client-swr-dedup rule and explain TanStack Query deduplication"

  - truth: "All files audited against web-design-guidelines (fetched live from GitHub) with all violations fixed"
    status: partial
    reason: "Plan 06 commit bb38c03 reverted web-design-guidelines fixes from Plans 03 and 05. The CategoryForm autoFocus justification comment was lost, LucideIconPicker autoFocus comment was lost, AccountForm autocomplete attributes were lost, and all files still have some remaining violations noted in the individual gap items above."
    artifacts:
      - path: "apps/web/src/components/accounts/AccountForm.tsx"
        issue: "Missing autoComplete attributes on 3 inputs (reverted)"
      - path: "apps/web/src/components/settings/CategoryForm.tsx"
        issue: "Missing autoComplete='off' and autoFocus justification comment (reverted)"
      - path: "apps/web/src/components/categories/LucideIconPicker.tsx"
        issue: "Missing autoFocus justification comment, straight ellipsis placeholder (reverted)"
    missing:
      - "All items listed in individual gap entries above"

human_verification:
  - test: "Verify no visual regressions after all fixes"
    expected: "All pages load, forms open/close, no broken layouts, category list truncates long names properly, AccountSheet parallel queries confirmed in Network tab"
    why_human: "Cannot run a browser environment in this verification context"
  - test: "Verify parallel query timing in AccountForm after fixes applied"
    expected: "Network tab shows useGetAccountMembers and useGetOrgMembers start simultaneously when /accounts page loads and an account sheet is opened"
    why_human: "Requires browser DevTools inspection during live navigation"
---

# Phase 02.3: Vercel Skills Audit & Guidelines — Re-Verification Report (3rd pass)

**Phase Goal:** Prune Next.js/RSC rules from the vercel-react-best-practices skill, fix all SPA violations in apps/web (waterfall fetches, setState-during-render), audit and fix composition patterns and web design guideline violations, and write CLAUDE.md with explicit Vercel skill trigger conditions so future agents apply these skills automatically.
**Verified:** 2026-04-07T23:30:00Z
**Status:** gaps_found
**Re-verification:** Yes — after Plan 06 CategoriesSettings regression fix

## Gap Closure Summary (from previous verification)

| Gap (previous) | Closed? | Evidence |
|----------------|---------|----------|
| CategoriesSettings: setLocalCategories called during render body (useRef guard regressed) | YES | Line 1: `import { useState, useRef, useEffect }`. Lines 55-61: useRef + useEffect guard present. |
| CategoriesSettings: min-w-0 truncate missing from category name span (regressed) | YES | Line 126: `<span className="min-w-0 flex-1 truncate text-sm">` |

## New Regressions Introduced by Plan 06

Plan 06 commit `bb38c03` was scoped to fix only `CategoriesSettings.tsx` but its `--stat` output shows 22 files changed with 1,941 lines deleted. The commit reverted all Plan 05 source code changes, including the parallel-fetch fix, autoComplete additions, ternary conversions, and autoFocus justification. The cleanup commit `c894583` restored documentation and skill files but did NOT restore the source code changes.

| Regression | Root Cause | Impact |
|-----------|-----------|--------|
| AccountForm waterfall re-introduced | bb38c03 reverted Plan 05 AccountForm changes | async-parallel violation active again |
| AccountForm autoComplete attributes lost | bb38c03 reverted Plan 03/05 changes | web-design-guidelines violation |
| AccountForm && conditionals not converted | bb38c03 reverted Plan 05 changes | rendering-conditional-render violation |
| CategoryForm autoComplete="off" lost | bb38c03 reverted Plan 05 changes | web-design-guidelines violation |
| CategoryForm ternary not applied | bb38c03 reverted Plan 05 changes | rendering-conditional-render violation |
| CategoryForm autoFocus justification lost | bb38c03 reverted Plan 03 changes | web-design-guidelines violation |
| RuleForm autoComplete="off" lost | bb38c03 reverted Plan 05 changes | web-design-guidelines violation |
| RuleForm ternary not applied | bb38c03 reverted Plan 05 changes | rendering-conditional-render violation |
| HouseholdSettingsForm autoComplete="off" lost | bb38c03 reverted Plan 05 changes | web-design-guidelines violation |
| HouseholdSettingsForm && conditionals not converted | bb38c03 reverted Plan 05 changes | rendering-conditional-render violation |
| CategoriesSettings motion-safe:animate-pulse lost | bb38c03 reverted Plan 05 changes | web-design-guidelines motion violation |
| CategoriesSettings && conditionals not converted | bb38c03 reverted Plan 05 changes | rendering-conditional-render violation |
| LucideIconPicker autoFocus comment lost | bb38c03 reverted Plan 03 changes | web-design-guidelines violation |
| LucideIconPicker placeholder straight ellipsis | bb38c03 reverted Plan 03 changes | web-design-guidelines typography violation |
| queryClient.ts client-swr-dedup comment lost | bb38c03 reverted Plan 02 changes | Documentation-only regression |

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | SKILL.md no longer lists RSC rules as active entries | VERIFIED | 7 deleted rule files absent from disk; section 3 shows only server-cache-lru, server-hoist-static-io, server-no-shared-module-state |
| 2  | bundle-dynamic-imports rule uses React.lazy() + Suspense examples | VERIFIED | `.claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` uses React.lazy() + Suspense; "for this project" SPA note present |
| 3  | SKILL.md retains server-cache-lru, server-hoist-static-io, server-no-shared-module-state with SPA notes | VERIFIED | All 3 in section 3 with explicit SPA context note naming deleted RSC rules |
| 4  | SKILL.md description and When to Apply updated to remove Next.js/RSC language | VERIFIED | Description: "React SPA performance optimization guidelines...adapted for TanStack Start/Router + React SPA" |
| 5  | The seven deleted RSC rule files no longer exist on disk | VERIFIED | `ls .claude/skills/vercel-react-best-practices/rules/` returns no RSC files |
| 6  | AccountForm fires both queries simultaneously — no sequential waterfall | FAILED | AccountForm outer component (line 62-74) calls only useGetAccountMembers. useGetOrgMembers is called inside AccountFormInner (line 78). Sequential waterfall active. |
| 7  | CategoriesSettings no longer calls setLocalCategories during the render body | VERIFIED | Line 1: useRef and useEffect imported. Lines 54-61: initialized ref + useEffect guard present. No in-render setState. |
| 8  | QueryClient has staleTime configured with client-swr-dedup documented | FAILED | staleTime: 1000 * 60 present but only "// 1 minute" comment — client-swr-dedup rule reference and deduplication explanation absent (reverted by bb38c03) |
| 9  | No forwardRef usage in apps/web custom components | VERIFIED | `grep -r "forwardRef" apps/web/src/` returns no matches |
| 10 | autoFocus in LucideIconPicker justified with comment | FAILED | Line 163: autoFocus without justification comment (reverted by bb38c03). Line 166: placeholder="Search icons..." uses straight ellipsis. |
| 11 | autocomplete attributes present on AccountForm name/institution/lastFour inputs | FAILED | No autoComplete attributes on any AccountForm inputs (reverted by bb38c03) |
| 12 | All text inputs in CategoryForm, RuleForm, HouseholdSettingsForm have autoComplete='off' | FAILED | Zero autoComplete attributes in any of the three form files (reverted by bb38c03) |
| 13 | Both skeleton loader divs in CategoriesSettings use motion-safe:animate-pulse | FAILED | Line 93: bare animate-pulse. Line 232: bare animate-pulse. motion-safe: prefix absent (reverted by bb38c03) |
| 14 | No && conditional JSX renders in AccountForm, CategoryForm, RuleForm, CategoriesSettings, HouseholdSettingsForm | FAILED | 9 occurrences across 5 files — all ternary conversions from Plan 05 were reverted by bb38c03 |
| 15 | All files audited against web-design-guidelines with all violations fixed | FAILED | Multiple violations listed in individual gap items |
| 16 | All files audited against vercel-composition-patterns (8 rules) with all violations fixed | VERIFIED | No composition pattern violations — confirmed clean in Plan 03 and no regressions in this area |
| 17 | CLAUDE.md exists with Vercel Skills section and 3 trigger conditions | VERIFIED | CLAUDE.md at project root; 3 Trigger: lines; all 8 composition rules; live URL; React 19 note; "NOT Next.js" stated |
| 18 | CategoriesSettings category name span has min-w-0 and truncate | VERIFIED | Line 126: `<span className="min-w-0 flex-1 truncate text-sm">` |

**Score:** 8/18 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `.claude/skills/vercel-react-best-practices/SKILL.md` | Pruned skill index — SPA-only rule set | VERIFIED | RSC rules absent; TanStack Start language present; SPA subset note in server-* section |
| `.claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` | Updated lazy-loading rule using React.lazy() | VERIFIED | React.lazy() + Suspense; SPA note present |
| `apps/web/src/components/accounts/AccountForm.tsx` | Waterfall-free — both queries fire in parallel | FAILED | Lines 62-74: only useGetAccountMembers in outer component. useGetOrgMembers in AccountFormInner (line 78). Sequential waterfall present. |
| `apps/web/src/components/accounts/AccountForm.tsx` | AccountForm with autocomplete attributes | FAILED | No autoComplete on name, institution, or lastFour inputs |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | CategoriesSettings without setState-during-render | VERIFIED | useRef + useEffect guard at lines 55-61 |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | motion-safe:animate-pulse on skeleton loaders | FAILED | Lines 93, 232: bare animate-pulse without motion-safe: |
| `apps/web/src/lib/queryClient.ts` | QueryClient with staleTime and client-swr-dedup comment | FAILED | staleTime present; comment missing rule reference |
| `apps/web/src/components/categories/LucideIconPicker.tsx` | Icon picker without unjustified autoFocus | FAILED | autoFocus present without justification comment; straight ellipsis in placeholder |
| `apps/web/src/components/settings/CategoryForm.tsx` | Category form with autoComplete and ternary conditionals | FAILED | No autoComplete; && conditional at line 82 not converted |
| `apps/web/src/components/settings/RuleForm.tsx` | Rule form with autoComplete on both text inputs | FAILED | No autoComplete on pattern or renameTo; && conditional at line 149 not converted |
| `apps/web/src/components/settings/HouseholdSettingsForm.tsx` | Household settings form with autoComplete | FAILED | No autoComplete; && conditionals at lines 60, 69 not converted |
| `CLAUDE.md` | Root project instructions with Vercel skills trigger conditions | VERIFIED | Contains "Vercel Skills — When to Apply" with 3 sections |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| SKILL.md Quick Reference | rules/*.md files on disk | Rule IDs in Quick Reference | WIRED | All referenced rules exist; RSC rules absent |
| AccountForm outer component | useGetAccountMembers + useGetOrgMembers | Both called at top of AccountForm | BROKEN | Only useGetAccountMembers called in outer component; useGetOrgMembers in AccountFormInner |
| CLAUDE.md vercel-react-best-practices section | .claude/skills/vercel-react-best-practices/SKILL.md | Read path reference | WIRED | Line 36: Read instruction present |
| CLAUDE.md web-design-guidelines section | Live fetch URL | Fetch URL in Action line | WIRED | WebFetch URL present |

### Data-Flow Trace (Level 4)

Not applicable — this phase modified skill documentation files, guidelines, and code quality fixes. No new data-rendering components were created.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| RSC rule files deleted | `ls .claude/skills/vercel-react-best-practices/rules/` | Only SPA-applicable rules present | PASS |
| React.lazy in bundle-dynamic-imports | `grep "React.lazy" bundle-dynamic-imports.md` | Match found | PASS |
| AccountForm parallel queries | `grep -n "useGetOrgMembers" AccountForm.tsx` | Line 78 — inside AccountFormInner, not outer AccountForm | FAIL |
| CategoriesSettings ref guard | `grep "initialized.current" CategoriesSettings.tsx` | 2 matches (declaration + condition) | PASS |
| CategoriesSettings min-w-0 span | `grep "min-w-0 flex-1 truncate" CategoriesSettings.tsx` | Line 126 match | PASS |
| motion-safe guard | `grep "motion-safe:animate-pulse" CategoriesSettings.tsx` | No matches | FAIL |
| && conditionals (all 5 files) | `grep "errors.length > 0 &&\|isSubmitting &&" (5 files)` | Matches in AccountForm, CategoryForm, RuleForm, HouseholdSettingsForm | FAIL |
| forwardRef absent | `grep -r "forwardRef" apps/web/src/` | No matches | PASS |
| CLAUDE.md trigger count | `grep -c "Trigger:" CLAUDE.md` | 3 | PASS |
| autoComplete in form files | `grep "autoComplete" CategoryForm/RuleForm/HouseholdSettingsForm` | No matches | FAIL |
| client-swr-dedup comment | `grep "client-swr-dedup" queryClient.ts` | No matches | FAIL |

### Requirements Coverage

No formal requirements (all tasks derive from CONTEXT.md decisions D-01 through D-06).

| Decision | Status | Evidence |
|----------|--------|----------|
| D-01: Delete RSC-only rules (not document as N/A) | SATISFIED | 7 rule files physically deleted |
| D-02: SPA-applicable rule categories prioritized | SATISFIED | SKILL.md and CLAUDE.md list only SPA-applicable categories |
| D-03: Audit + fix ALL violations — no deferral | FAILED | Parallel-fetch waterfall re-introduced; autoComplete, motion-safe, && ternary violations unresolved after Plan 06 regressions |
| D-04: No scope ceiling on refactor size | SATISFIED | Full AccountForm refactor was completed (and needs to be re-applied) |
| D-05: Guidelines in root CLAUDE.md | SATISFIED | CLAUDE.md exists with Vercel Skills section |
| D-06: Explicit trigger conditions as imperative sentences | SATISFIED | 3 Trigger: lines, each actionable |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `apps/web/src/components/accounts/AccountForm.tsx` | 62-74 | `useGetOrgMembers` called inside AccountFormInner, not the outer AccountForm | Blocker | Sequential waterfall — useGetOrgMembers cannot fire until useGetAccountMembers resolves |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | 93, 232 | `animate-pulse` without `motion-safe:` prefix | Warning | Animations play for users with prefers-reduced-motion enabled — violates web-design-guidelines motion rule |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | 216, 269 | `&& (` conditional JSX renders | Warning | Potential numeric 0 render; violates rendering-conditional-render rule |
| `apps/web/src/components/accounts/AccountForm.tsx` | 159, 327 | `&& (` and `&&` conditional JSX renders | Warning | Violates rendering-conditional-render rule |
| `apps/web/src/components/settings/CategoryForm.tsx` | 82 | `&& (` conditional JSX render | Warning | Violates rendering-conditional-render rule |
| `apps/web/src/components/settings/RuleForm.tsx` | 149 | `&& (` conditional JSX render | Warning | Violates rendering-conditional-render rule |
| `apps/web/src/components/settings/HouseholdSettingsForm.tsx` | 60, 69 | `&& (` and `&&` conditional JSX renders | Warning | Violates rendering-conditional-render rule |
| `apps/web/src/components/categories/LucideIconPicker.tsx` | 163, 166 | `autoFocus` without justification comment; `"Search icons..."` straight ellipsis | Warning | web-design-guidelines autoFocus and typography violations |
| `apps/web/src/lib/queryClient.ts` | 16 | staleTime without client-swr-dedup rule reference | Info | Missing documentation only — staleTime is functional |

### Human Verification Required

#### 1. Visual Regression Check

**Test:** Start the dev server (`pnpm dev`) and navigate to /, /dashboard, /accounts, /settings, /onboarding
**Expected:** All pages load without errors; forms open/submit; sheets slide in/out; category list truncates long names
**Why human:** Cannot run a browser environment in this verification context

#### 2. Parallel Query Timing Confirmation (after fixes applied)

**Test:** After the waterfall fix is re-applied, open browser DevTools Network tab, navigate to /accounts, open an account sheet
**Expected:** The account-members and org-members API requests show simultaneous start times
**Why human:** Network timing requires live browser observation

### Gaps Summary

Phase 02.3 successfully closed its 2 targeted gaps from the previous verification (CategoriesSettings useRef guard restored, min-w-0 truncate restored). However, Plan 06 commit `bb38c03` was executed against a wrong base and its diff shows 22 files modified with 1,941 lines deleted — far exceeding the 1-file, 3-edit scope described in the plan. The cleanup commit `c894583` restored planning/documentation files but did NOT restore source code changes.

**Root cause:** Plan 06 worktree initialization issue — same pattern as Plan 05. The worktree was initialized from the wrong base, causing the "fix" commit to overwrite all prior plan changes when the file was committed in its wrong-base state.

**Net effect:** All Plan 05 source-code fixes are lost. The codebase is at approximately Plan 03 state for the affected files, minus the autoFocus justification comments and LucideIconPicker placeholder typography fix that Plan 03 also applied.

**Files needing a targeted fix commit:**
1. `apps/web/src/components/accounts/AccountForm.tsx` — waterfall fix, autoComplete, ternary (Plans 02+03+05)
2. `apps/web/src/components/settings/CategoryForm.tsx` — autoComplete, ternary, autoFocus comment (Plans 03+05)
3. `apps/web/src/components/settings/RuleForm.tsx` — autoComplete, ternary (Plan 05)
4. `apps/web/src/components/settings/HouseholdSettingsForm.tsx` — autoComplete, ternary (Plan 05)
5. `apps/web/src/components/settings/CategoriesSettings.tsx` — motion-safe:, ternary (Plan 05)
6. `apps/web/src/components/categories/LucideIconPicker.tsx` — autoFocus comment, Unicode ellipsis (Plan 03)
7. `apps/web/src/lib/queryClient.ts` — client-swr-dedup comment (Plan 02)

---

_Verified: 2026-04-07T23:30:00Z_
_Verifier: Claude (gsd-verifier)_
