---
phase: 02.3-vercel-skills-audit-and-guidelines
verified: 2026-04-07T22:00:00Z
status: gaps_found
score: 11/13 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 10/13
  gaps_closed:
    - "autoComplete='off' missing on CategoryForm, RuleForm, HouseholdSettingsForm text inputs"
    - "animate-pulse without motion-safe: guard in CategoriesSettings skeleton loaders"
    - "&& conditional JSX renders across AccountForm, CategoryForm, RuleForm, CategoriesSettings, HouseholdSettingsForm"
  gaps_remaining:
    - "CategoriesSettings: setLocalCategories called during render body (useRef guard regressed)"
    - "CategoriesSettings: min-w-0 truncate missing from category name span (regressed)"
  regressions:
    - "CategoriesSettings.tsx: useRef + useEffect guard lost — Plan 05 was initialized from wrong base (bb5bdb1 main branch instead of 3fca26a phase branch). The merge commit a39488e had already reverted the Plan 02 fix; Plan 05 then stamped the in-render pattern as its 'after' state."
    - "CategoriesSettings.tsx: min-w-0 truncate removed from category name span by Plan 05 commit 9729f8c (same base mismatch issue)"
gaps:
  - truth: "CategoriesSettings no longer calls setLocalCategories during the render body — no double-render cycle"
    status: failed
    reason: "Plan 02 fix (useRef + useEffect guard) was reverted by the Plan 03 merge commit (a39488e) which merged from a worktree that did not include the Plan 02 change. Plan 05 was then initialized from the wrong base (bb5bdb1 main branch) and stamped the in-render pattern as its 'after' state, making the regression permanent."
    artifacts:
      - path: "apps/web/src/components/settings/CategoriesSettings.tsx"
        issue: "Lines 54-57: setLocalCategories(categories) called during render body. No useRef guard or useEffect present. The import shows only `import { useState } from 'react'` — useRef and useEffect are absent."
    missing:
      - "Restore useRef + useEffect guard in CategoriesSettings.tsx: add useRef and useEffect to the React import, replace the in-render conditional with useRef-guarded useEffect pattern"
  - truth: "All files audited against web-design-guidelines (fetched live from GitHub) with all violations fixed"
    status: partial
    reason: "Three of the four web-design-guidelines violations from the previous verification were fixed (autoComplete on 3 forms, motion-safe guard). However the min-w-0 truncate fix on the category name span was introduced in Plan 03 and then reverted by Plan 05 commit 9729f8c (same base mismatch)."
    artifacts:
      - path: "apps/web/src/components/settings/CategoriesSettings.tsx"
        issue: "Line 122: `<span className=\"flex-1 text-sm\">{cat.name}</span>` — missing min-w-0 and truncate. Plan 03 had fixed this to `<span className=\"min-w-0 flex-1 truncate text-sm\">` but Plan 05's wrong-base commit reverted it."
    missing:
      - "Restore min-w-0 truncate on category name span in CategoriesSettings.tsx line 122: change `className=\"flex-1 text-sm\"` to `className=\"min-w-0 flex-1 truncate text-sm\"`"
human_verification:
  - test: "Verify no visual regressions after all fixes"
    expected: "All pages load, forms open/close, no broken layouts, category list truncates long names properly"
    why_human: "Cannot run a browser environment in this verification context"
  - test: "Verify parallel query timing in AccountForm"
    expected: "Network tab shows useGetAccountMembers and useGetOrgMembers start simultaneously when /accounts page loads and an account sheet is opened"
    why_human: "Requires browser DevTools inspection during live navigation"
---

# Phase 02.3: Vercel Skills Audit & Guidelines — Re-Verification Report

**Phase Goal:** Prune Next.js/RSC rules from the vercel-react-best-practices skill, fix all SPA violations in apps/web (waterfall fetches, setState-during-render), audit and fix composition patterns and web design guideline violations, and write CLAUDE.md with explicit Vercel skill trigger conditions so future agents apply these skills automatically.
**Verified:** 2026-04-07T22:00:00Z
**Status:** gaps_found
**Re-verification:** Yes — after Plan 05 gap closure

## Gap Closure Summary

| Gap (previous) | Closed? | Evidence |
|----------------|---------|----------|
| autoComplete missing on CategoryForm, RuleForm, HouseholdSettingsForm | YES | `grep "autoComplete" CategoryForm.tsx` → line 76; RuleForm → 2 lines (139, 166); HouseholdSettingsForm → line 51 |
| animate-pulse without motion-safe: in CategoriesSettings | YES | `grep -c "motion-safe:animate-pulse" CategoriesSettings.tsx` → 2; no bare animate-pulse remains |
| && conditional JSX renders (9 occurrences, 5 files) | YES | `grep "errors.length > 0 &&\|isSubmitting &&\|cat.colour &&\|dialogCategory !== false &&"` → 0 matches across all 5 files |

## Regressions Introduced by Plan 05

Plan 05 SUMMARY.md noted: *"The worktree was initialized from `bb5bdb1` (main branch) rather than `3fca26a` (the correct phase branch base)."* This caused two Plan 02 and Plan 03 fixes to be overwritten.

| Regression | Cause | Impact |
|-----------|-------|--------|
| CategoriesSettings useRef guard lost | Plan 05 commit `9729f8c` applied to wrong base — the in-render `setLocalCategories` pattern was already present in that base (Plan 03 merge `a39488e` had overwritten Plan 02's fix) | Truth #7 from previous VERIFIED → now FAILED |
| CategoriesSettings min-w-0 truncate lost | Plan 05 commit `9729f8c` applied to wrong base — `min-w-0 truncate` from Plan 03 was absent in that base | Truth #12 partial (new sub-violation) |

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | SKILL.md no longer lists RSC rules as active entries | VERIFIED | 7 deleted rule files absent from disk; section 3 shows only server-cache-lru, server-hoist-static-io, server-no-shared-module-state |
| 2  | bundle-dynamic-imports rule uses React.lazy() + Suspense examples | VERIFIED | `grep "React.lazy" .../bundle-dynamic-imports.md` matches; no active next/dynamic usage |
| 3  | SKILL.md retains server-cache-lru, server-hoist-static-io, server-no-shared-module-state with SPA adaptation notes | VERIFIED | All 3 retained in section 3 with explicit SPA context note |
| 4  | SKILL.md description and When to Apply updated to remove Next.js/RSC language | VERIFIED | Description: "React SPA performance optimization guidelines...adapted for TanStack Start/Router" |
| 5  | The seven deleted RSC rule files no longer exist on disk | VERIFIED | `ls .claude/skills/vercel-react-best-practices/rules/` — RSC files absent |
| 6  | AccountForm fires both queries simultaneously with no sequential waterfall | VERIFIED | `useGetAccountMembers` (line 65–66) and `useGetOrgMembers` (line 67) both called in `AccountForm` outer component; combined gate `membersLoading \|\| orgLoading` on line 69 |
| 7  | CategoriesSettings no longer calls setLocalCategories during the render body | FAILED | Lines 54–57: `if (!catLoading && localCategories.length === 0 && categories.length > 0) { setLocalCategories(categories) }` — in-render call present. Import: `import { useState } from "react"` — useRef and useEffect absent. Regression introduced by Plan 05 base mismatch. |
| 8  | QueryClient has staleTime configured with client-swr-dedup documented | VERIFIED | `staleTime: 1000 * 60` with comment referencing `client-swr-dedup` rule |
| 9  | No forwardRef usage in apps/web custom components | VERIFIED | `grep -r "forwardRef" apps/web/src/` returns no matches |
| 10 | autoFocus in LucideIconPicker justified with comment | VERIFIED | Line 163: `autoFocus // Picker opened by user action — autoFocus on search is intentional and expected` |
| 11 | autocomplete attributes present on AccountForm name/institution/lastFour inputs | VERIFIED | `autoComplete="off"` on name (line 165), `autoComplete="organization"` on institution (line 214), `autoComplete="off"` on lastFour (line 235) |
| 12 | All files audited against web-design-guidelines with all violations fixed | PARTIAL | autoComplete and motion-safe gaps closed. BUT: CategoriesSettings category name span at line 122 missing `min-w-0 truncate` (regressed from Plan 05 base mismatch; Plan 03 had fixed this) |
| 13 | CLAUDE.md exists with Vercel Skills section and 3 trigger conditions | VERIFIED | CLAUDE.md at project root; `grep -c "Trigger:" CLAUDE.md` = 3; all 8 composition rules listed; live URL present; React 19 note present; "NOT Next.js" stated |

**Score:** 11/13 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `.claude/skills/vercel-react-best-practices/SKILL.md` | Pruned skill index — SPA-only rule set | VERIFIED | RSC rules absent; TanStack Start language present |
| `.claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` | Updated lazy-loading rule using React.lazy() | VERIFIED | Contains `React.lazy` + Suspense |
| `apps/web/src/components/accounts/AccountForm.tsx` | Waterfall-free — both queries fire in parallel | VERIFIED | Both hooks at lines 65–67 in outer component |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | CategoriesSettings without setState-during-render | FAILED | in-render `setLocalCategories` present (lines 54–57); useRef absent |
| `apps/web/src/lib/queryClient.ts` | QueryClient with staleTime documented | VERIFIED | staleTime 1000*60 with rule comment |
| `apps/web/src/components/categories/LucideIconPicker.tsx` | Icon picker without unjustified autoFocus | VERIFIED | autoFocus with justification comment |
| `apps/web/src/components/accounts/AccountForm.tsx` | AccountForm with autocomplete attributes | VERIFIED | 3 inputs have autoComplete |
| `apps/web/src/components/settings/CategoryForm.tsx` | Category form with autoComplete and ternary conditionals | VERIFIED | `autoComplete="off"` line 76; ternary at line 83 |
| `apps/web/src/components/settings/RuleForm.tsx` | Rule form with autoComplete on both text inputs | VERIFIED | 2 lines with `autoComplete="off"` (lines 139, 166) |
| `apps/web/src/components/settings/HouseholdSettingsForm.tsx` | Household settings form with autoComplete | VERIFIED | `autoComplete="off"` line 51; ternaries at lines 61, 70 |
| `CLAUDE.md` | Root project instructions with Vercel skills trigger conditions | VERIFIED | Contains "Vercel Skills — When to Apply" with 3 sections |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| SKILL.md Quick Reference | rules/*.md files on disk | Rule IDs in Quick Reference | WIRED | All referenced rules exist; RSC rules absent |
| AccountForm | useGetAccountMembers + useGetOrgMembers | Both called at top of AccountForm | WIRED | Lines 65–67 in outer component |
| CLAUDE.md vercel-react-best-practices section | .claude/skills/vercel-react-best-practices/SKILL.md | Read path reference | WIRED | Line 36: "Read .claude/skills/vercel-react-best-practices/SKILL.md before beginning" |
| CLAUDE.md web-design-guidelines section | Live fetch URL | Fetch URL in Action line | WIRED | Line 73: WebFetch URL present |

### Data-Flow Trace (Level 4)

Not applicable — this phase modified skill documentation files, guidelines, and code quality fixes. No new data-rendering components were created.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| RSC rule files deleted | `ls .claude/skills/vercel-react-best-practices/rules/` | Only SPA-applicable rules present | PASS |
| React.lazy in bundle-dynamic-imports | `grep "React.lazy" .../bundle-dynamic-imports.md` | Match found | PASS |
| AccountForm parallel queries | `grep -n "useGetOrgMembers\|useGetAccountMembers" AccountForm.tsx` | Both on lines 65–67 in outer component | PASS |
| CategoriesSettings ref guard | `grep "categoriesInitialized\|useRef\|useEffect" CategoriesSettings.tsx` | No matches — regression | FAIL |
| forwardRef absent | `grep -r "forwardRef" apps/web/src/` | No matches | PASS |
| CLAUDE.md trigger count | `grep -c "Trigger:" CLAUDE.md` | 3 | PASS |
| autoComplete closed (Plan 05 gaps) | `grep "autoComplete" CategoryForm.tsx RuleForm.tsx HouseholdSettingsForm.tsx` | Matches in all 3 files | PASS |
| motion-safe guard applied | `grep -c "motion-safe:animate-pulse" CategoriesSettings.tsx` | 2 | PASS |
| && conditionals eliminated | `grep "errors.length > 0 &&\|isSubmitting &&\|cat.colour &&\|dialogCategory !== false &&" (5 files)` | 0 matches | PASS |
| min-w-0 on category name span | `grep "min-w-0" CategoriesSettings.tsx` | No matches — regression | FAIL |

### Requirements Coverage

No formal requirements (all tasks derive from CONTEXT.md decisions D-01 through D-06).

| Decision | Status | Evidence |
|----------|--------|----------|
| D-01: Delete RSC-only rules (not document as N/A) | SATISFIED | 7 rule files physically deleted from disk |
| D-02: SPA-applicable rule categories prioritized | SATISFIED | SKILL.md and CLAUDE.md list only SPA-applicable categories |
| D-03: Audit + fix ALL violations — no deferral | PARTIAL | autoComplete, motion-safe, && ternary all fixed. Two regressions remain: CategoriesSettings setState-during-render re-introduced; min-w-0 truncate lost |
| D-04: No scope ceiling on refactor size | SATISFIED | Full AccountForm refactor completed |
| D-05: Guidelines in root CLAUDE.md | SATISFIED | CLAUDE.md exists with Vercel Skills section |
| D-06: Explicit trigger conditions as imperative sentences | SATISFIED | 3 Trigger: lines, each an actionable imperative sentence |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `apps/web/src/components/settings/CategoriesSettings.tsx` | 55–57 | `setLocalCategories(categories)` called during render body | Blocker | Double-render cycle on every component mount when data first loads; violates rerender-derived-state-no-effect rule |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | 122 | `<span className="flex-1 text-sm">` on category name — missing `min-w-0 truncate` | Warning | Long category names will overflow their flex container instead of truncating |

### Human Verification Required

#### 1. Visual Regression Check

**Test:** Start the dev server (`pnpm dev`) and navigate to /, /dashboard, /accounts, /settings, /onboarding
**Expected:** All pages load without errors; forms open/submit; sheets slide in/out; no layout breakage; long category names truncate in the settings list
**Why human:** Cannot run a browser environment in this verification context

#### 2. Parallel Query Timing Confirmation

**Test:** Open browser DevTools Network tab, navigate to /accounts, open an account sheet
**Expected:** The `account-members` and `org-members` API requests show simultaneous start times (not sequential)
**Why human:** Network timing requires live browser observation

### Gaps Summary

Phase 02.3 successfully closed all three gaps from the previous verification (autoComplete on 3 form components, motion-safe guard on skeleton loaders, && → ternary across 9 locations in 5 files). However, Plan 05 was executed from the wrong base branch (`bb5bdb1` main branch instead of `3fca26a` phase branch), causing two previously-fixed violations to be re-introduced:

**Regression 1 (CategoriesSettings setState-during-render):** The `useRef` + `useEffect` guard that Plan 02 introduced was overwritten by the Plan 03 merge commit (`a39488e`), which merged from a worktree that initialized before Plan 02 completed. Plan 05 then inherited this already-regressed state and baked the in-render pattern into its "after" state. The current file has `setLocalCategories(categories)` at lines 54–57 directly in the render body with no guard.

**Regression 2 (min-w-0 truncate on category name):** Plan 03 commit `235705e` applied `min-w-0 truncate` to the category name span. Plan 05 commit `9729f8c`, initialized from the wrong base, reverted this to plain `flex-1 text-sm`.

Both regressions are in `CategoriesSettings.tsx` and stem from the same root cause: Plan 05's worktree initialization error. A targeted fix to this single file would close both remaining gaps.

---

_Verified: 2026-04-07T22:00:00Z_
_Verifier: Claude (gsd-verifier)_
