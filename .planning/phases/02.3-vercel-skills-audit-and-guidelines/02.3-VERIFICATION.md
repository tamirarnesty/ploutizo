---
phase: 02.3-vercel-skills-audit-and-guidelines
verified: 2026-04-07T00:00:00Z
status: human_needed
score: 18/18 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 15/18
  gaps_closed:
    - "CategoriesSettings: both skeleton divs now use motion-safe:animate-pulse (lines 93, 232) — fixed by commit 807c49a"
    - "CategoriesSettings: all 3 && conditional JSX renders converted to ternary (lines 121, 216-219, 269) — fixed by commit 807c49a"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Verify no visual regressions after all fixes"
    expected: "All pages load, forms open/close, no broken layouts, category list truncates long names properly, AccountSheet parallel queries confirmed in Network tab"
    why_human: "Cannot run a browser environment in this verification context"
  - test: "Verify parallel query timing in AccountForm"
    expected: "Network tab shows useGetAccountMembers and useGetOrgMembers start simultaneously when /accounts page loads and an account sheet is opened"
    why_human: "Requires browser DevTools inspection during live navigation"
---

# Phase 02.3: Vercel Skills Audit & Guidelines — Final Re-Verification Report (5th pass)

**Phase Goal:** Prune Next.js/RSC rules from the vercel-react-best-practices skill, fix all SPA violations in apps/web (waterfall fetches, setState-during-render), audit and fix composition patterns and web design guideline violations, and write CLAUDE.md with explicit Vercel skill trigger conditions so future agents apply these skills automatically.
**Verified:** 2026-04-07T00:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after commit 807c49a fixing CategoriesSettings regressions

## Gap Closure Summary (from previous verification — 4th pass)

| Gap (previous) | Closed? | Evidence |
|----------------|---------|----------|
| CategoriesSettings: both skeleton divs using bare animate-pulse (lines 93, 232) | YES | Line 93: `className="h-10 motion-safe:animate-pulse rounded bg-muted"`. Line 232: `className="h-8 motion-safe:animate-pulse rounded bg-muted"`. Both confirmed in file. |
| CategoriesSettings: 3 && conditional JSX renders (lines 121, 216-219, 269) | YES | Line 121: `cat.colour ? (span...) : null`. Lines 216-219: `tagInputValue && !tags.some(...) ? (ComboboxItem...) : null`. Line 269: `dialogCategory !== false ? (CategoryDialog...) : null`. All confirmed. |

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|----------|
| 1  | SKILL.md no longer lists RSC rules as active entries | VERIFIED | 7 deleted rule files absent from disk; server-* section shows only server-cache-lru, server-hoist-static-io, server-no-shared-module-state |
| 2  | bundle-dynamic-imports rule uses React.lazy() + Suspense examples | VERIFIED | Rule file uses React.lazy() + Suspense; single `next/dynamic` reference is in a "don't use" disclaimer note |
| 3  | SKILL.md retains server-cache-lru, server-hoist-static-io, server-no-shared-module-state with SPA notes | VERIFIED | All 3 in section 3 with explicit SPA context note |
| 4  | SKILL.md description and When to Apply updated to remove Next.js/RSC language | VERIFIED | Description updated to "React SPA performance optimization guidelines...adapted for TanStack Start/Router + React SPA" |
| 5  | The seven deleted RSC rule files no longer exist on disk | VERIFIED | `ls .claude/skills/vercel-react-best-practices/rules/` contains no RSC-specific files |
| 6  | AccountForm fires both queries simultaneously — no sequential waterfall | VERIFIED | Outer AccountForm (line 63-85): useGetAccountMembers at line 66, useGetOrgMembers at line 67 in the same function body; combined loading gate `if (membersLoading \|\| orgLoading)` at line 69 |
| 7  | CategoriesSettings no longer calls setLocalCategories during the render body | VERIFIED | Line 1: useRef + useEffect imported. Lines 55-61: initialized ref + useEffect guard. No in-render setState. |
| 8  | QueryClient has staleTime configured with client-swr-dedup documented | VERIFIED | Lines 16-20: full client-swr-dedup comment with deduplication explanation. staleTime: 1000 * 60 (non-zero) |
| 9  | No forwardRef usage in apps/web custom components | VERIFIED | `grep -r "forwardRef" apps/web/src/` returns 0 matches |
| 10 | autoFocus in LucideIconPicker justified with comment | VERIFIED | Line 163: `autoFocus // Picker opened by user action — autoFocus on search is intentional and expected` |
| 11 | autocomplete attributes present on AccountForm name/institution/lastFour inputs | VERIFIED | Line 165: autoComplete="off" on account-name. Line 214: autoComplete="organization" on institution. Line 235: autoComplete="off" on last-four. |
| 12 | All text inputs in CategoryForm, RuleForm, HouseholdSettingsForm have autoComplete='off' | VERIFIED | CategoryForm line 76. RuleForm lines 139, 166. HouseholdSettingsForm line 51. All confirmed. |
| 13 | Both skeleton loader divs in CategoriesSettings use motion-safe:animate-pulse | VERIFIED | Line 93: `className="h-10 motion-safe:animate-pulse rounded bg-muted"`. Line 232: `className="h-8 motion-safe:animate-pulse rounded bg-muted"`. Both confirmed. |
| 14 | No && conditional JSX renders in AccountForm, CategoryForm, RuleForm, CategoriesSettings, HouseholdSettingsForm | VERIFIED | All 5 files clean. CategoriesSettings: line 121 ternary (cat.colour), lines 216-219 ternary (tagInputValue + tags.some), line 269 ternary (dialogCategory). grep returns 0 matches across all 5 files. |
| 15 | All files audited against web-design-guidelines with all violations fixed | VERIFIED | motion-safe guards present, ternary conditionals applied, autocomplete attributes added, autoFocus justified, Unicode ellipsis in LucideIconPicker placeholder |
| 16 | All files audited against vercel-composition-patterns (8 rules) with all violations fixed | VERIFIED | No forwardRef in apps/web; no boolean-prop violations; no render-prop violations |
| 17 | CLAUDE.md exists with Vercel Skills section and 3 trigger conditions | VERIFIED | CLAUDE.md at project root; `grep -c "Trigger:" CLAUDE.md` = 3; all 8 composition rules listed; live GitHub URL present; React 19 note present; "NOT Next.js" stated twice |
| 18 | CategoriesSettings category name span has min-w-0 and truncate | VERIFIED | Line 126: `<span className="min-w-0 flex-1 truncate text-sm">{cat.name}</span>` |

**Score:** 18/18 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|---------|--------|---------|
| `.claude/skills/vercel-react-best-practices/SKILL.md` | Pruned skill index — SPA-only rule set | VERIFIED | RSC rules absent; TanStack Start language present; SPA subset note in server-* section |
| `.claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` | Updated lazy-loading rule using React.lazy() | VERIFIED | React.lazy() + Suspense examples; SPA note present; `next/dynamic` only in a "don't use" disclaimer |
| `apps/web/src/components/accounts/AccountForm.tsx` | Waterfall-free — both queries fire in parallel | VERIFIED | Lines 66-67: both queries in outer AccountForm function body. Line 69: combined loading gate. |
| `apps/web/src/components/accounts/AccountForm.tsx` | AccountForm with autocomplete attributes | VERIFIED | Lines 165, 214, 235: autoComplete on all three text inputs |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | CategoriesSettings without setState-during-render | VERIFIED | useRef + useEffect guard at lines 55-61 |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | motion-safe:animate-pulse on skeleton loaders | VERIFIED | Line 93 and line 232: both use `motion-safe:animate-pulse` |
| `apps/web/src/lib/queryClient.ts` | QueryClient with staleTime and client-swr-dedup comment | VERIFIED | Lines 16-20: multi-line comment with client-swr-dedup rule reference. staleTime: 1000 * 60 |
| `apps/web/src/components/categories/LucideIconPicker.tsx` | Icon picker with justified autoFocus and Unicode placeholder | VERIFIED | Line 163: justification comment. Line 166: `placeholder="Search icons…"` (Unicode ellipsis) |
| `apps/web/src/components/settings/CategoryForm.tsx` | Category form with autoComplete and ternary conditionals | VERIFIED | Line 76: autoComplete="off". Line 83-85: ternary for FieldError. |
| `apps/web/src/components/settings/RuleForm.tsx` | Rule form with autoComplete on both text inputs | VERIFIED | Lines 139, 166: autoComplete="off". Lines 150-152: ternary for FieldError. |
| `apps/web/src/components/settings/HouseholdSettingsForm.tsx` | Household settings form with autoComplete | VERIFIED | Line 51: autoComplete="off". Lines 61-63: ternary for error. Line 70: ternary for Spinner. |
| `CLAUDE.md` | Root project instructions with Vercel skills trigger conditions | VERIFIED | Contains "Vercel Skills — When to Apply" with 3 sections, each with a Trigger: line |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| SKILL.md Quick Reference | rules/*.md files on disk | Rule IDs in Quick Reference | WIRED | All referenced rules exist; RSC rules absent from disk |
| AccountForm outer component | useGetAccountMembers + useGetOrgMembers | Both called at top of AccountForm | WIRED | Line 66: useGetAccountMembers. Line 67: useGetOrgMembers. Same function body. |
| CLAUDE.md vercel-react-best-practices section | .claude/skills/vercel-react-best-practices/SKILL.md | Read path reference in trigger section | WIRED | Read instruction present in Action line |
| CLAUDE.md web-design-guidelines section | Live fetch URL | Fetch URL in Action line | WIRED | `raw.githubusercontent.com/vercel-labs/web-interface-guidelines` present |

### Data-Flow Trace (Level 4)

Not applicable — this phase modified skill documentation files, guidelines, and code quality fixes (anti-pattern removal). No new data-rendering components were created.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| RSC rule files deleted | `ls .claude/skills/vercel-react-best-practices/rules/` (grep for RSC names) | No RSC files found | PASS |
| React.lazy in bundle-dynamic-imports | `grep "React.lazy" bundle-dynamic-imports.md` | Match found | PASS |
| next/dynamic only in disclaimer | `grep -n "next/dynamic" bundle-dynamic-imports.md` | Line 12: "The `next/dynamic` API is Next.js-only" (disclaimer, not usage) | PASS |
| AccountForm parallel queries | `grep -n "useGetOrgMembers" AccountForm.tsx` | Line 67 — inside outer AccountForm function body | PASS |
| AccountForm combined loading gate | `grep "membersLoading \|\| orgLoading" AccountForm.tsx` | Line 69 match | PASS |
| CategoriesSettings ref guard | `grep "initialized.current" CategoriesSettings.tsx` | Lines 57, 58 (2 matches) | PASS |
| motion-safe guard on both skeletons | `grep "motion-safe:animate-pulse" CategoriesSettings.tsx` | Lines 93 and 232 — 2 matches | PASS |
| No bare animate-pulse | `grep '"animate-pulse"' CategoriesSettings.tsx` | 0 matches | PASS |
| && conditionals absent | `grep "errors.length > 0 &&\|isSubmitting &&\|cat\.colour &&\|dialogCategory !== false &&" (all 5 files)` | 0 matches | PASS |
| min-w-0 span | `grep "min-w-0 flex-1 truncate text-sm" CategoriesSettings.tsx` | Line 126 match | PASS |
| forwardRef absent | `grep -r "forwardRef" apps/web/src/` | 0 matches | PASS |
| CLAUDE.md trigger count | `grep -c "Trigger:" CLAUDE.md` | 3 | PASS |
| autoComplete in form files | `grep "autoComplete" CategoryForm/RuleForm/HouseholdSettingsForm/AccountForm` | Matches in all 4 files | PASS |
| client-swr-dedup comment | `grep "client-swr-dedup" queryClient.ts` | Line 16 match | PASS |
| autoFocus justification in LucideIconPicker | `grep "autoFocus.*user action" LucideIconPicker.tsx` | Line 163 match | PASS |

### Requirements Coverage

No formal requirements (all tasks derive from CONTEXT.md decisions D-01 through D-06).

| Decision | Status | Evidence |
|----------|--------|----------|
| D-01: Delete RSC-only rules (not document as N/A) | SATISFIED | 7 rule files physically deleted from disk |
| D-02: SPA-applicable rule categories prioritized | SATISFIED | SKILL.md and CLAUDE.md list only SPA-applicable categories |
| D-03: Audit + fix ALL violations — no deferral | SATISFIED | All 18 truths verified. No remaining violations in any of the 5 audited form files or CategoriesSettings |
| D-04: No scope ceiling on refactor size | SATISFIED | Full AccountForm refactor completed and verified |
| D-05: Guidelines in root CLAUDE.md | SATISFIED | CLAUDE.md exists with Vercel Skills section |
| D-06: Explicit trigger conditions as imperative sentences | SATISFIED | 3 Trigger: lines, each actionable sentence |

### Anti-Patterns Found

No remaining anti-patterns. All previously identified issues have been resolved:
- motion-safe:animate-pulse applied to both skeleton divs in CategoriesSettings
- All && conditional JSX renders converted to ternary across all 5 files
- autoComplete attributes present on all form text inputs
- forwardRef absent from all custom components in apps/web

### Human Verification Required

#### 1. Visual Regression Check

**Test:** Start the dev server (`pnpm dev`) and navigate to /, /dashboard, /accounts, /settings, /onboarding
**Expected:** All pages load without errors; forms open/submit; sheets slide in/out; category list truncates long names properly
**Why human:** Cannot run a browser environment in this verification context

#### 2. Parallel Query Timing Confirmation

**Test:** Open browser DevTools Network tab, navigate to /accounts, open an account sheet
**Expected:** The account-members and org-members API requests show simultaneous start times (not sequential)
**Why human:** Network timing requires live browser observation

### Gaps Summary

All 18 must-have truths are now verified. Commit 807c49a closed the final 2 gaps from the 4th-pass verification:
- CategoriesSettings skeleton loaders now use `motion-safe:animate-pulse` on both divs (lines 93, 232)
- CategoriesSettings `&&` conditional JSX renders at lines 121, 216-219, and 269 converted to ternary

All automated checks pass. Phase goal is fully achieved at the automated verification level. The only remaining items are human-observable behaviors (visual regression check and parallel network timing confirmation) that cannot be verified programmatically.

---

_Verified: 2026-04-07T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
