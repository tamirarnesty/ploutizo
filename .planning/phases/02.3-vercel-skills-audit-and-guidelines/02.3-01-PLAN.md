---
phase: 02.3
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/skills/vercel-react-best-practices/SKILL.md
  - .claude/skills/vercel-react-best-practices/rules/server-auth-actions.md
  - .claude/skills/vercel-react-best-practices/rules/server-cache-react.md
  - .claude/skills/vercel-react-best-practices/rules/server-dedup-props.md
  - .claude/skills/vercel-react-best-practices/rules/server-serialization.md
  - .claude/skills/vercel-react-best-practices/rules/server-parallel-fetching.md
  - .claude/skills/vercel-react-best-practices/rules/server-parallel-nested-fetching.md
  - .claude/skills/vercel-react-best-practices/rules/server-after-nonblocking.md
  - .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "SKILL.md no longer lists any server-auth-actions, server-cache-react, server-dedup-props, server-serialization, server-parallel-fetching, server-parallel-nested-fetching, or server-after-nonblocking rule entries"
    - "bundle-dynamic-imports rule file uses React.lazy() + Suspense examples, not next/dynamic"
    - "SKILL.md retains server-cache-lru, server-hoist-static-io, server-no-shared-module-state with SPA adaptation notes"
    - "SKILL.md description and 'When to Apply' preamble updated to remove all Next.js/RSC language"
    - "The seven deleted rule files no longer exist on disk"
  artifacts:
    - path: ".claude/skills/vercel-react-best-practices/SKILL.md"
      provides: "Pruned skill index — SPA-only rule set"
      contains: "vercel-react-best-practices"
    - path: ".claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md"
      provides: "Updated lazy-loading rule using React.lazy()"
      contains: "React.lazy"
  key_links:
    - from: "SKILL.md Quick Reference"
      to: "rules/*.md files on disk"
      via: "rule IDs in Quick Reference section"
      pattern: "Every rule ID listed in SKILL.md must have a corresponding rules/ file"
---

<objective>
Prune all Next.js/RSC-specific rules from the local `vercel-react-best-practices` skill so future agents never apply server-component patterns to this TanStack Start/React SPA.

Purpose: Decision D-01 requires permanent deletion (not documentation) of RSC-only rules. Subsequent plans read the cleaned SKILL.md. If this plan runs with stale RSC rules, all audit work in Plans 02–03 risks false positives.
Output: Cleaned SKILL.md with 7 rule files deleted, `bundle-dynamic-imports.md` updated to use `React.lazy()`.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-CONTEXT.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete RSC-only rule files and update SKILL.md index</name>
  <read_first>
    - .claude/skills/vercel-react-best-practices/SKILL.md (full current index — must see what to remove before editing)
    - .claude/skills/vercel-react-best-practices/rules/server-auth-actions.md
    - .claude/skills/vercel-react-best-practices/rules/server-cache-react.md
    - .claude/skills/vercel-react-best-practices/rules/server-dedup-props.md
    - .claude/skills/vercel-react-best-practices/rules/server-serialization.md
    - .claude/skills/vercel-react-best-practices/rules/server-parallel-fetching.md
    - .claude/skills/vercel-react-best-practices/rules/server-parallel-nested-fetching.md
    - .claude/skills/vercel-react-best-practices/rules/server-after-nonblocking.md
  </read_first>

  <files>
    .claude/skills/vercel-react-best-practices/SKILL.md,
    .claude/skills/vercel-react-best-practices/rules/server-auth-actions.md (delete),
    .claude/skills/vercel-react-best-practices/rules/server-cache-react.md (delete),
    .claude/skills/vercel-react-best-practices/rules/server-dedup-props.md (delete),
    .claude/skills/vercel-react-best-practices/rules/server-serialization.md (delete),
    .claude/skills/vercel-react-best-practices/rules/server-parallel-fetching.md (delete),
    .claude/skills/vercel-react-best-practices/rules/server-parallel-nested-fetching.md (delete),
    .claude/skills/vercel-react-best-practices/rules/server-after-nonblocking.md (delete)
  </files>

  <action>
    Delete the following 7 rule files from disk — they are RSC/Next.js-only and produce false positives in a TanStack Start/React SPA (per D-01):
    ```
    rm .claude/skills/vercel-react-best-practices/rules/server-auth-actions.md
    rm .claude/skills/vercel-react-best-practices/rules/server-cache-react.md
    rm .claude/skills/vercel-react-best-practices/rules/server-dedup-props.md
    rm .claude/skills/vercel-react-best-practices/rules/server-serialization.md
    rm .claude/skills/vercel-react-best-practices/rules/server-parallel-fetching.md
    rm .claude/skills/vercel-react-best-practices/rules/server-parallel-nested-fetching.md
    rm .claude/skills/vercel-react-best-practices/rules/server-after-nonblocking.md
    ```

    Then update `SKILL.md`:

    1. Change the `description` frontmatter field from:
       `"React and Next.js performance optimization guidelines from Vercel Engineering. This skill should be used when writing, reviewing, or refactoring React/Next.js code..."`
       to:
       `"React SPA performance optimization guidelines from Vercel Engineering, adapted for TanStack Start/Router + React SPA. This skill should be used when writing, reviewing, or refactoring React components in apps/web. RSC and Next.js-specific rules have been removed."`

    2. In the `## When to Apply` section, remove the line "Writing new React components or Next.js pages" and replace with "Writing new React components in apps/web". Remove "Implementing data fetching (client or server-side)" and replace with "Implementing client-side data fetching with TanStack Query".

    3. In `## Rule Categories by Priority`, change row 3 from:
       `| 3 | Server-Side Performance | HIGH | \`server-\` |`
       to:
       `| 3 | Server-Side Performance (SPA subset) | MEDIUM | \`server-\` |`

    4. In `### 3. Server-Side Performance (HIGH)`, replace the entire contents with:
       ```
       ### 3. Server-Side Performance (SPA subset — MEDIUM)

       > **Note for this project:** This is a TanStack Start/Router React SPA. The following server-side rules apply to Hono API handlers in `apps/api`, and to module-level patterns in React components. RSC-only rules (server-auth-actions, server-cache-react, server-dedup-props, server-serialization, server-parallel-fetching, server-parallel-nested-fetching, server-after-nonblocking) have been permanently deleted.

       - `server-cache-lru` - Use LRU cache for cross-request caching in API handlers
       - `server-hoist-static-io` - Hoist static I/O (fonts, constants) to module level in any JS environment
       - `server-no-shared-module-state` - Avoid module-level mutable state shared across requests
       ```

    5. In `### 2. Bundle Size Optimization (CRITICAL)`, the `bundle-dynamic-imports` entry will be updated in Task 2. Leave it as-is in this task.
  </action>

  <verify>
    <automated>ls .claude/skills/vercel-react-best-practices/rules/ | grep -E "server-auth-actions|server-cache-react|server-dedup-props|server-serialization|server-parallel-fetching|server-parallel-nested-fetching|server-after-nonblocking" && echo "FAIL: files still exist" || echo "PASS: all RSC rule files deleted"</automated>
  </verify>

  <acceptance_criteria>
    - `ls .claude/skills/vercel-react-best-practices/rules/` contains none of: server-auth-actions.md, server-cache-react.md, server-dedup-props.md, server-serialization.md, server-parallel-fetching.md, server-parallel-nested-fetching.md, server-after-nonblocking.md
    - `grep -c "server-auth-actions\|server-cache-react\|server-dedup-props\|server-serialization\|server-parallel-fetching\|server-parallel-nested-fetching\|server-after-nonblocking" .claude/skills/vercel-react-best-practices/SKILL.md` returns 0
    - `grep "server-cache-lru" .claude/skills/vercel-react-best-practices/SKILL.md` matches (retained rule)
    - `grep "server-hoist-static-io" .claude/skills/vercel-react-best-practices/SKILL.md` matches (retained rule)
    - `grep "server-no-shared-module-state" .claude/skills/vercel-react-best-practices/SKILL.md` matches (retained rule)
    - `grep "TanStack Start" .claude/skills/vercel-react-best-practices/SKILL.md` matches (SPA language added)
    - `grep "RSC-only rules.*have been permanently deleted" .claude/skills/vercel-react-best-practices/SKILL.md` matches
  </acceptance_criteria>

  <done>Seven RSC-only rule files deleted from disk. SKILL.md updated to remove all seven entries from its Quick Reference, description updated to remove Next.js language, server-* section updated with SPA-only subset and explanatory note.</done>
</task>

<task type="auto">
  <name>Task 2: Update bundle-dynamic-imports.md to use React.lazy() instead of next/dynamic</name>
  <read_first>
    - .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md (must read current content before rewriting)
  </read_first>

  <files>
    .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md
  </files>

  <action>
    Rewrite `bundle-dynamic-imports.md` to replace all `next/dynamic` examples with `React.lazy()` + `Suspense` equivalents. The rule concept (lazy-load heavy components) applies to SPAs — only the Next.js-specific API needs replacing.

    The rewritten file should:

    1. Keep the rule ID `bundle-dynamic-imports` and same frontmatter structure
    2. Change the description to: "Use React.lazy() for heavy components that are not needed on initial load"
    3. Replace the "bad" example that uses `import dynamic from 'next/dynamic'` with:
       ```tsx
       // BAD: Loading HeavyChart on initial render increases bundle parsed on load
       import HeavyChart from './HeavyChart'

       export function Dashboard() {
         return <HeavyChart data={data} />
       }
       ```
    4. Replace the "good" example with:
       ```tsx
       // GOOD: React.lazy() defers parsing until component is actually rendered
       import { lazy, Suspense } from 'react'
       const HeavyChart = lazy(() => import('./HeavyChart'))

       export function Dashboard() {
         return (
           <Suspense fallback={<div>Loading chart...</div>}>
             <HeavyChart data={data} />
           </Suspense>
         )
       }
       ```
    5. Add a note: "For this project (TanStack Start/Vite SPA): use `React.lazy()` + `<Suspense>`. The `next/dynamic` API is Next.js-only."
    6. Also update the SKILL.md Quick Reference entry for `bundle-dynamic-imports` from `"Use next/dynamic for heavy components"` to `"Use React.lazy() + Suspense for heavy components"`.
  </action>

  <verify>
    <automated>grep "React.lazy" .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md && grep -c "next/dynamic" .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md | grep "^0$" && echo "PASS" || echo "FAIL: next/dynamic still referenced or React.lazy missing"</automated>
  </verify>

  <acceptance_criteria>
    - `grep "React.lazy" .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` matches
    - `grep "next/dynamic" .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` returns no lines (or only in a "this is Next.js-specific, don't use" comment)
    - `grep "React.lazy" .claude/skills/vercel-react-best-practices/SKILL.md` matches (Quick Reference entry updated)
    - `grep "Suspense" .claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` matches
  </acceptance_criteria>

  <done>bundle-dynamic-imports.md rewrites all next/dynamic examples to React.lazy() + Suspense. SKILL.md Quick Reference entry updated to match. The rule now applies cleanly to this Vite SPA without RSC artifacts.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `ls .claude/skills/vercel-react-best-practices/rules/ | wc -l` should return 54 (69 original − 7 deleted = 62 rules... wait, 69 rules listed but the count was higher; verify actual count vs. expected)
- `grep -c "^- \`" .claude/skills/vercel-react-best-practices/SKILL.md` — count of rule entries in Quick Reference should match actual rule files on disk
- No `next/dynamic` references remain in bundle-dynamic-imports.md
- Seven deleted filenames must not appear anywhere in SKILL.md
</verification>

<success_criteria>
- [ ] Seven RSC-only rule files deleted: server-auth-actions, server-cache-react, server-dedup-props, server-serialization, server-parallel-fetching, server-parallel-nested-fetching, server-after-nonblocking
- [ ] SKILL.md description no longer references Next.js or RSC
- [ ] SKILL.md server-* Quick Reference section lists only 3 retained rules
- [ ] bundle-dynamic-imports.md uses React.lazy() + Suspense examples (zero next/dynamic)
- [ ] `pnpm --filter web tsc --noEmit` exits 0 (skill changes don't affect app typecheck)
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-01-SUMMARY.md`
</output>
