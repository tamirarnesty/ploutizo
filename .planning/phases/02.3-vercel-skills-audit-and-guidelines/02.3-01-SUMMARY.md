---
phase: 02.3-vercel-skills-audit-and-guidelines
plan: "01"
subsystem: skills
tags: [react, tanstack-start, vite, react-lazy, skills, rsc]

requires: []
provides:
  - "Pruned vercel-react-best-practices skill with RSC-only rules removed"
  - "SKILL.md updated to reflect TanStack Start/React SPA context"
  - "bundle-dynamic-imports.md using React.lazy() + Suspense instead of next/dynamic"
affects:
  - "All future plans using vercel-react-best-practices skill"

tech-stack:
  added: []
  patterns:
    - "React.lazy() + Suspense for code-splitting in Vite SPA (replaces next/dynamic)"

key-files:
  created: []
  modified:
    - ".claude/skills/vercel-react-best-practices/SKILL.md"
    - ".claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md"

key-decisions:
  - "Permanently deleted 7 RSC-only skill rules (server-auth-actions, server-cache-react, server-dedup-props, server-serialization, server-parallel-fetching, server-parallel-nested-fetching, server-after-nonblocking) — they produce false positives in this TanStack Start/React SPA"
  - "React.lazy() + Suspense replaces next/dynamic in bundle-dynamic-imports rule for Vite SPA compatibility"

patterns-established:
  - "Skills are modified in .claude/skills/ (gitignored) and force-added to git for persistence"

requirements-completed: []

duration: 10min
completed: 2026-04-07
---

# Phase 02.3 Plan 01: Vercel Skills Audit — RSC Rule Pruning Summary

**Purged 7 RSC/Next.js-only rules from vercel-react-best-practices skill and rewrote bundle-dynamic-imports to use React.lazy() + Suspense, preventing false-positive RSC pattern suggestions in this TanStack Start/Vite SPA.**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-04-07T00:00:00Z
- **Completed:** 2026-04-07T00:10:00Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments

- Deleted 7 RSC-only rule files from `.claude/skills/vercel-react-best-practices/rules/`: server-auth-actions, server-cache-react, server-dedup-props, server-serialization, server-parallel-fetching, server-parallel-nested-fetching, server-after-nonblocking
- Updated SKILL.md: description now references TanStack Start/Router React SPA; "When to Apply" updated to TanStack Query; Rule Categories table row 3 downgraded from HIGH to MEDIUM; Server-Side Performance section rewritten as SPA subset with explanatory note listing deleted rules
- Rewrote bundle-dynamic-imports.md: all next/dynamic examples replaced with React.lazy() + Suspense, project-specific note added clarifying next/dynamic is Next.js-only; SKILL.md Quick Reference entry updated

## Task Commits

Each task was committed atomically:

1. **Task 1: Delete RSC-only rule files and update SKILL.md index** - `3e701b4` (feat)
2. **Task 2: Update bundle-dynamic-imports.md to use React.lazy()** - `4a20fbc` (feat)

## Files Created/Modified

- `.claude/skills/vercel-react-best-practices/SKILL.md` - Updated description, When to Apply, Rule Categories table, Server-Side Performance section
- `.claude/skills/vercel-react-best-practices/rules/bundle-dynamic-imports.md` - Rewritten to use React.lazy() + Suspense

## Decisions Made

- Permanently deleted the 7 RSC-only rule files rather than just commenting them out — deletion is the right signal that these patterns should never be applied in this codebase (per plan decision D-01)
- The "permanently deleted" note in SKILL.md explicitly names the deleted rules so future readers understand what was removed and why
- React.lazy() + Suspense is the correct code-splitting API for this Vite SPA; the single remaining `next/dynamic` reference in bundle-dynamic-imports.md is in an explicit "this is Next.js-only, don't use it" note which is acceptable per plan criteria

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- `.claude/skills/` is gitignored in both the main repo and the worktree `.gitignore`. Files were modified in the main repo's skill directory then copied to the worktree's `.claude/skills/` directory and committed with `git add --force` to override the gitignore rule. This is expected behavior for skill customization.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- vercel-react-best-practices skill is now SPA-clean. Plans 02.3-02 and 02.3-03 can proceed using this skill without RSC false positives.
- The 3 retained server-side rules (server-cache-lru, server-hoist-static-io, server-no-shared-module-state) apply to Hono API handlers in apps/api.

---
*Phase: 02.3-vercel-skills-audit-and-guidelines*
*Completed: 2026-04-07*
