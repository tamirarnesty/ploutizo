---
phase: 02.3
plan: "04"
type: execute
wave: 3
depends_on: ["02.3-01", "02.3-02", "02.3-03"]
files_modified:
  - CLAUDE.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "CLAUDE.md exists at the project root with a 'Vercel Skills — When to Apply' section"
    - "Each of the three skills has an explicit trigger condition written as an imperative sentence an agent can follow"
    - "vercel-react-best-practices section lists only SPA-applicable rule categories (no RSC/Next.js categories)"
    - "web-design-guidelines section includes the live fetch URL and output format instructions"
    - "vercel-composition-patterns section lists all 8 rules"
    - "React 19 note is present in the composition-patterns section"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Root project instructions with Vercel skills trigger conditions"
      contains: "Vercel Skills"
  key_links:
    - from: "CLAUDE.md vercel-react-best-practices section"
      to: ".claude/skills/vercel-react-best-practices/SKILL.md"
      via: "Read: path reference in trigger section"
      pattern: "Read:.*vercel-react-best-practices/SKILL.md"
    - from: "CLAUDE.md web-design-guidelines section"
      to: "https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md"
      via: "Fetch URL in Action line"
      pattern: "raw.githubusercontent.com/vercel-labs"
---

<objective>
Write the `CLAUDE.md` guidelines section with explicit trigger conditions for all three Vercel skills. Future GSD agents read CLAUDE.md automatically — these guidelines ensure skills are applied without being asked.

Purpose: D-05 requires guidelines in root CLAUDE.md. D-06 requires explicit trigger conditions (not descriptions). This is the final deliverable of Phase 02.3.
Output: CLAUDE.md created at project root with actionable Vercel skills section that any future agent can follow without additional context.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-CONTEXT.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-RESEARCH.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-01-SUMMARY.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-02-SUMMARY.md
@.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CLAUDE.md with Vercel skills trigger conditions</name>
  <read_first>
    - .claude/skills/vercel-react-best-practices/SKILL.md (read the PRUNED version — this plan runs after 02.3-01 which removed RSC rules; use the current state of the file to enumerate SPA-applicable rule categories)
    - .claude/skills/vercel-composition-patterns/SKILL.md (all 8 rules to enumerate)
    - .claude/skills/web-design-guidelines/SKILL.md (fetch URL to embed)
    - .planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-RESEARCH.md (CLAUDE.md format in Code Examples section — lines 347-368)
  </read_first>

  <files>CLAUDE.md</files>

  <action>
    CLAUDE.md does not currently exist. Create it at the project root with the following content.

    The file must contain exactly the structure below. Use the pruned SKILL.md (after Plan 01 runs) to enumerate the SPA-applicable rule categories under vercel-react-best-practices.

    Write CLAUDE.md with this exact content (adjust rule category names to match the actual pruned SKILL.md you read):

    ```markdown
    # Project Instructions

    ## Stack

    - **Framework:** TanStack Start + TanStack Router (React SPA — NOT Next.js, NOT RSC)
    - **UI:** shadcn/ui + ReUI components from packages/ui
    - **Data fetching:** TanStack Query (all server state)
    - **Forms:** TanStack Form + Zod (useAppForm composition hook from packages/ui)
    - **Styling:** Tailwind CSS v4
    - **Database:** Neon serverless (@neondatabase/serverless WebSocket Pool) + Drizzle ORM
    - **Auth:** Clerk (org-based tenancy)
    - **API:** Hono (apps/api)

    ## Critical Constraints

    - This project is a **React SPA** using TanStack Start/Router. It is NOT Next.js. RSC patterns, server actions, `React.cache()`, `next/dynamic`, and `"use server"` do not apply.
    - All data fetching in `apps/web` uses TanStack Query hooks from `apps/web/src/lib/data-access/`. Never add raw `fetch()` calls to components.
    - Form state always uses `useAppForm` from `@ploutizo/ui/components/form` (TanStack Form + Zod). Never use `useState` for form field values.
    - `packages/db` uses `@neondatabase/serverless` WebSocket Pool (not postgres.js). Set `neonConfig.webSocketConstructor` before constructing the Pool.
    - All API requests from `apps/web` go through `apiFetch` in `apps/web/src/lib/queryClient.ts` — never raw `fetch()`.

    ## Testing

    - Never invoke test runners directly (e.g. `npx jest`, `npx vitest`). Always use the project's test script: `pnpm test` or `pnpm --filter <package> test`.

    ---

    ## Vercel Skills — When to Apply

    Three skills are installed in `.claude/skills/`. Apply them as follows:

    ### vercel-react-best-practices

    **Trigger:** Any task that writes, refactors, or reviews React components or hooks in `apps/web`.

    **Action:** Read `.claude/skills/vercel-react-best-practices/SKILL.md` before beginning. Apply all applicable rule categories listed below.

    **SPA-applicable rule categories (Next.js/RSC rules permanently removed):**
    - **Eliminating Waterfalls (`async-*`):** Use `useQueries` for parallel fetches. Avoid `isLoading` early returns that gate other query hooks from mounting. For SPA loading states, fire all queries at the top level and show a combined skeleton.
    - **Bundle Size (`bundle-*`):** Avoid external library barrel imports where tree-shaking is uncertain. Internal `data-access/*/index.ts` barrels are fine (Vite bundles them). Use `React.lazy()` + `<Suspense>` (not `next/dynamic`) for heavy components. Add external heavy libraries to `optimizeDeps.include` in `vite.config.ts`.
    - **Server-Side Performance — SPA subset (`server-*`):** Applies to Hono API handlers in `apps/api`. In React components: hoist static data to module scope (`server-hoist-static-io`), avoid module-level mutable state shared across requests (`server-no-shared-module-state`).
    - **Client-Side Data Fetching (`client-*`):** TanStack Query already satisfies `client-swr-dedup` — same queryKey deduplicates across all component instances. Keep global `staleTime` non-zero on `QueryClient` (currently 60s). Deduplicate event listeners (`client-event-listeners`).
    - **Re-render Optimization (`rerender-*`):** No `setState` during render body — use `useEffect` + `useRef` guard for one-time state initialization. No inline component definitions inside render functions. Use primitive values as effect dependencies. Use `useCallback`/`useMemo` for stable references passed to memoized children.
    - **Rendering Performance (`rendering-*`):** Use ternary operators (not `&&`) for conditional rendering when the false branch should render nothing. Suppress known hydration mismatches with `suppressHydrationWarning` on `<html>`.
    - **JavaScript Performance (`js-*`):** Use `Map`/`Set` for repeated lookups. Build index maps for O(1) access instead of repeated `.find()` calls in lists.
    - **Advanced Patterns (`advanced-*`):** Store event handlers in refs for stable callbacks (`advanced-event-handler-refs`).

    ---

    ### vercel-composition-patterns

    **Trigger:** Any task that designs or refactors component APIs, adds boolean props to an existing component, or builds a reusable UI component intended for use across multiple pages.

    **Action:** Read `.claude/skills/vercel-composition-patterns/SKILL.md` before beginning. Apply all 8 rules.

    **All rules apply (this project is on React 19):**
    - `architecture-avoid-boolean-props` — Use composition instead of boolean mode switches
    - `architecture-compound-components` — Complex components with shared context use compound component pattern
    - `state-decouple-implementation` — Provider owns state management; consumers get interface only
    - `state-context-interface` — Define `{ state, actions, meta }` interface for context injection
    - `state-lift-state` — Move shared state into provider components
    - `patterns-explicit-variants` — Create named variant components instead of boolean flags
    - `patterns-children-over-render-props` — Prefer `children` composition over `renderX` props
    - `react19-no-forwardref` — **React 19 project.** Pass `ref` as a regular prop. Do not use `forwardRef` in any new or refactored custom component in `apps/web`. Shadcn components in `packages/ui` may retain `forwardRef` (third-party generated — do not migrate).

    ---

    ### web-design-guidelines

    **Trigger:** Any task that adds or modifies UI visible to the user — pages, dialogs, sheets, forms, tables, buttons, navigation, or layouts in `apps/web`.

    **Action:**
    1. Fetch the latest guidelines: `WebFetch https://raw.githubusercontent.com/vercel-labs/web-interface-guidelines/main/command.md`
    2. Read the target files
    3. Apply all rules from the fetched guidelines
    4. Output findings in the terse `file:line` format specified by the guidelines

    **Key rules (from guidelines as of 2026-04-03 — always fetch fresh for authoritative list):**
    - **Forms:** Add `autocomplete` attributes to all text inputs. Use `autocomplete="off"` for custom/non-browser-fillable fields. Use semantic values (`autocomplete="name"`, `autocomplete="organization"`, etc.) for fillable fields.
    - **Focus states:** All interactive elements must have visible focus indicators. Never use `outline: none` without a custom focus style.
    - **autoFocus:** Only use `autoFocus` when the input is the primary purpose of the current user action (e.g., a search input inside a dialog the user just opened). Add a justification comment.
    - **Truncation:** Flex children that render potentially long text must have `min-w-0` applied to prevent overflow.
    - **Accessibility:** All icon-only buttons must have `aria-label`. All interactive elements have appropriate ARIA roles where implicit roles are insufficient.
    - **Typography:** Use proper Unicode characters: `…` (not `...`), `"` `"` (not `"`), `'` `'` (not `'`).
    - **Motion:** Any `animate-*` or CSS transition > 200ms must respect `prefers-reduced-motion`.
    ```
  </action>

  <verify>
    <automated>test -f CLAUDE.md && grep -c "Vercel Skills" CLAUDE.md | grep -v "^0$" && echo "PASS" || echo "FAIL: CLAUDE.md missing or missing Vercel Skills section"</automated>
  </verify>

  <acceptance_criteria>
    - `test -f CLAUDE.md` exits 0 (file exists)
    - `grep "Vercel Skills — When to Apply" CLAUDE.md` matches
    - `grep "vercel-react-best-practices" CLAUDE.md` matches
    - `grep "vercel-composition-patterns" CLAUDE.md` matches
    - `grep "web-design-guidelines" CLAUDE.md` matches
    - `grep "raw.githubusercontent.com/vercel-labs/web-interface-guidelines" CLAUDE.md` matches (live fetch URL present)
    - `grep "Trigger:" CLAUDE.md | wc -l` returns 3 (one trigger per skill)
    - `grep "TanStack Start" CLAUDE.md` matches (SPA context documented)
    - `grep "react19-no-forwardref" CLAUDE.md` matches (React 19 rule referenced)
    - `grep "NOT Next.js" CLAUDE.md` matches (SPA constraint explicit)
    - `grep "RSC patterns.*server actions.*React.cache" CLAUDE.md` matches (RSC exclusions listed)
    - `pnpm --filter web tsc --noEmit` exits 0
  </acceptance_criteria>

  <done>CLAUDE.md created at project root. Contains three skill sections, each with an explicit trigger condition as an imperative sentence. vercel-react-best-practices lists SPA-applicable categories only. web-design-guidelines includes live fetch URL. React 19 noted in composition-patterns. All trigger conditions are actionable sentences that future agents can follow without reading CONTEXT.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Verify full Phase 02.3 results</name>
  <what-built>
    All Phase 02.3 work:
    - Plan 01: RSC rules removed from vercel-react-best-practices SKILL.md (7 files deleted, bundle-dynamic-imports.md updated to React.lazy())
    - Plan 02: AccountForm waterfall fixed (parallel queries), CategoriesSettings setState-during-render fixed (useRef guard), QueryClient staleTime documented
    - Plan 03: Composition patterns audit complete, web-design-guidelines audit complete, all violations fixed
    - Plan 04: CLAUDE.md created with explicit Vercel skills trigger conditions
  </what-built>
  <how-to-verify>
    1. Run: `pnpm --filter web tsc --noEmit` — must exit 0
    2. Run: `pnpm --filter web lint` — must exit 0
    3. Start the dev server: `pnpm dev` from repo root
    4. Navigate to each route in apps/web: /, /dashboard, /accounts, /settings, /onboarding
    5. Verify no visual regressions — pages load, forms open, sheets slide in/out
    6. Open browser DevTools Network tab and navigate to /accounts — verify the account members query and org members query fire simultaneously (parallel start times), not sequentially
    7. Read CLAUDE.md at the project root — verify the three Vercel skills sections have actionable trigger sentences
    8. Read .claude/skills/vercel-react-best-practices/SKILL.md — verify server-auth-actions, server-cache-react, server-dedup-props, server-serialization, server-parallel-fetching, server-parallel-nested-fetching, server-after-nonblocking are ALL absent
  </how-to-verify>
  <action>Human-only verification step. Run the commands listed in how-to-verify and navigate the app to confirm no regressions. Report any issues found to the executor.</action>
  <verify>
    <automated>pnpm --filter web tsc --noEmit && pnpm --filter web lint</automated>
  </verify>
  <done>All automated checks pass, app navigates without errors, parallel queries confirmed, CLAUDE.md has three skill sections with trigger conditions, RSC rules absent from SKILL.md.</done>
  <resume-signal>Type "approved" if all checks pass, or describe any issue found</resume-signal>
</task>

</tasks>

<verification>
Final phase verification:
- `pnpm --filter web tsc --noEmit` exits 0
- `pnpm --filter web lint` exits 0
- `test -f CLAUDE.md` exits 0
- `grep "Vercel Skills — When to Apply" CLAUDE.md` matches
- `ls .claude/skills/vercel-react-best-practices/rules/ | grep "server-auth-actions"` returns no lines
- App navigates without runtime errors
</verification>

<success_criteria>
- [ ] CLAUDE.md exists at project root with `## Vercel Skills — When to Apply` section
- [ ] Three skills, each with explicit `**Trigger:**` condition as an imperative sentence
- [ ] vercel-react-best-practices section: no RSC categories, all SPA-applicable categories listed with actionable descriptions
- [ ] vercel-composition-patterns section: all 8 rules listed, React 19 note present
- [ ] web-design-guidelines section: live fetch URL embedded, output format specified
- [ ] `pnpm --filter web tsc --noEmit` exits 0
- [ ] `pnpm --filter web lint` exits 0
- [ ] Human verification checkpoint passed
</success_criteria>

<output>
After completion, create `.planning/phases/02.3-vercel-skills-audit-and-guidelines/02.3-04-SUMMARY.md`
</output>
