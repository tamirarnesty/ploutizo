# Phase 4.2: Settlement UI - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-08
**Phase:** 4.2-Settlement-UI
**Areas discussed:** Page location, Sidebar nav scope, Settle CTA flow, eachPersonPaysOwn handling, /settlements route, Period selector wiring, Settle CTA placement, Component selection

---

## Page Location

| Option | Description | Selected |
|--------|-------------|----------|
| Settlement section + empty placeholders | Settlement on /dashboard with ghost stat cards for Phase 6.3 content | ✓ |
| Settlement only, no layout scaffolding | Just the settlement DataGrid, no placeholder sections | |
| Own /settlements route for now | Put settlement UI at /settlements, not /dashboard | |

**User's choice:** Settlement section + empty placeholders
**Notes:** Phase 6.3 builds full Overview dashboard; Phase 4.2 just claims the /dashboard route with placeholders.

---

## Page Header & Period Selector

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — header + period switcher | Include page header and period selector UI | ✓ |
| Static header only | Header text, no switcher control | |
| No header work in this phase | Skip header entirely | |

**User's choice:** Yes — header + period switcher
**Notes:** Period selector is UI-only in Phase 4.2 — not wired to API data. Phase 6.3 wires it.

---

## Sidebar Nav Scope

| Option | Description | Selected |
|--------|-------------|----------|
| Name + avatar only | MEMBERS section shows name and avatar | |
| Name + avatar + contextual value per page | Value changes based on current route | ✓ |
| Name + avatar + settlement balance | Static settlement balance regardless of page | |

**User's choice:** Name + avatar + contextual value per page
**Notes:** User wanted the value to be meaningful per page — led to the /expenses and /income route split decision.

---

## Placeholder Nav Items

| Option | Description | Selected |
|--------|-------------|----------|
| Budget | Add Budget placeholder | ✓ |
| Net Worth | Add Net Worth placeholder | ✓ |
| Savings | Add Savings placeholder | ✓ |

**User's choice:** Budget, Net Worth, Savings (all three)
**Notes:** Rendered as disabled with "soon" badge.

---

## /transactions Route Split

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add /expenses and /income now | Dedicated filtered pages for each transaction type | ✓ |
| Single value on sidebar regardless of page | Keep /transactions, change how sidebar aggregates | |
| Defer nav structure to Phase 6.3 | Don't restructure routes in this phase | |

**User's choice:** Yes — add /expenses and /income now
**Notes:** User decided this was the right call to enable per-page contextual MEMBERS values without a new API.

---

## /transactions Fate

| Option | Description | Selected |
|--------|-------------|----------|
| Keep as 'All transactions' | Route stays, no redirect | ✓ |
| Redirect to /expenses | Old route redirects to filtered view | |
| Rename to 'Transfers & Settlements' | Repurpose the route | |

**User's choice:** Keep as 'All transactions'

---

## Settle CTA Style

| Option | Description | Selected |
|--------|-------------|----------|
| Lightweight Dialog | shadcn Dialog, stays on dashboard | ✓ |
| Full page | Navigate to /settlements/new | |
| Side sheet | Slide-in panel from right | |

**User's choice:** Lightweight Dialog

---

## Settle Dialog Fields

| Option | Description | Selected |
|--------|-------------|----------|
| Amount + Source account + Date + Notes | Core settlement fields | ✓ |
| Amount + Date only | Minimal | |
| All fields including receipt upload | Full-featured | |

**User's choice:** Amount + Source account + Date + Notes
**Notes:** Amount pre-filled with member's outstanding balance. Source account = bank account select.

---

## eachPersonPaysOwn Handling

| Option | Description | Selected |
|--------|-------------|----------|
| No special treatment | All accounts treated uniformly | ✓ |
| Show split indicator badge | Visual cue for shared cards | |
| Separate section | Group shared vs non-shared cards | |

**User's choice:** No special treatment
**Notes:** `eachPersonPaysOwn` was removed in Phase 4.1 (D-07). No split-card concept remains.

---

## Settlement Summary Pane

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — include a summary pane | Per-person balance summary card beside the DataGrid | ✓ |
| No pane, inline in table only | Balances visible only in DataGrid rows | |

**User's choice:** Yes — include a summary pane
**Notes:** Simple stat rows in a card. Not a person-to-person cross-member summary — just per-person totals.

---

## Summary Pane Design

| Option | Description | Selected |
|--------|-------------|----------|
| Simple stat rows in a card | Name + balance amount, clean card layout | ✓ |
| DataGrid in the pane | Same DataGrid component for consistency | |
| Expandable list | Collapsible per-person rows | |

**User's choice:** Simple stat rows in a card

---

## Settle CTA Placement

| Option | Description | Selected |
|--------|-------------|----------|
| Per-member row, hover only | Button appears in breakdown row on hover | ✓ |
| Sticky per-card row | Always visible CTA at card level | |
| Summary pane CTA | Settle button in the summary card | |

**User's choice:** Per-member row, hover only
**Notes:** "should only show on hover to avoid cluttering the UI". DataGrid must use native features — no custom table builds.

---

## Member Pre-selection in Settle Dialog

**User's decision (free text):** Settle on behalf of anyone. Pre-select first member with balance > 0 (not just 0-index, which could select a member with ≤0 balance). User can switch to any member.

---

## Negative Balance UX

| Option | Description | Selected |
|--------|-------------|----------|
| Green + keep Settle CTA | Credit display, user can still settle | ✓ |
| Green, hide Settle CTA | No settle action for credits | |
| Warning state | Yellow/orange to flag anomaly | |

**User's choice:** Green + keep Settle CTA
**Notes:** "I might still want to pay the card because I know there will be more txns coming up, so it will overpay and then get used as a credit on the balance."

---

## /settlements Full Page Route

| Option | Description | Selected |
|--------|-------------|----------|
| Yes — add /settlements full page | Placeholder route now, content in Phase 6.3 | ✓ |
| No, defer entirely | Don't add the route | |

**User's choice:** Yes — add /settlements full page

---

## Period Selector Data Wiring

| Option | Description | Selected |
|--------|-------------|----------|
| UI only — no data wiring yet | Build the selector, wire it in Phase 6.3 | ✓ |
| Wire to settlement data only | Partial wiring | |
| Full wiring to all data | Complete implementation now | |

**User's choice:** UI only — no data wiring yet

---

## Credit Card Table Component

**User's decision (free text):** Use DataGrid's native features and capabilities. Mockups are inspiration. Do NOT build anything from scratch.

---

## Period Selector Component

**User's decision:** Existing shadcn Calendar component from `packages/ui/src/components/calendar.tsx` with the Presets feature (react-day-picker). Reuse as-is.

---

## MEMBERS Sidebar Data Source

**User's guidance:** Pointed to REQUIREMENTS.md / ROADMAP.md Phase 6.3 — full income-by-person comes from `GET /api/dashboard/income-by-person` (Phase 6.3). Phase 4.2 uses client-side aggregation from each page's own existing query.

---

## Claude's Discretion

- Exact stat card placeholder content and styling (ghost/skeleton vs. "coming soon" labels)
- DataGrid column configuration for per-member breakdown (avatar, progress bar, amount, label, hover Settle button)
- Specific Tailwind tokens and spacing within the `p-6` main padding established by `_layout.tsx`

## Deferred Ideas

- Full cross-member person-to-person settlement panel ("Emily owes Tamir $360") — replaced by per-person summary; deferred to Phase 6.3 or dedicated page
- Period switcher data wiring — UI only in Phase 4.2, wired in Phase 6.3
- Settlement threshold notifications (Settings > Profile) — noted for future phase
- Phase 6.3 `GET /api/dashboard/income-by-person` for full MEMBERS sidebar data
- `/settlements` full page content — placeholder route added, content in Phase 6.3
- "View all" links from credit card table → accounts page — Phase 6.3
