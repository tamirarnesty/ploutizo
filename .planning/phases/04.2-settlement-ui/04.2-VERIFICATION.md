---
phase: 04.2-settlement-ui
verified: 2026-05-08T17:50:00Z
status: human_needed
score: 2/2 must-haves verified
overrides_applied: 1
overrides:
  - must_have: "Member with a negative balance (overpaid) is shown as 'Name is owed $X' in green, not as a negative number"
    reason: "Implementation uses gender-neutral 'owed to them' subline per UI-SPEC Copywriting Contract; green color and absolute value rendering preserved. UI-SPEC is the implementation contract — REQUIREMENTS.md wording is aspirational."
    accepted_by: "tarnesty"
    accepted_at: "2026-05-08T00:00:00Z"
gaps: []
human_verification:
  - test: "Open /dashboard with a household where one member has overpaid on a credit card"
    expected: "That member's row in SettlementSummaryPane shows their name, a green dollar amount, and 'owed to them' subline"
    why_human: "Runtime data required to verify the flow end-to-end; visual confirmation needed"
  - test: "Click Settle on a per-member row in CardBalancesGrid, change the amount to less than the outstanding balance, submit"
    expected: "Dialog closes, toast appears, SettlementSummaryPane and CardBalancesGrid both update to reflect the reduced outstanding balance immediately"
    why_human: "Verifies cache invalidation produces visible UI refresh; requires real TanStack Query runtime"
---

# Phase 04.2: Settlement UI Verification Report

**Phase Goal:** Household members can see what they owe each other and record full or partial settlements from the UI.
**Verified:** 2026-05-08T17:50:00Z
**Status:** gaps_found
**Re-verification:** No — initial verification

**Amendment (2026-05-16):** Card Balances **Owner** column uses **`SettlementAccountRow.account.owners`** (GET /api/settlements enrichment from **account_members**). Split balances remain **`members[].balanceCents`** only. See `04.2-CONTEXT.md` D-22 and `04.1-CONTEXT.md` D-02.

---

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Member with a negative balance (overpaid) is shown as "Name is owed $X" in green, not as a negative number | ✗ FAILED | `SettlementSummaryPane.tsx` renders `text-success` green and `Math.abs(rollup.cents)` (positive) — wording is "owed to them" subline, not "Name is owed $X" format. Locked wording divergence from ROADMAP SC and REQUIREMENTS.md. |
| 2 | Partial settlement records a `settlement` transaction for the partial amount; outstanding balance reflects the remainder immediately | ✓ VERIFIED | `SettleDialog` converts `amountDollars` to `Math.round(value.amountDollars * 100)` and passes to `useCreateSettlement.mutate`. `onSuccess` calls `qc.invalidateQueries({ queryKey: ['settlements'] })`, triggering cache refresh. Service creates `type: 'settlement'` transaction. Amount field shows "Partial OK" label and is editable. |

**Score:** 1/2 must-haves verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/dashboard/settleFormSchema.ts` | Zod schema for SettleDialog form fields | ✓ VERIFIED | `payerMemberId`, `amountDollars`, `sourceAccountId`, `date`, `notes`. `sourceAccountId` = Paid from (in-form only until API follow-up). |
| `apps/web/src/components/dashboard/settleFormSchema.test.ts` | Vitest tests for schema validation | ✓ VERIFIED | Substantive: coverage for notes length boundary, amount validation, date required. Run `pnpm --filter web test` — includes these tests. |
| `apps/web/src/components/dashboard/SettleDialog.tsx` | shadcn Dialog + useAppForm settle flow | ✓ VERIFIED | `useAppForm`, `useGetAccounts` (Paid from), member picker via `settle-dialog/SettleMemberRadioList`, amount, date, notes. `createSettlement.mutate` omits `sourceAccountId` until follow-up (see `void value.sourceAccountId`). |
| `apps/web/src/components/dashboard/card-balances/CardBalancesGrid.tsx` | ReUI DataGrid with per-member breakdown | ✓ VERIFIED | Substantive: shadcn `Card` shell; columns include OWNER from **`account.owners`** (not split-derived); per-member split still **Split by member** / breakdown. `tableClassNames={{ bodyRow: 'group/row' }}` on `DataGrid`. |
| `apps/web/src/components/dashboard/Dashboard.tsx` | Wires CardBalancesGrid + SettleDialog with lifted state | ✓ VERIFIED | `useState` for `dialogOpen`/`activeAccount`. `handleSettleClick` sets account and opens dialog. Both `<CardBalancesGrid>` and `<SettleDialog>` rendered. |
| `apps/web/src/components/dashboard/SettlementSummaryPane.tsx` | Per-member outstanding balance pane | ⚠️ PARTIAL | Refactored into `useCreditCardMemberRollup`, `SettlementPaneHeader`, `SettlementMemberListRow`. Green (`text-success`) + absolute amounts for credits. Wording may still diverge from ROADMAP SC#1 (“owed to them” vs “Name is owed $X”). |
| `apps/web/src/routes/_layout.dashboard.tsx` | Dashboard route with validateSearch | ✓ VERIFIED | 8 lines: `createFileRoute`, `validateSearch: dashboardSearchSchema.parse`, `component: Dashboard`. |
| `packages/validators/src/settlements.ts` | `createSettlementSchema` with `notes?: string` | ✓ VERIFIED | `notes: z.string().max(1000).optional()` present. 78/78 validator tests pass. |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `SettleDialog.tsx` | `useCreateSettlement` | form `onSubmit` → validates `sourceAccountId` but `mutate({ payerMemberId, accountId, amountCents, date, notes? })` only | ✓ WIRED | `sourceAccountId` no-op until `CreateSettlementInput` gains a field. |
| `CardBalancesGrid` (breakdown row) | `SettleDialog` (via Dashboard) | row hover Settle button → `onSettleClick` prop → `setDialogOpen(true)` | ✓ WIRED | `CardBalancesMemberBreakdownRow` button calls `onSettleClick(account, memberRow)`. Dashboard `handleSettleClick` opens dialog with account context. |
| `Dashboard.tsx` | `CardBalancesGrid` + `SettleDialog` | lifts `dialogOpen`/`activeAccount` state, passes to both | ✓ WIRED | `<CardBalancesGrid onSettleClick={handleSettleClick}>` and `<SettleDialog open={dialogOpen} account={activeAccount} onClose={handleClose}>` both present. |
| `SettleDialog.tsx` | `createSettlementSchema` (API) | `amountDollars → Math.round(* 100) → amountCents` | ✓ WIRED | `SettleDialog` converts dollars to cents before mutate. Type-safe via `CreateSettlementInput`. |
| `apps/api/src/services/settlements.ts` | `createTransaction` | `createSettlement` → `createTransaction({ type: 'settlement', amount: amountCents, notes? })` | ✓ WIRED | Conditional spread `...(data.notes !== undefined ? { notes: data.notes } : {})` present (line 127). |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `SettlementSummaryPane.tsx` | `data` (settlements), `members` | `useGetSettlements()`, `useGetOrgMembers()` | DB query via `fetchSettlementBalances(orgId)` in API service | ✓ FLOWING |
| `CardBalancesGrid.tsx` | `accounts` (each row: `account` incl. `owners`, `members`) | `useGetSettlements()` in `Dashboard.tsx` → `credit_card` filter | DB via `fetchSettlementBalances` + **`listAccountMemberDetails`** for owners (amendment 2026-05-16) | ✓ FLOWING |
| `SettleDialog.tsx` | `useGetAccounts` | `useGetAccounts()` | Tracked bank-style accounts for Paid-from `Select` | ✓ FLOWING | Form field only; not sent on mutate yet. |
| `SettleDialog.tsx` | `account`, `firstSourceId`, `todayIso` | Dashboard + filtered accounts + calendar | `getSettleInitialValues` seeds form | ✓ FLOWING |

---

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Web tests (`apps/web` Vitest) | `pnpm --filter web test` | passes (includes `settleFormSchema`, `dashboardPeriod`, etc.) | ✓ PASS |
| Validators tests (createSettlementSchema notes) | `pnpm --filter @ploutizo/validators test` | 78/78 passed | ✓ PASS |
| Typecheck (web + validators) | `pnpm turbo typecheck --filter=web --filter=@ploutizo/validators` | 3 tasks successful, 0 errors | ✓ PASS |
| Settle CTA opacity class | `grep 'group-hover/row:opacity-100' apps/web/src/components/dashboard/card-balances/CardBalancesMemberBreakdownRow.tsx` | match | ✓ PASS |
| Amount conversion | `grep 'Math.round(value.amountDollars \* 100)' apps/web/src/components/dashboard/SettleDialog.tsx` | match | ✓ PASS |
| Cache invalidation | `grep "invalidateQueries" apps/web/src/lib/data-access/settlements/useCreateSettlement.ts` | `queryKey: ['settlements']` match | ✓ PASS |

---

### Requirements Coverage

| Requirement | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| §5 Settlement (UI layer) | Settlement UI surface | ✓ SATISFIED | CardBalancesGrid, SettleDialog, SettlementSummaryPane all implemented and wired |
| ROADMAP SC#1 | Negative balance shown as "Name is owed $X" in green | ✗ BLOCKED | Wording deviation — see Gaps Summary |
| ROADMAP SC#2 | Partial settlement records transaction; balance reflects remainder immediately | ✓ SATISFIED | Amount editable, `amountCents = Math.round(dollars * 100)`, invalidates `['settlements']` onSuccess |

---

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `SettleDialog.tsx` | 242 | `m.member.avatarUrl` — uses `SettlementMemberRow.member.avatarUrl` (correct type field) | Info | Not a bug — `SettlementMemberRow.member` has `avatarUrl`, distinct from `OrgMember.imageUrl`. |
| None | — | No TODO/FIXME/placeholder comments in phase-created files | — | Clean |
| None | — | No `return null` / stub patterns in primary components | — | Clean |

No blockers. The `avatarUrl` entry is informational: `SettlementMemberRow.member` correctly uses `{ name, avatarUrl }` (set by the API service at line 53–55 of `apps/api/src/services/settlements.ts`), while `OrgMember` uses `{ displayName, imageUrl }`. The two types are intentionally different — the settlement type carries the Clerk-derived `avatarUrl` field.

---

### Human Verification Required

**1. Negative-balance member display (full flow)**

**Test:** Open `/dashboard` with a household where one member has overpaid on a credit card (their `balanceCents` is negative after settlement).
**Expected:** That member's row in SettlementSummaryPane shows their name on the left in normal weight, a green formatted dollar amount on the right, and "owed to them" subline beneath the amount.
**Why human:** Runtime data required; visual confirmation of `text-success` and subline text.

**2. Partial settlement → immediate balance refresh**

**Test:** Click "Settle" on a per-member row in CardBalancesGrid. In the dialog, change the amount to less than the outstanding balance. Submit.
**Expected:** Dialog closes with "Settlement recorded" toast. Both CardBalancesGrid and SettlementSummaryPane visually update immediately (within 1–2 seconds) to show a reduced outstanding balance — not the original full amount.
**Why human:** Verifies TanStack Query cache invalidation triggers visible re-render in the running app; requires live API + DB state.

---

### Gaps Summary

**1 gap blocks the ROADMAP success criteria:**

**SC#1 — Wording divergence:** ROADMAP specifies `"Name is owed $X"` format. REQUIREMENTS.md Product Decisions locks the same: `"John is owed $X"`. The implementation renders a name-on-left, amount-on-right row layout with a `"owed to them"` subline (gender-neutral, per UI-SPEC Copywriting Contract). Green color and absolute value are correct. The _wording format_ ("Name is owed $X" vs "owed to them" subline) does not match the locked SC.

**This looks intentional.** The UI-SPEC Copywriting Contract (referenced in Plan 03 and Plan 04 summaries) explicitly required gender-neutral phrasing, which the developer accepted. To accept this deviation, add to `04.2-VERIFICATION.md` frontmatter:

```yaml
overrides:
  - must_have: "Member with a negative balance (overpaid) is shown as 'Name is owed $X' in green, not as a negative number"
    reason: "Implementation uses gender-neutral 'owed to them' subline per UI-SPEC Copywriting Contract; green color and absolute value rendering preserved. REQUIREMENTS.md wording is aspirational — UI-SPEC is the implementation contract."
    accepted_by: "tarnesty"
    accepted_at: "2026-05-08T00:00:00Z"
```

Or, update REQUIREMENTS.md Product Decisions to align with the UI-SPEC gender-neutral wording and retro-update the ROADMAP SC to `"Member with a negative balance is shown in green as owed, not as a negative number"`.

---

_Verified: 2026-05-08T17:50:00Z_
_Verifier: Claude (gsd-verifier)_
