---
phase: 04.2
plan: 08
type: execute
wave: 5
depends_on: [07]
files_modified:
  - .planning/phases/04.2-settlement-ui/04.2-CONTEXT.md
  - .planning/phases/04.2-settlement-ui/04.2-PATTERNS.md
  - apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx
  - apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx
  - apps/web/src/components/dashboard/card-balances/CardBalancesMemberBreakdownRow.tsx
  - apps/web/src/components/dashboard/card-balances/buildCardBalancesColumns.tsx
  - apps/web/src/components/dashboard/card-balances/types.ts
  - apps/web/src/components/dashboard/Dashboard.tsx
  - apps/web/src/components/dashboard/SettleDialog.tsx
  - apps/web/src/components/dashboard/settle-dialog/getSettleInitialValues.ts
  - apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.test.tsx
  - apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.test.tsx
autonomous: true
requirements:
  - "§5 Settlement"
must_haves:
  truths:
    - "D-10 is explicitly superseded in phase context from hover-only settle CTA to sketch winner 006A click-trigger action dropdown"
    - "Settle entry point is Action-column DropdownMenu (icon+text trigger), not hover-only row affordance"
    - "Dropdown supports keyboard interaction: Enter/Space open, Arrow keys navigate, Esc closes and returns focus"
    - "Split member badges expose delayed amount tooltips (`delay={500}`) and support keyboard focus interaction"
    - "Tab traversal shows visible focus styles on Action trigger and badge tooltip triggers"
    - "Settlement dialog flow remains unchanged in behavior (preselect member intent, submit mutation, invalidation, close semantics)"
  artifacts:
    - path: .planning/phases/04.2-settlement-ui/04.2-CONTEXT.md
      provides: "Explicit D-10 supersession note aligned with sketch winner 006A"
      contains: "click-trigger DropdownMenu"
    - path: .planning/phases/04.2-settlement-ui/04.2-PATTERNS.md
      provides: "Updated analog mapping for decomposed card-balances interaction files"
      contains: "CardBalancesActionCell.tsx"
    - path: apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx
      provides: "Action button + dropdown settle interaction with member amount options"
      contains: "DropdownMenuTrigger"
    - path: apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx
      provides: "Segmented split + badge-level tooltip interactions with delay"
      contains: "Tooltip delay={500}"
    - path: apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.test.tsx
      provides: "Keyboard interaction coverage for action dropdown open/nav/close focus loop"
      contains: "Enter"
    - path: apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.test.tsx
      provides: "Tooltip focus/Tab visibility assertions"
      contains: "focus-visible"
  key_links:
    - from: apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx
      to: apps/web/src/components/dashboard/Dashboard.tsx
      via: "Menu member selection invokes onSettleClick(account, member)"
      pattern: "onSettleClick"
    - from: apps/web/src/components/dashboard/Dashboard.tsx
      to: apps/web/src/components/dashboard/SettleDialog.tsx
      via: "Selected member id is passed as dialog preselection seed"
      pattern: "initialPayerMemberId"
    - from: apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx
      to: packages/ui/src/components/tooltip.tsx
      via: "Badge tooltip is per-instance Tooltip root with delay config"
      pattern: "delay={500}"
---

<objective>
Implement only the remaining interaction-heavy Card Balances deltas for Phase 04.2 after layout semantics are complete in Plan 07.

Purpose: ship the sketch-validated settle interaction model with explicit keyboard accessibility and no settlement-flow regressions, while resolving the locked-decision doc conflict.

Output: context/pattern doc alignment, Action dropdown + badge tooltip interaction implementation, and focused keyboard/a11y regression tests.
</objective>

<execution_context>
@/Users/tamirarnesty/Developer/ploutizo/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/tamirarnesty/Developer/ploutizo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04.2-settlement-ui/04.2-CONTEXT.md
@.planning/phases/04.2-settlement-ui/04.2-PATTERNS.md
@.planning/phases/04.2-settlement-ui/04.2-RESEARCH.md
@.planning/phases/04.2-settlement-ui/04.2-UI-SPEC.md
@.planning/phases/04.2-settlement-ui/04.2-07-PLAN.md
@.cursor/skills/sketch-findings-ploutizo/SKILL.md
@.cursor/skills/sketch-findings-ploutizo/references/actions-tooltips-and-a11y.md
@apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx
@apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx
@apps/web/src/components/dashboard/card-balances/CardBalancesMemberBreakdownRow.tsx
@apps/web/src/components/dashboard/card-balances/buildCardBalancesColumns.tsx
@apps/web/src/components/dashboard/Dashboard.tsx
@apps/web/src/components/dashboard/SettleDialog.tsx
@apps/web/src/components/dashboard/settle-dialog/getSettleInitialValues.ts
@apps/web/src/components/transactions/TransactionColumns.tsx
@packages/ui/src/components/dropdown-menu.tsx
@packages/ui/src/components/tooltip.tsx

<interfaces>
From `@ploutizo/ui/components/dropdown-menu`:
- `DropdownMenuTrigger` and `DropdownMenuItem` carry built-in keyboard semantics (Enter/Space open, Arrow navigation, Esc close)

From `@ploutizo/ui/components/tooltip`:
- `Tooltip` root supports per-instance delay; set `delay={500}` at each badge tooltip root

From `apps/web/src/lib/data-access/settlements/useCreateSettlement.ts`:
- Mutation contract remains `CreateSettlementInput`; invalidation stays `['settlements']`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align phase docs by superseding D-10 and refreshing card-balances interaction analog mapping</name>
  <files>.planning/phases/04.2-settlement-ui/04.2-CONTEXT.md, .planning/phases/04.2-settlement-ui/04.2-PATTERNS.md</files>
  <read_first>
    - .planning/phases/04.2-settlement-ui/04.2-CONTEXT.md
    - .planning/phases/04.2-settlement-ui/04.2-PATTERNS.md
    - .cursor/skills/sketch-findings-ploutizo/references/actions-tooltips-and-a11y.md
  </read_first>
  <behavior>
    - D-10 conflict is resolved in source docs before UI implementation starts
    - Context clearly records click-trigger Action dropdown decision from sketch winner 006A
    - Pattern map explicitly lists analogs for `CardBalancesActionCell`, `CardBalancesBreakdownCell`, and `CardBalancesMemberBreakdownRow`
  </behavior>
  <action>
    - In `04.2-CONTEXT.md`, add an explicit supersession note for D-10: hover-only settle CTA is superseded by sketch winner 006A click-trigger dropdown action model, while preserving D-08/D-09/D-11 settlement flow constraints.
    - In `04.2-PATTERNS.md`, append/update mapping rows for decomposed interaction files (`CardBalancesActionCell.tsx`, `CardBalancesBreakdownCell.tsx`, `CardBalancesMemberBreakdownRow.tsx`) and reference their analogs (`TransactionColumns.tsx`, `dropdown-menu.tsx`, `tooltip.tsx`).
    - Keep wording scoped to remaining 04.2 work only; do not edit unrelated phase decisions.
  </action>
  <verify>
    <automated>rg "D-10|supersed|006A|click-trigger|DropdownMenu" .planning/phases/04.2-settlement-ui/04.2-CONTEXT.md && rg "CardBalancesActionCell|CardBalancesBreakdownCell|CardBalancesMemberBreakdownRow" .planning/phases/04.2-settlement-ui/04.2-PATTERNS.md</automated>
  </verify>
  <acceptance_criteria>
    - `04.2-CONTEXT.md` explicitly states D-10 supersession to click-trigger Action dropdown
    - `04.2-PATTERNS.md` includes explicit analog references for the decomposed interaction files touched in this plan
  </acceptance_criteria>
  <done>Decision and pattern artifacts are aligned with the interaction implementation direction before code changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Action dropdown settle flow and badge tooltip interactions with explicit keyboard accessibility</name>
  <files>apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx, apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx, apps/web/src/components/dashboard/card-balances/CardBalancesMemberBreakdownRow.tsx, apps/web/src/components/dashboard/card-balances/buildCardBalancesColumns.tsx, apps/web/src/components/dashboard/card-balances/types.ts, apps/web/src/components/dashboard/Dashboard.tsx, apps/web/src/components/dashboard/SettleDialog.tsx, apps/web/src/components/dashboard/settle-dialog/getSettleInitialValues.ts</files>
  <read_first>
    - .planning/phases/04.2-settlement-ui/04.2-PATTERNS.md
    - .cursor/skills/sketch-findings-ploutizo/references/actions-tooltips-and-a11y.md
    - apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx
    - apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx
    - apps/web/src/components/dashboard/card-balances/CardBalancesMemberBreakdownRow.tsx
    - apps/web/src/components/dashboard/SettleDialog.tsx
    - packages/ui/src/components/dropdown-menu.tsx
    - packages/ui/src/components/tooltip.tsx
  </read_first>
  <behavior>
    - Per D-10 supersession, settle entry point is Action-column click-trigger dropdown, not hover-only row control
    - Dropdown trigger opens with Enter/Space and click
    - Menu navigation works with ArrowUp/ArrowDown, selection with Enter, and Esc closes menu returning focus to trigger
    - Badge tooltips show amounts with `delay={500}` and can be triggered via hover and keyboard focus
    - Tab traversal keeps visible focus state on Action trigger and badge tooltip triggers
    - Existing settlement submission/invalidation behavior remains unchanged (per D-08/D-09/D-11 and current hook contract)
  </behavior>
  <action>
    - Implement `CardBalancesActionCell.tsx` using shadcn `DropdownMenu` primitives and an icon+text `Button` trigger in the Action column.
    - Update `buildCardBalancesColumns.tsx` to wire Action column ownership of settle interaction and remove hover-only settle controls from breakdown rows.
    - Refactor `CardBalancesBreakdownCell.tsx` + `CardBalancesMemberBreakdownRow.tsx` to keep segmented bar + badges only, with per-badge `<Tooltip delay={500}>` amount content.
    - Keep tooltip trigger elements as component-library interactive primitives (no raw production `<button>` patchwork) and preserve visible focus styles.
    - Wire selected member through `Dashboard.tsx` and `SettleDialog.tsx` via existing preselection path (`initialPayerMemberId`/equivalent) while preserving fallback behavior in `getSettleInitialValues.ts`.
    - Do not alter settlement mutation payload shape or invalidate query keys beyond existing `['settlements']`.
  </action>
  <verify>
    <automated>pnpm --filter @ploutizo/web test --run && pnpm turbo typecheck --filter=@ploutizo/web</automated>
  </verify>
  <acceptance_criteria>
    - Action column trigger opens dropdown via Enter, Space, and click
    - ArrowUp/ArrowDown move active menu option, Enter confirms option, Esc closes menu and restores trigger focus
    - Tab traversal exposes visible focus ring/style on action trigger and member badge tooltip triggers
    - Badge tooltips render amount content with `delay={500}` and are reachable via keyboard focus
    - Settlement dialog opens for selected member and submit behavior remains unchanged
  </acceptance_criteria>
  <done>Accessible dropdown + tooltip interactions are implemented without regressing settlement behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add focused keyboard/a11y and interaction regression tests for Action dropdown and badges</name>
  <files>apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.test.tsx, apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.test.tsx, apps/web/src/components/dashboard/settle-dialog/getSettleInitialValues.ts</files>
  <read_first>
    - apps/web/src/components/dashboard/card-balances/CardBalancesActionCell.tsx
    - apps/web/src/components/dashboard/card-balances/CardBalancesBreakdownCell.tsx
    - apps/web/src/components/dashboard/dashboardPeriod.test.ts
    - apps/web/src/components/dashboard/settleFormSchema.test.ts
  </read_first>
  <behavior>
    - Tests validate keyboard open/navigation/close behavior for Action dropdown
    - Tests validate Tab/focus-visible behavior and focus-triggered tooltip interaction on member badges
    - Tests preserve deterministic selected-member seeding behavior for settle dialog helper logic
  </behavior>
  <action>
    - Add `CardBalancesActionCell.test.tsx` covering Enter/Space open, Arrow navigation, Enter selection, and Esc close/focus return.
    - Add `CardBalancesBreakdownCell.test.tsx` covering badge tooltip delay prop presence and focus-driven tooltip trigger behavior.
    - If helper logic changes while wiring selected member, extend assertions around `getSettleInitialValues` to lock explicit-member precedence and fallback.
    - Keep tests stable and component-focused; avoid broad end-to-end harnesses in this plan.
  </action>
  <verify>
    <automated>pnpm --filter @ploutizo/web test --run && pnpm turbo typecheck --filter=@ploutizo/web</automated>
  </verify>
  <acceptance_criteria>
    - Automated tests fail if dropdown keyboard semantics regress (Enter/Space/Arrow/Esc)
    - Automated tests fail if tooltip focus/Tab accessibility contract regresses
    - Any settle-member seed changes are covered by deterministic assertions
  </acceptance_criteria>
  <done>Interaction-heavy accessibility and no-regression behavior is guarded by focused automated tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard and pointer input -> dropdown/tooltip controls | Untrusted interaction paths must not produce hidden/unreachable settle behavior |
| Action selection -> settlement dialog submit path | UI preselection convenience must not alter mutation authorization or payload constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.2-08-01 | Tampering | Action menu member selection | mitigate | Restrict selection to members present in settlement row data and preserve existing server-side schema validation |
| T-04.2-08-02 | Spoofing | Keyboard focus/overlay state | mitigate | Use shadcn menu primitives with explicit keyboard acceptance criteria and tests for Enter/Space/Arrow/Esc/focus return |
| T-04.2-08-03 | Information Disclosure | Badge tooltip amounts | accept | Tooltip reveals amounts already visible in row context; no additional sensitive data introduced |
| T-04.2-08-04 | Denial of Service | Overlay interaction churn | mitigate | Keep tooltip/menu behavior component-scoped and covered by focused tests to prevent unstable rerender loops |
| T-04.2-08-05 | Elevation of Privilege | Dialog preselection | mitigate | Keep preselection as UI-only seed; mutation payload remains validated and tenant/member authorization stays API-enforced |
</threat_model>

<verification>
- `pnpm --filter @ploutizo/web test --run` passes with new interaction tests
- `pnpm turbo typecheck --filter=@ploutizo/web` passes
- Manual keyboard check: Action trigger opens on Enter/Space, Arrow keys navigate, Enter selects, Esc closes and focus returns to trigger
- Manual keyboard check: Tab order reaches action trigger and member badges with visible focus styling
- Manual keyboard check: badge tooltip content can appear on focus and respects delayed behavior
- Manual regression check: selecting menu member opens settle dialog preselected for that member, and submit flow still succeeds
</verification>

<success_criteria>
- D-10 conflict is resolved via explicit context supersession aligned to sketch winner 006A
- Pattern map includes explicit analog references for interaction decomposition files
- Interaction-heavy dropdown/tooltip behavior is implemented with explicit keyboard accessibility criteria
- Focused automated tests cover keyboard and no-regression settle flow behavior
- Scope remains remaining-work-only for unfinished 04.2 deltas
</success_criteria>

<output>
After completion, create `.planning/phases/04.2-settlement-ui/04.2-08-SUMMARY.md`
</output>
