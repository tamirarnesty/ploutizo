---
phase: "04.2"
plan: "03"
subsystem: "web"
tags: ["sidebar", "navigation", "settlements", "members", "react"]
dependency_graph:
  requires: ["04.2-01"]
  provides: ["SidebarMembersSection", "AppSidebar nav items"]
  affects: ["apps/web/src/components/AppSidebar.tsx", "apps/web/src/components/SidebarMembersSection.tsx"]
tech_stack:
  added: ["@ploutizo/ui/components/badge", "lucide-react icons: BarChart3, Coins, PiggyBank, TrendingUp"]
  patterns: ["route-gated useQuery with enabled flag", "TanStack Query deduplication via shared queryKey", "sidebar collapsed hide via group-data-[collapsible=icon]:hidden"]
key_files:
  created:
    - apps/web/src/components/SidebarMembersSection.tsx
  modified:
    - apps/web/src/components/AppSidebar.tsx
decisions:
  - "OrgMember type uses displayName (not name) and imageUrl (not avatarUrl) — plan had incorrect field names, corrected at implementation"
  - "SidebarMembersSection uses useQuery directly with fetchTransactions + enabled flag (not useGetTransactions which accepts no second options arg)"
  - "navigate({ to: '/dashboard', search: {...} }) pattern for PeriodRangePicker (useNavigate without from constraint avoids TanStack Router search reducer type narrowing)"
metrics:
  duration: "~45 minutes"
  completed_date: "2026-05-08"
  tasks_completed: 2
  files_changed: 9
---

# Phase 4.2 Plan 03: Sidebar MEMBERS Section + Nav Restructure Summary

Restructured `AppSidebar.tsx` with Expenses/Income nav links, three disabled "soon" placeholder items (Budget, Net Worth, Savings), and a new `SidebarMembersSection` component that renders per-member rows with route-driven contextual values — settlement balance on `/dashboard`, expense/income totals on `/expenses`/`/income`, nothing on other routes.

## Tasks

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create SidebarMembersSection component | c140223 | apps/web/src/components/SidebarMembersSection.tsx (+ cross-plan fixes) |
| 2 | Update AppSidebar.tsx — nav items + MEMBERS section | 944b616 | apps/web/src/components/AppSidebar.tsx |

## Verification

All acceptance criteria pass (noted exception: `grep -q ">soon<"` — see Deviations #3).

- `SidebarMembersSection.tsx` exists with MEMBERS label, useGetSettlements, fetchTransactions, useGetOrgMembers, enabled flags, text-success, collapsed hide
- `AppSidebar.tsx` has Expenses/Income nav, Budget/Net Worth/Savings placeholders, Badge import, SidebarMembersSection mounted between nav and Settings groups
- App typechecks cleanly (`pnpm turbo typecheck --filter=web`)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] OrgMember field name correction**
- **Found during:** Task 1 implementation
- **Issue:** Plan specified `member.name` and `member.avatarUrl ?? null` for OrgMember rows. The actual OrgMember interface (packages/types/src/index.ts line 41) uses `displayName` and `imageUrl`. The plan confused OrgMember with SettlementMemberRow.member.
- **Fix:** Used `member.displayName` and `member.imageUrl` throughout SidebarMembersSection.tsx
- **Files modified:** apps/web/src/components/SidebarMembersSection.tsx
- **Commit:** c140223

**2. [Rule 3 - Blocking] Cross-plan lint/type/format errors from Plan 02 work blocked commit**
- **Found during:** Task 1 commit attempt
- **Issues:** Multiple errors from Plan 02 (Dashboard wave) files staged in the working tree:
  - `StatCardPlaceholder.tsx`: ESLint error — `value === null` unnecessary condition (value typed as `string | undefined`)
  - `PeriodRangePicker.tsx`: TypeScript error — `useNavigate({ from: '/_layout/dashboard' })` search reducer type incompatible; `navigate({ search: () => ({...}) })` pattern returned `never` type
  - `Dashboard.tsx`: ESLint import/order error (local imports after external imports)
  - `SettlementSummaryPane.tsx` + `StatCardPlaceholder.tsx`: Prettier formatting violations
  - `routeTree.gen.ts`: Stale (dashboard route `validateSearch` not reflected); caused `PeriodRangePicker.tsx` type errors
- **Fix:**
  - Removed `=== null` from StatCardPlaceholder.tsx
  - Changed `useNavigate({ from: '/_layout/dashboard' })` to `useNavigate()` and `navigate({ to: '/dashboard', search: {...} })`
  - Fixed import order in Dashboard.tsx via ESLint auto-fix
  - Ran `pnpm --filter web build` to regenerate routeTree.gen.ts
  - Ran prettier on affected files
- **Files modified:** StatCardPlaceholder.tsx, PeriodRangePicker.tsx, Dashboard.tsx, SettlementSummaryPane.tsx, routeTree.gen.ts, _layout.dashboard.tsx, dashboardSearch.ts (included from Plan 02 working tree)
- **Commit:** c140223

**3. [Plan Bug] `grep -q ">soon<"` acceptance criterion fails after prettier**
- **Found during:** Task 2 verification
- **Issue:** The plan's acceptance criterion checks for literal string `>soon<` in AppSidebar.tsx. Prettier formats the Badge as multiline JSX (children on new line), so the literal `>soon<` string is not present. The badge content `soon` IS present and renders correctly.
- **Fix:** No code fix needed — the badge text is correct. This is a false acceptance test; the multiline formatted version is equivalent.
- **Action:** Documented only. The functional behavior (badge rendering "soon") is correct.

## Known Stubs

None — SidebarMembersSection displays real data from live queries. Empty states (no members, zero balances) render correctly with no subline.

## Threat Flags

None — no new input boundaries. Sidebar is read-only display using existing tenant-scoped query hooks.

## Self-Check

- [x] `apps/web/src/components/SidebarMembersSection.tsx` exists
- [x] `apps/web/src/components/AppSidebar.tsx` modified
- [x] Task 1 commit c140223 exists
- [x] Task 2 commit 944b616 exists
- [x] Typecheck passes clean
