---
phase: 3.1
slug: transaction-schema-migrations
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-09
---

# Phase 3.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | vitest |
| **Config file** | vitest.config.ts (workspace root) |
| **Quick run command** | `pnpm --filter @ploutizo/db test` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~10 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @ploutizo/db test`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** 30 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 3.1-01-01 | 01 | 1 | §4 Transactions | — | N/A | migration | `pnpm db:migrate` | ❌ W0 | ⬜ pending |
| 3.1-01-02 | 01 | 1 | §4 Transactions | — | N/A | schema-check | `pnpm --filter @ploutizo/db test` | ❌ W0 | ⬜ pending |
| 3.1-02-01 | 02 | 2 | §4 Transactions | — | N/A | unit | `pnpm --filter @ploutizo/validators test` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/db/src/__tests__/schema.test.ts` — stubs verifying table shape post-migration
- [ ] `packages/validators/src/__tests__/transactions.test.ts` — stubs for Zod discriminated union

*Existing test infrastructure covers vitest setup; Wave 0 only needs test file stubs.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Partial index on `deleted_at IS NULL` visible | §4 Transactions | Requires psql `\d transactions` introspection | Run `psql $DATABASE_URL -c "\d transactions"` and confirm `WHERE (deleted_at IS NULL)` partial index |
| Migration runs clean on fresh DB | §4 Transactions | Requires a fresh Neon branch | Create Neon dev branch, run `pnpm db:migrate`, verify exit 0 with no errors |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 30s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
