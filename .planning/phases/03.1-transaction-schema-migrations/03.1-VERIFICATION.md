---
phase: 03.1-transaction-schema-migrations
verified: 2026-04-09T16:27:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Confirm migration 0002_easy_stellaris.sql applied to live Neon database"
    expected: "SELECT tablename FROM pg_tables WHERE schemaname='public' AND tablename IN ('import_batches','transactions','transaction_assignees','transaction_tags') returns all 4 rows. psql \\d transactions shows deleted_at column, partial index transactions_active_idx WHERE deleted_at IS NULL, and composite index on (org_id, account_id)."
    result: "PASSED 2026-04-13 via Neon MCP. All 4 tables present. deleted_at (timestamptz, nullable) and recurring_template_id (uuid, nullable) confirmed. transactions_active_idx (WHERE deleted_at IS NULL) and transactions_org_account_idx (org_id, account_id) both present."
---

# Phase 3.1: Transaction Schema & Migrations Verification Report

**Phase Goal:** The transactions data model is fully defined in the database with all type-specific columns, join tables, and indexes in place. Zod discriminated unions for all 6 transaction types added to `@ploutizo/validators`. This phase is the schema gate that 3.2–3.4 depend on.
**Verified:** 2026-04-09T16:27:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Migration runs cleanly against a fresh database with no errors | ✓ VERIFIED | Confirmed 2026-04-13 via Neon MCP: all 4 tables present (`import_batches`, `transactions`, `transaction_assignees`, `transaction_tags`). `deleted_at` (timestamptz, nullable), `recurring_template_id` (uuid, nullable), `transactions_active_idx` (partial WHERE deleted_at IS NULL), and `transactions_org_account_idx` (org_id, account_id) all confirmed on live DB. |
| 2 | `transactions` table has all 6 type-specific nullable column groups and `deleted_at` | ✓ VERIFIED | `transactions.ts` lines 53–91: expense/refund group (`categoryId`, `refundOf`), income group (`incomeType`, `incomeSource`), transfer group (`toAccountId`), settlement group (`settledAccountId`), contribution group (`investmentType`), plus `deletedAt timestamp`. All nullable. Migration SQL at lines 24–45 confirms all columns. |
| 3 | Partial index on `deleted_at IS NULL` confirmed | ✓ VERIFIED | `transactions.ts` line 99: `index('transactions_active_idx').on(t.deletedAt).where(sql\`deleted_at IS NULL\`)`. Migration SQL line 64: `CREATE INDEX "transactions_active_idx" ON "transactions" USING btree ("deleted_at") WHERE deleted_at IS NULL;` |

**Score:** 3/3 truths verified (Truth 1 requires human confirmation for live DB)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/db/src/schema/import-batches.ts` | importBatches table — org-scoped CSV batch tracking | ✓ VERIFIED | 27-line substantive file. pgTable with id, orgId, source, importedAt, rowCount, createdAt. org_id FK → orgs with CASCADE. Index on orgId. |
| `packages/db/src/schema/transactions.ts` | transactions, transactionAssignees, transactionTags tables | ✓ VERIFIED | 151-line substantive file. Three tables: transactions (20 columns, 6 type-specific nullable groups, soft-delete, 4 indexes), transactionAssignees (5 columns, unique index + tx index), transactionTags (composite PK join table). Self-referential `refundOf` FK uses `AnyPgColumn` lazy arrow. |
| `packages/db/drizzle/0002_easy_stellaris.sql` | Migration SQL for all 4 tables and indexes | ✓ VERIFIED | 65-line file. Creates import_batches, transaction_assignees, transaction_tags, transactions. 7 FK constraints, 7 indexes including partial index WHERE deleted_at IS NULL. |
| `packages/db/drizzle/meta/0002_snapshot.json` | Drizzle schema snapshot metadata | ✓ VERIFIED | File exists. Journal entry idx:2 tag `0002_easy_stellaris` confirmed in `_journal.json`. |
| `packages/db/src/schema/index.ts` | Barrel export including new tables | ✓ VERIFIED | Lines 6–7: `export * from './import-batches.js'` and `export * from './transactions.js'` present. |
| `packages/validators/src/index.ts` | createTransactionSchema, updateTransactionSchema, TransactionFormSchema, 4 type exports | ✓ VERIFIED | Lines 108–195. `createTransactionSchema` is a `z.discriminatedUnion` over all 6 branches. `updateTransactionSchema` is a flat partial. `TransactionFormSchema = createTransactionSchema`. All 4 type exports present. `assigneeSchema` also exported for Phase 3.2. |
| `packages/validators/src/index.test.ts` | 18 new unit tests covering all branches | ✓ VERIFIED | 3 new describe blocks (lines 287–388). All 53 tests pass: `pnpm --filter @ploutizo/validators test` — 1 file, 53 tests, 0 failures. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `packages/db/src/schema/index.ts` | `import-batches.ts` | `export * from './import-batches.js'` | ✓ WIRED | Line 6 confirmed |
| `packages/db/src/schema/index.ts` | `transactions.ts` | `export * from './transactions.js'` | ✓ WIRED | Line 7 confirmed |
| `packages/db/src/index.ts` | `packages/db/src/schema/index.ts` | `export * from './schema/index.js'` | ✓ WIRED | Line 4 confirmed — all schema exports flow through main package entry |
| `transactions.ts` | `import-batches.ts` | `import { importBatches }` | ✓ WIRED | Line 27: `import { importBatches } from './import-batches.js'` — `importBatchId` FK uses it |
| `transactions.ts` | `enums.ts` | `import { transactionTypeEnum, incomeTypeEnum, investmentTypeEnum }` | ✓ WIRED | Line 23. All 3 enums confirmed in `enums.ts` with correct values. |
| Zod validators | `@ploutizo/db` schema | Local const arrays (no import) | ✓ CORRECT | `incomeTypeValues` and `investmentTypeValues` defined as local const in validators — intentional package boundary: validators must remain db-free per CLAUDE.md. Values match enums exactly. |
| `0002_easy_stellaris.sql` | `_journal.json` | idx:2 entry | ✓ TRACKED | Journal entry `{"idx": 2, "tag": "0002_easy_stellaris"}` confirmed |

### Data-Flow Trace (Level 4)

Not applicable for this phase — no components or pages render dynamic data. This phase delivers schema and validators only.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| All 53 validator tests pass | `pnpm --filter @ploutizo/validators test` | 53 passed (0 failed) | ✓ PASS |
| `createTransactionSchema` rejects amount=0 | Covered in test suite line 299 | Test `'rejects amount=0'` passes | ✓ PASS |
| `createTransactionSchema` rejects datetime as date | Covered in test suite line 311 | Test `'rejects ISO datetime as date'` passes | ✓ PASS |
| Income type requires `incomeType` field | Covered in test suite line 352 | Test `'rejects income payload missing incomeType'` passes | ✓ PASS |
| Transfer type requires `toAccountId` field | Covered in test suite line 363 | Test `'rejects transfer payload missing toAccountId'` passes | ✓ PASS |
| `updateTransactionSchema` accepts empty object | Covered in test suite line 377 | Test passes | ✓ PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| §4 Transactions (schema foundation) | 03.1-01, 03.1-02 | Single-table design for all 6 transaction types, join tables, soft-delete, Zod validators | ✓ SATISFIED | Drizzle schema with 4 tables (transactions, transactionAssignees, transactionTags, importBatches), migration 0002, Zod discriminated union over 6 types, 53 passing tests |

**Orphaned requirements:** None. §4 schema foundation is the only requirement scoped to Phase 3.1. The full §4 requirement covers API (Phase 3.2), list UI (Phase 3.3), and forms (Phase 3.4) — those are correctly deferred.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `packages/db/src/schema/transactions.ts` | 94 | `updatedAt: timestamp('updated_at').notNull().defaultNow()` — no ON UPDATE trigger | ⚠️ Warning | `updated_at` will remain frozen at insert time on any UPDATE. Identified in REVIEW.md as WR-01. Acceptable for Phase 3.1 (no UPDATE paths yet); Phase 3.2 API must set `updatedAt: new Date()` in every update call explicitly, or add a migration with a Postgres trigger. |
| `packages/db/src/schema/transactions.ts` | 47 | `amount: integer('amount').notNull()` — no CHECK constraint | ⚠️ Warning | No database-level enforcement of the "unsigned cents" invariant. Identified in REVIEW.md as WR-02. Zod validation enforces `z.number().int().positive()` at the API boundary, but a raw SQL insert or a bug in the API could silently insert zero/negative amounts. |
| `packages/validators/src/index.ts` | 179–190 | `updateTransactionSchema` is a flat partial with no discriminated-union enforcement | ⚠️ Warning | Cross-type field pollution (e.g. `toAccountId` on an income transaction) is not blocked at the Zod layer. Identified in REVIEW.md as WR-03. Acceptable by design for v1 PATCH semantics; API layer must enforce type compatibility explicitly. |
| `packages/validators/src/index.ts` | 101 | `parseFloat(v) >= 0` with message `'Must be a positive number.'` — zero is accepted but message says "positive" | ℹ️ Info | Misleading error message on `HouseholdSettingsFormSchema`. Pre-existing issue, not introduced in Phase 3.1. Identified in REVIEW.md as IN-01. |
| `packages/db/src/schema/transactions.ts` | 99 | `sql\`deleted_at IS NULL\`` — raw SQL string, column rename not caught by TypeScript | ℹ️ Info | Known Drizzle limitation. Identified in REVIEW.md as IN-02. Low risk if column name is stable. |

No anti-patterns are blockers for Phase 3.1 goals. All identified issues are warnings (deferred to Phase 3.2) or info-level. No stubs, no placeholders, no empty implementations found.

### Human Verification Required

#### 1. Live Neon Database State

**Test:** Connect to Neon via `psql $DATABASE_URL` (from `apps/api/.env`) and run:
```sql
SELECT tablename FROM pg_tables WHERE schemaname = 'public' AND tablename IN ('import_batches', 'transactions', 'transaction_assignees', 'transaction_tags');
\d transactions
```
**Expected:**
- All 4 table names returned by the SELECT
- `\d transactions` shows `deleted_at` column (timestamp with time zone, nullable), `recurring_template_id` uuid column (no FK), and the index `transactions_active_idx` with `WHERE deleted_at IS NULL`
- `(org_id, account_id)` composite index present

**Why human:** The migration file and journal confirm it was generated and tracked. The SUMMARY documents it was applied successfully. But live Neon DB state cannot be confirmed programmatically from this environment — direct psql or Drizzle Studio access required to rule out a failed apply that was not caught.

### Gaps Summary

No gaps blocking Phase 3.1 goal achievement. All required schema artifacts exist with substantive implementations. All key exports are wired through the package barrel. 53 tests pass.

Three warnings from the code review (WR-01 through WR-03) are notable issues but are not blockers for this phase — they are deferred constraints that Phase 3.2 must address when it builds UPDATE paths and API handlers. These are tracked in `03.1-REVIEW.md`.

One human verification item blocks `status: passed` — the live Neon database state cannot be confirmed without direct DB access. All automated checks pass.

---

_Verified: 2026-04-09T16:27:00Z_
_Verifier: Claude (gsd-verifier)_
