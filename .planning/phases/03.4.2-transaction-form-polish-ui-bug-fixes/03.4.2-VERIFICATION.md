---
phase: 03.4.2-transaction-form-polish-ui-bug-fixes
verified: 2026-04-24T16:30:00Z
status: human_needed
score: 5/5 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Edit mode amount recalculation — change the Amount field on an existing transaction and confirm assignee split amounts update immediately without toggling members"
    expected: "Each assignee's amountCents is recalculated via lrmSplit on every amount change after the first render"
    why_human: "useEffect with isInitialRender guard cannot be exercised programmatically without mounting the component with a live TanStack Form context"
  - test: "Notes + Tags layout — open the transaction form and verify Notes textarea renders full-width above the Tags field (not side-by-side)"
    expected: "Notes and Tags are stacked vertically; Notes textarea is at least 3 rows tall"
    why_human: "Visual layout; rows={3} and flex-col are confirmed in code but rendered height and stacking require visual check"
  - test: "Enter key creates tag — type a new tag name in the transaction form tag picker, highlight the 'Create' option with keyboard navigation, press Enter"
    expected: "Tag is created without requiring a mouse click"
    why_human: "Keyboard event dispatch through the Base UI Combobox requires interactive browser verification"
  - test: "Category edit reflects immediately — edit a category's name/colour/icon and confirm the change appears in the CategoriesSettings list without a page reload"
    expected: "Category list updates as soon as the mutation settles (query invalidation refetch populates localCategories)"
    why_human: "Requires a live mutation + TanStack Query invalidation cycle; cannot be verified by static analysis"
---

# Phase 03.4.2: Transaction Form Polish & UI Bug Fixes — Verification Report

**Phase Goal:** Fix four targeted bugs surfaced after phase 03.4.1: (1) edit-mode amount change doesn't recalculate assignee splits, (2) Notes/Tags layout is cramped equal-width flex row, (3) Enter key on "Create" tag option does nothing, (4) category edits don't reflect in the list without a page reload.
**Verified:** 2026-04-24T16:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | In edit mode, changing the Amount field causes assignee split amountCents values to recalculate via lrmSplit | ✓ VERIFIED | `isInitialRender = useRef(true)` at line 61; effect at lines 62-71 skips first render only, then fires on `amountCents` change without `!transaction` guard; `useRef` in import line 1 |
| 2 | Notes field is rendered full-width above the Tags field (stacked, not side-by-side) | ✓ VERIFIED | `className="flex flex-col gap-4"` at TransactionForm.tsx:505; no `flex-1` on Notes Field or Tags Field wrappers inside that section |
| 3 | Notes textarea has at least 3 rows minimum height | ✓ VERIFIED | `rows={3}` at TransactionForm.tsx:513 |
| 4 | Pressing Enter while the "Create" ComboboxItem is highlighted creates the tag | ✓ VERIFIED | `filteredItems={createOption ? [...filteredTagNames, createOption] : filteredTagNames}` at TransactionTagPicker.tsx:101 — createOption is included in keyboard-navigable items |
| 5 | After editing a category, the updated category is visible in CategoriesSettings list immediately | ✓ VERIFIED | `initialized` ref and guard removed; useEffect at CategoriesSettings.tsx:74-78 runs `setLocalCategories(categories)` on every `categories` refetch with no initialization guard; `useRef` absent from imports (line 1: only `useEffect, useState`) |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/transactions/AssigneeSection.tsx` | isInitialRender ref replaces !transaction guard | ✓ VERIFIED | Lines 61-71: correct implementation |
| `apps/web/src/components/transactions/TransactionForm.tsx` | flex-col stacked Notes+Tags, rows=3 | ✓ VERIFIED | Lines 504-542: correct layout and rows |
| `apps/web/src/components/transactions/TransactionTagPicker.tsx` | filteredItems includes createOption | ✓ VERIFIED | Line 101: correct conditional spread |
| `apps/web/src/components/settings/CategoriesSettings.tsx` | initialized ref removed, sync effect ungated | ✓ VERIFIED | Lines 74-78: effect runs on refetch; no useRef in imports |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| AssigneeSection amountCents effect | lrmSplit | useEffect dependency on amountCents | ✓ WIRED | Effect calls `onChange(lrmSplit(amountCents, pressedMemberIds))` at line 68 |
| TransactionForm amount field | AssigneeSection amountCents | form.Subscribe + Math.round | ✓ WIRED | Lines 546-572: amount subscribes and passes `Math.round((amount ?? 0) * 100)` to AssigneeSection |
| TransactionTagPicker createOption | filteredItems keyboard nav | conditional spread | ✓ WIRED | Line 101: createOption included when non-null |
| CategoriesSettings categories query | localCategories state | ungated useEffect | ✓ WIRED | Lines 74-78: effect runs on every categories + catLoading change |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| AssigneeSection | amountCents | TransactionForm form.Subscribe on `amount` field | Yes — form state derived from user input | ✓ FLOWING |
| CategoriesSettings | localCategories | useGetCategories() → TanStack Query → API | Yes — DB-backed query, invalidated on mutation | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — all four bugs involve interactive browser behavior (form state changes, keyboard events, mutation/refetch cycles) that cannot be exercised with static command-line checks.

### Requirements Coverage

No requirement IDs declared for this phase (requirements: [] in PLAN frontmatter). The four phase goals map directly to the four must-haves, all verified above.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| TransactionTagPicker.tsx | 26 | Comment still says "real tags only — create option excluded" (stale after fix) | ℹ️ Info | Misleading doc comment; does not affect runtime behavior |

The comment at TransactionTagPicker.tsx line 26 says `filteredItems` excludes the create option, but the implementation now includes it. Stale doc comment only — no behavioral impact.

### Human Verification Required

#### 1. Edit-mode amount recalculation

**Test:** Open an existing transaction in edit mode. Note the current assignee split amounts. Change the Amount field value. Without toggling any assignee toggles, verify that each assignee's displayed split amount updates to reflect the new total.
**Expected:** Splits recalculate immediately via lrmSplit on every subsequent amount change.
**Why human:** useEffect with isInitialRender guard cannot be verified without a live React + TanStack Form mount.

#### 2. Notes + Tags visual layout

**Test:** Open the transaction form (create or edit). Verify the Notes textarea appears full-width above the Tags combobox, not side-by-side in equal-width columns.
**Expected:** Stacked vertical layout; Notes textarea is visibly 3 rows tall.
**Why human:** Visual layout and rendered row height require browser rendering.

#### 3. Enter key creates tag

**Test:** In the transaction form Tags field, type a new tag name that doesn't exist. Use the arrow keys to highlight the "Create" option in the dropdown. Press Enter.
**Expected:** Tag is created and added to the field without clicking.
**Why human:** Keyboard event propagation through Base UI Combobox requires interactive browser testing.

#### 4. Category edit reflects immediately

**Test:** In Settings > Categories, click Edit on any category. Change the name or colour. Save. Verify the updated name/colour appears immediately in the list without refreshing the page.
**Expected:** List updates as soon as the dialog closes (TanStack Query invalidation refetches and the ungated useEffect sets localCategories).
**Why human:** Requires a live mutation cycle + query invalidation + React re-render to confirm.

### Gaps Summary

No gaps. All five must-haves have correct implementations in the codebase. One stale doc comment (TransactionTagPicker.tsx:26) is informational only.

Status is `human_needed` because the four bug fixes all involve interactive runtime behavior (form state, keyboard events, mutation cycles) that require a browser to confirm the wiring actually produces the expected UX outcome.

---

_Verified: 2026-04-24T16:30:00Z_
_Verifier: Claude (gsd-verifier)_
