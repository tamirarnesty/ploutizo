---
phase: 03.4.4-app-shell-resilience-404-page-error-boundary-inserted
verified: 2026-04-30T00:00:00Z
status: human_needed
score: 6/6 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Navigate to /nonexistent-route in the running app"
    expected: "404 page renders — status bar shows '● STATUS — 404' and 'ploutizo/~/missing', dot-art block visible at low opacity, ERROR · 0x404 label, 'Page not found' heading, subtext, and 'Go to Dashboard' button present. No blank screen."
    why_human: "TanStack Router route-miss dispatch to notFoundComponent is runtime behavior; code wiring is verified but render outcome requires a browser."
  - test: "Trigger an unhandled render error (temporarily throw in a mounted component)"
    expected: "ErrorBoundary renders — status bar shows '● STATUS — ERROR' and 'ploutizo/~/error', ERR art block at opacity, ERROR · 0x500 label, 'Something went wrong' heading, 'Try again' and 'Go to Dashboard' buttons. 'Try again' calls reset() and recovers the tree. No blank screen."
    why_human: "errorComponent dispatch is runtime; reset() recovery is an interaction requiring a real browser session."
  - test: "With dev server running, inspect the error detail section after triggering an error"
    expected: "error.message rendered as monospace text below buttons. 'Show details' button visible, collapsed by default. Clicking expands a scrollable <pre> with error.stack (max-h-[200px]). Clicking 'Hide details' collapses it."
    why_human: "import.meta.env.DEV toggle is a runtime conditional and the expand/collapse interaction requires human click verification."
  - test: "Run a production build (pnpm turbo build) and confirm ErrorBoundary output"
    expected: "Nothing rendered below the action buttons — no error.message, no 'Show details' toggle, no stack trace. Vite tree-shakes the import.meta.env.DEV block."
    why_human: "Production strip of the DEV block requires a production build and can't be confirmed from source alone."
  - test: "Visual inspection — aesthetic match to design spec"
    expected: "Status bar, dot-art at opacity-40, monospace error label, h1 heading, body-sm subtext, and action buttons all conform to the terminal/dot-art visual contract. Both pages are full-page centered with no sidebar."
    why_human: "Visual correctness — font rendering, spacing, opacity effect — is not verifiable from code."
---

# Phase 03.4.4: App Shell Resilience — 404 & Error Boundary Verification Report

**Phase Goal:** App shell resilience — users never see a blank screen on unknown routes (404) or unhandled render errors (error boundary). Both pages render the terminal/dot-art aesthetic.
**Verified:** 2026-04-30
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Navigating to an unknown route renders the 404 page (never a blank screen) | ✓ VERIFIED (code) | `__root.tsx` line 111: `notFoundComponent: NotFound` — NotFound imported at line 18 from `../components/not-found/NotFound` |
| 2 | An unhandled render error renders the error boundary (never a blank screen) | ✓ VERIFIED (code) | `__root.tsx` line 112: `errorComponent: ErrorBoundary` — ErrorBoundary imported at line 19 |
| 3 | 404 page shows terminal/dot-art aesthetic with status bar, art block, label, heading, subtext, and action button | ✓ VERIFIED (code) | NotFound.tsx: status bar (`● STATUS — 404` / `ploutizo/~/missing`), `ART_404` in `<pre opacity-40>`, `ERROR · 0x404` label, `<Text as="h1">Page not found</Text>`, body-sm subtext, "Go to Dashboard" Button |
| 4 | Error boundary shows same aesthetic with 'Try again' and 'Go to Dashboard' buttons | ✓ VERIFIED (code) | ErrorBoundary.tsx: matching structure with ART_ERR, `ERROR · 0x500`, "Something went wrong" h1, `<Button onClick={reset}>Try again</Button>`, `<Button render={<Link to="/dashboard" />} variant="outline">Go to Dashboard</Button>` |
| 5 | Dev mode expands error.message + collapsible error.stack; production shows nothing below buttons | ✓ VERIFIED (code) | ErrorBoundary.tsx line 60: `{import.meta.env.DEV && (...)}`; `useState(false)` at line 15 controls toggle; expanded `<pre>` with `max-h-[200px] overflow-y-auto` at line 73 |
| 6 | Both pages render standalone (no sidebar, full-page centered layout) | ✓ VERIFIED (code) | Both components: `flex min-h-screen flex-col items-center justify-center bg-background px-6` outer wrapper with `w-full max-w-lg` content column; no sidebar/nav/layout imports |

**Score:** 6/6 truths verified at code level

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/web/src/components/not-found/NotFound.tsx` | 404 not-found component; exports `NotFound` | VERIFIED | 53 lines; exports `const NotFound = () => { ... }` at line 12; all spec elements present |
| `apps/web/src/components/error-boundary/ErrorBoundary.tsx` | Root error boundary component; exports `ErrorBoundary` | VERIFIED | 82 lines; exports `const ErrorBoundary = ({ error, reset }: ErrorComponentProps)` at line 14; `useState` toggle, `import.meta.env.DEV` gate |
| `apps/web/src/routes/__root.tsx` | Root route with notFoundComponent and errorComponent wired | VERIFIED | Lines 111–112: `notFoundComponent: NotFound` and `errorComponent: ErrorBoundary` inside `createRootRoute({...})`; all existing logic (beforeLoad, head, shellComponent) untouched |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `apps/web/src/routes/__root.tsx` | `apps/web/src/components/not-found/NotFound.tsx` | `notFoundComponent` on `createRootRoute` | WIRED | Import at line 18; used at line 111 |
| `apps/web/src/routes/__root.tsx` | `apps/web/src/components/error-boundary/ErrorBoundary.tsx` | `errorComponent` on `createRootRoute` | WIRED | Import at line 19; used at line 112 |

### Data-Flow Trace (Level 4)

Not applicable. Both components render static copy + one receives `ErrorComponentProps` (`error`, `reset`) from TanStack Router's error boundary mechanism — no API calls or DB queries involved.

### Behavioral Spot-Checks

Step 7b: SKIPPED. Both components require a running dev server and browser interaction. Runtime checks without a server (curl, node module imports) are not applicable to React components wired into TanStack Router.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| Add notFoundComponent to createRootRoute | 03.4.4-01 | notFoundComponent key on root route | SATISFIED | `__root.tsx` line 111 |
| Add errorComponent to createRootRoute | 03.4.4-01 | errorComponent key on root route | SATISFIED | `__root.tsx` line 112 |
| 404 page matches terminal/dot-art design spec | 03.4.4-01 | Status bar, art, label, heading, subtext, action button | SATISFIED (code) / NEEDS HUMAN (visual) | All structural elements present; visual match requires browser |
| Error boundary matches design spec with dev mode detail | 03.4.4-01 | Same aesthetic + dev toggle | SATISFIED (code) / NEEDS HUMAN (runtime) | `import.meta.env.DEV` gate and toggle wired; runtime behavior needs browser |

### Anti-Patterns Found

No anti-patterns detected.

- No TODO/FIXME/placeholder comments in any of the three modified files
- No hardcoded colors — only design tokens: `bg-background`, `text-muted-foreground`, `border-border`, `bg-muted`
- No `forwardRef` (React 19 compliance)
- No `return null` / empty stub patterns
- No raw `fetch()` calls

### Notable Deviation (Accepted)

**Button navigation pattern:** Plan suggested `<Link to="/dashboard"><Button>...</Button></Link>` as the primary pattern. Implementation uses `<Button render={<Link to="/dashboard" />}>`. This is the ReUI/Base UI render-prop slot pattern — the modern equivalent of `asChild`, and explicitly allowed by the plan ("use `asChild` prop if Button supports it"). Not a gap.

### Human Verification Required

#### 1. 404 Page Render — Unknown Route

**Test:** Start the dev server and navigate to `/nonexistent-route` (or any URL not in the router config)
**Expected:** Full 404 page renders: status bar with `● STATUS — 404` on the left and `ploutizo/~/missing` on the right, dot-art block visible at reduced opacity, `ERROR · 0x404` error label, `Page not found` heading, subtext "This page wandered off. It may have moved or never existed.", and a "Go to Dashboard" button that navigates to `/dashboard`.
**Why human:** TanStack Router notFoundComponent dispatch is runtime behavior; route-miss handling can't be exercised without a running router instance.

#### 2. Error Boundary Render — Unhandled Render Error

**Test:** Temporarily add `throw new Error("test error")` inside a mounted component, load that route, observe the error boundary, then click "Try again"
**Expected:** ErrorBoundary renders with `● STATUS — ERROR`, ERR art block, `ERROR · 0x500`, "Something went wrong" heading, "Try again" and "Go to Dashboard" buttons. "Try again" calls `reset()` and recovers the component tree. "Go to Dashboard" navigates to `/dashboard`.
**Why human:** errorComponent dispatch and reset() interaction require real React rendering and user clicks.

#### 3. Dev Mode Error Detail Toggle

**Test:** With the error boundary triggered in dev mode, observe the section below action buttons and click "Show details"
**Expected:** `error.message` visible as monospace text below buttons. "Show details" button present. Clicking expands a scrollable `<pre>` containing `error.stack` (max height 200px, scrollable). Clicking "Hide details" collapses it back.
**Why human:** import.meta.env.DEV is a compile-time constant visible to runtime only; the toggle is an interaction requiring a real browser click.

#### 4. Production Build — No Dev Detail Leak

**Test:** Run `pnpm turbo build` then preview the build; trigger an error and inspect the ErrorBoundary output
**Expected:** Nothing rendered below the action buttons — no `error.message` text, no toggle button, no stack trace. The `import.meta.env.DEV` block is tree-shaken by Vite.
**Why human:** Production strip requires a production build artifact, not inspectable from source.

#### 5. Visual Aesthetic Match

**Test:** Compare both pages visually against the UI-SPEC.md design contract
**Expected:** Terminal/dot-art aesthetic — monospace status bar, ghost art at opacity-40, monospace error label, typographic hierarchy (h1 → body-sm), button spacing. No sidebar or app chrome visible on either page.
**Why human:** Font rendering, spacing, and opacity effects are visual properties not verifiable from Tailwind class names alone.

### Gaps Summary

No code gaps found. All artifacts exist, are substantive, and are wired correctly. Phase goal is achievable per code evidence. Five human verification items remain to confirm runtime behavior and visual fidelity.

---

_Verified: 2026-04-30_
_Verifier: Claude (gsd-verifier)_
