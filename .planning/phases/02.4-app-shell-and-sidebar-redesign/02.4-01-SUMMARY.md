---
plan: "02.4-01"
phase: "02.4"
status: checkpoint_pending
checkpoint_type: human-verify
tasks_complete: 2
tasks_total: 3
completed: ""
---

# Plan 02.4-01 Summary: App Shell and Sidebar Rebuild

## What Was Built

Rebuilt the app shell and sidebar into their final form. Tasks 1 and 2 are complete; Task 3 is a human visual verification checkpoint.

### Task 1: AppSidebar.tsx rebuilt
- Added `collapsible="icon"` to the Sidebar element — enables icon rail collapse
- SidebarHeader content wrapped in `group-data-[collapsible=icon]:hidden` so the entire header (Ploutizo wordmark + OrganizationSwitcher) disappears in icon-only mode
- Removed `settingsItems` array and settings SidebarGroup from sidebar content
- Primary nav items (Dashboard, Accounts) now use `tooltip={label}` on SidebarMenuButton
- SidebarFooter rebuilt: left cluster has ThemeToggle + Settings footer link; right has SidebarTrigger collapse toggle
- UserButton removed from AppSidebar (moves to TopBar)

### Task 2: TopBar.tsx created + _layout.tsx restructured
- Created `apps/web/src/components/TopBar.tsx`: slim `h-10` bar with `border-b border-border bg-background`, mobile-only `SidebarTrigger className="md:hidden"`, spacer div, and UserButton pinned right
- Restructured `apps/web/src/routes/_layout.tsx`: SidebarProvider wraps entire shell; inner `flex flex-col h-screen w-full` div puts TopBar above the sidebar+content row; old `h-14` header removed
- Deleted dead file `apps/web/src/components/layout/LayoutShell.tsx` (no importers)

## Key Files

### Created
- `apps/web/src/components/TopBar.tsx` — Slim top bar: mobile hamburger + UserButton

### Modified
- `apps/web/src/components/AppSidebar.tsx` — Collapsible icon rail, footer settings link
- `apps/web/src/routes/_layout.tsx` — SidebarProvider shell with TopBar above sidebar+content

### Deleted
- `apps/web/src/components/layout/LayoutShell.tsx` — Dead code, removed

## Commits
- `36d1bcf` — feat(02.4-01): rebuild AppSidebar with collapsible icon rail and footer settings link
- `91dc8f7` — feat(02.4-01): create TopBar and restructure _layout.tsx shell with SidebarProvider

## Checkpoint Pending

Task 3 requires human visual verification in the browser. See plan for full verification checklist.

## Self-Check

All acceptance criteria verified:
- `collapsible="icon"` present on Sidebar element ✓
- `group-data-[collapsible=icon]:hidden` on SidebarHeader content wrapper ✓
- `isSettingsActive` uses `location.pathname.startsWith("/settings")` ✓
- `settingsItems` array removed ✓
- `UserButton` absent from AppSidebar ✓
- `SidebarTrigger` in SidebarFooter ✓
- `tooltip={label}` on primary nav SidebarMenuButtons ✓
- `tooltip="Settings"` on footer Settings SidebarMenuButton ✓
- `TopBar.tsx` exists with `h-10 border-b border-border bg-background` ✓
- `SidebarTrigger className="md:hidden"` in TopBar ✓
- `_layout.tsx` has SidebarProvider wrapping entire shell ✓
- `_layout.tsx` does not contain `h-14` ✓
- `LayoutShell.tsx` deleted ✓
- No TypeScript errors in changed files ✓
