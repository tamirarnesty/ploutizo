---
plan: "02.4-03"
phase: "02.4"
status: complete
gap_closure: true
tasks_complete: 3
tasks_total: 3
completed: "2026-04-08"
---

# Plan 02.4-03 Summary: TopBar + AppSidebar + Layout Restructure

Gap closure plan applying approved mockup corrections.

## What Was Built

### TopBar.tsx — full rebuild
- Added Ploutizo wordmark (`<span>`) and `OrganizationSwitcher` into the top bar
- Mobile `SidebarTrigger` is now left of the wordmark (`md:hidden`)
- `UserButton showName` renders full name next to avatar
- Replaced `bg-background border-b border-border` with `bg-sidebar relative z-20`
  - `bg-sidebar` matches the sidebar background (same chrome layer)
  - `relative z-20` ensures TopBar paints above the sidebar's `fixed inset-y-0 z-10` container

### AppSidebar.tsx — inset variant + Settings in content
- Added `variant="inset"` — SidebarInset automatically gets `rounded-xl shadow-sm` via peer CSS
- Removed `SidebarHeader` entirely (wordmark + org switcher moved to TopBar)
- Removed `OrganizationSwitcher` import
- Settings link moved from `SidebarFooter` to bottom of `SidebarContent` via `SidebarGroup className="mt-auto"`
- `SidebarFooter` now only contains `ThemeToggle` (left) + `SidebarTrigger` (right)

### _layout.tsx — simplified shell
- Removed extra inner `flex flex-col h-screen w-full` wrapper div
- `SidebarProvider className="flex-col"` overrides default flex-row so TopBar stacks above the sidebar+inset row
- Cleaner tree: `SidebarProvider > TopBar | (flex row: AppSidebar + SidebarInset)`

## Commits
- `75164a2` — feat(02.4-03): rebuild TopBar with wordmark, org switcher, full-name UserButton
- `7aebdd5` — feat(02.4-03): rebuild AppSidebar with inset variant, Settings in content, clean footer
- `38ee727` — feat(02.4-03): simplify LayoutShell — SidebarProvider flex-col wraps TopBar + sidebar row
