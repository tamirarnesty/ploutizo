---
phase: "03.4"
plan: "19"
type: execute
wave: 12
depends_on: ["03.4-17", "03.4-18"]
files_modified:
  - apps/web/src/components/transactions/TransactionTypeFields.tsx
  - apps/web/src/components/transactions/ExpenseFields.tsx
  - apps/web/src/components/transactions/TransferFields.tsx
  - apps/web/src/components/transactions/IncomeFields.tsx
  - apps/web/src/components/transactions/RefundFields.tsx
  - apps/web/src/components/transactions/ContributionFields.tsx
  - apps/web/src/components/transactions/SettlementFields.tsx
  - apps/web/src/components/transactions/AssigneeRow.tsx
  - apps/web/src/lib/data-access/transactions/queries.ts
  - apps/web/src/lib/data-access/transactions/useGetTransaction.ts
  - apps/web/src/lib/data-access/transactions/useGetTransactions.ts
  - apps/web/src/lib/data-access/transactions/useSearchTransactions.ts
  - apps/web/src/lib/data-access/transactions/index.ts
autonomous: true
gap_closure: true
requirements: ["§4 Transactions (create/edit forms)"]
must_haves:
  truths:
    - "No ReturnType<typeof ...> usage in any transaction component file — all use TransactionFormInstance"
    - "No {' '} whitespace literals in JSX labels in type-specific field files"
    - "AssigneeRow uses shared Input props const — no duplicated type/autoComplete/className"
    - "fetch* utility functions are in queries.ts; hook files import from queries.ts"
  artifacts:
    - path: "apps/web/src/lib/data-access/transactions/queries.ts"
      provides: "fetchTransaction, fetchTransactions, fetchSearchTransactions"
      contains: "fetchTransaction\|fetchTransactions\|fetchSearchTransactions"
    - path: "apps/web/src/components/transactions/AssigneeRow.tsx"
      provides: "shared Input props const"
      contains: "sharedInputProps"
  key_links:
    - from: "apps/web/src/lib/data-access/transactions/useGetTransaction.ts"
      to: "apps/web/src/lib/data-access/transactions/queries.ts"
      via: "import fetchTransaction"
      pattern: "from './queries'"
    - from: "apps/web/src/components/transactions/ExpenseFields.tsx"
      to: "apps/web/src/components/transactions/hooks/useTransactionForm.ts"
      via: "TransactionFormInstance import"
      pattern: "TransactionFormInstance"
---

<objective>
Code quality sweep: replace all remaining `ReturnType<typeof useTransactionForm>['form']` usages with `TransactionFormInstance`; fix `{' '}` whitespace literals in type-specific field files; extract shared Input props in AssigneeRow; move fetch* utilities to a shared queries.ts.

Purpose: Depends on Plans 17 (TransactionFormInstance exported) and 18 (TransactionForm.tsx no longer has {' '}). Closes Gaps 9 (Part B), 11, 12, and 13 (type-specific files).
Output: Clean type usage and code structure across all transaction components and data-access files.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/PROJECT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace ReturnType usages in 7 type-specific field components + fix {' '} in 4 of them</name>
  <files>
    apps/web/src/components/transactions/TransactionTypeFields.tsx
    apps/web/src/components/transactions/ExpenseFields.tsx
    apps/web/src/components/transactions/TransferFields.tsx
    apps/web/src/components/transactions/IncomeFields.tsx
    apps/web/src/components/transactions/RefundFields.tsx
    apps/web/src/components/transactions/ContributionFields.tsx
    apps/web/src/components/transactions/SettlementFields.tsx
  </files>
  <read_first>
    - apps/web/src/components/transactions/TransactionTypeFields.tsx (full file)
    - apps/web/src/components/transactions/ExpenseFields.tsx (lines 1–15)
    - apps/web/src/components/transactions/TransferFields.tsx (lines 1–15)
    - apps/web/src/components/transactions/IncomeFields.tsx (lines 1–25, 60–68)
    - apps/web/src/components/transactions/RefundFields.tsx (lines 1–20, 25–33)
    - apps/web/src/components/transactions/ContributionFields.tsx (lines 1–25, 28–34)
    - apps/web/src/components/transactions/SettlementFields.tsx (lines 1–25, 21–27)
  </read_first>
  <action>
    Apply the same two-part change to all 7 files:

    **Part A — Replace ReturnType import and usage:**

    In each file, the current import pattern is:
    ```ts
    import type { useTransactionForm } from './hooks/useTransactionForm'
    ```
    And the prop type is:
    ```ts
    form: ReturnType<typeof useTransactionForm>['form']
    ```

    Replace with:
    ```ts
    import type { TransactionFormInstance } from './hooks/useTransactionForm'
    ```
    And:
    ```ts
    form: TransactionFormInstance
    ```

    Apply to all 7 files:
    - TransactionTypeFields.tsx (line 15 import, line 34 prop)
    - ExpenseFields.tsx (line 10 import, line 13 prop)
    - TransferFields.tsx (line 10 import, line 13 prop)
    - IncomeFields.tsx (line 11 import, line 22 prop)
    - RefundFields.tsx (line 13 import, line 16 prop)
    - ContributionFields.tsx (line 10 import, line 22 prop)
    - SettlementFields.tsx (line 11 import, line 14 prop)

    Note: RefundLinker.tsx was already updated in Plan 17.

    **Part B — Fix {' '} whitespace literals in 4 files (Gap 13):**

    In each file that has `{' '}` before an optional badge, replace `FieldName{' '}<Text ...>` with `FieldName<Text className="ml-1 ...">`:

    **SettlementFields.tsx** (line 23):
    ```tsx
    // Before:
    Settlement account{' '}
    <Text as="span" variant="body-sm" className="font-normal text-muted-foreground">

    // After:
    Settlement account
    <Text as="span" variant="body-sm" className="ml-1 font-normal text-muted-foreground">
    ```

    **ContributionFields.tsx** (line 30):
    ```tsx
    // Before:
    Investment type{' '}
    <Text as="span" variant="body-sm" className="font-normal text-muted-foreground">

    // After:
    Investment type
    <Text as="span" variant="body-sm" className="ml-1 font-normal text-muted-foreground">
    ```

    **RefundFields.tsx** (line 27):
    ```tsx
    // Before:
    Category{' '}
    <Text as="span" variant="body-sm" className="font-normal text-muted-foreground">

    // After:
    Category
    <Text as="span" variant="body-sm" className="ml-1 font-normal text-muted-foreground">
    ```

    **IncomeFields.tsx** (line 64):
    ```tsx
    // Before:
    Income source{' '}
    <Text as="span" variant="body-sm" className="font-normal text-muted-foreground">

    // After:
    Income source
    <Text as="span" variant="body-sm" className="ml-1 font-normal text-muted-foreground">
    ```

    TransactionTypeFields.tsx and ExpenseFields.tsx and TransferFields.tsx have no {' '} to fix — only the ReturnType replacement applies.
  </action>
  <verify>
    <automated>grep -rn "ReturnType<typeof useTransactionForm>" /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/</automated>
  </verify>
  <acceptance_criteria>
    - Zero occurrences of `ReturnType<typeof useTransactionForm>` in apps/web/src/components/transactions/ (RefundLinker.tsx was fixed in Plan 17)
    - All 7 files import `TransactionFormInstance` from `./hooks/useTransactionForm`
    - Zero `{' '}` literals in SettlementFields.tsx, ContributionFields.tsx, RefundFields.tsx, IncomeFields.tsx
    - Each of those 4 files has `ml-1` added to the optional badge Text span
  </acceptance_criteria>
  <done>All ReturnType usages replaced with TransactionFormInstance; {' '} fixed in 4 type-specific files</done>
</task>

<task type="auto">
  <name>Task 2: Extract fetch* to queries.ts + deduplicate AssigneeRow Input props</name>
  <files>
    apps/web/src/lib/data-access/transactions/queries.ts
    apps/web/src/lib/data-access/transactions/useGetTransaction.ts
    apps/web/src/lib/data-access/transactions/useGetTransactions.ts
    apps/web/src/lib/data-access/transactions/useSearchTransactions.ts
    apps/web/src/lib/data-access/transactions/index.ts
    apps/web/src/components/transactions/AssigneeRow.tsx
  </files>
  <read_first>
    - apps/web/src/lib/data-access/transactions/useGetTransaction.ts (full file)
    - apps/web/src/lib/data-access/transactions/useGetTransactions.ts (lines 71–97)
    - apps/web/src/lib/data-access/transactions/useSearchTransactions.ts (full file)
    - apps/web/src/lib/data-access/transactions/index.ts (full file)
    - apps/web/src/components/transactions/AssigneeRow.tsx (lines 43–76)
  </read_first>
  <action>
    **Part A — Create queries.ts with all three fetch utilities:**

    Create `apps/web/src/lib/data-access/transactions/queries.ts`:
    ```ts
    import { apiFetch } from '@/lib/queryClient'
    import type { TransactionRow, TransactionListResponse, TransactionQueryParams } from './useGetTransactions'

    export const fetchTransaction = async (id: string): Promise<TransactionRow> => {
      const r = await apiFetch<{ data: TransactionRow }>(`/api/transactions/${id}`)
      return r.data
    }

    export const fetchTransactions = async (
      params: TransactionQueryParams
    ): Promise<TransactionListResponse> => {
      const qs = new URLSearchParams()
      qs.set('page', String(params.page))
      qs.set('limit', String(params.limit))
      qs.set('sort', params.sort)
      qs.set('order', params.order)
      if (params.type) qs.set('type', params.type)
      if (params.dateFrom) qs.set('dateFrom', params.dateFrom)
      if (params.dateTo) qs.set('dateTo', params.dateTo)
      if (params.accountId) qs.set('accountId', params.accountId)
      if (params.categoryId) qs.set('categoryId', params.categoryId)
      if (params.assigneeId) qs.set('assigneeId', params.assigneeId)
      if (params.tagIds) qs.set('tagIds', params.tagIds)
      if (params.description) qs.set('description', params.description)
      return apiFetch<TransactionListResponse>(`/api/transactions?${qs.toString()}`)
    }

    export const fetchSearchTransactions = async (description: string): Promise<TransactionRow[]> => {
      const qs = new URLSearchParams()
      qs.set('description', description)
      qs.set('limit', '20')
      const r = await apiFetch<TransactionListResponse>(`/api/transactions?${qs.toString()}`)
      return r.data
    }
    ```

    **Part B — Update hook files to import from queries.ts:**

    `useGetTransaction.ts` — remove `fetchTransaction` function body, add import:
    ```ts
    import { useQuery } from '@tanstack/react-query'
    import type { UseQueryResult } from '@tanstack/react-query'
    import type { TransactionRow } from './useGetTransactions'
    import { fetchTransaction } from './queries'

    export const useGetTransaction = (
      id: string | null,
      options?: { initialData?: TransactionRow }
    ): UseQueryResult<TransactionRow> => {
      return useQuery({
        queryKey: ['transaction', id],
        queryFn: () => fetchTransaction(id!),
        enabled: id !== null,
        initialData: options?.initialData,
      })
    }
    ```
    Note: the `initialData` signature added in Plan 15 must be preserved here.

    `useGetTransactions.ts` — remove `fetchTransactions` function body, add import:
    ```ts
    import { fetchTransactions } from './queries'
    ```
    Keep all interfaces (TransactionAssignee, TransactionTag, TransactionRow, TransactionQueryParams, TransactionListResponse) in place — they are re-exported from index.ts and used by other files.

    `useSearchTransactions.ts` — remove `fetchSearchTransactions` function body, add import:
    ```ts
    import { fetchSearchTransactions } from './queries'
    ```

    **Part C — Update index.ts to re-export from queries.ts:**

    Add `export * from './queries'` to `index.ts` so consumers importing from `@/lib/data-access/transactions` still get the fetch utilities.

    Note: `fetchTransactions`, `fetchTransaction`, `fetchSearchTransactions` are currently exported from their hook files AND re-exported via `export * from './useGetTransactions'` etc. in index.ts. After this change they are in queries.ts and re-exported. No external import paths break.

    **Part D — Extract shared Input props in AssigneeRow.tsx:**

    Current code has two near-identical `<Input>` blocks in percent/dollar branches. Both share `type="number"`, `autoComplete="off"`, `className="w-20 text-right"`.

    Add a shared const before the return:
    ```ts
    const sharedInputProps = {
      type: 'number' as const,
      autoComplete: 'off' as const,
      className: 'w-20 text-right',
    } as const
    ```

    Then spread into both branches:
    ```tsx
    {mode === 'percent' ? (
      <Input
        {...sharedInputProps}
        value={percentage.toFixed(1)}
        onChange={(e) => { ... }}
      />
    ) : (
      <Input
        {...sharedInputProps}
        value={(amountCents / 100).toFixed(2)}
        onChange={(e) => { ... }}
      />
    )}
    ```
  </action>
  <verify>
    <automated>grep -rn "fetchTransaction\|fetchTransactions\|fetchSearchTransactions" /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/lib/data-access/transactions/</automated>
  </verify>
  <acceptance_criteria>
    - `queries.ts` exists and exports `fetchTransaction`, `fetchTransactions`, `fetchSearchTransactions`
    - `useGetTransaction.ts`, `useGetTransactions.ts`, `useSearchTransactions.ts` no longer define the fetch functions — they import from `./queries`
    - `index.ts` exports from `./queries`
    - `useGetTransaction.ts` still has the `initialData` option (not lost in refactor)
    - `AssigneeRow.tsx` has `const sharedInputProps = { type: 'number', autoComplete: 'off', className: ... }`
    - Both Input elements in AssigneeRow spread `sharedInputProps`
  </acceptance_criteria>
  <done>fetch utilities in queries.ts; hook files import from queries; AssigneeRow uses shared Input props const</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| module boundary | queries.ts is internal; no new external boundary introduced |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-19-01 | Tampering | queries.ts | accept | Refactor only; all API calls still go through apiFetch with auth headers |
| T-03.4-19-02 | Spoofing | TransactionFormInstance type | accept | Type-only change; no runtime boundary |
</threat_model>

<verification>
1. `grep -rn "ReturnType<typeof useTransactionForm>" /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/` — should return 0 results
2. `grep -c "{' '}" /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/SettlementFields.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/ContributionFields.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/RefundFields.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/IncomeFields.tsx` — all should be 0
3. `ls /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/lib/data-access/transactions/queries.ts` — file exists
4. `grep -n "sharedInputProps" /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/AssigneeRow.tsx` — should appear
</verification>

<success_criteria>
- Zero ReturnType<typeof> in transaction components
- Zero {' '} literals in type-specific field files
- queries.ts contains all three fetch utilities
- AssigneeRow uses shared Input props
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-transaction-forms-ui/03.4-19-SUMMARY.md`
</output>
