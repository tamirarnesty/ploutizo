---
phase: "03.4"
plan: "11"
type: execute
wave: 6
depends_on: ["03.4-05"]
files_modified:
  - apps/web/src/components/transactions/TransactionForm.tsx
autonomous: true
gap_closure: true
requirements: ["§4 Transactions (create/edit forms)"]

must_haves:
  truths:
    - "Delete button in edit footer is icon-only (Trash2 icon, no label text)"
    - "Delete button has aria-label='Delete transaction' for accessibility"
    - "Footer layout is not cramped at 560px sheet width"
    - "Delete button still opens the confirmation AlertDialog"
  artifacts:
    - path: "apps/web/src/components/transactions/TransactionForm.tsx"
      provides: "Icon-only Delete button in edit footer"
      contains: "size=\"icon\""
  key_links:
    - from: "AlertDialogTrigger render"
      to: "icon-only Button"
      via: "size='icon' aria-label='Delete transaction'"
      pattern: "size=\"icon\""
---

<objective>
Make the Delete button in the edit mode footer icon-only to prevent cramping at 560px sheet width.

Purpose: The current footer has "Delete transaction" text + "Discard changes" + "Save changes" — three full-width button labels at 560px is too cramped. An icon-only delete button frees horizontal space while the aria-label preserves accessibility.

Output: Single change to the AlertDialogTrigger render Button in TransactionForm.tsx footer.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/PROJECT.md

<interfaces>
<!-- Current footer AlertDialogTrigger render (TransactionForm.tsx ~lines 329-341) -->
<AlertDialogTrigger
  render={
    <Button
      variant="ghost"
      type="button"
      className="text-destructive hover:text-destructive"
    />
  }
>
  <Trash2 size={16} className="mr-1" aria-hidden="true" />
  Delete transaction
</AlertDialogTrigger>

<!-- Target: icon-only version -->
<AlertDialogTrigger
  render={
    <Button
      variant="ghost"
      size="icon"
      type="button"
      aria-label="Delete transaction"
      className="text-destructive hover:bg-destructive/10 hover:text-destructive"
    />
  }
>
  <Trash2 size={16} aria-hidden="true" />
</AlertDialogTrigger>

<!-- Changes:
  1. Add size="icon" to Button
  2. Add aria-label="Delete transaction" to Button
  3. Remove "mr-1" from Trash2 className (no longer needed with icon-only)
  4. Remove "Delete transaction" text from AlertDialogTrigger children
  5. Add hover:bg-destructive/10 to className for visual feedback on hover
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert Delete button to icon-only in edit footer</name>
  <files>
    apps/web/src/components/transactions/TransactionForm.tsx
  </files>
  <read_first>
    - apps/web/src/components/transactions/TransactionForm.tsx (footer section: lines ~327-365, AlertDialogTrigger with "Delete transaction" text)
  </read_first>
  <action>
    In the edit-mode footer section (inside `isEditing ? (...) : (&lt;div /&gt;)`),
    find the AlertDialogTrigger block.

    **Current:**
    ```tsx
    <AlertDialogTrigger
      render={
        <Button
          variant="ghost"
          type="button"
          className="text-destructive hover:text-destructive"
        />
      }
    >
      <Trash2 size={16} className="mr-1" aria-hidden="true" />
      Delete transaction
    </AlertDialogTrigger>
    ```

    **Replace with:**
    ```tsx
    <AlertDialogTrigger
      render={
        <Button
          variant="ghost"
          size="icon"
          type="button"
          aria-label="Delete transaction"
          className="text-destructive hover:bg-destructive/10 hover:text-destructive"
        />
      }
    >
      <Trash2 size={16} aria-hidden="true" />
    </AlertDialogTrigger>
    ```

    Changes made:
    - Added `size="icon"` to Button
    - Added `aria-label="Delete transaction"` to Button (required for icon-only button accessibility — per web-design-guidelines)
    - Removed `className="mr-1"` from Trash2 (margin no longer needed without label)
    - Removed "Delete transaction" text node from children
    - Added `hover:bg-destructive/10` to Button className for hover feedback

    Do NOT change the AlertDialogContent (title, description, action buttons) — those remain unchanged.
  </action>
  <verify>
    <automated>grep -n 'size="icon"' apps/web/src/components/transactions/TransactionForm.tsx && grep -n 'aria-label="Delete transaction"' apps/web/src/components/transactions/TransactionForm.tsx && grep -n "Delete transaction" apps/web/src/components/transactions/TransactionForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'size="icon"' TransactionForm.tsx` returns match on the AlertDialogTrigger Button
    - `grep 'aria-label="Delete transaction"' TransactionForm.tsx` returns match on the same Button
    - `grep "Delete transaction" TransactionForm.tsx` returns matches ONLY inside AlertDialogContent (title/description/action) — NOT as a text node in the AlertDialogTrigger children
    - `grep "mr-1" TransactionForm.tsx` returns NO matches (margin class removed from Trash2)
    - `grep "hover:bg-destructive/10" TransactionForm.tsx` returns match on the Button
    - TypeScript compiles without errors: `pnpm --filter web typecheck` exits 0
  </acceptance_criteria>
  <done>Delete button is icon-only with aria-label; footer no longer cramped at 560px; AlertDialog still opens on click.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → delete handler | AlertDialogAction still gates the actual delete mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-11-01 | Elevation of Privilege | delete button | accept | Delete is gated by AlertDialog confirmation; icon-only change doesn't affect the confirmation flow |
</threat_model>

<verification>
```bash
# Confirm icon-only attributes
grep 'size="icon"' apps/web/src/components/transactions/TransactionForm.tsx
grep 'aria-label="Delete transaction"' apps/web/src/components/transactions/TransactionForm.tsx

# Confirm text label removed from trigger (should only appear in dialog body)
grep -c "Delete transaction" apps/web/src/components/transactions/TransactionForm.tsx
# Expected: 2 (AlertDialogTitle + AlertDialogAction — NOT the trigger children)
```
</verification>

<success_criteria>
- Edit mode footer shows icon-only Trash2 button on the left; "Discard changes" and "Save changes" on the right
- No overflow or cramping at 560px sheet width
- Clicking the trash icon opens the AlertDialog confirmation
- Confirming in the dialog deletes the transaction and closes the sheet
- Screen reader users see "Delete transaction" via aria-label
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-transaction-forms-ui/03.4-11-SUMMARY.md`
</output>
