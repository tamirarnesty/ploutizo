---
phase: "03.4"
plan: "01"
subsystem: "api + web/lib"
tags: ["lrm", "vitest", "api-patch", "discriminated-union", "description-filter"]
dependency_graph:
  requires: []
  provides:
    - "lrmSplit pure function (apps/web/src/lib/lrm.ts)"
    - "vitest test runner for apps/web"
    - "PATCH /api/transactions/:id accepts createTransactionSchema (discriminated union)"
    - "GET /api/transactions?description= ILIKE filter on description OR merchant"
    - "PATCH refundOf cross-org ownership guard"
  affects:
    - "All downstream plans in 03.4 that use lrmSplit (03.4-03 SplitSection)"
    - "03.4-02 form submit — PATCH payload now discriminated union"
    - "03.4-02 RefundLinker combobox — description filter now active on GET"
tech_stack:
  added:
    - "vitest ^4.1.2 (devDependency in apps/web)"
  patterns:
    - "TDD RED/GREEN flow: test written first, then implementation"
    - "Drizzle parameterized sql template for ILIKE — not string interpolation"
    - "updateTransaction service signature updated to accept CreateTransactionInput"
key_files:
  created:
    - "apps/web/src/lib/lrm.ts"
    - "apps/web/src/lib/lrm.test.ts"
    - "apps/web/vitest.config.ts"
  modified:
    - "apps/web/package.json (added test script + vitest devDependency)"
    - "apps/api/src/routes/transactions.ts (PATCH schema swap, refundOf guard, description param)"
    - "apps/api/src/lib/queries/transactions.ts (description field + ILIKE condition)"
    - "apps/api/src/services/transactions.ts (updateTransaction signature)"
    - "apps/api/src/__tests__/transactions.test.ts (fixed PATCH tests + 5 new tests)"
decisions:
  - "updateTransaction service accepts CreateTransactionInput instead of UpdateTransactionInput — the schema swap on the PATCH route necessitates matching the service signature; the service body already casts to Record<string,unknown> for the query layer so no logic changes"
  - "Existing TXN-PATCH-01/02 tests updated to send full discriminated union payloads — the schema swap from flat partial to discriminated union is a breaking change for the test fixtures; updated as Rule 1 auto-fix"
metrics:
  duration: "~20 minutes"
  completed: "2026-04-17"
  tasks_completed: 3
  files_changed: 8
---

# Phase 03.4 Plan 01: Foundation — LRM + vitest + API patches Summary

Vitest added to apps/web, lrmSplit pure function with 5 passing unit tests, PATCH route swapped to createTransactionSchema discriminated union with refundOf guard, GET description ILIKE filter wired end-to-end with parameterized Drizzle sql.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 (RED) | vitest + lrm.test.ts RED phase | 6fc542f | apps/web/package.json, vitest.config.ts, lrm.test.ts |
| 1 (GREEN) | lrmSplit implementation | 07d6661 | apps/web/src/lib/lrm.ts |
| 2 | API patches — PATCH schema swap + refundOf guard + GET description filter | 342b79a | routes/transactions.ts, queries/transactions.ts, services/transactions.ts, transactions.test.ts |
| 3 | Extend API tests | 570c196 | apps/api/src/__tests__/transactions.test.ts |

## Verification

- `pnpm --filter web test`: 5/5 lrm tests pass
- `pnpm --filter api test`: 71/71 tests pass (66 existing + 5 new)
- `grep createTransactionSchema apps/api/src/routes/transactions.ts` — appears in PATCH /:id validator
- `grep ILIKE apps/api/src/lib/queries/transactions.ts` — appears in buildConditions
- `grep description apps/api/src/lib/queries/transactions.ts` — appears in ListQueryParams type

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Updated existing PATCH tests to use full discriminated union payload**
- **Found during:** Task 2 — swapping PATCH validator from updateTransactionSchema to createTransactionSchema
- **Issue:** TXN-PATCH-01 and TXN-PATCH-02 sent `{ description: 'X' }` (flat partial), which fails the discriminated union validator (requires `type` field). Tests would fail with 400 instead of expected 200/404.
- **Fix:** Updated both tests to send full expense payload with `type`, `accountId`, `amount`, `date` fields.
- **Files modified:** apps/api/src/__tests__/transactions.test.ts
- **Commit:** 342b79a

**2. [Rule 1 - Bug] Updated updateTransaction service signature to accept CreateTransactionInput**
- **Found during:** Task 2 — TypeScript would error at the call site in routes/transactions.ts after schema swap
- **Issue:** Service function typed as `z.infer<typeof updateTransactionSchema>` (flat partial); route now passes `createTransactionSchema` validated data (discriminated union)
- **Fix:** Changed import from `updateTransactionSchema` to `CreateTransactionInput` type, updated function signature. No logic changes needed — service body already uses `Record<string, unknown>` cast for the query layer.
- **Files modified:** apps/api/src/services/transactions.ts
- **Commit:** 342b79a

## Known Stubs

None — all functionality is fully wired. lrmSplit is a pure function with no stubs. API patches are live code changes, not placeholders.

## Threat Flags

None — all threat mitigations from the plan's threat register are implemented:
- T-03.4-01 (SQL injection via description): mitigated — Drizzle parameterized sql template
- T-03.4-02 (cross-org refundOf in PATCH): mitigated — checkRefundOfOwnership guard added
- T-03.4-03 (PATCH schema enforcement): mitigated — createTransactionSchema discriminated union

## Self-Check: PASSED

- apps/web/src/lib/lrm.ts: EXISTS
- apps/web/src/lib/lrm.test.ts: EXISTS
- apps/web/vitest.config.ts: EXISTS
- apps/api/src/routes/transactions.ts: createTransactionSchema on PATCH CONFIRMED
- apps/api/src/lib/queries/transactions.ts: ILIKE in buildConditions CONFIRMED
- Commits 6fc542f, 07d6661, 342b79a, 570c196: ALL PRESENT
