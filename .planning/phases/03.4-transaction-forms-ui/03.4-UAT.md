---
status: diagnosed
phase: 03.4-transaction-forms-ui
source: [03.4-12-SUMMARY.md, 03.4-13-SUMMARY.md, 03.4-14-SUMMARY.md]
started: 2026-04-18T03:00:00Z
updated: 2026-04-18T03:30:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Create expense transaction
expected: Fill out the form (amount, date, description, category). Submit. A success toast appears. The sheet closes. The new transaction row shows in the table with the correct values.
result: pass

### 2. Create refund with linked-transaction search
expected: Open the create form and switch type to Refund. The RefundLinker dropdown pre-loads recent expense transactions on open (no search needed). Typing a search term returns matching results. Selecting one links it. Form submits successfully.
result: pass
noted: "works - yes. however, description should auto-fill with 'Refund of <linked tx description>', amount should auto-fill, and refunds show as negative (red) when they should be positive (green)"

### 3. Edit existing transaction
expected: Click edit on a row. The sheet opens pre-filled. Amount shows with comma formatting (e.g. 1,000 not 1000). Selecting a date in the calendar closes the calendar automatically. The row action button (⋯) is hidden at rest and only appears on row hover.
result: pass
noted: "sheet fetches transaction data on open — should use TransactionRow data already in the table as initialData for useGetTransaction"

### 4. Delete transaction
expected: Open the delete dialog (from sheet or row action menu). Click Delete. The AlertDialog closes. The sheet closes. The transaction row disappears from the table. A success toast appears. No stale data after dismiss.
result: pass
noted: "both entry points work; however table updates (row disappears) before toast and sheet close — bad UX"

### 5. Switch between transactions
expected: Open transaction A in the sheet. Close it. Open transaction B. The form shows only B's data — no leftover values from A.
result: pass

## Summary

total: 5
passed: 5
issues: 13
pending: 0
skipped: 0
blocked: 0

## Gaps

<!-- UAT gaps (from browser testing) -->

- truth: "Selecting a linked transaction auto-fills the description as 'Refund of <linked tx description>'"
  status: failed
  reason: "User reported: description should auto-fill with 'Refund of <description of linked transaction>'"
  severity: major
  test: 2
  root_cause: "RefundLinker onSelect only sets linkedTransactionId; does not call form.setFieldValue for description or amount"
  artifacts:
    - path: "apps/web/src/components/transactions/RefundLinker.tsx"
      issue: "onSelect does not propagate description/amount to parent form"
  missing:
    - "onSelect should call form.setFieldValue('description', 'Refund of ' + linkedTx.description)"
    - "onSelect should call form.setFieldValue('amount', linkedTx.amount)"
  debug_session: ""

- truth: "Selecting a linked transaction auto-fills the amount field with the linked transaction's amount"
  status: failed
  reason: "User reported: it should auto-fill the price"
  severity: major
  test: 2
  root_cause: "Covered by description auto-fill gap above"
  artifacts:
    - path: "apps/web/src/components/transactions/RefundLinker.tsx"
      issue: "Amount not forwarded on select"
  missing:
    - "Covered by the description auto-fill fix above"
  debug_session: ""

- truth: "Refund transactions display as positive (green) in the transaction table — they return money"
  status: failed
  reason: "User reported: refunds show as negative (red) when they should be positive (green)"
  severity: major
  test: 2
  root_cause: "Amount color logic in TransactionColumns is based on raw value sign; does not account for type=refund"
  artifacts:
    - path: "apps/web/src/components/transactions/TransactionColumns.tsx"
      issue: "Amount cell color logic does not account for transaction type=refund"
  missing:
    - "When type === 'refund', render amount as positive (green) regardless of sign"
  debug_session: ""

- truth: "Opening the edit sheet uses the already-loaded TransactionRow as initialData — no separate fetch triggered"
  status: failed
  reason: "User reported: sheet is fetching transaction data on open; use TransactionRow as initialData"
  severity: minor
  test: 3
  root_cause: "useGetTransaction takes only id; TransactionRow from list cache not passed as initialData"
  artifacts:
    - path: "apps/web/src/lib/data-access/transactions/useGetTransaction.ts"
      issue: "No initialData option"
  missing:
    - "Accept optional initialData: TransactionRow in useGetTransaction"
    - "Pass TransactionRow from table row into sheet and into useGetTransaction"
  debug_session: ""

- truth: "Delete sequence: toast and sheet close happen before or simultaneously with the table row removal"
  status: failed
  reason: "User reported: table updates (row disappears) before the toast and sheet close — bad UX"
  severity: major
  test: 4
  root_cause: "setQueriesData optimistic update fires synchronously at mutation start; toast/close fire in onSuccess after API responds"
  artifacts:
    - path: "apps/web/src/lib/data-access/transactions/useDeleteTransaction.ts"
      issue: "Optimistic update runs before onSuccess"
  missing:
    - "Move list cache update to onSuccess so row disappears after toast and sheet close"
  debug_session: ""

<!-- Code review gaps (from manual review) -->

- truth: "RefundLinker uses ComboboxEmpty for empty state"
  status: failed
  reason: "Code review: empty state rendered as a plain <div> instead of ComboboxEmpty component"
  severity: minor
  test: cr
  root_cause: "RefundLinker.tsx lines 124-130 render a manual div with className for empty state; ComboboxEmpty exists in the UI library and is used in CategoriesSettings and TransactionTagPicker"
  artifacts:
    - path: "apps/web/src/components/transactions/RefundLinker.tsx"
      issue: "Lines 124-130: manual empty state div instead of ComboboxEmpty"
  missing:
    - "Replace manual empty state div with <ComboboxEmpty>"
  debug_session: ""

- truth: "RefundLinker renders each option as a multi-row layout: description title, secondary date row, price on the right"
  status: failed
  reason: "Code review: options rendered as single line with bullet dot separators using invalid unicode (\\u2022)"
  severity: major
  test: cr
  root_cause: "txLabel() at line 26-27 joins description, date, amount with \\u2022; rendered as plain span in ComboboxItem (line 121)"
  artifacts:
    - path: "apps/web/src/components/transactions/RefundLinker.tsx"
      issue: "Lines 26-27, 119-123: single-line label with dot separators"
  missing:
    - "Replace txLabel() with a multi-row ComboboxItem: description as title, secondary row with date, amount inlined right across both rows"
    - "Give the item enough height (min-h or py) to accommodate two lines"
  debug_session: ""

- truth: "No ReturnType<typeof ...> usage — all prop types use explicit named type aliases"
  status: failed
  reason: "Code review: 11 instances of ReturnType<typeof ...> across the transaction components and hooks"
  severity: major
  test: cr
  root_cause: "Form prop typed as ReturnType<typeof useTransactionForm>['form'] in 8 component files; mutation props typed as ReturnType<typeof useCreateTransaction> in hooks/useTransactionForm.ts"
  artifacts:
    - path: "apps/web/src/components/transactions/hooks/useTransactionForm.ts"
      issue: "Lines 103-104: ReturnType on mutation types"
    - path: "apps/web/src/components/transactions/RefundLinker.tsx"
      issue: "Line 22: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/TransferFields.tsx"
      issue: "Line 13: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/IncomeFields.tsx"
      issue: "Line 22: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/RefundFields.tsx"
      issue: "Line 16: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/ExpenseFields.tsx"
      issue: "Line 13: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/ContributionFields.tsx"
      issue: "Line 22: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/SettlementFields.tsx"
      issue: "Line 14: ReturnType for form prop"
    - path: "apps/web/src/components/transactions/TransactionTypeFields.tsx"
      issue: "Line 34: ReturnType for form prop"
  missing:
    - "Export TransactionFormInstance type from useTransactionForm.ts (typeof useTransactionForm return type inlined as named alias)"
    - "Export CreateMutation and UpdateMutation named types"
    - "Replace all ReturnType<typeof ...> usages with the named aliases"
  debug_session: ""

- truth: "No linting errors in the transaction form files"
  status: failed
  reason: "Code review: linting errors present across transaction form files"
  severity: major
  test: cr
  root_cause: "Likely unused imports, missing deps, or rule violations introduced during rapid UAT fix iterations"
  artifacts: []
  missing:
    - "Run pnpm --filter web lint and fix all reported errors"
  debug_session: ""

- truth: "Data access files separate fetch utilities from hooks — fetch* functions in queries.ts, use* hooks in their own files"
  status: failed
  reason: "Code review: useGetTransaction.ts, useGetTransactions.ts, useSearchTransactions.ts each export both a fetch* utility and a use* hook"
  severity: minor
  test: cr
  root_cause: "fetch* functions co-located with use* hooks; should be split so hooks files only export hooks"
  artifacts:
    - path: "apps/web/src/lib/data-access/transactions/useGetTransaction.ts"
      issue: "Exports fetchTransaction (util) and useGetTransaction (hook)"
    - path: "apps/web/src/lib/data-access/transactions/useGetTransactions.ts"
      issue: "Exports fetchTransactions (util) and useGetTransactions (hook)"
    - path: "apps/web/src/lib/data-access/transactions/useSearchTransactions.ts"
      issue: "Exports fetchSearchTransactions (util) and useSearchTransactions (hook)"
  missing:
    - "Move fetch* functions to a shared queries.ts file in the transactions data-access directory"
    - "Import fetch* from queries.ts in the hook files"
  debug_session: ""

- truth: "AssigneeRow uses a single shared <Input> config with no duplication"
  status: failed
  reason: "Code review: two near-identical <Input> elements (percent mode lines 44-58, dollar mode lines 60-76) with same type/autoComplete/className"
  severity: minor
  test: cr
  root_cause: "AssigneeRow.tsx duplicates Input props across two conditional branches instead of extracting shared props"
  artifacts:
    - path: "apps/web/src/components/transactions/AssigneeRow.tsx"
      issue: "Lines 44-76: duplicate Input config in percent and dollar branches"
  missing:
    - "Extract shared Input props (type, autoComplete, className) to a const; spread into both branches with only the differing onChange"
  debug_session: ""

- truth: "No {' '} whitespace literals in JSX — spacing handled via CSS gap or label structure"
  status: failed
  reason: "Code review: 8 instances of {' '} used to add space before optional badge text in labels"
  severity: minor
  test: cr
  root_cause: "Labels structured as 'FieldName{' '}(optional)' rather than using flex+gap or wrapping the optional badge in a span with ml-1"
  artifacts:
    - path: "apps/web/src/components/transactions/TransactionForm.tsx"
      issue: "Lines 262, 286, 307: {' '} before optional indicators"
    - path: "apps/web/src/components/transactions/SettlementFields.tsx"
      issue: "Line 23: {' '} in label"
    - path: "apps/web/src/components/transactions/ContributionFields.tsx"
      issue: "Line 30: {' '} in label"
    - path: "apps/web/src/components/transactions/RefundFields.tsx"
      issue: "Line 27: {' '} in label"
    - path: "apps/web/src/components/transactions/RefundLinker.tsx"
      issue: "Line 66: {' '} in label"
    - path: "apps/web/src/components/transactions/IncomeFields.tsx"
      issue: "Line 64: {' '} in label"
  missing:
    - "Replace 'Text{' '}(optional)' patterns with a flex wrapper + gap-1, or wrap the optional badge in a <span className='ml-1'>"
  debug_session: ""

- truth: "Description field is required in the transaction form schema"
  status: failed
  reason: "Code review: description is z.string().optional() — without description or merchant a transaction has no identifying info"
  severity: major
  test: cr
  root_cause: "packages/validators/src/transactions.ts line 22: description defined as optional in baseTransactionSchema"
  artifacts:
    - path: "packages/validators/src/transactions.ts"
      issue: "Line 22: description: z.string().optional()"
  missing:
    - "Change description to z.string().min(1) in baseTransactionSchema"
    - "Verify form UI marks description as required and shows validation error"
  debug_session: ""
