---
phase: "03.4"
plan: "20"
type: execute
wave: 13
depends_on: ["03.4-19"]
files_modified:
  # Determined at runtime — likely candidates based on UAT fix session:
  - apps/web/src/components/transactions/RefundLinker.tsx
  - apps/web/src/lib/data-access/transactions/useDeleteTransaction.ts
  - apps/web/src/components/transactions/hooks/useTransactionForm.ts
autonomous: true
gap_closure: true
requirements: ["§4 Transactions (create/edit forms)"]
must_haves:
  truths:
    - "pnpm --filter web lint exits with code 0 — no errors in transaction files"
  artifacts:
    - path: "/tmp/lint-output.txt"
      provides: "lint run output"
      contains: "0 errors"
  key_links: []
---

<objective>
Run the web lint check after all code quality changes are applied and fix any remaining errors in transaction component and data-access files.

Purpose: Runs after Plan 19 (code quality sweep) so the full set of changes is stable before lint runs. Captures and fixes any accumulated issues from the UAT fix iterations plus the refactor passes.
Output: Clean lint output for the web app.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/PROJECT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run lint and fix all errors in transaction files</name>
  <files>
    apps/web/src/components/transactions/RefundLinker.tsx
    apps/web/src/lib/data-access/transactions/useDeleteTransaction.ts
    apps/web/src/components/transactions/hooks/useTransactionForm.ts
    (additional files as reported by lint output)
  </files>
  <read_first>
    - Read the lint output before making any fixes. Do not guess at errors — let the linter tell you.
  </read_first>
  <action>
    Step 1: Run lint and capture output:
    ```bash
    pnpm --filter web lint 2>&1 | tee /tmp/lint-output.txt
    ```

    Step 2: Read `/tmp/lint-output.txt`. Identify all errors (not warnings) in files under:
    - `apps/web/src/components/transactions/`
    - `apps/web/src/lib/data-access/transactions/`

    Step 3: For each error, apply the minimal fix:
    - Unused import → remove the import
    - Missing hook dependency → add to deps array or use `// eslint-disable-next-line react-hooks/exhaustive-deps` with a one-line justification if the dep would cause an infinite loop (e.g. a stable setFieldValue reference that triggers on every render)
    - Unused variable → remove or prefix with `_`
    - Explicit `any` → replace with correct type from the file's existing imports
    - Do NOT fix warnings — only errors

    Step 4: Re-run lint to confirm clean:
    ```bash
    pnpm --filter web lint 2>&1 | tee /tmp/lint-output-2.txt
    ```

    Step 5: If errors remain, continue fixing until clean. Do not suppress errors with blanket `/* eslint-disable */` comments — fix the underlying issue.

    Common expected errors based on UAT fix session:
    - Unused `import type { useTransactionForm }` if any file still has it after Plans 17/19 replaced it
    - `no-explicit-any` in `Snapshot` type in useDeleteTransaction.ts
    - React hook dependency warnings in RefundLinker (debounce timer ref + query hooks)
  </action>
  <verify>
    <automated>pnpm --filter web lint 2>&1 | grep -c " error "</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter web lint` exits with code 0
    - Final lint run shows 0 errors in transaction files
    - No errors suppressed with blanket `/* eslint-disable */` unless pre-existing
  </acceptance_criteria>
  <done>Lint passes clean for the web app</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev tooling | lint is a development-only tool; no runtime or security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-20-01 | Information Disclosure | lint output | accept | Output is local dev tooling; no secrets in lint results |
</threat_model>

<verification>
1. `pnpm --filter web lint 2>&1 | grep " error "` — should return nothing (0 error lines)
</verification>

<success_criteria>
- Zero lint errors in the web app
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-transaction-forms-ui/03.4-20-SUMMARY.md`
</output>
