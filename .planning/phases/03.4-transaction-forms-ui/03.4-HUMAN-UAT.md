---
status: partial
phase: 03.4-transaction-forms-ui
source: [03.4-VERIFICATION.md]
started: 2026-04-18T02:20:00Z
updated: 2026-04-18T22:30:00Z
---

## Current Test

[awaiting re-test] All 7 prior defects resolved via gap-closure plans 12–21. Re-testing required to confirm fixes.

## Tests

### 1. Create expense transaction
expected: Sheet closes, success toast, new transaction row appears in table with correct values
result: [pending re-test — prior fix: plan 12 added onSuccess toast + optimistic update]

### 2. Create refund with linked-transaction search
expected: Dropdown shows up to 10 recent expenses without typing; after debounce switches to search results; selecting auto-fills description as "Refund of [tx]" and amount
result: [pending re-test — prior fix: plan 12 fixed RefundLinker items/render-fn bug + pre-load]

### 3. Edit existing transaction
expected: Sheet opens pre-populated; amount formatted (e.g. 1,000.00); calendar closes on date pick; edit button visible on row hover only
result: [pending re-test — prior fixes: plan 13 row-hover CSS; plan 14 FormattedAmountInput + calendar auto-close]

### 4. Delete transaction via trash icon
expected: AlertDialog opens, confirm removes row after toast+close; row disappears AFTER toast and sheet close
result: [pending re-test — prior fix: plan 14 AlertDialog Close primitive + onClose wiring]

### 5. Switch between transactions (key-based remount)
expected: Edit TX-A, close, edit TX-B — form shows only TX-B data, no TX-A stale values
result: [pending re-test — was passing previously, verify still holds]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

| # | Description | Root Cause | Plan |
|---|-------------|-----------|------|
| 1 | No toast on create | useCreateTransaction has no onSuccess toast call | 03.4-12 |
| 2 | RefundLinker shows no items + search broken | Same items={}/render-fn bug as TagPicker; also no pre-load of recent expenses | 03.4-12 |
| 3 | Row action button always visible | [tr:hover_&] CSS not applied — using always-visible classes | 03.4-13 |
| 4 | Amount not formatted (1000 → 1,000) | type="number" input never formats; needs text input + Intl.NumberFormat on blur | 03.4-14 |
| 5 | Calendar stays open after date select | Popover is uncontrolled; needs controlled open state closed in onSelect | 03.4-14 |
| 6 | AlertDialog does not close after delete confirm | AlertDialogAction is a plain Button, not AlertDialogPrimitive.Close | 03.4-14 |
| 7 | Delete: Sheet doesn't close, item reappears | Follows from #6 (onClose never called); delete API may also be failing | 03.4-14 |
