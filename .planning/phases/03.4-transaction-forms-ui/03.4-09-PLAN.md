---
phase: "03.4"
plan: "09"
type: execute
wave: 6
depends_on: ["03.4-05"]
files_modified:
  - apps/web/src/components/transactions/TransactionColumns.tsx
autonomous: true
gap_closure: true
requirements: ["§4 Transactions (create/edit forms)"]

must_haves:
  truths:
    - "Row action button (⋮) is always visible in the actions column"
    - "Button uses muted foreground color at rest and foreground on hover"
    - "No opacity-0 or group-hover classes remain on the trigger button"
  artifacts:
    - path: "apps/web/src/components/transactions/TransactionColumns.tsx"
      provides: "Actions column with always-visible trigger button"
      contains: "text-muted-foreground hover:text-foreground"
  key_links:
    - from: "DropdownMenuTrigger Button"
      to: "visible state"
      via: "className without opacity-0"
      pattern: "text-muted-foreground hover:text-foreground"
---

<objective>
Fix the row action button (⋮) in TransactionColumns so it is always visible.

Purpose: The DataGrid `&lt;tr&gt;` element does not have a Tailwind `group` class, so `group-hover:opacity-100` never fires. The button is invisible unless focused or the dropdown is open. Removing the opacity hiding makes the button always visible with a subtle muted color — consistent with modern table action UIs.

Output: Single className change on the DropdownMenuTrigger's render Button.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/PROJECT.md

<interfaces>
<!-- Current (broken) className on the trigger Button in TransactionColumns.tsx line ~282 -->
className="opacity-0 group-hover:opacity-100 data-[state=open]:opacity-100 focus-visible:opacity-100 @media_(hover:_none):opacity-100"

<!-- Fix: replace entirely with -->
className="text-muted-foreground hover:text-foreground"

<!-- Why: DataGrid <tr> has no 'group' class, so group-hover never fires.
     The button renders but is transparent. Click works by accident.
     Simple always-visible approach is correct for this use case. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove opacity hiding from actions column trigger button</name>
  <files>
    apps/web/src/components/transactions/TransactionColumns.tsx
  </files>
  <read_first>
    - apps/web/src/components/transactions/TransactionColumns.tsx (current: line ~282, opacity-0 className on DropdownMenuTrigger render Button)
  </read_first>
  <action>
    In the `actions` column cell renderer (around line 275-290), find the DropdownMenuTrigger:

    Current:
    ```tsx
    <DropdownMenuTrigger
      render={
        <Button
          variant="ghost"
          size="icon"
          aria-label="Transaction actions"
          className="opacity-0 group-hover:opacity-100 data-[state=open]:opacity-100 focus-visible:opacity-100 @media_(hover:_none):opacity-100"
        >
          <MoreHorizontal className="size-4" />
        </Button>
      }
    />
    ```

    Replace the className with:
    ```tsx
    className="text-muted-foreground hover:text-foreground"
    ```

    Full updated trigger:
    ```tsx
    <DropdownMenuTrigger
      render={
        <Button
          variant="ghost"
          size="icon"
          aria-label="Transaction actions"
          className="text-muted-foreground hover:text-foreground"
        >
          <MoreHorizontal className="size-4" />
        </Button>
      }
    />
    ```

    No other changes to TransactionColumns.tsx.
  </action>
  <verify>
    <automated>grep -n "opacity-0\|group-hover" apps/web/src/components/transactions/TransactionColumns.tsx && grep -n "text-muted-foreground hover:text-foreground" apps/web/src/components/transactions/TransactionColumns.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "opacity-0" TransactionColumns.tsx` returns NO matches
    - `grep "group-hover" TransactionColumns.tsx` returns NO matches
    - `grep "text-muted-foreground hover:text-foreground" TransactionColumns.tsx` returns exactly one match on the trigger Button
    - `grep 'aria-label="Transaction actions"' TransactionColumns.tsx` still returns a match (button not removed)
  </acceptance_criteria>
  <done>Row action button is always visible with muted foreground color; opacity hiding removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → action handlers | No new trust boundary introduced; existing Edit/Delete handlers unchanged |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4-09-01 | Elevation of Privilege | actions menu | accept | Actions already fire existing handlers; visibility change doesn't affect authorization |
</threat_model>

<verification>
```bash
# Confirm opacity classes removed
grep "opacity-0\|group-hover" apps/web/src/components/transactions/TransactionColumns.tsx
# Expected: 0 matches

# Confirm replacement class present
grep "text-muted-foreground hover:text-foreground" apps/web/src/components/transactions/TransactionColumns.tsx
# Expected: 1 match
```
</verification>

<success_criteria>
- The ⋮ button is visible on every row without hovering
- Button shows muted foreground color at rest, foreground on hover
- Clicking the button opens the Edit / Delete dropdown menu
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-transaction-forms-ui/03.4-09-SUMMARY.md`
</output>
