---
phase: 03.3
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/api/src/__tests__/transactions.test.ts
autonomous: true
requirements:
  - "§4 Transactions (list view)"
must_haves:
  truths:
    - "PATCH /api/transactions/:id/restore returns 200 with {data:{id}} when transaction exists and belongs to the org"
    - "PATCH /api/transactions/:id/restore returns 404 when transaction ID belongs to a different org"
    - "GET /api/transactions?sort=type orders results by transaction type"
    - "GET /api/transactions?sort=category and sort=account are accepted as valid sort values"
  artifacts:
    - path: "apps/api/src/__tests__/transactions.test.ts"
      provides: "Failing test cases for restore route and extended sort params"
      contains: "TXN-RESTORE-01"
  key_links:
    - from: "apps/api/src/__tests__/transactions.test.ts"
      to: "apps/api/src/routes/transactions.ts"
      via: "Hono app.request()"
      pattern: "PATCH.*restore"
---

<objective>
Add failing test stubs for the `PATCH /:id/restore` route and the extended `sort=type|category|account` query parameters to the existing transactions test file. These tests must be RED before Plan 02 implements the production code.

Purpose: Wave 0 — establish Nyquist-compliant test coverage before implementing the restore endpoint and sort expansion.
Output: 5 new failing test cases in `apps/api/src/__tests__/transactions.test.ts`.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.3-transaction-list-ui/03.3-CONTEXT.md
@.planning/phases/03.3-transaction-list-ui/03.3-RESEARCH.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add failing tests for restore route and sort params</name>
  <files>apps/api/src/__tests__/transactions.test.ts</files>
  <read_first>
    - apps/api/src/__tests__/transactions.test.ts (full file — understand mock setup, existing test patterns, vi.mock structure)
    - apps/api/src/routes/transactions.ts (understand current routes; confirm PATCH /:id/restore does NOT yet exist)
    - apps/api/src/services/transactions.ts (understand deleteTransaction pattern to mirror for restoreTransaction)
  </read_first>
  <behavior>
    RED phase — all new tests must FAIL before Plan 02 ships.

    Tests to add:

    TXN-RESTORE-01: `PATCH /:id/restore — 200 with matching orgId`
    - Add `restoreTransaction: vi.fn().mockResolvedValue({ id: 'txn_1' })` to the `vi.mock('../services/transactions', ...)` factory (alongside existing mocks).
    - Import `restoreTransaction` from `'../services/transactions'` at the top of the file (alongside other imports).
    - Test: `app.request('/txn_1/restore', { method: 'PATCH' })` → expect status 200, body `{ data: { id: 'txn_1' } }`.

    TXN-RESTORE-02: `PATCH /:id/restore — 404 when not found (wrong org or already active)`
    - Override mock: `vi.mocked(restoreTransaction).mockResolvedValueOnce(null)`
    - Test: `app.request('/txn_1/restore', { method: 'PATCH' })` → expect status 404, body `{ error: { code: 'NOT_FOUND' } }`.

    TXN-LIST-SORT-01: `GET /?sort=type — passes sort=type to listTransactions`
    - Spy on `listTransactions` mock.
    - Test: `app.request('/?sort=type')` → expect status 200, expect `vi.mocked(listTransactions)` called with params where `params.sort === 'type'`.

    TXN-LIST-SORT-02: `GET /?sort=category — passes sort=category to listTransactions`
    - Same pattern as SORT-01 but `sort=category`.

    TXN-LIST-SORT-03: `GET /?sort=account — passes sort=account to listTransactions`
    - Same pattern as SORT-01 but `sort=account`.

    Group all 5 in a `describe('PATCH /:id/restore', ...)` block for restore tests and extend the existing `describe('GET /api/transactions', ...)` block for sort tests.

    These tests MUST fail (routes don't exist yet). Run `pnpm --filter @ploutizo/api test` and confirm failures before committing.
  </behavior>
  <action>
    1. Read the full `transactions.test.ts` to understand current structure.
    2. Add `restoreTransaction: vi.fn().mockResolvedValue({ id: 'txn_1' })` to the service mock factory object (inside the `vi.mock('../services/transactions', () => ({ ... }))` block).
    3. Add `import { ..., restoreTransaction } from '../services/transactions'` to existing import at top.
    4. Add a new `describe('PATCH /:id/restore', () => { ... })` block after the DELETE describe block with TXN-RESTORE-01 and TXN-RESTORE-02.
    5. Locate the existing GET describe block and add TXN-LIST-SORT-01, TXN-LIST-SORT-02, TXN-LIST-SORT-03 as new `it()` cases.
    6. Run `pnpm --filter @ploutizo/api test` — tests must show failures for the 5 new cases (route 404 / assertion failures). If any of the 5 new tests pass, the test is invalid — fix it.
    7. Commit: `test(03.3-01): add failing tests for restore route and extended sort params`
  </action>
  <verify>
    <automated>pnpm --filter @ploutizo/api test 2>&1 | grep -E "(RESTORE|SORT|failed|passed)" | head -20</automated>
  </verify>
  <done>
    - `transactions.test.ts` contains describe block `PATCH /:id/restore` with TXN-RESTORE-01 and TXN-RESTORE-02
    - `transactions.test.ts` contains TXN-LIST-SORT-01, TXN-LIST-SORT-02, TXN-LIST-SORT-03 in the GET describe block
    - `pnpm --filter @ploutizo/api test` shows exactly 5 new failures (all others green)
    - No new test passes before Plan 02 ships
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test file → route handler | Hono app.request() simulates HTTP; auth mocked via @hono/clerk-auth vi.mock |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.3-01 | Tampering | Test mock integrity | accept | Test file only; no production security surface. Mocks are scoped to test isolation. |
</threat_model>

<verification>
`pnpm --filter @ploutizo/api test` runs in < 10s and shows the 5 new cases as failures with all pre-existing cases still passing.
</verification>

<success_criteria>
- `apps/api/src/__tests__/transactions.test.ts` has 5 new test cases: TXN-RESTORE-01, TXN-RESTORE-02, TXN-LIST-SORT-01, TXN-LIST-SORT-02, TXN-LIST-SORT-03
- All 5 fail (RED) before Plan 02 implementation
- All pre-existing tests remain green
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-transaction-list-ui/03.3-01-SUMMARY.md`
</output>
