---
phase: "03.3"
plan: "01"
subsystem: "api/tests"
tags: ["tdd", "transactions", "restore", "sort"]
dependency_graph:
  requires: []
  provides: ["TXN-RESTORE-01", "TXN-RESTORE-02", "TXN-LIST-SORT-01", "TXN-LIST-SORT-02", "TXN-LIST-SORT-03"]
  affects: ["apps/api/src/__tests__/transactions.test.ts"]
tech_stack:
  added: []
  patterns: ["TDD RED phase — failing tests committed before implementation"]
key_files:
  created: []
  modified:
    - "apps/api/src/__tests__/transactions.test.ts"
decisions:
  - "restoreTransaction mock added to vi.mock factory so Plan 02 can implement the route without touching the test file"
  - "Sort tests use mockClear() before each to avoid call count contamination from prior tests"
metrics:
  duration: "5m"
  completed: "2026-04-13"
  tasks_completed: 1
  files_modified: 1
---

# Phase 03.3 Plan 01: Failing Tests for Restore Route and Extended Sort Params Summary

5 new failing test cases (RED) added to `apps/api/src/__tests__/transactions.test.ts` before Plan 02 implements production code. All 49 pre-existing tests remain green.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add failing tests for restore route and sort params | b51ffd0 | apps/api/src/__tests__/transactions.test.ts |

## Deviations from Plan

None — plan executed exactly as written.

## Test Results (RED state verified)

```
Tests  5 failed | 49 passed (54)
```

| Test ID | Failure Reason |
|---------|----------------|
| TXN-RESTORE-01 | No PATCH /:id/restore handler — Hono returns 404 via /:id PATCH fallthrough |
| TXN-RESTORE-02 | No handler — empty body parse fails with SyntaxError |
| TXN-LIST-SORT-01 | Route normalizes `sort=type` → `'date'`; expects `'type'` |
| TXN-LIST-SORT-02 | Route normalizes `sort=category` → `'date'`; expects `'category'` |
| TXN-LIST-SORT-03 | Route normalizes `sort=account` → `'date'`; expects `'account'` |

## Known Stubs

None — this plan is test-only (no production code, no UI).

## Threat Flags

None — test file only, no production security surface.

## Self-Check: PASSED

- `apps/api/src/__tests__/transactions.test.ts` — FOUND
- Commit b51ffd0 — FOUND
