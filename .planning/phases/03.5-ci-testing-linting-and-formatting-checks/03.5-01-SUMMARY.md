---
plan: 03.5-01
status: complete
completed: "2026-04-27"
commit: aed23f7
---

# Summary: Add format:check turbo task and package scripts

## What Was Built

Added `format:check` as a turbo task and package-level scripts to enable CI-safe format checking without writing files.

## Changes

### turbo.json
- Added `"format:check": { "dependsOn": ["^format:check"] }` task after the `format` task

### apps/web/package.json
- Added `"format:check": "prettier --check \"**/*.{ts,tsx}\""` after the `format` script

### packages/ui/package.json
- Added `"format:check": "prettier --check \"**/*.{ts,tsx}\""` after the `format` script

## Verification

`pnpm turbo format:check` runs successfully — exits non-zero when files need formatting (correct behavior; the repo has unformatted files in packages/ui).

## Key Files

- `turbo.json` — task graph with format:check
- `apps/web/package.json` — format:check script
- `packages/ui/package.json` — format:check script

## Self-Check: PASSED

All must-haves satisfied:
- ✓ `pnpm turbo format:check` exits non-zero when files need formatting (task graph works)
- ✓ `turbo.json` contains `format:check` task with `dependsOn: ["^format:check"]`
- ✓ `apps/web/package.json` has `format:check` script: `prettier --check "**/*.{ts,tsx}"`
- ✓ `packages/ui/package.json` has `format:check` script: `prettier --check "**/*.{ts,tsx}"`
