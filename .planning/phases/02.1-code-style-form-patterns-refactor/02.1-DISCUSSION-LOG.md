# Phase 02.1: Code Style & Form Patterns Refactor - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-02
**Phase:** 02.1-code-style-form-patterns-refactor
**Areas discussed:** Schema location, Ownership state, HouseholdSettings, __none__ sentinel, Async defaults

---

## Schema location

| Option | Description | Selected |
|--------|-------------|----------|
| Co-located in component | Schema defined at top of component file | |
| packages/validators | All Zod schemas alongside API schemas | ✓ |
| Per-feature schema file | e.g. accounts/accountSchema.ts | |

**User's choice:** packages/validators
**Notes:** Consistent with where API Zod schemas already live.

---

## Ownership state (AccountSheet)

| Option | Description | Selected |
|--------|-------------|----------|
| Form fields | ownership + memberIds become form.Field values; async existingMembers sets defaultValues | ✓ |
| Separate React state | Keep ownershipOverride/memberIdsOverride as useState alongside form | |

**User's choice:** Form fields (Recommended)

---

## HouseholdSettings

| Option | Description | Selected |
|--------|-------------|----------|
| Migrate it too | Consistent — every form uses the same pattern | ✓ |
| Leave as-is | One field, adding form library adds boilerplate with no gain | |

**User's choice:** Migrate it too

---

## __none__ sentinel (RuleDialog)

| Option | Description | Selected |
|--------|-------------|----------|
| Keep __none__ sentinel | Low risk; Zod transforms to null before API call | |
| Replace with null/undefined | Cleaner schema; requires Radix Select verification | ✓ |

**User's choice:** Replace with null/undefined
**Notes:** Researcher/planner must verify Radix Select handles null as a controlled value.

---

## Async defaults (AccountSheet ownership)

| Option | Description | Selected |
|--------|-------------|----------|
| Loading gate | Don't render form until existingMembers resolves; spinner in sheet | ✓ |
| form.reset() on data arrive | Form renders with empty defaults, resets via useEffect | |
| key prop re-mount | Re-mounts form when data loads; causes a flash | |

**User's choice:** Loading gate (Recommended)
**Notes:** No useEffect needed; form initializes with complete data.

---

## Claude's Discretion

- Exact Zod schema field names and transforms
- AppForm wrapper vs direct useAppForm usage
- Error message copy
- Loading spinner design inside AccountSheet

## Deferred Ideas

None.
