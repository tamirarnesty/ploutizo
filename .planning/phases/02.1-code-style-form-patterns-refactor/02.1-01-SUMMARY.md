---
phase: "02.1-code-style-form-patterns-refactor"
plan: "01"
subsystem: "ui-forms"
tags: ["tanstack-form", "zod", "validators", "shadcn", "foundation"]
dependency_graph:
  requires: []
  provides:
    - "useAppForm hook importable from @ploutizo/ui/components/form"
    - "FieldGroup, Field, FieldLabel, FieldError, FieldDescription from @ploutizo/ui/components/field"
    - "AccountFormSchema, CategoryFormSchema, RuleFormSchema, HouseholdSettingsFormSchema from @ploutizo/validators"
  affects:
    - "Wave 2 plans that import useAppForm and form schemas"
tech_stack:
  added:
    - "@tanstack/react-form@1.28.6"
    - "@tanstack/zod-form-adapter@0.42.1"
  patterns:
    - "createFormHookContexts + createFormHook composition pattern for project-wide useAppForm"
    - "PascalCase form schema naming (D-03a)"
    - "Form schemas in packages/validators only (D-03c)"
key_files:
  created:
    - "packages/ui/src/components/form.tsx"
    - "packages/ui/src/components/field.tsx"
  modified:
    - "packages/validators/src/index.ts"
    - "packages/validators/src/index.test.ts"
    - "packages/ui/package.json"
    - "apps/web/package.json"
decisions:
  - "Exports map in packages/ui/package.json uses glob ./components/* so no manual entries needed for form.tsx or field.tsx"
  - "Pre-existing typecheck failures in ReUI data-grid components are out of scope — confirmed pre-existed before this plan"
metrics:
  duration_seconds: 227
  completed_date: "2026-04-03"
  tasks_completed: 2
  files_modified: 6
---

# Phase 02.1 Plan 01: TanStack Form Foundation Summary

**One-liner:** TanStack Form v1 composition hook and shadcn field primitives installed in packages/ui; four PascalCase Zod form schemas with 14 passing unit tests added to packages/validators.

## What Was Built

### Task 1: Install deps, install Field primitives, create form.tsx

- Installed `@tanstack/react-form@1.28.6` and `@tanstack/zod-form-adapter@0.42.1` into `packages/ui`
- Installed `@tanstack/react-form@1.28.6` into `apps/web` (per D-01, makes the dependency visible)
- Used `pnpm dlx shadcn@latest add field --overwrite` to generate `packages/ui/src/components/field.tsx` with FieldGroup, Field, FieldLabel, FieldError, FieldDescription, and additional primitives (FieldLegend, FieldSeparator, FieldSet, FieldContent, FieldTitle)
- Created `packages/ui/src/components/form.tsx` with `createFormHookContexts + createFormHook` composition pattern, exporting `useAppForm`, `fieldContext`, `formContext`, `useFieldContext`
- Confirmed exports map glob `"./components/*": "./src/components/*.tsx"` covers both new files — no manual entries required

**Commit:** `974bb74`

### Task 2: Add form schemas to packages/validators (TDD)

**RED phase:** Added 14 failing tests to `packages/validators/src/index.test.ts` for all four form schemas — confirmed 14 failures before implementing schemas.

**GREEN phase:** Appended form schemas to `packages/validators/src/index.ts`:
- `AccountFormSchema` — name required, type enum, ownership enum (personal/shared), memberIds UUID array
- `CategoryFormSchema` — reuses `createCategorySchema` minus `sortOrder`
- `RuleFormSchema` — reuses `createMerchantRuleSchema` minus `assigneeId`/`priority`, overrides pattern with min(1) message
- `HouseholdSettingsFormSchema` — dollar string field with non-negative refine
- All four inferred type exports: `AccountForm`, `CategoryForm`, `RuleForm`, `HouseholdSettingsForm`

All 35 tests pass (21 existing + 14 new). Zero typecheck errors.

**Commit:** `df8d84e`

## Verification Results

- `pnpm test --filter @ploutizo/validators` — 35 tests pass
- `pnpm --filter @ploutizo/validators typecheck` — zero errors
- `packages/ui/src/components/form.tsx` exports: `useAppForm`, `fieldContext`, `formContext`, `useFieldContext`
- `packages/ui/src/components/field.tsx` exports: `Field`, `FieldLabel`, `FieldDescription`, `FieldError`, `FieldGroup` (plus extras)
- `packages/ui/package.json` — `@tanstack/react-form@1.28.6`, `@tanstack/zod-form-adapter@0.42.1`
- `apps/web/package.json` — `@tanstack/react-form@1.28.6`

## Deviations from Plan

### Out-of-Scope Discovery

**Pre-existing typecheck failures in packages/ui:** `pnpm --filter @ploutizo/ui typecheck` had 18 errors in `src/components/reui/data-grid/` from `@ploutizo/components` import aliases. These pre-existed before this plan (confirmed by git stash test). No action taken per scope boundary rule. Deferred to existing tracking.

No other deviations. Plan executed as written.

## Known Stubs

None. All four schemas are fully wired with real Zod validation logic. No placeholder data.

## Self-Check: PASSED

- `packages/ui/src/components/form.tsx` — FOUND
- `packages/ui/src/components/field.tsx` — FOUND
- `packages/validators/src/index.ts` — FOUND (AccountFormSchema, CategoryFormSchema, RuleFormSchema, HouseholdSettingsFormSchema exported)
- Commit `974bb74` — FOUND
- Commit `df8d84e` — FOUND
