---
status: complete
phase: 03.3.1-api-layering-and-structural-refactor-inserted
source: [03.3.1-01-SUMMARY.md, 03.3.1-02-SUMMARY.md, 03.3.1-03-SUMMARY.md, 03.3.1-04-SUMMARY.md, 03.3.1-05-SUMMARY.md, 03.3.1-06-SUMMARY.md, 03.3.1-07-SUMMARY.md]
started: 2026-04-14T00:00:00Z
updated: 2026-04-14T00:06:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Cold Start Smoke Test
expected: Kill any running server/service. Clear ephemeral state (temp DBs, caches, lock files). Start the API from scratch (e.g. pnpm --filter api dev or pnpm --filter api build && start). Server boots without errors, and a basic API call (e.g. GET /api/accounts or health check) returns a live response (not a 500 or startup crash).
result: pass

### 2. Accounts API — list and validation error
expected: GET /api/accounts (with valid org auth) returns an array of accounts. POST /api/accounts with a missing required field returns a 422 response with shape { error: { code: "VALIDATION_ERROR", ... } }.
result: issue
reported: "Create Account form shows [Object object] instead of validation error message"
severity: minor

### 3. Categories API — list and reorder
expected: GET /api/categories returns a categories array. Reorder (PUT/PATCH) with valid IDs succeeds. Invalid request body returns { error: { code: "VALIDATION_ERROR" } }.
result: pass

### 4. Tags API — list and create
expected: GET /api/tags returns a tags array. POST /api/tags with a valid name creates a tag. Duplicate or invalid input returns a structured error response.
result: pass

### 5. Merchant Rules — invalid regex returns INVALID_REGEX
expected: POST or PATCH /api/merchant-rules with an invalid regex pattern (e.g. "[unclosed") returns 400 with body { error: { code: "INVALID_REGEX" } }. Valid regex creates/updates the rule successfully.
result: pass

### 6. Households API — settings retrieval
expected: GET /api/household returns household info. GET /api/household/settings returns settings object. Both scoped to the authenticated org (other org's data not visible).
result: pass

### 7. Transactions API — list
expected: GET /api/transactions (with valid org auth) returns a paginated/filtered transactions response. Malformed query params return a 400/422 structured error, not a 500.
result: pass

### 8. Centralized error shapes — 404 and generic domain errors
expected: Request to a valid route with a non-existent resource ID (e.g. GET /api/accounts/nonexistent-id) returns 404 with { error: { code: "NOT_FOUND" } }. Any domain error returns a consistent shape (never a raw stack trace or HTML error page).
result: issue
reported: "Unmatched routes return plain-text '404 Not Found' — app.notFound() not registered in index.ts"
severity: minor
fixed: "Added app.notFound() in index.ts — commit 1c7a8cd. User confirmed fix works."

## Summary

total: 8
passed: 6
issues: 2
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Form validation errors display human-readable messages"
  status: failed
  reason: "User reported: Create Account form shows [Object object] instead of validation error message"
  severity: minor
  test: 2
  root_cause: "AccountForm.tsx:219 uses String(field.state.meta.errors[0]) — TanStack Form errors[0] is a ValidationError object, not a string"
  artifacts:
    - path: "apps/web/src/components/accounts/AccountForm.tsx"
      issue: "line 219: String(errors[0]) coerces object to [Object object]"
  missing:
    - "Use errors[0]?.message or errors[0]?.toString() across all forms"
  debug_session: ""

- truth: "Unmatched API routes return JSON error shape { error: { code: 'NOT_FOUND' } }"
  status: fixed
  reason: "User reported: curl returns plain-text '404 Not Found' with no JSON body"
  severity: minor
  test: 8
  root_cause: "app.notFound() handler not registered in index.ts"
  artifacts:
    - path: "apps/api/src/index.ts"
      issue: "Missing app.notFound() registration — fixed in commit 1c7a8cd"
  missing: []
  debug_session: ""
