---
phase: "03.3.1"
plan: "03"
subsystem: "api"
tags: ["query-layer", "drizzle", "refactor", "api-infrastructure"]
dependency_graph:
  requires:
    - "03.3.1-01 (RED tests for infrastructure)"
  provides:
    - "Pure Drizzle query functions for accounts route"
    - "Pure Drizzle query functions for categories route"
    - "Pure Drizzle query functions for tags route"
    - "Pure Drizzle query functions for merchant-rules route"
    - "Pure Drizzle query functions for households route"
  affects:
    - "apps/api/src/lib/queries/accounts.ts"
    - "apps/api/src/lib/queries/categories.ts"
    - "apps/api/src/lib/queries/tags.ts"
    - "apps/api/src/lib/queries/merchant-rules.ts"
    - "apps/api/src/lib/queries/households.ts"
tech_stack:
  added: []
  patterns:
    - "Pure query layer — no business logic, no getAuth, no DomainError throws"
    - "DrizzleTransaction type derived from db.transaction() inference"
    - "org-scoped WHERE clauses on all tenant-scoped operations"
key_files:
  created:
    - "apps/api/src/lib/queries/accounts.ts"
    - "apps/api/src/lib/queries/categories.ts"
    - "apps/api/src/lib/queries/tags.ts"
    - "apps/api/src/lib/queries/merchant-rules.ts"
    - "apps/api/src/lib/queries/households.ts"
  modified: []
decisions:
  - "households.ts contains only local DB queries — Clerk REST API calls remain in the route (future service layer); fetchOrgMemberWithUser extracts the externalId needed by the route's self-removal guard and Clerk membership delete"
  - "DrizzleTransaction type exported from accounts, categories, merchant-rules only (not tags, households — those functions don't participate in db.transaction() calls)"
metrics:
  duration_seconds: 420
  completed_date: "2026-04-14"
  tasks_completed: 2
  tasks_total: 2
  files_created: 5
  files_modified: 0
---

# Phase 03.3.1 Plan 03: Query Layer for Remaining Routes

5 new `lib/queries/` files extracting all Drizzle DB calls from accounts, categories, tags, merchant-rules, and households routes — pure I/O layer with org-scoped WHERE clauses, no business logic.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create query files for accounts, categories, and tags | 48d9851 | accounts.ts, categories.ts, tags.ts |
| 2 | Create query files for merchant-rules and households | 93b993f | merchant-rules.ts, households.ts |

## Deviations from Plan

None — plan executed exactly as written.

## Known Stubs

None — pure query functions with no UI rendering paths.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes. All query functions include `eq(table.orgId, orgId)` in WHERE clauses per T-03.3.1-03-01 mitigation.

## Self-Check: PASSED

- FOUND: apps/api/src/lib/queries/accounts.ts
- FOUND: apps/api/src/lib/queries/categories.ts
- FOUND: apps/api/src/lib/queries/tags.ts
- FOUND: apps/api/src/lib/queries/merchant-rules.ts
- FOUND: apps/api/src/lib/queries/households.ts
- FOUND: commit 48d9851 (Task 1)
- FOUND: commit 93b993f (Task 2)
- Build: ESM build success (248.54 KB, no TypeScript errors)
- Tests: 62 passing; 2 pre-existing RED failures from Plan 01 (webhookAuth.test.ts, tenantGuard orgId assertion) — not caused by this plan
