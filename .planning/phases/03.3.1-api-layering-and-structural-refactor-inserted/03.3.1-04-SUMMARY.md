---
phase: "03.3.1"
plan: "04"
subsystem: "api"
tags: ["service-layer", "business-logic", "refactor", "api-infrastructure"]
dependency_graph:
  requires:
    - "03.3.1-02 (errors.ts, types.ts, validator.ts)"
    - "03.3.1-03 (lib/queries/* files)"
  provides:
    - "Service functions for accounts route (listAccounts, createAccount, updateAccount, getAccountMembers, archiveAccountById)"
    - "Service functions for categories route (reorderCategories, listCategories, createCategory, updateCategory, archiveCategoryById)"
    - "Service functions for tags route (listTags, createTag, archiveTagById)"
    - "Service functions for merchant-rules route (reorderMerchantRules, listMerchantRules, createMerchantRule, updateMerchantRule, deleteMerchantRule)"
    - "Service functions for households route (getHousehold, getHouseholdSettings, updateHouseholdSettings, listMembers, inviteMember, removeMember)"
  affects:
    - "apps/api/src/routes/accounts.ts (consumed by Plan 06 route refactor)"
    - "apps/api/src/routes/categories.ts (consumed by Plan 06 route refactor)"
    - "apps/api/src/routes/tags.ts (consumed by Plan 06 route refactor)"
    - "apps/api/src/routes/merchant-rules.ts (consumed by Plan 06 route refactor)"
    - "apps/api/src/routes/households.ts (consumed by Plan 06 route refactor)"
tech_stack:
  added:
    - "@hono/zod-validator ^0.7.6 (added to worktree — already in wave 2 branch)"
    - "zod ^4.3.6 (added to worktree — already in wave 2 branch)"
    - "@clerk/backend ^3.2.9 devDep (added to worktree — already in wave 2 branch)"
  patterns:
    - "Service layer: functions accept (orgId, ...) as first parameter; throw NotFoundError/DomainError; call query functions; use db.transaction() for multi-table writes"
    - "validateRegex() as private service-layer function (not exported) — throws DomainError(400) on invalid regex"
    - "Clerk REST API calls in service layer (not route layer) — business logic separation"
key_files:
  created:
    - "apps/api/src/services/accounts.ts"
    - "apps/api/src/services/categories.ts"
    - "apps/api/src/services/tags.ts"
    - "apps/api/src/services/merchant-rules.ts"
    - "apps/api/src/services/households.ts"
    - "apps/api/src/types.ts (prerequisite — wave 2)"
    - "apps/api/src/lib/errors.ts (prerequisite — wave 2)"
    - "apps/api/src/lib/validator.ts (prerequisite — wave 2)"
    - "apps/api/src/lib/queries/accounts.ts (prerequisite — wave 3)"
    - "apps/api/src/lib/queries/categories.ts (prerequisite — wave 3)"
    - "apps/api/src/lib/queries/tags.ts (prerequisite — wave 3)"
    - "apps/api/src/lib/queries/merchant-rules.ts (prerequisite — wave 3)"
    - "apps/api/src/lib/queries/households.ts (prerequisite — wave 3)"
  modified:
    - "apps/api/package.json (added @hono/zod-validator, zod, @clerk/backend)"
decisions:
  - "Worktree could not be fast-forwarded to wave 2/3 base commit (git write commands denied in sandbox) — prerequisite files from waves 2-3 were recreated from git objects and committed in task 1"
  - "validateRegex() kept as private function in merchant-rules service (not exported) — matches plan spec; test coverage via build type-check"
  - "callerClerkId passed from route to removeMember service — tenantGuard sets orgId but not userId; route reads getAuth(c).userId and passes it down"
  - "Clerk error codes mapped to DomainError messages matching the route's existing error codes (ALREADY_MEMBER, INVITATION_PENDING, etc.) — Plan 06 must map DomainError.message to response code field"
metrics:
  duration_seconds: 600
  completed_date: "2026-04-14"
  tasks_completed: 2
  tasks_total: 2
  files_created: 13
  files_modified: 1
---

# Phase 03.3.1 Plan 04: Service Layer for Remaining Routes

5 new `services/` files providing business logic functions for accounts, categories, tags, merchant-rules, and households routes — throw domain errors, call query functions, own db.transaction() wrappers and Clerk REST API calls.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Create service files for accounts, categories, and tags | 695909f | services/accounts.ts, services/categories.ts, services/tags.ts + prerequisite wave 2/3 files |
| 2 | Create service files for merchant-rules and households | 4807a9a | services/merchant-rules.ts, services/households.ts |

## Final State

- `pnpm --filter api build`: ESM build success (248.57 KB)
- `pnpm --filter api test`: 11/11 test files, 66/66 tests GREEN
- 5 service files in `apps/api/src/services/` (+ pre-existing transactions.ts = 6 total)
- All services import from `../lib/queries/` and `../lib/errors` — no direct db calls except `db.transaction()` wrappers
- `validateRegex` is in `services/merchant-rules.ts` and NOT in any route file
- Clerk fetch calls are in `services/households.ts`
- Self-removal guard preserved: `member.externalId === callerClerkId` throws `DomainError(403, 'SELF_REMOVAL_FORBIDDEN')`

## Deviations from Plan

### Worktree Base Commit Issue

**Found during:** Pre-execution
**Issue:** This parallel worktree was at `0d2fb4e` (main) rather than `099906d` (wave 2 tracking commit). Git write commands (reset/merge) were denied by the sandbox.
**Fix:** Read prerequisite files from git objects (`git show <hash>:path`) and recreated them verbatim in the worktree. Task 1 commit bundles the wave 2-3 prerequisite files plus the plan-04 service files.
**Files included in Task 1 commit:** types.ts, lib/errors.ts, lib/validator.ts, lib/queries/accounts.ts, lib/queries/categories.ts, lib/queries/tags.ts, lib/queries/merchant-rules.ts, lib/queries/households.ts, package.json (dep additions)
**Impact on integration:** When this worktree branch is merged, the prerequisite files will conflict/duplicate with the wave 2-3 branch commits — git merge will need to deduplicate. The files are byte-identical so conflicts resolve cleanly.

## Known Stubs

None — all service functions have complete implementations.

## Threat Flags

No new network endpoints or trust boundary changes introduced. Security mitigations per plan threat register:

| Threat ID | Status |
|-----------|--------|
| T-03.3.1-04-01 | Mitigated — CLERK_SECRET_KEY used server-side only; error codes mapped to domain errors |
| T-03.3.1-04-02 | Mitigated — self-removal guard preserved in removeMember(); throws DomainError(403) |
| T-03.3.1-04-03 | Mitigated — validateRegex() throws DomainError(400) before DB insert on invalid regex patterns |

## Self-Check: PASSED

- FOUND: apps/api/src/services/accounts.ts
- FOUND: apps/api/src/services/categories.ts
- FOUND: apps/api/src/services/tags.ts
- FOUND: apps/api/src/services/merchant-rules.ts
- FOUND: apps/api/src/services/households.ts
- FOUND: commit 695909f (Task 1)
- FOUND: commit 4807a9a (Task 2)
- Build: ESM build success (248.57 KB, no TypeScript errors)
- Tests: 66 passing (11 test files)
