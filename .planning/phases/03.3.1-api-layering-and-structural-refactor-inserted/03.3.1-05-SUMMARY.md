---
plan: 03.3.1-05
phase: 03.3.1
status: complete
completed: 2026-04-14
---

# Plan 03.3.1-05 Summary

## What Was Built

- `apps/api/src/middleware/tenantGuard.ts` — patched to add `AppEnv` generic on `createMiddleware<AppEnv>` and `c.set('orgId', orgId)` before `await next()`
- `apps/api/src/middleware/webhookAuth.ts` — new middleware; verifies Svix HMAC signatures via `c.req.text()` (not json); sets `c.get('webhookEvent')` on context
- `apps/api/src/services/webhooks.ts` — typed webhook event dispatch using `Extract<WebhookEvent, { type: '...' }>['data']` narrowing; 5 handlers + `dispatchWebhookEvent`

## Key Decisions

- `webhookAuth.ts` reads raw payload via `c.req.text()` — JSON parsing would break Svix HMAC verification
- `tenantGuard.ts` now fully typed via `AppEnv`; `c.set('orgId', orgId)` is called after the seenOrgs upsert guard
- `services/webhooks.ts` uses `@clerk/backend` `WebhookEvent` discriminated union for type-safe handler dispatch
- Fixed test mock: `vi.fn().mockImplementation(function() {...})` instead of arrow function — Vitest 4.x requires a regular function/class when the mock is used as a constructor

## Test Results

All 66 tests GREEN (11 test files). `webhookAuth.test.ts` 3/3, `tenantGuard.test.ts` 8/8.

## key-files

### created
- apps/api/src/middleware/webhookAuth.ts
- apps/api/src/services/webhooks.ts

### modified
- apps/api/src/middleware/tenantGuard.ts
- apps/api/src/__tests__/webhookAuth.test.ts

## Self-Check: PASSED
