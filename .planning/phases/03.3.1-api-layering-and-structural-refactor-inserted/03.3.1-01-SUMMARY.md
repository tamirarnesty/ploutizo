---
phase: "03.3.1"
plan: "01"
subsystem: "api"
tags: ["tdd", "red-tests", "api-infrastructure", "testing"]
dependency_graph:
  requires: []
  provides:
    - "RED tests for appValidator VALIDATION_ERROR shape"
    - "RED tests for DomainError/NotFoundError + onError handler"
    - "RED tests for webhookAuth Svix signature verification"
    - "RED test asserting c.get('orgId') is set by tenantGuard"
  affects:
    - "apps/api/src/__tests__/tenantGuard.test.ts"
tech_stack:
  added:
    - "zod (direct dep on apps/api — required for validator test imports)"
  patterns:
    - "Hono test-app pattern (in-process request via app.request())"
    - "vi.mock for svix Webhook class with mockVerify spy"
key_files:
  created:
    - "apps/api/src/__tests__/validator.test.ts"
    - "apps/api/src/__tests__/errors.test.ts"
    - "apps/api/src/__tests__/webhookAuth.test.ts"
  modified:
    - "apps/api/src/__tests__/tenantGuard.test.ts"
    - "apps/api/package.json"
decisions:
  - "Added zod as direct api dependency — validator.test.ts imports zod directly for test schema construction; pnpm strict hoisting meant it was not accessible from @ploutizo/validators transitively"
metrics:
  duration_seconds: 152
  completed_date: "2026-04-14"
  tasks_completed: 2
  tasks_total: 2
  files_created: 3
  files_modified: 2
---

# Phase 03.3.1 Plan 01: TDD Red Tests for API Infrastructure

RED test scaffold for appValidator, DomainError/NotFoundError, webhookAuth middleware, and tenantGuard orgId propagation — all tests fail before implementation files exist.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Write failing tests for appValidator and domain errors | f825443 | validator.test.ts, errors.test.ts, package.json, pnpm-lock.yaml |
| 2 | Write failing tests for webhookAuth + update tenantGuard test | 8e89230 | webhookAuth.test.ts, tenantGuard.test.ts |

## Final State

- 4 test files failing (3 new: module-not-found RED; 1 updated: assertion RED)
- 7 pre-existing test files passing (54 tests passing)
- 1 new failing assertion in tenantGuard.test.ts (`sets orgId on context` — undefined vs 'org_context_test')

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Added zod as direct api dependency**
- **Found during:** Task 1
- **Issue:** `validator.test.ts` imports `zod` directly for test schema construction. pnpm strict hoisting does not expose `zod` from `@ploutizo/validators` to `apps/api` — the test would fail with "Cannot find package 'zod'" even after `../lib/validator` is created.
- **Fix:** `pnpm --filter api add zod` — adds zod as a direct `apps/api` dependency so the test (and the upcoming `lib/validator.ts` implementation) can resolve it.
- **Files modified:** `apps/api/package.json`, `pnpm-lock.yaml`
- **Commit:** f825443

## Known Stubs

None — this plan creates test files only, no implementation stubs.

## Threat Flags

None — test files only; no new network endpoints, auth paths, or DB access.

## Self-Check: PASSED

- FOUND: apps/api/src/__tests__/validator.test.ts
- FOUND: apps/api/src/__tests__/errors.test.ts
- FOUND: apps/api/src/__tests__/webhookAuth.test.ts
- FOUND: .planning/phases/03.3.1-api-layering-and-structural-refactor-inserted/03.3.1-01-SUMMARY.md
- FOUND: commit f825443 (Task 1)
- FOUND: commit 8e89230 (Task 2)
