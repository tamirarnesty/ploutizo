---
phase: 04.1
plan: 02
type: execute
wave: 2
depends_on: ["04.1-01"]
files_modified:
  - packages/validators/src/accounts.ts
  - packages/validators/src/index.test.ts
  - packages/types/src/index.ts
  - apps/api/src/__tests__/accounts.test.ts
  - apps/web/src/lib/data-access/accounts/useCreateAccount.ts
  - apps/web/src/lib/data-access/accounts/useUpdateAccount.ts
  - apps/web/src/components/accounts/AccountForm.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "No file in apps/, packages/validators, or packages/types references eachPersonPaysOwn (excluding apps/web/.output build artifacts and apps/api/dist)"
    - "Account create form no longer renders the 'Each person pays their own' checkbox or the Advanced collapsible that contained it"
    - "POST /api/accounts and PATCH /api/accounts/:id payloads do not include eachPersonPaysOwn"
    - "All consumer types (Account interface, mutation body interfaces) drop the eachPersonPaysOwn field"
    - "pnpm turbo typecheck and pnpm test pass"
  artifacts:
    - path: "packages/validators/src/accounts.ts"
      provides: "createAccountSchema, updateAccountSchema, AccountFormSchema (without eachPersonPaysOwn)"
      contains: "createAccountSchema"
    - path: "packages/types/src/index.ts"
      provides: "Account interface (without eachPersonPaysOwn)"
      contains: "interface Account"
    - path: "apps/web/src/components/accounts/AccountForm.tsx"
      provides: "Account create/edit form with the Advanced collapsible removed"
  key_links:
    - from: "AccountForm.tsx"
      to: "AccountFormSchema"
      via: "form validators"
      pattern: "AccountFormSchema.safeParse"
    - from: "useCreateAccount.ts / useUpdateAccount.ts"
      to: "POST /api/accounts"
      via: "apiFetch"
      pattern: "apiFetch.*\\/api\\/accounts"
---

<objective>
Remove `eachPersonPaysOwn` from the entire stack per D-07. The flag was designed for a person-to-person settlement model that this phase replaces with a card-centric model. After this plan, no validator, type, API mock, or UI component references the flag.

Purpose: Plan 01 dropped the column. Without this plan, `pnpm turbo typecheck` fails because validators, the `Account` interface, mutation body interfaces, mock fixtures, and `AccountForm.tsx` still reference the field. This plan must complete before any verification can pass.

Output: Validators, types, API tests, and the web AccountForm component cleaned up. Typecheck and full test suite pass.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.1-settlement-api/04.1-CONTEXT.md
@CLAUDE.md
@packages/validators/src/accounts.ts
@packages/types/src/index.ts
@apps/api/src/__tests__/accounts.test.ts
@apps/web/src/components/accounts/AccountForm.tsx
@apps/web/src/lib/data-access/accounts/useCreateAccount.ts
@apps/web/src/lib/data-access/accounts/useUpdateAccount.ts
@.claude/skills/vercel-react-best-practices/SKILL.md
@.claude/skills/vercel-composition-patterns/SKILL.md

<interfaces>
<!-- Current shapes the executor will modify. -->

From packages/validators/src/accounts.ts (current):
```typescript
export const createAccountSchema = z.object({
  name: z.string().min(1, 'Account name is required.'),
  type: z.enum(accountTypeValues, { error: 'Account type is required.' }),
  institution: z.string().optional(),
  lastFour: z.string().max(4).optional(),
  eachPersonPaysOwn: z.boolean().optional().default(false),  // <-- DELETE
  memberIds: z.array(z.string().uuid()).optional().default([]),
})

export const AccountFormSchema = z.object({
  name: z.string().min(1, 'Account name is required.'),
  type: z.enum(accountTypeValues, { error: 'Account type is required.' }),
  institution: z.string().optional(),
  lastFour: z.string().max(4).optional(),
  eachPersonPaysOwn: z.boolean().default(false),  // <-- DELETE
  ownership: z.enum(['personal', 'shared']),
  memberIds: z.array(z.string().uuid()).default([]),
})
```

From packages/types/src/index.ts (current):
```typescript
export interface Account {
  id: string
  orgId: string
  name: string
  type: AccountType
  institution: string | null
  lastFour: string | null
  eachPersonPaysOwn: boolean   // <-- DELETE
  archivedAt: string | null
  createdAt: string
  updatedAt: string
  owners: AccountOwner[]
}
```

Mutation body interfaces in `apps/web/src/lib/data-access/accounts/useCreateAccount.ts` (lines 5-13) and `useUpdateAccount.ts` (lines 5-13) BOTH contain `eachPersonPaysOwn?: boolean;` — DELETE that line in each.

API test mock fixtures in `apps/api/src/__tests__/accounts.test.ts` reference the field at lines 31, 50, 78, 100, 144, 186 — six mock object literal sites. Each line is a property assignment like `eachPersonPaysOwn: false,`. DELETE every such line.

AccountForm.tsx changes (the visually significant cleanup):
- Line 120-122: `useState` for `advancedOpen` initialized from `account?.eachPersonPaysOwn` — DELETE.
- Line 135: `eachPersonPaysOwn` default value in form `defaultValues` — DELETE.
- Line 162: `eachPersonPaysOwn: value.eachPersonPaysOwn,` in payload — DELETE.
- Lines 362-400 (the entire `{/* Field 7: eachPersonPaysOwn */}` block, including the wrapping `<form.AppField>`, `<Collapsible>`, and trailing newline): DELETE.
- Imports become unused after deletion. Remove from the imports at lines 1-54:
  - Line 1 `useState` is still used (for `advancedOpen` only). After deletion, verify no remaining `useState` calls; if none, remove `import { useState } from 'react';` entirely.
  - Lines 3-7 Collapsible imports: check whether any other site in this file uses `<Collapsible>`. If not (a grep against the file post-edit will confirm), delete the whole `from '@ploutizo/ui/components/collapsible'` import block.
  - Line 21 `Checkbox`: deleted block was the only consumer in this file. Verify and remove.
  - Line 19 `ChevronDown`: deleted block was the only consumer in this file. Verify and remove. `Archive` MUST stay (still used in archive button).

  The executor MUST grep each candidate import against the post-deletion file body to confirm before removing. Removing a still-used import will cause TS errors caught by typecheck.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove eachPersonPaysOwn from validators, types, mutation body interfaces, and API test fixtures</name>
  <read_first>
    - packages/validators/src/accounts.ts (delete two field references)
    - packages/validators/src/index.test.ts (delete 3 test sites — see acceptance criteria)
    - packages/types/src/index.ts (delete one interface field)
    - apps/web/src/lib/data-access/accounts/useCreateAccount.ts (delete one line in body interface)
    - apps/web/src/lib/data-access/accounts/useUpdateAccount.ts (delete one line in body interface)
    - apps/api/src/__tests__/accounts.test.ts (delete six mock-fixture lines at 31, 50, 78, 100, 144, 186)
  </read_first>
  <action>
    Edit each file as follows.

    **`packages/validators/src/accounts.ts`** — delete the `eachPersonPaysOwn` line in BOTH `createAccountSchema` and `AccountFormSchema`. The `updateAccountSchema` derives from `createAccountSchema.partial()` so no separate edit needed. After this, `createAccountSchema` has 5 fields (name, type, institution, lastFour, memberIds); `AccountFormSchema` has 6 (name, type, institution, lastFour, ownership, memberIds).

    **`packages/validators/src/index.test.ts`** — three sites reference `eachPersonPaysOwn`:
    - Line 54 area: a fixture object literal that includes `eachPersonPaysOwn: true` — delete that line.
    - Line 65-69 area: a test block titled `it('defaults eachPersonPaysOwn to false', ...)` that asserts `result.data.eachPersonPaysOwn` is `false`. The whole `it(...)` block (open paren through closing `})`) MUST be deleted because the field no longer exists. Do not leave a stub.
    - Line 148 area: another fixture object with `eachPersonPaysOwn: false,` — delete that line.

    **`packages/types/src/index.ts`** — delete `eachPersonPaysOwn: boolean` (line 25) from the `Account` interface.

    **`apps/web/src/lib/data-access/accounts/useCreateAccount.ts`** — delete line `eachPersonPaysOwn?: boolean;` from the `CreateAccountBody` interface.

    **`apps/web/src/lib/data-access/accounts/useUpdateAccount.ts`** — delete line `eachPersonPaysOwn?: boolean;` from the `UpdateAccountBody` interface.

    **`apps/api/src/__tests__/accounts.test.ts`** — delete the property assignment line `eachPersonPaysOwn: false,` (or `eachPersonPaysOwn: true,` at line 186) at each of the six sites: 31, 50, 78, 100, 144, 186. After deletion, surrounding object literals must remain syntactically valid (commas adjusted only if the deleted line was the trailing line — Drizzle/TypeScript object literals tolerate trailing commas, so plain deletion is safe).

    Do NOT touch AccountForm.tsx in this task — that is Task 2 (UI is a larger surface).
  </action>
  <verify>
    <automated>cd /Users/tarnesty/Developer/personal/ploutizo && pnpm --filter @ploutizo/validators typecheck && pnpm --filter @ploutizo/types typecheck && pnpm --filter @ploutizo/validators test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eachPersonPaysOwn" packages/validators/src/accounts.ts` == 0
    - `grep -c "eachPersonPaysOwn" packages/validators/src/index.test.ts` == 0
    - `grep -c "eachPersonPaysOwn" packages/types/src/index.ts` == 0
    - `grep -c "eachPersonPaysOwn" apps/web/src/lib/data-access/accounts/useCreateAccount.ts` == 0
    - `grep -c "eachPersonPaysOwn" apps/web/src/lib/data-access/accounts/useUpdateAccount.ts` == 0
    - `grep -c "eachPersonPaysOwn" apps/api/src/__tests__/accounts.test.ts` == 0
    - `pnpm --filter @ploutizo/validators typecheck` exits 0
    - `pnpm --filter @ploutizo/types typecheck` exits 0
    - `pnpm --filter @ploutizo/validators test` exits 0
  </acceptance_criteria>
  <done>Validators, types, API test fixtures, and mutation body interfaces have no remaining reference to `eachPersonPaysOwn`. Validator typecheck and tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove the Advanced collapsible (eachPersonPaysOwn checkbox) from AccountForm.tsx</name>
  <read_first>
    - apps/web/src/components/accounts/AccountForm.tsx (read in full — multiple edit sites)
    - .claude/skills/vercel-react-best-practices/SKILL.md (UI in apps/web triggers this skill — apply the SPA-applicable rule categories)
    - .claude/skills/vercel-composition-patterns/SKILL.md (component refactor triggers this skill — React 19 no forwardRef, etc.)
  </read_first>
  <action>
    Apply the four edits below. The web design guidelines and React best practices skills apply because this is a UI change in `apps/web` — the executor MUST read both SKILL.md files (web-design-guidelines is fetched via WebFetch per CLAUDE.md) before beginning, and not introduce any new pattern violations.

    **Edit 1 — line 120-122 (useState):** delete the line:
    ```typescript
    const [advancedOpen, setAdvancedOpen] = useState(
      account?.eachPersonPaysOwn ?? false
    );
    ```
    No other code in this file uses `useState`, so the `import { useState } from 'react';` at line 1 becomes dead — DELETE the import too. Verify by grep: post-edit, `grep -c "useState" apps/web/src/components/accounts/AccountForm.tsx` must be 0.

    **Edit 2 — line 135 (defaultValues):** in the `useAppForm({ defaultValues: {...} })` block, delete the line:
    ```typescript
    eachPersonPaysOwn: account?.eachPersonPaysOwn ?? false,
    ```

    **Edit 3 — line 162 (onSubmit payload):** in the `onSubmit` block's `payload` object literal, delete:
    ```typescript
    eachPersonPaysOwn: value.eachPersonPaysOwn,
    ```

    **Edit 4 — lines 362-400 (the entire Field 7 JSX block):** delete the block starting with the comment `{/* Field 7: eachPersonPaysOwn (inside Collapsible — advancedOpen is local useState, UI-only) */}` through the closing `</form.AppField>` (line 400 area). After deletion, the JSX flow goes directly from Field 6 (`form.Subscribe` for ownership/memberIds) into the `form.Subscribe` for `errorMap.onSubmit`.

    **Cleanup unused imports (after Edits 1-4 are applied):**
    - `Collapsible`, `CollapsibleContent`, `CollapsibleTrigger` (lines 3-7 import block): delete the entire import statement IF a grep confirms `Collapsible` no longer appears in the file body.
    - `Checkbox` (line 21): delete the import IF a grep confirms no remaining `<Checkbox` JSX in the file body. NOTE: Checkbox is used ONLY in the deleted block — it should be safe to remove.
    - `ChevronDown` (line 19, in the `lucide-react` import alongside `Archive`): KEEP `Archive` (used in the archive button at line 429). Edit the import to remove only `ChevronDown`. Resulting line: `import { Archive } from 'lucide-react';`.
    - `Label` (line 23): used in the deleted block ONLY — delete the import after grep-confirming.

    The executor MUST run `pnpm --filter @ploutizo/web typecheck` after the edits. The TS compiler will flag any import that should have been deleted but wasn't (or vice versa). Do not commit until typecheck is clean.

    **Skill compliance — flag during execution:**
    - vercel-react-best-practices: deleting `useState` removes one rerender source; verify no `setState` during render remains in this file.
    - web-design-guidelines (fetch fresh per CLAUDE.md trigger): the form still has all `autoComplete` attributes after the edit (Account name `off`, Institution `organization`, Last 4 `off`). No regressions in `aria-label`, focus, or motion.
    - composition: no boolean prop additions; no forwardRef introduced.
  </action>
  <verify>
    <automated>cd /Users/tarnesty/Developer/personal/ploutizo && pnpm turbo typecheck && pnpm turbo test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "eachPersonPaysOwn" apps/web/src/components/accounts/AccountForm.tsx` == 0
    - `grep -c "advancedOpen" apps/web/src/components/accounts/AccountForm.tsx` == 0
    - `grep -c "Collapsible" apps/web/src/components/accounts/AccountForm.tsx` == 0
    - `grep -c "ChevronDown" apps/web/src/components/accounts/AccountForm.tsx` == 0
    - `grep -c "useState" apps/web/src/components/accounts/AccountForm.tsx` == 0
    - `pnpm turbo typecheck` exits 0
    - `pnpm turbo test` exits 0 (full repo test suite, including the validators, API, and web)
    - Repo-wide grep — excluding build outputs — confirms no remaining live code references: `grep -rn "eachPersonPaysOwn" apps packages --include='*.ts' --include='*.tsx' | grep -v '\.output' | grep -v '/dist/' | grep -v node_modules | wc -l` outputs `0`
  </acceptance_criteria>
  <done>AccountForm.tsx no longer renders the Advanced collapsible. The full repo typechecks and tests pass. The `eachPersonPaysOwn` flag is fully retired across the live source tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web client → /api/accounts | Account create/update form posts JSON; tenantGuard enforces orgId scoping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1.02-01 | Tampering | Account create/update payload | mitigate | `appValidator('json', createAccountSchema)` strips unknown fields. After this plan, the schema rejects any `eachPersonPaysOwn` field — Zod's default object mode silently strips unknown keys, so an old client posting the old field receives a 200 but the field has no effect. Acceptable v1 behavior since the column is dropped. |
| T-04.1.02-02 | Information disclosure | Account list response | accept | Removing `eachPersonPaysOwn` from `Account` interface is a backwards-incompatible response shape, but only this single-tenant codebase consumes the field. No external API contract. |
| T-04.1.02-03 | Spoofing | tenantGuard | mitigate | Existing `tenantGuard` middleware unchanged. orgId comes from Clerk JWT via `c.get('orgId')`. |
</threat_model>

<verification>
- `grep -rn "eachPersonPaysOwn" apps packages` returns ZERO live source results (excludes `.output`, `dist`, `node_modules`)
- `pnpm turbo typecheck` exits 0
- `pnpm turbo test` exits 0
- AccountForm.tsx no longer imports `Collapsible`, `Checkbox`, `ChevronDown`, `Label`, or `useState`
- Account create form still passes through Personal/Shared toggle, Name, Type, Institution, Last 4, Owner picker
</verification>

<success_criteria>
The `eachPersonPaysOwn` flag is fully removed from the live source tree. The Account form UI no longer shows the Advanced collapsible. All packages typecheck and tests pass. The phase is unblocked for Plan 03's settlement balance API to land without typecheck collisions.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-settlement-api/04.1-02-SUMMARY.md` recording:
- Files modified
- Repo-wide grep result (must be 0)
- Tests + typecheck result
</output>
