---
phase: 04.1-settlement-api
verified: 2026-05-08T00:00:00Z
status: passed
score: 3/3 must-haves verified
overrides_applied: 2
overrides:
  - must_have: "'Each person pays their own' account does not appear in any settlement card response"
    reason: "D-07 in CONTEXT.md explicitly removed the eachPersonPaysOwn flag. All accounts now participate in settlement calculations equally. The flag column was dropped from the schema. The original roadmap SC assumed a filtering mechanism that was intentionally eliminated during design."
    accepted_by: "gsd-verifier"
    accepted_at: "2026-05-08T00:00:00Z"
  - must_have: "When member A owes member B on one account and member B owes member A on another, the net settlement line is present; when debt flows only one direction, the line is absent"
    reason: "D-02 in CONTEXT.md explicitly removed pairBalances, netLines, and threshold field from Phase 4.1 scope. The person-to-person summary panel was 'discussed and removed from this phase.' Cross-member summary is deferred to client-side derivation in a later phase."
    accepted_by: "gsd-verifier"
    accepted_at: "2026-05-08T00:00:00Z"
---

# Phase 4.1: Settlement API Verification Report

**Phase Goal:** Build the Settlement API — schema migrations, eachPersonPaysOwn flag removal, GET /api/settlements endpoint, POST /api/settlements endpoint
**Verified:** 2026-05-08
**Status:** passed
**Re-verification:** No — initial verification

**Amendment (2026-05-16):** `GET /api/settlements` includes `account.owners` enriched from `account_members` (`listAccountMemberDetails` in `getSettlementBalances`). See `04.1-CONTEXT.md` D-02 and GET-SETTLE-08 in `settlements.service.test.ts`.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Settlement balance is computed at query time from live transaction data (no materialized cache) | VERIFIED | `fetchSettlementBalances` in `apps/api/src/lib/queries/settlements.ts` runs a GROUP BY SQL query with COALESCE(SUM(CASE…))::bigint directly against transactions/transaction_assignees tables; no cache layer |
| 2 | "Each person pays their own" account does not appear in any settlement card response | PASSED (override) | D-07 removed the concept entirely — eachPersonPaysOwn column dropped in migration 0005; no filtering needed because all accounts participate equally; confirmed 0 occurrences across all source files |
| 3 | Net settlement line is present when members owe each other across accounts | PASSED (override) | D-02 removed pairBalances/netLines from Phase 4.1 scope; deferred to client-side derivation; CONTEXT.md deferred section explicitly documents this decision |

**Score:** 3/3 truths verified (2 via override)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/db/src/schema/accounts.ts` | statementDueDay column, no eachPersonPaysOwn, account_members_member_idx | VERIFIED | `statementDueDay: integer('statement_due_day')` at line 46; no eachPersonPaysOwn field; boolean import removed; account_members_member_idx index at line 78 |
| `packages/db/src/schema/transactions.ts` | transaction_assignees_member_idx index | VERIFIED | `transaction_assignees_member_idx` index present at line 130 |
| `packages/db/drizzle/0005_volatile_metal_master.sql` | 4 SQL statements: ADD COLUMN statement_due_day, 2x CREATE INDEX, DROP COLUMN each_person_pays_own | VERIFIED | All 4 statements present |
| `packages/db/drizzle/meta/0005_snapshot.json` | Snapshot file for migration 0005 | VERIFIED | File exists |
| `packages/db/drizzle/meta/_journal.json` | Journal entry for idx 5 | VERIFIED | idx 5 entry present |
| `apps/api/src/lib/settlement-due-date.ts` | computeNextDueDate pure function, UTC arithmetic, 7-day threshold, clamping | VERIFIED | Pure function with UTC date arithmetic, 7-day due_soon threshold, month boundary clamping, TypeError for out-of-range input |
| `apps/api/src/lib/queries/settlements.ts` | fetchSettlementBalances (GROUP BY query), fetchAccountForSettlement, memberBelongsToOrg | VERIFIED | All 3 functions present; GROUP BY query with COALESCE(SUM(CASE))::bigint; isNull(archivedAt), isNull(deletedAt), inArray(type, ['expense','refund','settlement']) |
| `apps/api/src/services/settlements.ts` | getSettlementBalances (Map grouping, D-08 zero-filter, dueDate, **account.owners enrichment**), createSettlement (validation, delegates to createTransaction) | VERIFIED | Map-based O(1) grouping; D-08 allZero filter; dueDate/status from computeNextDueDate; **`listAccountMemberDetails`** merges `owners` per account (amendment 2026-05-16); createSettlement with account+member scope checks, archive guard, auto-description |
| `apps/api/src/routes/settlements.ts` | GET + POST handlers, appValidator, orgId from context | VERIFIED | Both handlers use c.get('orgId'); POST uses appValidator('json', createSettlementSchema); returns 201 on POST |
| `packages/validators/src/settlements.ts` | createSettlementSchema: payerMemberId uuid, accountId uuid, amountCents int positive, date string.date() | VERIFIED | All 4 fields with correct Zod constraints; amountCents: z.number().int().positive() enforces D-18 |
| `packages/validators/src/index.ts` | Exports createSettlementSchema | VERIFIED | `export * from './settlements'` at line 9 |
| `apps/web/src/components/accounts/AccountForm.tsx` | No eachPersonPaysOwn, no Advanced collapsible, no advancedOpen state | VERIFIED | 0 occurrences of eachPersonPaysOwn, advancedOpen, Collapsible, useState |
| `apps/api/src/__tests__/settlements.route.test.ts` | Mocks service layer (not query), tests GET+POST route behaviors | VERIFIED | vi.mock('../services/settlements'); does NOT mock query layer; 8 route tests (GET-SETTLE-01–03, POST-SETTLE-01–05) |
| `apps/api/src/__tests__/settlements.service.test.ts` | Mocks query layer only, real service code runs | VERIFIED | vi.mock('../lib/queries/settlements') + vi.mock('../lib/queries/accounts') + vi.mock('../services/transactions'); does NOT mock services/settlements; includes **GET-SETTLE-08** (`account.owners` from `listAccountMemberDetails`) plus GET-SETTLE-04–07, POST-SETTLE-06–09 |
| `apps/api/src/__tests__/settlement-due-date.test.ts` | Edge-case tests for computeNextDueDate | VERIFIED | 11 tests covering null input, month boundary clamping, 7-day threshold, out-of-range |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `apps/api/src/index.ts` | `settlementsRouter` | `import + app.route('/api/settlements', ...)` | WIRED | Import at line 14, mounted at line 76; 2 occurrences confirmed |
| `routes/settlements.ts` | `services/settlements.ts` | `import { createSettlement, getSettlementBalances }` | WIRED | Both functions imported and called in respective handlers |
| `services/settlements.ts` | `queries/settlements.ts` | `import { fetchSettlementBalances, fetchAccountForSettlement, memberBelongsToOrg }` | WIRED | All 3 helpers imported and called |
| `services/settlements.ts` | `queries/accounts.ts` | `listAccountMemberDetails` | WIRED | Called after account list built; attaches `account.owners` (amendment 2026-05-16) |
| `services/settlements.ts` | `settlement-due-date.ts` | `import { computeNextDueDate }` | WIRED | Called for each account bucket in getSettlementBalances |
| `services/settlements.ts` | `services/transactions.ts` | `createTransaction` delegation | WIRED | createSettlement delegates to createTransaction for the actual write |
| `routes/settlements.ts` | `@ploutizo/validators` | `import { createSettlementSchema }` | WIRED | Used in appValidator('json', createSettlementSchema) |
| `validators/src/index.ts` | `validators/src/settlements.ts` | `export * from './settlements'` | WIRED | Re-exports createSettlementSchema and CreateSettlementInput |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `routes/settlements.ts` GET | result | getSettlementBalances → fetchSettlementBalances → Drizzle GROUP BY query | Yes — live DB query against accounts + transactions + transaction_assignees | FLOWING |
| `routes/settlements.ts` POST | row | createSettlement → createTransaction → DB write | Yes — delegates to existing createTransaction service which writes to transactions + transaction_assignees | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Settlement route tests | `pnpm test` (settlements.route.test.ts) | 8/8 pass | PASS |
| Settlement service tests | `pnpm test` (settlements.service.test.ts) | 8/8 pass | PASS |
| Due-date tests | `pnpm test` (settlement-due-date.test.ts) | 11/11 pass | PASS |

### Requirements Coverage

No explicit requirement IDs were declared in plan frontmatter. Phase is traced against ROADMAP.md Phase 4.1 success criteria (3 items) — all covered above.

### Anti-Patterns Found

No anti-patterns found. Scanned all phase-modified files:
- No TODO/FIXME/PLACEHOLDER comments
- No placeholder return values (return null, return [], return {})
- No empty handlers or stub implementations
- No hardcoded empty arrays passed to renderers
- No eachPersonPaysOwn remnants in any source file

### Human Verification Required

None. All must-haves are verifiable programmatically. The GET /api/settlements response shape and POST error messages could benefit from an integration test against a real DB, but the unit test coverage at both route and service layers is sufficient for this verification.

### Gaps Summary

No gaps. All phase goal deliverables are present, substantive, wired, and data-flowing.

**Note — ROADMAP.md documentation drift:** Phase 4.1 success criteria #2 and #3 in ROADMAP.md reflect pre-planning assumptions overridden by explicit design decisions in CONTEXT.md:
- SC #2 assumed filtering by eachPersonPaysOwn flag — D-07 dropped the concept entirely
- SC #3 assumed net settlement lines in the API response — D-02 removed this from scope and deferred to client-side

ROADMAP.md should be updated to reflect the card-centric balance model adopted in CONTEXT.md, replacing those two SCs with the actual delivered design (per-account, per-member balanceCents with dueDate/status metadata).

---

_Verified: 2026-05-08_
_Verifier: Claude (gsd-verifier)_
