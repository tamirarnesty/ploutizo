---
phase: "04.1"
plan: "02"
subsystem: "validators, types, api, web"
tags: ["cleanup", "field-removal", "eachPersonPaysOwn"]
dependency_graph:
  requires: ["04.1-01"]
  provides: ["clean-validator-schemas", "clean-account-type", "clean-account-form"]
  affects: ["packages/validators", "packages/types", "apps/api", "apps/web"]
tech_stack:
  added: []
  patterns: ["delete-dead-field"]
key_files:
  created: []
  modified:
    - packages/validators/src/accounts.ts
    - packages/validators/src/index.test.ts
    - packages/types/src/index.ts
    - apps/web/src/lib/data-access/accounts/useCreateAccount.ts
    - apps/web/src/lib/data-access/accounts/useUpdateAccount.ts
    - apps/api/src/__tests__/accounts.test.ts
    - apps/web/src/components/accounts/AccountForm.tsx
decisions:
  - "Deleted the eachPersonPaysOwn test block entirely rather than leaving a stub — field no longer exists in schema"
  - "Both tasks committed separately per plan; pre-commit typecheck enforced correctness"
metrics:
  duration: "~25 min"
  completed: "2026-05-08"
  tasks: 2
  files: 7
---

# Phase 04.1 Plan 02: Remove eachPersonPaysOwn Summary

Retire `eachPersonPaysOwn` from the entire stack. The flag was designed for a person-to-person settlement model replaced by the card-centric model introduced in this phase.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Remove from validators, types, mutation interfaces, API tests | ff84086 | 6 files |
| 2 | Remove Advanced collapsible from AccountForm | 7a3267d | 1 file |

## Files Modified

| File | Change |
|------|--------|
| `packages/validators/src/accounts.ts` | Deleted `eachPersonPaysOwn` from `createAccountSchema` and `AccountFormSchema` |
| `packages/validators/src/index.test.ts` | Deleted 3 sites: fixture line, full `it('defaults eachPersonPaysOwn')` block, AccountFormSchema fixture line |
| `packages/types/src/index.ts` | Deleted `eachPersonPaysOwn: boolean` from `Account` interface |
| `apps/web/src/lib/data-access/accounts/useCreateAccount.ts` | Deleted `eachPersonPaysOwn?: boolean` from `CreateAccountBody` |
| `apps/web/src/lib/data-access/accounts/useUpdateAccount.ts` | Deleted `eachPersonPaysOwn?: boolean` from `UpdateAccountBody` |
| `apps/api/src/__tests__/accounts.test.ts` | Deleted 6 mock-fixture property lines |
| `apps/web/src/components/accounts/AccountForm.tsx` | Removed Field 7 JSX block, advancedOpen useState, and 5 unused imports (useState, Collapsible*, Checkbox, ChevronDown, Label) |

## Verification

**Repo-wide grep result:**
```
grep -rn "eachPersonPaysOwn" apps packages --include='*.ts' --include='*.tsx' | grep -v .output | grep -v /dist/ | grep -v node_modules | wc -l
0
```

**Tests:** `pnpm turbo test` — 81 API tests + 5 web tests + 69 validator tests: all pass

**Typecheck:** `pnpm turbo typecheck` — 6/6 packages pass

**Pre-commit hooks passed on both commits.**

## Deviations from Plan

None. Plan executed exactly as written.

### Skill Compliance (Task 2 — UI change in apps/web)

- `vercel-react-best-practices`: Deleting `useState` removes one unnecessary rerender source. No `setState` during render remains in AccountForm.tsx.
- `web-design-guidelines`: All existing `autoComplete` attributes preserved (account-name `off`, account-institution `organization`, account-last-four `off`). No regressions in `aria-label` or focus.
- `vercel-composition-patterns`: No boolean props added. No `forwardRef` introduced.

## Known Stubs

None.

## Threat Flags

None. The field removal shrinks the attack surface: unknown `eachPersonPaysOwn` fields posted by stale clients are silently stripped by Zod's default `strip` mode. No new trust boundaries introduced.

## Self-Check: PASSED

- [x] `ff84086` exists: `git log --oneline | grep ff84086`
- [x] `7a3267d` exists: `git log --oneline | grep 7a3267d`
- [x] `packages/validators/src/accounts.ts` — modified, no eachPersonPaysOwn
- [x] `packages/types/src/index.ts` — modified, no eachPersonPaysOwn
- [x] `apps/web/src/components/accounts/AccountForm.tsx` — modified, no Field 7 block
- [x] Repo-wide grep: 0
- [x] `pnpm turbo typecheck`: PASS
- [x] `pnpm turbo test`: PASS
