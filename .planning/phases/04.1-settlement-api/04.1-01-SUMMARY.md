---
phase: "04.1"
plan: "01"
subsystem: "db"
tags: ["schema", "migration", "settlement", "drizzle"]
dependency_graph:
  requires: []
  provides: ["accounts.statementDueDay", "account_members_member_idx", "transaction_assignees_member_idx"]
  affects: ["04.1-02", "04.1-03"]
tech_stack:
  added: []
  patterns: ["drizzle-kit generate (expect TTY workaround)", "drizzle-kit migrate"]
key_files:
  created:
    - packages/db/drizzle/0005_volatile_metal_master.sql
    - packages/db/drizzle/meta/0005_snapshot.json
  modified:
    - packages/db/src/schema/accounts.ts
    - packages/db/src/schema/transactions.ts
    - packages/db/drizzle/meta/_journal.json
decisions:
  - "boolean import removed from accounts.ts (noUnusedLocals: true enforcement)"
  - "drizzle-kit generate requires expect(1) workaround in non-TTY executor context — selected create column (not rename) for statement_due_day"
  - "Journal entry is idx 5 (not idx 4 as plan stated) — there were already 5 prior migrations (idx 0-4); plan had a count error; outcome is correct"
metrics:
  duration: "~4 minutes"
  completed: "2026-05-08T13:02:18Z"
  tasks_completed: 2
  tasks_total: 2
  files_created: 2
  files_modified: 3
---

# Phase 04.1 Plan 01: Settlement Schema Migration Summary

Settlement schema migration: adds `statement_due_day` integer column (nullable) to `accounts`, drops `each_person_pays_own` boolean, and adds `account_members_member_idx` + `transaction_assignees_member_idx` performance indexes for balance queries.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Edit Drizzle schema files | e45a813 | packages/db/src/schema/accounts.ts, packages/db/src/schema/transactions.ts |
| 2 | Generate and apply migration | fb8a6c4 | packages/db/drizzle/0005_volatile_metal_master.sql, meta/_journal.json, meta/0005_snapshot.json |

## Migration File

Generated: `packages/db/drizzle/0005_volatile_metal_master.sql`

```sql
ALTER TABLE "accounts" ADD COLUMN "statement_due_day" integer;--> statement-breakpoint
CREATE INDEX "account_members_member_idx" ON "account_members" USING btree ("member_id");--> statement-breakpoint
CREATE INDEX "transaction_assignees_member_idx" ON "transaction_assignees" USING btree ("member_id");--> statement-breakpoint
ALTER TABLE "accounts" DROP COLUMN "each_person_pays_own";
```

`pnpm db:migrate` exited 0. Dev DB schema updated.

## Verification

- `pnpm --filter @ploutizo/db typecheck` passes (pre-commit hook ran full turbo typecheck on all 6 packages — all pass)
- Generated SQL contains all 4 required statements (grep verified)
- `_journal.json` updated with idx 5 entry (`0005_volatile_metal_master`)
- `0005_snapshot.json` created

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing Critical Functionality] Removed `boolean` import after dropping `eachPersonPaysOwn`**
- **Found during:** Task 1
- **Issue:** Plan explicitly noted that `boolean` becomes unused after the field drop and `noUnusedLocals: true` is set in `packages/db/tsconfig.json`. The executor removed it as instructed.
- **Fix:** Removed `boolean` from the `drizzle-orm/pg-core` import list; added `integer`.
- **Files modified:** packages/db/src/schema/accounts.ts
- **Commit:** e45a813

### Process Deviations

**1. drizzle-kit generate requires TTY (expect workaround)**
- `pnpm db:generate` ran non-interactively and hit drizzle-kit's TTY guard when detecting the potential column rename (`each_person_pays_own` → `statement_due_day`).
- Resolution: Used `expect(1)` to spawn with a pseudo-TTY and select "create column" (correct per plan — this is a DROP + ADD, not a rename).
- No SQL hand-editing was performed.

**2. Journal idx is 5, not 4**
- The plan stated "new entry with `idx: 4` (zero-indexed entry for the 5th migration)". However there were already 5 prior migrations (idx 0–4), making this the 6th at idx 5. The outcome is correct; the plan had a count error in the doc comment.

## Known Stubs

None — this plan is schema-only. No UI rendering or data-source wiring involved.

## Threat Surface Scan

No new network endpoints, auth paths, file access patterns, or schema changes at trust boundaries beyond those documented in the plan's threat model. T-04.1.01-01 (migration SQL integrity) mitigated via grep acceptance criteria verified before `db:migrate` was run.

## Self-Check: PASSED

- [x] packages/db/src/schema/accounts.ts — contains `statementDueDay: integer('statement_due_day')`
- [x] packages/db/src/schema/transactions.ts — contains `transaction_assignees_member_idx`
- [x] packages/db/drizzle/0005_volatile_metal_master.sql — exists with 4 required statements
- [x] packages/db/drizzle/meta/0005_snapshot.json — exists
- [x] packages/db/drizzle/meta/_journal.json — has idx 5 entry
- [x] Commits e45a813 and fb8a6c4 exist on branch gsd/phase-04.1-settlement-api
