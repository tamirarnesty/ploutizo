# Phase 03.2.1: Household Settings Consolidation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-11
**Phase:** 03.2.1-household-improvement-new-ui-with-settings-consolidation-and
**Areas discussed:** Page Structure, Member Display, Invite Form UI, Org Name Editing, Invite API

---

## Todo Folded

**Consolidate household settings page with member invitations** (score 0.9) — folded in full. All solution points captured in CONTEXT.md decisions D-01 through D-20.

---

## Page Structure

| Option | Description | Selected |
|--------|-------------|----------|
| Flat sections with dividers | Single scrollable page, `<Separator />` between sections. Same pattern as existing Household page. | ✓ |
| Card-per-section | Each section in a shadcn `<Card>`. More visual weight. | |

**Section order selected:** Overview → Members → Invite (inline in Members section) → Settlement Threshold

---

## Member Display

| Option | Description | Selected |
|--------|-------------|----------|
| Avatar + name + role | Show imageUrl as Avatar, initials fallback. Clean now that data is available. | ✓ |
| Name + role only | Plain text list. Ignores new imageUrl data. | |

**Member actions:**

| Option | Description | Selected |
|--------|-------------|----------|
| No actions, display only | All admin in v1, remove is non-trivial. | |
| Remove member button | Confirm dialog → DELETE API → Clerk + local DB removal. | ✓ |

**Remove flow:** Confirm dialog → `DELETE /api/households/members/:memberId`. Self-removal disabled (button hidden/disabled on "You" row).

---

## Invite Form UI

| Option | Description | Selected |
|--------|-------------|----------|
| Inline below member list | Email input + Send invite button. No dialog. Always visible. | ✓ |
| Sheet / dialog triggered by button | + Invite member button opens sheet. | |

**Success feedback:** Success toast + clear input ("Invite sent to email@example.com")

**Error feedback:** Specific error toasts per error code (already member, pending invite, generic fallback)

---

## Org Name Editing

| Option | Description | Selected |
|--------|-------------|----------|
| Read-only display only | Show name + avatar as overview header. No editing. | ✓ |
| Inline editable name field | PATCH endpoint calling Clerk org update. More work. | |

**Org image:** Avatar with initials fallback (consistent with member list pattern)

---

## Invite API

**User note:** "invite flow through API, not UI directly" — confirmed as `POST /api/households/invitations` calling Clerk REST API from Hono, not `useOrganization().inviteMember()` from the client.

| Option | Description | Selected |
|--------|-------------|----------|
| Call Clerk backend API only | Hono calls Clerk REST POST /v1/organizations/{orgId}/invitations. Role hardcoded org:admin. | ✓ |
| Call Clerk + record in local invitations table | Extra DB work; Clerk already tracks this. | |

**Error mapping:** Clerk error codes → specific app error codes → specific toast messages.

---

## Claude's Discretion

- Avatar size for member rows
- Exact toast wording
- Spacing/padding within sections
- Whether to inline OrganizationMembersSettings logic or delete and rewrite
- Loading skeleton for overview section

## Deferred Ideas

- Role management → v2
- Org name editing → OrganizationSwitcher handles it
- Local invitations table → v2
- Pending invitations list → future phase
- Remove-self / leave household → post-v1
