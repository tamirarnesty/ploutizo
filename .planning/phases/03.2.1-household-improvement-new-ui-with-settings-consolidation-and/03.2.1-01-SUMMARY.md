---
phase: 03.2.1
plan: 01
subsystem: packages/ui
tags: [shadcn, components, avatar, sonner, toast]
dependency_graph:
  requires: []
  provides:
    - "@ploutizo/ui/components/avatar — Avatar, AvatarImage, AvatarFallback, AvatarGroup, AvatarGroupCount, AvatarBadge"
    - "@ploutizo/ui/components/sonner — Toaster"
  affects:
    - "apps/web (unblocks plans 02 and 03 which import avatar and sonner)"
tech_stack:
  added:
    - "sonner@^2.0.7 (npm package, added to packages/ui dependencies)"
  patterns:
    - "shadcn CLI invoked from packages/ui directory (pnpm dlx shadcn@latest add)"
    - "glob exports pattern ./components/* auto-includes new components"
key_files:
  created:
    - packages/ui/src/components/avatar.tsx
    - packages/ui/src/components/sonner.tsx
  modified:
    - packages/ui/package.json
    - pnpm-lock.yaml
decisions:
  - "shadcn CLI run from packages/ui/ (pnpm dlx), not packages/ui filter — pnpm --filter does not support dlx in this pnpm version"
  - "sonner.tsx uses next-themes useTheme — confirmed next-themes is already a packages/ui dependency via theme-provider.tsx re-export"
metrics:
  duration: "61s"
  completed: "2026-04-12T01:06:15Z"
  tasks_completed: 2
  files_changed: 4
---

# Phase 03.2.1 Plan 01: Install Avatar and Sonner Components Summary

Install shadcn Avatar and Sonner toast components into `packages/ui`, unblocking Plans 02 and 03 which depend on `@ploutizo/ui/components/avatar` and `@ploutizo/ui/components/sonner`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Install Avatar and Sonner components via shadcn CLI | 67fda56 | packages/ui/src/components/avatar.tsx, packages/ui/src/components/sonner.tsx, packages/ui/package.json, pnpm-lock.yaml |
| 2 | Verify build passes after component installs | (no files changed) | pnpm build exit 0 |

## Deviations from Plan

**1. [Rule 3 - Blocking] pnpm --filter does not support dlx**

- **Found during:** Task 1
- **Issue:** `pnpm --filter @ploutizo/ui dlx shadcn@latest add avatar` fails with "Unknown option: 'recursive'"
- **Fix:** Ran `pnpm dlx shadcn@latest add avatar` directly from `packages/ui/` directory — same effect
- **Files modified:** None (only affected how CLI was invoked)
- **Commit:** 67fda56

## Known Stubs

None.

## Threat Flags

None — generated UI primitive files with no user data handling.

## Self-Check: PASSED

- packages/ui/src/components/avatar.tsx: FOUND
- packages/ui/src/components/sonner.tsx: FOUND
- Commit 67fda56: FOUND (git log confirmed)
- pnpm build: exit 0
