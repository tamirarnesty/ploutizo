---
phase: 03.2.1
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/ui/src/components/avatar.tsx
  - packages/ui/src/components/sonner.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Avatar component is importable from @ploutizo/ui/components/avatar"
    - "Sonner toaster component is importable from @ploutizo/ui/components/sonner"
    - "Project builds without errors after installs"
  artifacts:
    - path: "packages/ui/src/components/avatar.tsx"
      provides: "Avatar, AvatarImage, AvatarFallback components"
    - path: "packages/ui/src/components/sonner.tsx"
      provides: "Toaster component (shadcn Sonner)"
  key_links:
    - from: "apps/web/src/components/settings/HouseholdSettings.tsx"
      to: "@ploutizo/ui/components/avatar"
      via: "named import"
      pattern: "from.*@ploutizo/ui/components/avatar"
    - from: "apps/web/src/components/settings/HouseholdSettings.tsx"
      to: "@ploutizo/ui/components/sonner"
      via: "named import"
      pattern: "from.*@ploutizo/ui/components/sonner"
---

<objective>
Install the Avatar and Sonner (toast) shadcn components into packages/ui. These are confirmed missing from packages/ui/src/components/ and are required by the UI work in Plan 03.

Purpose: Unblock Plans 02 and 03 which depend on Avatar (member row avatars, org overview) and Sonner (invite success/error toasts). Without this Wave 0 step, both plans produce import errors.
Output: avatar.tsx and sonner.tsx committed to packages/ui/src/components/, project builds clean.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.2.1-household-improvement-new-ui-with-settings-consolidation-and/03.2.1-CONTEXT.md
@.planning/phases/03.2.1-household-improvement-new-ui-with-settings-consolidation-and/03.2.1-RESEARCH.md

<interfaces>
<!-- Key config/files the executor needs to understand before running CLI commands. -->

From apps/web/components.json (shadcn config — CLI must be run from apps/web/):
```json
{
  "style": "base-nova",
  "baseColor": "stone",
  "cssVariables": true
}
```

packages/ui/src/components/ currently contains: alert-dialog.tsx, badge.tsx, button.tsx, input.tsx, label.tsx, separator.tsx, spinner.tsx, tabs.tsx, textarea.tsx — but NOT avatar.tsx or sonner.tsx.

packages/ui/package.json has `shadcn` as a devDependency at ^4.2.0.

The correct install command (from apps/web/ dir, targeting packages/ui):
  pnpm --filter @ploutizo/ui dlx shadcn@latest add avatar
  pnpm --filter @ploutizo/ui dlx shadcn@latest add sonner
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Avatar and Sonner components via shadcn CLI</name>
  <files>packages/ui/src/components/avatar.tsx, packages/ui/src/components/sonner.tsx</files>
  <read_first>
    - apps/web/components.json (shadcn config — defines style preset and output paths)
    - packages/ui/src/components/ (verify avatar.tsx and sonner.tsx do not exist before running)
    - packages/ui/package.json (confirm shadcn devDep present)
  </read_first>
  <action>
    Run from the repo root (shadcn CLI reads apps/web/components.json automatically when invoked via pnpm filter):

    Step 1 — Install Avatar:
      cd /Users/tarnesty/Developer/personal/ploutizo
      pnpm --filter @ploutizo/ui dlx shadcn@latest add avatar

    If that fails (CLI doesn't pick up components.json from filter context), run from apps/web/:
      cd apps/web && pnpm dlx shadcn@latest add avatar

    Step 2 — Install Sonner:
      pnpm --filter @ploutizo/ui dlx shadcn@latest add sonner
    (or from apps/web/ if filter approach failed)

    Both components should land in packages/ui/src/components/ as:
    - avatar.tsx — exports `Avatar`, `AvatarImage`, `AvatarFallback`
    - sonner.tsx — exports `Toaster` (wraps sonner's Toaster with theme prop)

    After install, verify packages/ui exports map includes the new components. The glob pattern `./components/*` in packages/ui/package.json exports field should auto-include them — no manual package.json edits needed.

    Also verify that the sonner npm package is now a dependency of packages/ui (shadcn add installs it automatically). If not, add manually:
      pnpm --filter @ploutizo/ui add sonner
  </action>
  <verify>
    <automated>ls /Users/tarnesty/Developer/personal/ploutizo/packages/ui/src/components/avatar.tsx && ls /Users/tarnesty/Developer/personal/ploutizo/packages/ui/src/components/sonner.tsx && echo "Both files exist"</automated>
  </verify>
  <done>
    - packages/ui/src/components/avatar.tsx exists and exports Avatar, AvatarImage, AvatarFallback
    - packages/ui/src/components/sonner.tsx exists and exports Toaster
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build passes after component installs</name>
  <files></files>
  <read_first>
    - packages/ui/src/components/avatar.tsx (check exports are correct)
    - packages/ui/src/components/sonner.tsx (check exports are correct)
  </read_first>
  <action>
    Run the full project build to confirm no type errors or import resolution failures:

      pnpm build

    If build fails with a missing `sonner` npm package error, install it:
      pnpm --filter @ploutizo/ui add sonner

    Then re-run pnpm build until it passes.

    Note: Do not modify the generated avatar.tsx or sonner.tsx files — override behavior at usage sites per CLAUDE.md constraint. If the generated files have minor style issues (e.g. wrong import path), fix the import paths only, not the component logic.
  </action>
  <verify>
    <automated>pnpm build</automated>
  </verify>
  <done>
    `pnpm build` exits 0 with no type errors. Both avatar.tsx and sonner.tsx are present in packages/ui/src/components/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → packages/ui | shadcn CLI writes files to packages/ui; output is generated code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.2.1-W0-01 | Tampering | avatar.tsx / sonner.tsx (generated) | accept | Generated files are read-only UI primitives with no user data handling; no mitigations required |
</threat_model>

<verification>
- packages/ui/src/components/avatar.tsx exists
- packages/ui/src/components/sonner.tsx exists
- `pnpm build` exits 0
</verification>

<success_criteria>
Both `@ploutizo/ui/components/avatar` and `@ploutizo/ui/components/sonner` resolve without errors. Project build is green.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2.1-household-improvement-new-ui-with-settings-consolidation-and/03.2.1-01-SUMMARY.md`
</output>
