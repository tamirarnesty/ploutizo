---
plan: "03.4.1-01"
phase: "03.4.1"
status: complete
completed: "2026-04-20"
---

# Plan 03.4.1-01: DB Schema Migration — COMPLETE

## What Was Built

Applied the v2 DB schema migration to the `transactions` table:
- Dropped 5 obsolete columns: `merchant`, `income_source`, `to_account_id`, `settled_account_id`, `investment_type`
- Added 3 new columns: `counterpart_account_id` (FK → accounts, ON DELETE SET NULL), `raw_description` (text), `notes` (text)
- Made `description` NOT NULL
- Migration applied in safe order: ADD nullable → backfill → NOT NULL → DROP

## Key Files

- `packages/db/src/schema/transactions.ts` — Drizzle schema updated
- `packages/db/drizzle/0004_empty_mephistopheles.sql` — Migration with manual backfill DML

## Deviations

- Drizzle Kit generated `SET NOT NULL` before `ADD COLUMN` — manually reordered to correct safe migration order
- Added 4 `UPDATE` backfill statements not generated by Drizzle Kit:
  - `counterpart_account_id` ← `to_account_id` for transfer rows
  - `counterpart_account_id` ← `settled_account_id` for settlement rows
  - `description` backfill for any NULL rows
  - `raw_description` ← `merchant` for imported rows

## Self-Check: PASSED

- ✓ DB has `counterpart_account_id`, `raw_description`, `notes`
- ✓ DB does NOT have `merchant`, `income_source`, `to_account_id`, `settled_account_id`, `investment_type`
- ✓ `description` is NOT NULL
- ✓ TypeScript compiles clean (`pnpm --filter @ploutizo/db typecheck`)
- ✓ Migration applied without errors
