---
phase: 03.4.1-transaction-table-redesign
verified: 2026-04-21T00:00:00Z
status: human_needed
score: 11/11
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 9/11
  gaps_closed:
    - "All 6 transaction form variants submit successfully including transfer, settlement, contribution, and linked refund (CR-01 fixed via DescriptionSyncer)"
    - "Single-member org auto-selects assignee on create — form state matches UI state (WR-03 fixed via amountCents-reactive useEffect)"
    - "PATCH type change nulls stale type-specific columns (WR-01 fixed via typeSpecificNulls in updateTransaction)"
    - "categoryId 'is_not' filter includes uncategorized (NULL) transactions (WR-04 fixed via or(ne, isNull))"
  gaps_remaining: []
  regressions: []
human_verification:
  - test: "Open create-transaction drawer, select type = Transfer, fill in both account fields; verify description auto-fills and submit succeeds"
    expected: "Transaction saves; description in DB matches template (e.g. 'Transfer from Checking to Savings'). No 'Description is required.' validation error."
    why_human: "DescriptionSyncer fires useEffect on mount/lockedValue change — correct wiring confirmed statically; timing and actual form submission require browser + auth session"
  - test: "In a single-member org, open create-transaction drawer; do NOT click the assignee toggle; enter an amount; submit"
    expected: "Transaction saves with 100% split assigned to the single member (amountCents-reactive useEffect fires after amount entry)"
    why_human: "WR-03 fix uses amountCents as effect dep — requires real form interaction with amount entry to verify sync fires at correct time and produces correct centage values"
  - test: "Lock a transfer description, click unlock, edit to custom text, save, re-open"
    expected: "Custom description persists; field is NOT re-locked to template on re-open"
    why_human: "UnlockResetter session state transition requires browser interaction"
  - test: "Create a transfer (A→B), then edit it to change type to Expense; reopen the table row"
    expected: "Account column shows plain account name (not 'A → B'); no counterpartAccountId FK in DB"
    why_human: "WR-01 fix — typeSpecificNulls confirmed in code; actual DB state and table re-render require browser"
  - test: "Apply filter 'Category is not [any category]'; verify uncategorized transactions appear in results"
    expected: "Uncategorized transactions (NULL categoryId) are included — WR-04 or(ne, isNull) fix"
    why_human: "SQL NULL semantics fix confirmed in code; actual query result set requires live DB + browser"
  - test: "Apply date filter 'is not Apr 20, 2026', refresh the page; verify filter persists with correct operator"
    expected: "URL contains dateRange_op=is_not; filter chip shows 'is not' operator; table excludes Apr 20 rows"
    why_human: "URL round-trip confirmed statically; actual calendar interaction and filter chip display require browser"
  - test: "Account A→B rendering: create a transfer, view table; create an expense, view table"
    expected: "Transfer row shows 'AccountA → AccountB'; expense row shows plain account name only"
    why_human: "Visual table rendering with real data requires browser"
  - test: "Refund sub-line clickthrough: create refund linked to expense; click the ↩ sub-line"
    expected: "Original transaction opens in edit drawer"
    why_human: "onOpenOriginal callback wiring cannot be fully verified without browser interaction"
  - test: "Internal filter shortcut: click 'Internal' in filter bar"
    expected: "Table shows only transfer + settlement + contribution rows; type filter chip appears"
    why_human: "Multi-value type filter state and visual result require browser"
  - test: "Amount colour rendering: view table rows for all 6 transaction types"
    expected: "Expense: red negative; income and refund: green positive; transfer/settlement/contribution: muted unsigned"
    why_human: "Computed Tailwind colours require browser render"
---

# Phase 03.4.1: Transaction Table Redesign — Verification Report

**Phase Goal:** Simplify the transaction schema and deliver a redesigned form and table that work correctly for all 6 transaction types. The current schema has redundant nullable columns and the form/table are expense-focused; this phase fixes both.
**Verified:** 2026-04-21T00:00:00Z
**Status:** human_needed
**Re-verification:** Yes — after gap closure (plans 07–10)

## Re-verification Summary

All 4 gaps from the initial verification (2026-04-20) are closed. Score advances from 9/11 to 11/11.

| Gap | Plan that closed it | Closure evidence |
|-----|---------------------|-----------------|
| CR-01: locked description not synced to form state | Not plan 10 (earlier plan or concurrent) | `DescriptionSyncer` component (TransactionForm.tsx:127-134) wired at line 347-350; `onSync={field.handleChange}` |
| WR-03: single-member auto-press not synced to form state | Not plan 10 (earlier plan or concurrent) | `useEffect(() => { if (!transaction && amountCents > 0 && pressedMemberIds.length > 0) onChange(...) }, [amountCents])` at AssigneeSection.tsx:62-67 |
| WR-01: PATCH type change leaves stale FK columns | Not plan 10 (earlier plan or concurrent) | `typeSpecificNulls` block in `updateTransaction` at services/transactions.ts:144-158 |
| WR-04: categoryId `is_not` excludes NULL rows | Not plan 10 (earlier plan or concurrent) | `or(ne(transactions.categoryId, params.categoryId), isNull(transactions.categoryId))` at queries/transactions.ts:165-167 |

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | DB schema drops 5 stale columns and adds counterpartAccountId, rawDescription, notes | ✓ VERIFIED | packages/db/src/schema/transactions.ts: no merchant/incomeSource/toAccountId/settledAccountId/investmentType; counterpartAccountId, rawDescription, notes present |
| 2 | Migration runs without data loss | ? HUMAN | Migration file exists; data-loss behaviour requires running against real DB |
| 3 | All 6 Zod schemas updated: stale fields removed, new fields added, description required | ✓ VERIFIED | packages/validators/src/transactions.ts: baseTransactionSchema has notes, no merchant; transferSchema has counterpartAccountId uuid required; description min(1) on base |
| 4 | API query layer uses TX_COLUMNS projection with new fields and alias joins | ✓ VERIFIED | queries/transactions.ts: TX_COLUMNS includes counterpartAccountName, rawDescription, notes, refundOfId/Date/AmountCents; both queries have counterpartAccount alias join and refundOf alias join |
| 5 | All 6 transaction form variants submit successfully including transfer, settlement, contribution, and linked refund | ✓ VERIFIED | CR-01 closed: DescriptionSyncer renders inside AppField render prop at TransactionForm.tsx:347-351, calling field.handleChange via onSync when isLocked && lockedValue |
| 6 | Form has notes field and tags side-by-side at bottom | ✓ VERIFIED | TransactionForm.tsx: notes AppField and tags in flex gap-4 row; always rendered |
| 7 | Toggle-based assignee section with LRM split recalculation | ✓ VERIFIED | AssigneeSection.tsx: Toggle per orgMember, handleToggle calls lrmSplit, Collapsible customize-split expander present |
| 8 | Single-member org auto-selects assignee on create — form state matches UI state | ✓ VERIFIED | WR-03 closed: useEffect at AssigneeSection.tsx:62-67 fires when amountCents > 0 in create mode; calls onChange(lrmSplit(amountCents, pressedMemberIds)) |
| 9 | Table account column renders plain name for single-account types and A→B for transfer/settlement | ✓ VERIFIED | TransactionColumns.tsx: counterpartAccountName && `${accountName} → ${counterpartAccountName}` branch present; plain accountName fallback |
| 10 | Amount column is signed and colour-coded per type | ✓ VERIFIED | TransactionColumns.tsx: text-destructive + U+2212 for expense; text-emerald-600 for income/refund; text-muted-foreground unsigned for transfer/settlement/contribution |
| 11 | Filter operator params round-trip URL → API → buildConditions for all 6 filter fields | ✓ VERIFIED | transactionSearch.ts: 6 _op fields; routes/transactions.ts: all 6 parsed; buildConditions: all 6 have operator branching |

**Score:** 11/11 truths verified

### Plan 10 Must-Haves (UAT Gap Closure)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| P10-1 | Contribution picker trigger shows account name only (no INVESTMENT prefix) | ✓ VERIFIED | ContributionFields.tsx:44-48: `return a ? a.name : 'Select account'` inside SelectValue; SelectItem still has `${a.type.toUpperCase()} — ${a.name}` |
| P10-2 | After editing notes and saving, reopening the edit sheet shows the updated note without a page refresh | ✓ VERIFIED | useUpdateTransaction.ts:16-22: onSuccess calls `qc.setQueryData(['transaction', id], updatedRow)`; onSettled invalidates list cache |
| P10-3 | Transfer form 'To account' picker excludes the currently-selected source account | ✓ VERIFIED | TransferFields.tsx:40-43: `useMemo(() => accounts.filter((a) => a.id !== sourceAccountId), ...)` inside TransferDestinationField; hooks called at proper component boundary |
| P10-4 | Date filter shows correct operator set (is/is not/before/after/between/not between) | ✓ VERIFIED | TransactionFilterFields.tsx:189-196: explicit 6-operator array on dateRange field |
| P10-5 | Single-date ops show single-month calendar; range ops show dual-month calendar | ✓ VERIFIED | TransactionFilterFields.tsx:133-145: `isSingleDateOp(operator)` branches mode="single" vs mode="range" numberOfMonths={2} |
| P10-6 | Trigger label shows plain date for single-date ops and A – B for range ops | ✓ VERIFIED | TransactionFilterFields.tsx:100-110: label computed from isSingleDateOp(operator); single shows `MMM d, yyyy`; range shows `... – ...` with en-dash |
| P10-7 | Switching operators migrates values without crashing | ✓ VERIFIED | TransactionFilterFields.tsx:73-91: prevOperatorRef + valuesRef + migrateValues function; effect only calls onChange when shape changes |
| P10-8 | No duplicate React key warning in FilterOperatorDropdown | ✓ VERIFIED | TransactionFilterFields.tsx:189-196: exactly one `value: 'is'` entry in explicit operators list; overrides ReUI default that had duplicate |

**Plan 10 score:** 8/8 must-haves verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/db/src/schema/transactions.ts` | Schema v2 with new columns | ✓ VERIFIED | counterpartAccountId, rawDescription, notes present; 5 stale columns absent |
| `packages/validators/src/transactions.ts` | 6 updated Zod schemas | ✓ VERIFIED | All 6 discriminated union variants updated; description required on all |
| `apps/api/src/lib/queries/transactions.ts` | TX_COLUMNS + alias joins + operator branching for all 6 date operators | ✓ VERIFIED | buildConditions handles is/is_not/before/after/between/not_between with eq/ne/not(and()) |
| `apps/api/src/services/transactions.ts` | updateTransaction with type-change nulling | ✓ VERIFIED | WR-01 closed: typeSpecificNulls block explicitly nulls counterpartAccountId, refundOf, incomeType, categoryId on type change |
| `apps/api/src/routes/transactions.ts` | POST/PATCH org-scoping guards + 6 _op params | ✓ VERIFIED | counterpartAccount and refundOf org checks present; all 6 op params parsed |
| `apps/web/src/components/transactions/TransactionForm.tsx` | Form for all 6 types with locked descriptions synced to state | ✓ VERIFIED | CR-01 closed: DescriptionSyncer component at lines 127-134; wired at lines 347-350 |
| `apps/web/src/components/transactions/AssigneeSection.tsx` | Toggle section with auto-press and LRM sync on mount | ✓ VERIFIED | WR-03 closed: useEffect at lines 62-67 syncs to form state when amountCents > 0 in create mode |
| `apps/web/src/components/transactions/TransactionColumns.tsx` | Amount, account A→B, refund sub-line, opacity | ✓ VERIFIED | All 4 column features substantively implemented |
| `apps/web/src/components/transactions/transactionSearch.ts` | 6 _op fields in URL schema including dateRange_op comment with all 6 values | ✓ VERIFIED | Line 28: comment lists 'between' | 'after' | 'before' | 'is' | 'is_not' | 'not_between' |
| `apps/web/src/components/transactions/ContributionFields.tsx` | Trigger shows a.name only; dropdown retains INVESTMENT prefix | ✓ VERIFIED | SelectValue at line 47 returns a.name; SelectItem at line 55 retains full prefix |
| `apps/web/src/lib/data-access/transactions/useUpdateTransaction.ts` | setQueryData on per-record key in onSuccess; invalidate list in onSettled | ✓ VERIFIED | Lines 16-22: both present |
| `apps/web/src/components/transactions/TransferFields.tsx` | counterpartAccountId options exclude current accountId; hooks at proper boundary | ✓ VERIFIED | TransferDestinationField inner component with useMemo + useRef guard useEffect |
| `apps/web/src/components/transactions/TransactionFilterFields.tsx` | DateRangeFilterRenderer with operator-aware mode + label + migration + 6 explicit operators | ✓ VERIFIED | All features present; SINGLE_DATE_OPS/RANGE_OPS sets, migrateValues, valuesRef, prevOperatorRef |
| `apps/web/src/components/transactions/Transactions.tsx` | filtersToSearch + searchToFilters handle is/is_not/not_between | ✓ VERIFIED | Both functions handle all 6 date operators; lines 55-77 and 143-151 |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| TransactionForm.tsx isLocked branch | field state | DescriptionSyncer onSync=field.handleChange | ✓ WIRED | DescriptionSyncer at line 347-350 calls field.handleChange(lockedValue) via useEffect |
| AssigneeSection initialPressedIds | form.assignees | useEffect on amountCents | ✓ WIRED | Lines 62-67: fires when !transaction && amountCents > 0 && pressedMemberIds.length > 0 |
| updateTransaction data.type change | stale FK columns | typeSpecificNulls merge | ✓ WIRED | services/transactions.ts:144-158: typeSpecificNulls built and Object.assign'd into updateData |
| buildConditions categoryId is_not | NULL rows | or(ne, isNull) | ✓ WIRED | queries/transactions.ts:165-167: or(ne(...), isNull(...)) for is_not op |
| DateRangeFilterRenderer | operator prop | CustomRendererProps.operator | ✓ WIRED | TransactionFilterFields.tsx:197-203: operator passed through to DateRangeFilterRenderer |
| filtersToSearch | dateRange_op URL param | Transactions.tsx serialisation | ✓ WIRED | Lines 55-77: all 6 ops handled; 'between' omitted per buildCleanSearch strip at line 43 |
| buildConditions | drizzle WHERE clause | dateRange_op param | ✓ WIRED | queries/transactions.ts:133-155: is/is_not/not_between cases all present |
| filtersToSearch _op fields | URL params | buildCleanSearch strips defaults | ✓ WIRED | Non-default operators written to URL; defaults stripped |
| URL _op params | buildConditions | routes parser + ListQueryParams | ✓ WIRED | Full chain: URL → c.req.query → params → buildConditions branching |
| counterpartAccountId POST/PATCH | org ownership check | checkCounterpartAccountOwnership | ✓ WIRED | Guard present before DB write in both POST and PATCH |
| TX_COLUMNS | counterpartAccount alias | counterpartAccount.name | ✓ WIRED | alias() join used in both list and single-fetch queries |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| TransactionColumns.tsx amount cell | row.amountCents | TX_COLUMNS → DB | Yes — integer column | ✓ FLOWING |
| TransactionColumns.tsx account cell | row.accountName, row.counterpartAccountName | TX_COLUMNS alias joins → DB | Yes — joined from accounts table | ✓ FLOWING |
| TransactionColumns.tsx description cell + sub-line | row.description, row.refundOfId, row.refundOfDate, row.refundOfAmountCents | TX_COLUMNS alias joins → DB | Yes — refundOf alias join returns real values | ✓ FLOWING |
| TransactionForm.tsx locked description | lockedValue (computed) + field.state.value (synced) | DescriptionSyncer useEffect → field.handleChange | Synced to form state on mount/change — CR-01 fixed | ✓ FLOWING |
| AssigneeSection pressedIds | pressedMemberIds → onChange(lrmSplit(...)) | amountCents-reactive useEffect | Real member IDs + real split values forwarded to form state | ✓ FLOWING |

### Behavioral Spot-Checks

Step 7b: SKIPPED — behavioral checks require a running browser + auth session. The phase produces browser-rendered React components with form state that cannot be meaningfully spot-checked via CLI without running the full SPA stack.

### Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| D-01 | 01, 02 | Schema v2 columns | ✓ SATISFIED | DB schema and validators updated |
| D-02 | 01 | Migration without data loss | ? NEEDS HUMAN | Migration file present; data-loss requires runtime check |
| D-03 | 01, 03, 04 | API reads/writes new columns | ✓ SATISFIED | TX_COLUMNS, services, routes all updated |
| D-04 | 01, 03 | Single transaction fetch with new fields | ✓ SATISFIED | fetchTransactionById uses same TX_COLUMNS + alias joins |
| D-05 | 01 | Org-scoping enforced on all operations | ✓ SATISFIED | counterpartAccount org check + refundOf org check in POST/PATCH |
| D-06 | 02, 04 | All 6 Zod types updated | ✓ SATISFIED | All 6 discriminated union variants updated |
| D-07 | 02, 04, 05 | description required on all 6 types | ✓ SATISFIED | baseTransactionSchema.description z.string().min(1) |
| D-08 | 02, 04 | counterpartAccountId on transfer/settlement | ✓ SATISFIED | required on transfer, optional on settlement |
| D-09 | 05 | Form field components renamed/updated | ✓ SATISFIED | ContributionFields, TransferFields, SettlementFields, TransactionTypeFields updated |
| D-10 | 05, 06 | notes field on form (always shown) | ✓ SATISFIED | notes AppField present in TransactionForm.tsx |
| D-11 | 06 | Toggle-based assignees with LRM split | ✓ SATISFIED | AssigneeSection substantively implemented; LRM calculation present |
| D-12 | 06 | "Customize split" expander | ✓ SATISFIED | Collapsible customize-split section in AssigneeSection |
| D-13 | 01, 06 | refundOf org-ownership validation | ✓ SATISFIED | checkRefundOfOwnership guard in POST and PATCH |
| D-14 | 05 | tags always shown (Notes + Tags row) | ✓ SATISFIED | flex gap-4 row with notes and tags in TransactionForm.tsx |
| D-15 | 05, 06 | Soft delete and restore wired | ✓ SATISFIED | delete and restore routes present in routes/transactions.ts |
| D-16 | 06 | Locked description for transfer/settlement/contribution | ✓ SATISFIED | CR-01 closed: DescriptionSyncer syncs lockedValue to field state via useEffect + onSync=field.handleChange |
| D-17 | 06 | Locked description for linked refund | ✓ SATISFIED | shouldLock includes `type === 'refund' && !!refundOf`; same DescriptionSyncer path |
| D-18 | 06 | Unlock affordance (UnlockResetter component) | ✓ SATISFIED | UnlockResetter component present in TransactionForm.tsx |
| D-19 | 06 | Single-member auto-press sync | ✓ SATISFIED | WR-03 closed: amountCents-reactive useEffect in AssigneeSection syncs to form state |
| D-20 | 05 | Settlement "Paid from" field | ✓ SATISFIED | SettlementFields.tsx updated |
| D-21 | 06 | Contribution account picker shows member name | ✓ SATISFIED | ContributionFields.tsx trigger shows a.name only; plan 10 P10-1 |
| D-22 | 07 | Refund sub-line in table (↩ date · amount) | ✓ SATISFIED | TransactionColumns.tsx: sub-line present with U+21A9 and U+00B7 |
| D-23 | 03, 04, 07 | Account A→B column rendering | ✓ SATISFIED | counterpartAccountName conditional in TransactionColumns.tsx |
| D-24 | 07 | Amount sign/colour per type | ✓ SATISFIED | text-destructive/text-emerald-600/text-muted-foreground per type group |
| D-25 | 07, 08 | Filter bar "Internal" shortcut + operator persistence | ✓ SATISFIED | Internal filter in Transactions.tsx; all 6 _op fields in URL schema and buildConditions; date filter now has 6 ops including is/is_not/not_between |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| apps/api/src/routes/transactions.ts | 155-162 | Over-broad catch returns 400 for any error in PATCH | WARNING (WR-02) | DB/infra errors surface as "bad request" instead of 500 — deferred, out of scope |
| TransactionTypeFields.tsx | 46, 175, 182 | Dead onTypeChange prop (superseded by UnlockResetter) | INFO | Dead code; no functional impact |
| TransactionForm.tsx + TransactionColumns.tsx | 119 / 47 | LOCKED_TYPES / isInternalType hardcoded independently | INFO | Maintenance hazard; no functional impact |
| IncomeFields.tsx | — | File returns null; call site removed; unused | INFO | Dead file; no functional impact |

All BLOCKER and WARNING anti-patterns from the initial verification are resolved:
- CR-01 (TransactionForm locked description) → RESOLVED via DescriptionSyncer
- WR-03 (AssigneeSection mount sync) → RESOLVED via amountCents-reactive useEffect
- WR-01 (PATCH type change stale FKs) → RESOLVED via typeSpecificNulls
- WR-04 (categoryId is_not NULL exclusion) → RESOLVED via or(ne, isNull)

### Human Verification Required

#### 1. Locked description create flow (CR-01 fix)

**Test:** Open create-transaction drawer, select type = Transfer, fill in both account fields. Verify auto-filled description appears in description input. Submit without touching description.
**Expected:** Transaction saves; description in DB matches generated template (e.g. "Transfer from Checking to Savings"). No validation error.
**Why human:** DescriptionSyncer confirmed wired statically; timing of useEffect and actual form state at submit requires browser + auth session.

#### 2. Single-member auto-press (WR-03 fix)

**Test:** In a single-member org, open create-transaction drawer. Do NOT click the assignee toggle. Enter an amount. Fill required fields and submit.
**Expected:** Transaction saves with 100% split assigned to the single member.
**Why human:** Fix uses amountCents as effect dep — requires real form interaction with amount entry to verify the sync fires at the correct time with correct cent values.

#### 3. Type-switch FK nulling (WR-01 fix)

**Test:** Create a transfer transaction (A→B). Edit it and change type to Expense. Close and reopen; view in table.
**Expected:** Account column shows plain account name (not "A → B"); no counterpartAccountId FK persists.
**Why human:** typeSpecificNulls confirmed in code; actual DB state and table re-render require browser.

#### 4. categoryId is_not with uncategorized rows (WR-04 fix)

**Test:** Apply filter "Category is not [any category]". Verify uncategorized transactions appear in results.
**Expected:** Uncategorized transactions (NULL categoryId) are included in the result set.
**Why human:** SQL NULL semantics fix confirmed in code; actual query result set requires live DB + browser.

#### 5. Date filter is_not + page refresh round-trip

**Test:** Apply date filter "is not Apr 20, 2026". Refresh the page. Verify filter persists with the "is not" operator active.
**Expected:** URL contains dateRange_op=is_not; filter chip shows "is not" operator; table excludes Apr 20 rows.
**Why human:** URL round-trip confirmed statically via filtersToSearch/searchToFilters; actual calendar interaction and filter chip display require browser.

#### 6. Account A→B rendering

**Test:** Create a transfer transaction, view in table. Create an expense, view in table.
**Expected:** Transfer row shows "AccountA → AccountB"; expense row shows plain account name only.
**Why human:** Table rendering with real joined data requires browser.

#### 7. Refund sub-line clickthrough

**Test:** Create a refund linked to an existing expense. Find refund row in table. Click the ↩ sub-line.
**Expected:** Original transaction opens in edit drawer showing correct details.
**Why human:** onOpenOriginal callback wiring to drawer state cannot be fully verified without browser interaction.

#### 8. Internal filter shortcut

**Test:** Click the "Internal" shortcut in the transaction filter bar.
**Expected:** Table shows only transfer + settlement + contribution rows; active filter chip appears for type filter.
**Why human:** Multi-value type filter state and visual result require browser.

#### 9. Amount colour rendering

**Test:** View table rows for each of the 6 transaction types.
**Expected:** Expense: red negative; income and refund: green positive; transfer/settlement/contribution: muted unsigned.
**Why human:** Computed Tailwind colours require browser render.

#### 10. Migration data integrity

**Test:** Run migration against a DB with pre-migration data; verify existing rows are intact post-migration.
**Expected:** No data loss; all existing rows readable with new schema.
**Why human:** Cannot verify data-loss behaviour without running against real DB.

## Gaps Summary

No blocking gaps remain. All 4 gaps from the initial verification are closed:

- **CR-01** (locked description not written to form state): closed by `DescriptionSyncer` component rendering inside the AppField render prop, using `useEffect` to call `field.handleChange(lockedValue)` when `isLocked && lockedValue`.
- **WR-03** (single-member auto-press UI/form state mismatch): closed by an `amountCents`-reactive `useEffect` in `AssigneeSection` that calls `onChange(lrmSplit(amountCents, pressedMemberIds))` when a non-zero amount is present in create mode.
- **WR-01** (PATCH type change leaves stale FK columns): closed by a `typeSpecificNulls` object in `updateTransaction` that explicitly nulls `counterpartAccountId`, `refundOf`, `incomeType`, and `categoryId` based on the new type before the DB update.
- **WR-04** (categoryId is_not excludes NULL rows): closed by replacing `ne(...)` with `or(ne(...), isNull(...))` in `buildConditions`.

Status is `human_needed` — all automated truths pass but 10 human-observable behaviours require browser verification, including the 4 post-fix confirmation tests listed above.

---

_Verified: 2026-04-21T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
