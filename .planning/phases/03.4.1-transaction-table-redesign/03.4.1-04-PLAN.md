---
phase: "03.4.1"
plan: "04"
type: execute
wave: 3
depends_on:
  - "03.4.1-03"
files_modified:
  - apps/web/src/lib/data-access/transactions/useGetTransactions.ts
  - apps/web/src/components/transactions/types.ts
  - apps/web/src/components/transactions/hooks/useTransactionForm.ts
autonomous: true
requirements:
  - D-03
  - D-06
  - D-07
  - D-08
  - D-23
  - D-24

must_haves:
  truths:
    - "TransactionRow has counterpartAccountId, counterpartAccountName, rawDescription, notes, refundOfId, refundOfDate, refundOfAmountCents"
    - "TransactionRow does NOT have merchant, incomeSource, toAccountId, settledAccountId, investmentType"
    - "TransactionRow.description is string (non-null) — D-04 NOT NULL cascade"
    - "TransactionFormValues has counterpartAccountId and notes fields"
    - "TransactionFormValues does NOT have merchant, incomeSource, toAccountId, settledAccountId, investmentType"
    - "useTransactionForm populates counterpartAccountId from transaction.counterpartAccountId in edit mode"
  artifacts:
    - path: "apps/web/src/lib/data-access/transactions/useGetTransactions.ts"
      provides: "Updated TransactionRow interface matching new API response shape"
      contains: "counterpartAccountName"
    - path: "apps/web/src/components/transactions/types.ts"
      provides: "Updated TransactionFormValues interface"
      contains: "counterpartAccountId"
    - path: "apps/web/src/components/transactions/hooks/useTransactionForm.ts"
      provides: "Updated defaultValues using new field names"
      contains: "counterpartAccountId"
  key_links:
    - from: "apps/web/src/lib/data-access/transactions/useGetTransactions.ts"
      to: "apps/web/src/components/transactions/TransactionColumns.tsx"
      via: "TransactionRow type"
      pattern: "TransactionRow"
    - from: "apps/web/src/components/transactions/types.ts"
      to: "apps/web/src/components/transactions/TransactionForm.tsx"
      via: "TransactionFormValues"
      pattern: "TransactionFormValues"
---

<objective>
Update the frontend type layer to match the new API response shape: TransactionRow, TransactionFormValues, and useTransactionForm defaultValues.

Purpose: TypeScript type cascade — all form components and table columns use these types. Updating them here causes compile errors throughout the component tree that guide Plan 05/06/07 executors to the exact lines that need updates.
Output: Updated type files that compile and correctly map new field names.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.4.1-transaction-table-redesign/03.4.1-CONTEXT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.4.1-transaction-table-redesign/03.4.1-PATTERNS.md

<interfaces>
<!-- Current TransactionRow (useGetTransactions.ts lines 20-47): -->
<!--   Remove: merchant, incomeSource, toAccountId, settledAccountId, investmentType -->
<!--   Change: description: string | null → description: string  (D-04: NOT NULL) -->
<!--   Add: counterpartAccountId, counterpartAccountName, rawDescription, notes, refundOfId, refundOfDate, refundOfAmountCents -->

<!-- Current TransactionFormValues (types.ts lines 11-42): -->
<!--   Remove: merchant, incomeSource, toAccountId, settledAccountId, investmentType -->
<!--   Add: counterpartAccountId: string, notes: string -->

<!-- useTransactionForm.ts — defaultValues in edit mode populate from transaction object: -->
<!--   Remove field refs: merchant, incomeSource, toAccountId, settledAccountId, investmentType -->
<!--   Add field refs: counterpartAccountId: transaction?.counterpartAccountId ?? '' -->
<!--                   notes: transaction?.notes ?? '' -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update TransactionRow and TransactionFormValues interfaces</name>
  <files>
    apps/web/src/lib/data-access/transactions/useGetTransactions.ts,
    apps/web/src/components/transactions/types.ts
  </files>

  <read_first>
    Read useGetTransactions.ts in full (it's short — ~80 lines).
    Read types.ts in full (~56 lines).
  </read_first>

  <action>
**In useGetTransactions.ts — update TransactionRow interface (lines 20-47):**

1. Change `description: string | null` → `description: string` (D-04: description is now NOT NULL in DB)

2. Remove these fields:
   - `merchant: string | null`
   - `incomeSource: string | null`
   - `toAccountId: string | null`
   - `settledAccountId: string | null`
   - `investmentType: string | null`

3. Add these fields (after `refundOf: string | null`):
   ```typescript
   counterpartAccountId: string | null
   counterpartAccountName: string | null
   rawDescription: string | null
   notes: string | null
   refundOfId: string | null
   refundOfDate: string | null
   refundOfAmountCents: number | null
   ```

**In types.ts — update TransactionFormValues interface (lines 11-42):**

1. Remove field comments and declarations:
   - `merchant: string`
   - `incomeSource: string`
   - `toAccountId: string`
   - `settledAccountId: string`
   - `investmentType: string`

2. Add after `refundOf: string`:
   ```typescript
   // transfer + settlement (D-03: single counterpart FK)
   counterpartAccountId: string
   ```

3. Add at the bottom of the form fields (before `assignees`):
   ```typescript
   // notes (D-21: optional, always rendered)
   notes: string
   ```

4. Update the comment for `refundOf` — it now only applies to the refund type.
  </action>

  <verify>
    <automated>cd /Users/tarnesty/Developer/personal/ploutizo && pnpm --filter apps/web build 2>&1 | grep -E "error TS|warning|✓" | head -30</automated>
  </verify>

  <done>Both interface files updated. TypeScript will report errors in downstream consumers (TransactionForm, TransactionColumns, etc.) — this is expected and correct. Those errors are fixed in Plans 05, 06, 07.</done>
</task>

<task type="auto">
  <name>Task 2: Update useTransactionForm defaultValues</name>
  <files>apps/web/src/components/transactions/hooks/useTransactionForm.ts</files>

  <read_first>
    Read apps/web/src/components/transactions/hooks/useTransactionForm.ts in full.
    Identify the defaultValues object (create mode defaults + edit mode population from transaction).
  </read_first>

  <action>
In `useTransactionForm.ts`, update the `defaultValues` object:

**Create mode defaults — remove old fields, add new ones:**
- Remove: `merchant: ''`, `incomeSource: ''`, `toAccountId: ''`, `settledAccountId: ''`, `investmentType: ''`
- Add: `counterpartAccountId: ''`, `notes: ''`

**Edit mode defaults (from `transaction` prop) — update field mapping:**
- Remove: `merchant: transaction.merchant ?? ''`, `incomeSource: transaction.incomeSource ?? ''`, `toAccountId: transaction.toAccountId ?? ''`, `settledAccountId: transaction.settledAccountId ?? ''`, `investmentType: transaction.investmentType ?? ''`
- Add: `counterpartAccountId: transaction.counterpartAccountId ?? ''`, `notes: transaction.notes ?? ''`

**Type-switch reset values in `handleTypeChange` (or equivalent):**
- Remove: `form.setFieldValue('incomeSource', '')`, `form.setFieldValue('toAccountId', '')`, `form.setFieldValue('settledAccountId', '')`, `form.setFieldValue('investmentType', '')`
- Add: `form.setFieldValue('counterpartAccountId', '')` — reset on type change (so switching away from transfer/settlement clears the counterpart)

Note: `notes` is a base field (D-21 — always rendered) and should NOT be reset on type change.
  </action>

  <verify>
    <automated>cd /Users/tarnesty/Developer/personal/ploutizo && pnpm --filter apps/web build 2>&1 | grep -E "error TS|hooks/useTransactionForm" | head -20</automated>
  </verify>

  <done>useTransactionForm.ts uses counterpartAccountId and notes in defaultValues. Old field names removed. TypeScript errors in this file resolved; downstream errors remain for Plans 05/06/07 to fix.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API response → TransactionRow | JSON deserialization into typed interface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4.1-R1 | Tampering | description nullability change | mitigate | description changed from `string | null` to `string` in TransactionRow. If old rows arrive with null (pre-migration), the type would lie. Migration (Plan 01) ensures this cannot happen — backfill runs before NOT NULL constraint. |
| T-03.4.1-R2 | Information Disclosure | counterpartAccountId in form defaults | accept | counterpartAccountId is a read-only FK value; exposing it in form default values is equivalent to existing accountId — no new disclosure risk. Org-scoping enforced at API write (Plan 03). |
</threat_model>

<verification>
- `pnpm --filter apps/web build` reports TypeScript errors only in files that reference the old fields (merchant, incomeSource, toAccountId, settledAccountId, investmentType) — these are expected and fixed in Plans 05/06/07
- useGetTransactions.ts and types.ts compile cleanly themselves
- useTransactionForm.ts compiles cleanly (no errors referencing old field names)
</verification>

<success_criteria>
- TransactionRow has all 7 new fields; does not have 5 dropped fields
- description is non-null string in TransactionRow
- TransactionFormValues has counterpartAccountId and notes; does not have old fields
- useTransactionForm defaultValues use new field names
</success_criteria>

<output>
After completion, create `.planning/phases/03.4.1-transaction-table-redesign/03.4.1-04-SUMMARY.md`
</output>
