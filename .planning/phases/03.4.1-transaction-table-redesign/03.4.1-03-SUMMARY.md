---
phase: 03.4.1-transaction-table-redesign
plan: "03"
subsystem: api
tags: [drizzle, hono, transactions, security, org-scoping]

requires:
  - phase: 03.4.1-01
    provides: DB schema with counterpartAccountId, rawDescription, notes; dropped merchant/toAccountId/settledAccountId/incomeSource/investmentType
  - phase: 03.4.1-02
    provides: Validator schema v2 with counterpartAccountId, removed old fields

provides:
  - TX_COLUMNS with counterpartAccountName (alias join), refundOfId/refundOfDate/refundOfAmountCents (self-join)
  - Multi-value type filter via comma-separated values (inArray)
  - Description filter searches rawDescription (not merchant)
  - T-03.4.1-T1 mitigated: counterpartAccountId org-scoping in POST and PATCH
  - T-03.4.1-T2 verified: refundOf org-scoping already correct

affects:
  - 03.4.1-04 (TransactionRow type in frontend data-access — shape depends on TX_COLUMNS)
  - 03.4.1-05 (table column rendering — counterpartAccountName, refundOf flat fields)
  - 03.4.1-08 (filter bar — type filter multi-value for Internal shortcut)

tech-stack:
  added: []
  patterns:
    - "Drizzle alias pattern: alias(table, 'alias_name') declared at module scope for reuse across queries"
    - "Service wrapper pattern: query layer exports raw function, service layer re-exports as check* wrapper for routes"

key-files:
  created: []
  modified:
    - apps/api/src/lib/queries/transactions.ts
    - apps/api/src/services/transactions.ts
    - apps/api/src/routes/transactions.ts

key-decisions:
  - "counterpartAccountBelongsToOrg() added to query layer; checkCounterpartAccountOwnership() wrapper in service layer — mirrors existing refundOfExists/checkRefundOfOwnership pattern"
  - "DomainError called as DomainError(400, message) — status-first, matching existing codebase signature"
  - "T-03.4.1-T2 (refundOf): existing checkRefundOfOwnership() already correctly passes orgId — no change needed"
  - "description filter searches both description AND rawDescription (OR clause) — preserves backward compatibility while adding rawDescription coverage"

patterns-established:
  - "Alias joins: const counterpartAccounts = alias(accounts, 'counterpart_accounts') at module scope — reused in both buildListQuery and fetchTransactionById"
  - "Multi-value filter: split(',').map(trim), then eq() for single / inArray() for multiple"

requirements-completed:
  - D-03
  - D-23
  - D-24
  - D-25

duration: 25min
completed: "2026-04-20"
---

# Phase 03.4.1 Plan 03: API Query Layer — Counterpart Joins, Refund Self-Join, Org-Scoping Summary

**TX_COLUMNS updated with counterpartAccountName alias join and flat refundOf fields; multi-value type filter; T-03.4.1-T1 org-scoping validation in POST and PATCH**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-20T22:00:00Z
- **Completed:** 2026-04-20T22:25:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- TX_COLUMNS: removed 5 dropped columns (merchant, incomeSource, toAccountId, settledAccountId, investmentType); added counterpartAccountId, counterpartAccountName, rawDescription, notes, refundOfId, refundOfDate, refundOfAmountCents
- Both buildListQuery and fetchTransactionById have the counterpartAccounts and refundSource left joins
- type filter upgraded to handle comma-separated values via inArray (D-25 "Internal" shortcut)
- description filter now searches rawDescription alongside description (OR clause)
- T-03.4.1-T1 mitigated: counterpartAccountId org-scoping check added to POST and PATCH before DB write
- T-03.4.1-T2 verified: existing refundOf check already passes orgId correctly

## Task Commits

1. **Task 1: Update TX_COLUMNS, add alias joins, fix type filter and description search** - `c312fb9` (feat)
2. **Task 2: Add org-scoping validation for counterpartAccountId (T1) in routes** - `6108fb3` (feat)

## Files Created/Modified

- `apps/api/src/lib/queries/transactions.ts` — alias imports, counterpartAccounts/refundSource aliases, TX_COLUMNS overhaul, type filter multi-value, description filter fix, joins in buildListQuery + fetchTransactionById, counterpartAccountBelongsToOrg() helper
- `apps/api/src/services/transactions.ts` — added checkCounterpartAccountOwnership() service wrapper
- `apps/api/src/routes/transactions.ts` — T1 counterpartAccountId org-scoping check in POST and PATCH handlers

## Decisions Made

- Followed existing `refundOfExists → checkRefundOfOwnership` pattern for new T1 check: query helper in lib/queries, service wrapper in services, route calls service. Keeps layers clean.
- `DomainError(400, message)` matches existing codebase (status first), not the plan sample's `DomainError(message, 400)`.
- Description filter uses OR clause across description AND rawDescription (not replacing description — backward compatible for any existing callers using description field content).

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] DomainError argument order**
- **Found during:** Task 2
- **Issue:** Plan sample showed `throw new DomainError('message', 400)` but existing codebase uses `DomainError(400, 'message')` (status first).
- **Fix:** Used `DomainError(400, message)` to match existing calls in the same file.
- **Files modified:** apps/api/src/routes/transactions.ts
- **Committed in:** 6108fb3 (Task 2 commit)

---

**Total deviations:** 1 auto-fixed (Rule 1 - argument order correction)
**Impact on plan:** Correctness fix only. No scope change.

## Issues Encountered

- Worktree `git reset --hard` was denied by sandbox — resolved by merging Wave 1 base commit (`9b785de`) and Plan 02 commit (`c32aa3f`) via fast-forward and merge. Both merged cleanly before editing.
- Worktree had no node_modules — resolved by symlinking from main project for the build check, then removing symlinks after verification.

## Threat Flags

None — all new surface (counterpartAccounts join, refundSource join) is read-only and scoped to the transaction's own FK values. T1 and T2 mitigations are in place.

## Next Phase Readiness

- Plan 04 (TransactionRow type update in frontend data-access) can proceed: TX_COLUMNS shape is final
- Plan 05 (table column rendering) can proceed: counterpartAccountName and refundOfId/Date/AmountCents available in API response
- Plan 08 (filter bar Internal shortcut) can proceed: type filter accepts `transfer,settlement,contribution` as comma-separated string

---
*Phase: 03.4.1-transaction-table-redesign*
*Completed: 2026-04-20*
