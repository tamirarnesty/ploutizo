# Phase 03.4.1: Transaction v2 — Schema Migration, Form & Table Redesign - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-19
**Phase:** 03.4.1 — Transaction v2: Schema Migration, Form & Table Redesign
**Areas discussed:** income_source & investmentType, Settlement "Paid from" storage, Assignee Toggle redesign, Table date grouping, Locked description format, Refund sub-line

---

## income_source & investmentType

| Option | Description | Selected |
|--------|-------------|----------|
| Yes, drop both from the form | Remove incomeSource from IncomeFields.tsx and investmentType from ContributionFields.tsx | ✓ |
| Keep income_source, drop investmentType | income_source survives in form but not persisted | |
| Keep both in the form | Both fields stay but data won't persist | |

**User's choice:** Drop both entirely from the form.
**Notes:** Consistent with schema removal. incomeType enum (direct_deposit/e_transfer/etc.) still stays — only the free-text income_source field is removed.

---

## Settlement "Paid from" Storage

| Option | Description | Selected |
|--------|-------------|----------|
| Add counterpart_method text column | New nullable text column for untracked options | |
| Store in notes field | Use notes column for payment method | |
| Drop untracked options for now | Tracked accounts only; defer Cash/E-Transfer/Cheque/Other | ✓ |

**User's choice:** Drop untracked options — unlikely use case; add in future if desired.
**Notes:** counterpart_account_id stores UUID or null. No counterpart_method column added in this phase.

---

## Assignee Toggle Redesign

### Customize split expander

| Option | Description | Selected |
|--------|-------------|----------|
| Same % / $ rows as today | Expander reveals current SplitSection % \| $ rows | ✓ |
| % only in expander | Only percentage inputs, $ computed | |
| You decide | Claude picks cleanest approach | |

**User's choice:** Same % / $ rows as today — familiar interaction.

### Toggle initialization

| Option | Description | Selected |
|--------|-------------|----------|
| All un-pressed — empty start | No assignees until user presses Toggle | ✓ |
| All pressed — everyone auto-assigned | All members auto-toggled, user removes | |

**User's choice:** All un-pressed for 2+ members; single org member auto-pressed (100% split).

---

## Table Date Grouping

| Option | Description | Selected |
|--------|-------------|----------|
| Flat array with sentinel rows | Pre-process data, insert fake date header objects | |
| TanStack Table built-in grouping | Use grouping API | |
| You decide | Claude picks cleanest approach | |

**User's choice:** Defer date grouping entirely — keep table simple, no DataGrid hacks.

---

## Locked Description Format

| Option | Description | Selected |
|--------|-------------|----------|
| Account-name based | "Transfer from [A] to [B]", "Settlement — paid from [Account]", etc. | ✓ |
| You decide | Claude picks format strings | |

**User's choice:** Account-name based, generated in real-time from selected accounts.

---

## Refund Sub-line in Table

| Option | Description | Selected |
|--------|-------------|----------|
| In scope for this phase | Sub-line with ↩ date · amount; click opens original in TransactionSheet | ✓ |
| Defer to a later phase | No sub-line; refundOf stored but not displayed | |

**User's choice:** In scope. API needs to join refundOfTransaction (id, date, amountCents) in refund rows.

---

## Claude's Discretion

- Lock/unlock affordance styling
- Sub-line rendering approach (inside cell vs stacked)
- Toggle button sizing/spacing
- AssigneeSection structure relative to SplitSection
- "Internal" filter param design (typeGroup vs comma-separated types)
