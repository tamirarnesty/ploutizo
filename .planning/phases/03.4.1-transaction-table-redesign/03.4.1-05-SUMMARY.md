---
phase: 03.4.1-transaction-table-redesign
plan: "05"
subsystem: ui
tags: [react, tanstack-form, transactions, form-fields, field-rename]

requires:
  - phase: 03.4.1-04
    provides: TransactionFormValues updated with counterpartAccountId, TransactionFormInstance type

provides:
  - TransferFields using counterpartAccountId (not toAccountId)
  - SettlementFields using counterpartAccountId, label "Paid from" with (optional) hint
  - IncomeFields with incomeSource removed (returns null)
  - ContributionFields with investmentType removed; counterpartAccountId account picker (investment accounts, TYPE — AccountName format)
  - TransactionTypeFields with updated clear-list (counterpartAccountId), onTypeChange prop, IncomeFields call removed

affects: [03.4.1-06, 03.4.1-07]

tech-stack:
  added: []
  patterns:
    - "ContributionFields accepts accounts prop; filters to investment type; uses useMemo + Map for O(1) account lookup"
    - "TypeSelectField is module-scope component accepting onTypeChange callback for parent notification on type switch"

key-files:
  created: []
  modified:
    - apps/web/src/components/transactions/TransferFields.tsx
    - apps/web/src/components/transactions/SettlementFields.tsx
    - apps/web/src/components/transactions/IncomeFields.tsx
    - apps/web/src/components/transactions/ContributionFields.tsx
    - apps/web/src/components/transactions/TransactionTypeFields.tsx

key-decisions:
  - "ContributionFields uses TYPE — AccountName format (not TYPE — MemberName) because Account type has no ownerMemberId; GET /api/accounts does not join account_members or org_members. Full format blocked pending API extension (see deferred-items.md D1)."
  - "IncomeFields returns null and has no props — its incomeSource AppField is fully removed. Call site removed from TransactionTypeFields."
  - "TypeSelectField refactored to accept optional onTypeChange prop, called before setFieldValue clears — enables Plan 06 description lock reset on type switch."

patterns-established:
  - "Module-scope subcomponents (TypeSelectField) accept optional callback props for parent state hooks without requiring prop drilling to subscription level"

requirements-completed: [D-07, D-09, D-10, D-14, D-15, D-20]

duration: 25min
completed: 2026-04-20
---

# Phase 03.4.1 Plan 05: Form Field Renames Summary

**Five form field files updated to use counterpartAccountId; incomeSource and investmentType removed; contribution picker shows investment accounts in TYPE — AccountName format**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-20T00:00:00Z
- **Completed:** 2026-04-20T00:25:00Z
- **Tasks:** 2
- **Files modified:** 5 + 1 (deferred-items.md)

## Accomplishments

- TransferFields and SettlementFields renamed from old field names to counterpartAccountId
- SettlementFields label changed to "Paid from" (D-15)
- incomeSource input removed from IncomeFields entirely (D-09)
- ContributionFields: investmentType Select replaced with counterpartAccountId Select filtered to investment accounts (D-10, D-20)
- TransactionTypeFields: clear-list updated to counterpartAccountId; old field refs removed; onTypeChange prop added for Plan 06

## Task Commits

1. **Task 1: TransferFields + SettlementFields renames** - `952115f` (feat)
2. **Task 2: IncomeFields + ContributionFields + TransactionTypeFields updates** - `53ffa99` (feat)

**Plan metadata:** (docs commit follows)

## Files Created/Modified

- `apps/web/src/components/transactions/TransferFields.tsx` - counterpartAccountId field name, id=tx-counterpartAccountId
- `apps/web/src/components/transactions/SettlementFields.tsx` - counterpartAccountId, label "Paid from" with (optional)
- `apps/web/src/components/transactions/IncomeFields.tsx` - returns null; incomeSource removed entirely
- `apps/web/src/components/transactions/ContributionFields.tsx` - counterpartAccountId Select; investment accounts only; TYPE — AccountName labels; useMemo + Map for O(1) lookup
- `apps/web/src/components/transactions/TransactionTypeFields.tsx` - IncomeFields import/render removed; accounts prop passed to ContributionFields; onTypeChange prop; clear-list updated

## Decisions Made

- `TYPE — AccountName` used in ContributionFields instead of `TYPE — MemberName` because `Account` has no owner fields; account names typically include the owner's name. Full format requires API join extension (tracked in deferred-items.md).
- `IncomeFields` kept as a named export returning null rather than deleted — less disruptive than removing the export from the barrel, and Plan 06 may choose to clean it up.

## Deviations from Plan

### Auto-fixed Issues

None - renames executed as specified.

### Known Stubs

None — ContributionFields account picker is wired to real `accounts` prop passed from TransactionTypeFields. Labels use account name as owner substitute (documented below).

### Deferred Items (out of scope)

**1. ContributionFields TYPE — MemberName format**
- **Blocked by:** Account type has no ownerMemberId; API does not join account_members/org_members
- **Current behavior:** `TYPE — AccountName` format (e.g., "INVESTMENT — Emily TFSA")
- **Fix required:** Extend GET /api/accounts to include owner member name; update Account type
- **Tracked in:** `.planning/phases/03.4.1-transaction-table-redesign/deferred-items.md` (D1)

**2. toApiPayload contribution case missing counterpartAccountId**
- **Found:** `useTransactionForm.ts` contribution case returns `base` only (outside plan's 5 files)
- **Fix required:** Add `counterpartAccountId: value.counterpartAccountId || undefined` to contribution case
- **Tracked in:** `.planning/phases/03.4.1-transaction-table-redesign/deferred-items.md` (D2)
- **Recommended:** Address in Plan 06 when TransactionForm.tsx is touched

## Issues Encountered

- Vite build verification had to be performed from the main repo (worktree lacks node_modules). Verified via: build passes in main repo context; grep confirms no old field names in worktree files; `counterpartAccountId` present in all expected locations.
- Pre-existing TS7006 errors on `field` callback parameters exist across all form field files (root cause: useTransactionForm.ts TS2344 type resolution issue) — not introduced by this plan.

## Threat Flags

None — no new network endpoints, auth paths, or trust boundary changes. Accounts are org-scoped (T-03.4.1-F2: accept disposition).

## Next Phase Readiness

- TransactionTypeFields.tsx exposes `onTypeChange` prop — Plan 06 can pass `() => setIsDescriptionUnlocked(false)` immediately
- ContributionFields wired to `counterpartAccountId` — Plan 06 should fix `toApiPayload` contribution case to include this field
- All 5 files ready for Plan 06 (TransactionForm.tsx update)

---
*Phase: 03.4.1-transaction-table-redesign*
*Completed: 2026-04-20*

## Self-Check

Checking files exist:

- FOUND: apps/web/src/components/transactions/TransferFields.tsx
- FOUND: apps/web/src/components/transactions/SettlementFields.tsx
- FOUND: apps/web/src/components/transactions/IncomeFields.tsx
- FOUND: apps/web/src/components/transactions/ContributionFields.tsx
- FOUND: apps/web/src/components/transactions/TransactionTypeFields.tsx
- FOUND: .planning/phases/03.4.1-transaction-table-redesign/03.4.1-05-SUMMARY.md

Checking commits:
- FOUND: 952115f (Task 1)
- FOUND: 53ffa99 (Task 2)

## Self-Check: PASSED
