---
phase: 03.4.1-transaction-table-redesign
plan: "04"
subsystem: web/types
tags: [typescript, transactions, type-layer, form-values]

requires:
  - phase: 03.4.1-03
    provides: TX_COLUMNS with counterpartAccountName, refundOfId/Date/AmountCents; dropped merchant/incomeSource/toAccountId/settledAccountId/investmentType

provides:
  - TransactionRow interface matching new API response shape (counterpartAccountName, flat refund fields, description non-null)
  - TransactionFormValues with counterpartAccountId and notes; old fields removed
  - useTransactionForm defaultValues using new field names; toApiPayload updated

affects:
  - 03.4.1-05 (TransactionColumns.tsx references TransactionRow — downstream TS errors now visible)
  - 03.4.1-06 (TransactionForm.tsx + field components reference TransactionFormValues — downstream TS errors now visible)
  - 03.4.1-07 (AssigneeSection — no direct dependency on changed fields)

tech-stack:
  added: []
  patterns:
    - "Type cascade: updating TransactionRow + TransactionFormValues produces compile errors in downstream consumers that guide Plan 05/06/07 executors"

key-files:
  created: []
  modified:
    - apps/web/src/lib/data-access/transactions/useGetTransactions.ts
    - apps/web/src/components/transactions/types.ts
    - apps/web/src/components/transactions/hooks/useTransactionForm.ts

key-decisions:
  - "description changed from string | null to string in TransactionRow (D-04: NOT NULL in DB, Plan 01 backfill ensures no null rows)"
  - "counterpartAccountId unifies toAccountId (transfer) and settledAccountId (settlement) per D-03"
  - "notes added to base payload in toApiPayload (always sent, trimmed, undefined if empty) — D-21"
  - "toApiPayload: contribution case now returns base only (investmentType removed); income case drops incomeSource"
  - "Pre-existing TS error on line 10 of useTransactionForm.ts (ReturnType<typeof useAppForm<T>>) confirmed pre-existing, not introduced by this plan"

metrics:
  duration: 15min
  completed: "2026-04-20"
---

# Phase 03.4.1 Plan 04: Frontend Type Layer — TransactionRow, TransactionFormValues, useTransactionForm Summary

**TransactionRow and TransactionFormValues updated to match API v2 shape; 5 old fields removed, 7 new fields added; useTransactionForm defaultValues and toApiPayload use new names**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-04-20
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- `TransactionRow`: removed `merchant`, `incomeSource`, `toAccountId`, `settledAccountId`, `investmentType`; changed `description` from `string | null` to `string`; added `counterpartAccountId`, `counterpartAccountName`, `rawDescription`, `notes`, `refundOfId`, `refundOfDate`, `refundOfAmountCents`
- `TransactionFormValues`: removed `merchant`, `incomeSource`, `toAccountId`, `settledAccountId`, `investmentType`; added `counterpartAccountId` (transfer + settlement D-03), `notes` (D-21 always rendered)
- `buildDefaultValues` (create mode): new empty fields `counterpartAccountId: ''`, `notes: ''`; old fields gone
- `buildDefaultValues` (edit mode): `counterpartAccountId: transaction.counterpartAccountId ?? ''`, `notes: transaction.notes ?? ''`; `description: transaction.description` (no nullish coalescing needed — non-null)
- `toApiPayload`: `notes` in base payload (trimmed, undefined if empty); transfer/settlement → `counterpartAccountId`; income drops `incomeSource`; contribution drops `investmentType`

## Task Commits

1. **Task 1: Update TransactionRow and TransactionFormValues interfaces** — `df5fd00` (feat)
2. **Task 2: Update useTransactionForm defaultValues and toApiPayload** — `3b93a17` (feat)

## Files Created/Modified

- `apps/web/src/lib/data-access/transactions/useGetTransactions.ts` — TransactionRow interface overhauled
- `apps/web/src/components/transactions/types.ts` — TransactionFormValues overhauled
- `apps/web/src/components/transactions/hooks/useTransactionForm.ts` — buildDefaultValues + toApiPayload updated

## Deviations from Plan

None — plan executed exactly as written.

## Downstream TypeScript Errors (Expected)

Per plan: the following files now have TS errors referencing removed fields. These are intentional — they mark the exact lines Plans 05/06/07 need to fix:

- `RefundLinker.tsx` — 4 errors referencing `merchant` (Plans 05/06)
- `TransactionColumns.tsx` — 1 error referencing `merchant` (Plan 05)
- `TransactionForm.tsx` — 1 error referencing `"merchant"` field name (Plan 06)

Pre-existing errors (unrelated to this plan): `packages/ui` path alias errors, `RuleForm.tsx`, unused vars in unrelated files.

## Known Stubs

None.

## Threat Flags

None — changes are type-layer only; no new network surface introduced.

## Self-Check: PASSED

- `apps/web/src/lib/data-access/transactions/useGetTransactions.ts` — FOUND
- `apps/web/src/components/transactions/types.ts` — FOUND
- `apps/web/src/components/transactions/hooks/useTransactionForm.ts` — FOUND
- Commit `df5fd00` — FOUND
- Commit `3b93a17` — FOUND

---
*Phase: 03.4.1-transaction-table-redesign*
*Completed: 2026-04-20*
