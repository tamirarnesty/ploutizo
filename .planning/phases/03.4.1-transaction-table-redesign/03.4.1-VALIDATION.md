---
phase: 03.4.1
slug: transaction-table-redesign
status: draft
nyquist_compliant: true
wave_0_complete: true
created: 2026-04-19
---

# Phase 03.4.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | Vitest |
| **Config file** | `vitest.config.ts` (packages/validators, apps/api) |
| **Quick run command** | `pnpm --filter @ploutizo/validators test` |
| **Full suite command** | `pnpm test` |
| **Estimated runtime** | ~15 seconds |

---

## Sampling Rate

- **After every task commit:** Run `pnpm --filter @ploutizo/validators test`
- **After every plan wave:** Run `pnpm test`
- **Before `/gsd-verify-work`:** Full suite must be green
- **Max feedback latency:** ~15 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| VAL-01 | schema/validators | 1 | §4.1 Schema | — | `merchant`/`income_source`/`toAccountId` rejected; `notes`/`counterpartAccountId` accepted | unit | `pnpm --filter @ploutizo/validators test` | ❌ W0 | ⬜ pending |
| VAL-02 | schema/validators | 1 | §4.1 Schema | — | `transferTransactionSchema` requires `counterpartAccountId`, rejects `toAccountId` | unit | `pnpm --filter @ploutizo/validators test` | ❌ W0 | ⬜ pending |
| VAL-03 | schema/validators | 1 | §4.1 Schema | — | `updateTransactionSchema` accepts `counterpartAccountId`, rejects old FK fields | unit | `pnpm --filter @ploutizo/validators test` | ❌ W0 | ⬜ pending |
| API-01 | api | 2 | §4.1 API | T-cross-org | GET /transactions joins counterpartAccount + refundOfTransaction in response | manual | — | ❌ manual-only | ⬜ pending |
| API-02 | api | 2 | §4.1 Table | — | GET /transactions?type=transfer,settlement,contribution returns all 3 types | manual | — | ❌ manual-only | ⬜ pending |
| FE-01 | table | 3 | §4.1 Table | — | Amount cell shows `-$X.XX` + text-destructive for expense; `+$X.XX` + green for income/refund; unsigned muted for others | unit | `pnpm --filter apps/web test` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `packages/validators/src/__tests__/transactions.test.ts` — stubs for VAL-01, VAL-02, VAL-03
- [ ] Amount cell renderer unit test in `apps/web` — covers FE-01 (if web test runner is configured)

*Existing infrastructure (Vitest) is already configured in packages/validators — test files only need to be created.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| GET /transactions returns `counterpartAccount` and `refundOfTransaction` joins | §4.1 API | Requires live DB + seed data; no integration test infra yet | Create a transfer transaction, call GET, verify `counterpartAccount.name` in response |
| Internal filter shortcut returns transfer + settlement + contribution | §4.1 Table | Frontend filter UI; no E2E test runner configured | Open transaction list, click "Internal" filter, verify only transfer/settlement/contribution rows visible |
| Cross-org account access blocked via `counterpartAccountId` | Security | Requires two orgs in test DB | Attempt POST with `counterpartAccountId` belonging to different org; expect 400/403 |

---

## Validation Sign-Off

- [x] All tasks have `<automated>` verify or Wave 0 dependencies
- [x] Sampling continuity: no 3 consecutive tasks without automated verify
- [x] Wave 0 covers all MISSING references (VAL-01/02/03 stubs; FE-01 qualified as "(if configured)")
- [x] No watch-mode flags
- [x] Feedback latency < 15s
- [x] `nyquist_compliant: true` set in frontmatter

**Approval:** approved 2026-04-19
