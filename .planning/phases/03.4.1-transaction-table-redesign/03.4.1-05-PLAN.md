---
phase: "03.4.1"
plan: "05"
type: execute
wave: 4
depends_on:
  - "03.4.1-04"
files_modified:
  - apps/web/src/components/transactions/TransferFields.tsx
  - apps/web/src/components/transactions/SettlementFields.tsx
  - apps/web/src/components/transactions/IncomeFields.tsx
  - apps/web/src/components/transactions/ContributionFields.tsx
  - apps/web/src/components/transactions/TransactionTypeFields.tsx
autonomous: true
requirements:
  - D-07
  - D-09
  - D-10
  - D-14
  - D-15
  - D-20

must_haves:
  truths:
    - "TransferFields uses counterpartAccountId field (not toAccountId)"
    - "SettlementFields uses counterpartAccountId for Paid from field (not settledAccountId)"
    - "IncomeFields has no incomeSource input (incomeType Select remains)"
    - "ContributionFields has no investmentType Select; account picker shows TFSA — Emily format"
    - "TransactionTypeFields type-switch clear-list does not reference old field names"
  artifacts:
    - path: "apps/web/src/components/transactions/TransferFields.tsx"
      provides: "counterpartAccountId Select for transfer destination"
      contains: "counterpartAccountId"
    - path: "apps/web/src/components/transactions/SettlementFields.tsx"
      provides: "counterpartAccountId Select for Paid from (optional)"
      contains: "counterpartAccountId"
    - path: "apps/web/src/components/transactions/IncomeFields.tsx"
      provides: "incomeType Select only — incomeSource input removed"
    - path: "apps/web/src/components/transactions/ContributionFields.tsx"
      provides: "Account picker with member name alongside type"
      contains: "ownerMemberName"
    - path: "apps/web/src/components/transactions/TransactionTypeFields.tsx"
      provides: "Updated type-switch clear-list using counterpartAccountId"
      contains: "counterpartAccountId"
  key_links:
    - from: "apps/web/src/components/transactions/TransferFields.tsx"
      to: "apps/web/src/components/transactions/hooks/useTransactionForm.ts"
      via: "form.AppField name='counterpartAccountId'"
      pattern: "counterpartAccountId"
---

<objective>
Update the simple type-specific form field components to use the new field names and remove deprecated fields. No new UI behaviors — pure field rename and cleanup.

Purpose: These files contain TypeScript errors after Plan 04's type interface updates. This plan resolves them with targeted renames.
Output: 5 form field files with correct field names, no TypeScript errors.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.4.1-transaction-table-redesign/03.4.1-CONTEXT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.4.1-transaction-table-redesign/03.4.1-PATTERNS.md

<interfaces>
<!-- TransactionFormValues (updated in Plan 04): -->
<!--   Has: counterpartAccountId: string, notes: string -->
<!--   Removed: merchant, incomeSource, toAccountId, settledAccountId, investmentType -->

<!-- useGetAccounts() — existing hook; returns Account[] with id, name, type, ownerMemberId, ownerMemberName -->
<!-- The contribution account picker needs ownerMemberName — check if it's already in the Account type -->
<!-- If not, it may need a join or may already exist from the org-members query -->

<!-- Label patterns from UI-SPEC: -->
<!--   Settlement "Paid from" field label: "Paid from" (not "Settled Account") -->
<!--   Settlement "(optional)" hint in FieldLabel -->
<!--   Contribution picker format: {account.type.toUpperCase()} — {account.ownerMemberName} (em dash U+2014) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename toAccountId → counterpartAccountId in TransferFields; rename settledAccountId → counterpartAccountId in SettlementFields</name>
  <files>
    apps/web/src/components/transactions/TransferFields.tsx,
    apps/web/src/components/transactions/SettlementFields.tsx
  </files>

  <read_first>
    Read TransferFields.tsx in full. Read SettlementFields.tsx in full.
    Note the current field names used in form.AppField name= props and any labels.
  </read_first>

  <action>
**TransferFields.tsx** (D-07 — transfer uses counterpartAccountId):
- Change `form.AppField name="toAccountId"` → `form.AppField name="counterpartAccountId"`
- No label changes needed (label is "To Account" or similar — keep as-is)
- Ensure the account Select still shows all household accounts from `useGetAccounts()`

**SettlementFields.tsx** (D-14, D-15 — settlement Paid from field):
- Change `form.AppField name="settledAccountId"` → `form.AppField name="counterpartAccountId"`
- Change the field label to "Paid from" (per UI-SPEC D-15)
- Add "(optional)" hint to the FieldLabel: wrap "Paid from" text + add `<Text as="span" variant="body-sm" className="font-normal text-muted-foreground">(optional)</Text>` in the FieldLabel
- The Select shows tracked accounts only: use `useGetAccounts()` as-is (all household accounts are tracked accounts per D-14)
- Add `autoComplete="off"` on the Select trigger if not already present

**Both files:** Verify no other references to the old field names remain.
  </action>

  <verify>
    <automated>cd /Users/tarnesty/Developer/personal/ploutizo && pnpm --filter apps/web build 2>&1 | grep -E "TransferFields|SettlementFields|error TS" | head -20</automated>
  </verify>

  <done>TransferFields and SettlementFields use counterpartAccountId. SettlementFields label is "Paid from" with (optional) hint. No TypeScript errors in these files.</done>
</task>

<task type="auto">
  <name>Task 2: Remove incomeSource from IncomeFields; remove investmentType from ContributionFields; add member name to contribution picker; fix TransactionTypeFields clear-list</name>
  <files>
    apps/web/src/components/transactions/IncomeFields.tsx,
    apps/web/src/components/transactions/ContributionFields.tsx,
    apps/web/src/components/transactions/TransactionTypeFields.tsx
  </files>

  <read_first>
    Read IncomeFields.tsx in full. Read ContributionFields.tsx in full. Read TransactionTypeFields.tsx in full.
    For ContributionFields: check what data useGetAccounts() returns — specifically whether ownerMemberName is present in the Account type. If not present, check useGetOrgMembers() to see if a member lookup is needed.
  </read_first>

  <action>
**IncomeFields.tsx** (D-09 — incomeSource removed):
- Remove the `incomeSource` form.AppField entirely (the whole JSX block: field wrapper, label, Input).
- Keep the `incomeType` Select — it remains required for income transactions.

**ContributionFields.tsx** (D-10, D-20):
- Remove the `investmentType` Select form.AppField entirely.
- The account picker `form.AppField name="accountId"` (or `counterpartAccountId` if it was already using that) stays but needs updated SelectItem labels.
- Update SelectItem rendering to show `{account.type.toUpperCase()} — {account.ownerMemberName}` format:
  - Em dash: U+2014 `—` (not double hyphen `--`)
  - Example: `TFSA — Emily`
  - If `ownerMemberName` is not on the Account object from `useGetAccounts()`, use `useGetOrgMembers()` to build a memberId → displayName map and join it. Use `useMemo` + a `Map` for O(1) lookup (vercel-react-best-practices js-map-set rule).
  - Filter SelectItems to accounts owned by a single member (investment accounts). If the Account type has `isJoint: boolean` or `ownerCount`, filter out joint accounts. If no such field exists, show all accounts and let the user distinguish by label — note this in SUMMARY.

**TransactionTypeFields.tsx** (D-07 — type-switch clear-list):
- Find the type-change handler (the `form.setFieldValue(...)` calls that clear type-specific fields on type switch).
- Remove: `form.setFieldValue('incomeSource', '')`, `form.setFieldValue('toAccountId', '')`, `form.setFieldValue('settledAccountId', '')`, `form.setFieldValue('investmentType', '')`
- Add: `form.setFieldValue('counterpartAccountId', '')` — clear on type switch
- Keep: all other setFieldValue calls (categoryId, refundOf, etc.) that are already there
- **Add `onTypeChange` prop** to the component interface:
  ```typescript
  interface TransactionTypeFieldsProps {
    form: /* existing form type */
    onTypeChange?: () => void  // called after any type-switch, before field clears
  }
  ```
  Call `onTypeChange?.()` at the start of the type-change handler (before the `form.setFieldValue` calls).
  Plan 06 will pass `() => setIsDescriptionUnlocked(false)` to this prop.

Do NOT touch TransactionForm.tsx in this plan — it is handled in Plan 06.
  </action>

  <verify>
    <automated>cd /Users/tarnesty/Developer/personal/ploutizo && pnpm --filter apps/web build 2>&1 | grep -E "IncomeFields|ContributionFields|TransactionTypeFields|error TS" | head -20</automated>
  </verify>

  <done>IncomeFields has no incomeSource input. ContributionFields has no investmentType Select; account picker shows "TYPE — MemberName" format. TransactionTypeFields clear-list uses counterpartAccountId. No TypeScript errors in these 3 files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Form field → form state → API | User-entered form values submitted via TanStack Form |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4.1-F1 | Tampering | Old field names submitted in form payload | mitigate | toApiPayload in useTransactionForm maps form state to API body. Old fields removed from TransactionFormValues (Plan 04) — they cannot appear in form state. |
| T-03.4.1-F2 | Information Disclosure | Contribution picker shows all accounts | accept | Accounts are org-scoped; all org members can see all accounts. No new disclosure risk. |
</threat_model>

<verification>
- `pnpm --filter apps/web build` reports no errors in TransferFields, SettlementFields, IncomeFields, ContributionFields, TransactionTypeFields
- Remaining build errors (if any) are in TransactionForm.tsx, TransactionColumns.tsx, Transactions.tsx — fixed in Plans 06 and 07
</verification>

<success_criteria>
- All 5 form field files compile without TypeScript errors
- No references to toAccountId, settledAccountId, incomeSource, investmentType remain in these files
- Contribution picker shows "TYPE — MemberName" format labels
- Settlement "Paid from" label and (optional) hint visible
</success_criteria>

<output>
After completion, create `.planning/phases/03.4.1-transaction-table-redesign/03.4.1-05-SUMMARY.md`
</output>
