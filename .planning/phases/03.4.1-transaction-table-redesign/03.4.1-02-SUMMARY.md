---
phase: "03.4.1"
plan: "02"
subsystem: validators
tags: [tdd, validators, zod, schema-v2, field-rename]
dependency_graph:
  requires: []
  provides: [counterpartAccountId validator contract, notes field in base schema]
  affects: [apps/api route validation, apps/web TanStack Form schemas]
tech_stack:
  added: []
  patterns: [Zod v4 discriminated union shape inspection via .options array]
key_files:
  created:
    - packages/validators/src/__tests__/transactions.test.ts
  modified:
    - packages/validators/src/transactions.ts
    - packages/validators/src/index.test.ts
decisions:
  - "Zod v4 discriminated unions expose .options (array) not .optionsMap — used .options.find() with .def.values.includes() to locate variants by discriminant"
  - "ZodLiteral.def.values is a plain array in Zod v4 (not a Set) — .includes() not .has()"
  - "index.test.ts transfer tests updated inline (Rule 3 — blocking fix) — descriptions updated to counterpartAccountId"
metrics:
  duration: "~4 minutes"
  completed_date: "2026-04-21"
  tasks_completed: 2
  files_changed: 3
---

# Phase 03.4.1 Plan 02: Validator Schema v2 — Field Renames and Removals Summary

TDD implementation of Zod schema v2: removed `merchant`, `incomeSource`, `toAccountId`, `settledAccountId`, `investmentType`; added `notes`, `counterpartAccountId`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| RED | Failing tests VAL-01, VAL-02, VAL-03 | 59caedb | packages/validators/src/__tests__/transactions.test.ts |
| GREEN | Validator implementation + fix blocking tests | c4148f4 | packages/validators/src/transactions.ts, index.test.ts, __tests__/transactions.test.ts |

## What Was Built

Updated `packages/validators/src/transactions.ts` to match schema v2 decisions D-06, D-07, D-08:

- `baseTransactionSchema`: removed `merchant`, added `notes: z.string().optional()`
- `incomeTransactionSchema`: removed `incomeSource` (free-text context goes in `notes`)
- `transferTransactionSchema`: renamed `toAccountId` → `counterpartAccountId: z.string().uuid()` (required)
- `settlementTransactionSchema`: renamed `settledAccountId` → `counterpartAccountId: z.string().uuid().optional()`
- `contributionTransactionSchema`: removed `investmentType` (identified via account type instead)
- `updateTransactionSchema`: removed `incomeSource`, `toAccountId`, `settledAccountId`, `investmentType`; added `counterpartAccountId: z.string().uuid().optional()`
- Removed `investmentTypeValues` const (no longer needed)

## TDD Gate Compliance

- RED commit `59caedb`: `test(03.4.1-02)` — 11 failing tests for VAL-01, VAL-02, VAL-03
- GREEN commit `c4148f4`: `feat(03.4.1-02)` — all 70 tests pass

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Updated stale transfer tests in index.test.ts**
- **Found during:** GREEN phase
- **Issue:** `index.test.ts` lines 357–366 tested `toAccountId` on the transfer schema — would fail after rename
- **Fix:** Updated test descriptions and payload keys to `counterpartAccountId`
- **Files modified:** `packages/validators/src/index.test.ts`
- **Commit:** c4148f4

**2. [Rule 3 - Blocking] Corrected Zod v4 discriminated union API usage in tests**
- **Found during:** GREEN verification
- **Issue:** Tests used `createTransactionSchema.optionsMap.get(type)` — this is Zod v3 API; Zod v4.3.6 uses `.options` (array)
- **Fix:** Rewrote helper `getVariantShape()` to use `.options.find()` with `.def.values.includes()`. ZodLiteral.def.values is a plain array in Zod v4 (confirmed via Node REPL inspection)
- **Files modified:** `packages/validators/src/__tests__/transactions.test.ts`
- **Commit:** c4148f4

### Worktree Base Deviation

The `git reset --hard` permission was denied. Worktree was at commit `463117b` (one commit ahead of target `f556d40`) — the extra commit is a merged PR commit that includes all the same planning files as the target. No functional impact; planning files were read from the correct paths.

## Verification

```
pnpm --filter @ploutizo/validators test
# Tests: 70 passed (2 test files)

pnpm --filter @ploutizo/validators typecheck
# Exit 0 — no TypeScript errors
```

## Known Stubs

None — validators contain no UI stubs or placeholder data.

## Threat Flags

No new security-relevant surface introduced. T-03.4.1-V1 mitigated: `toAccountId`/`settledAccountId` no longer declared in any schema — Zod strips unknown keys at parse time, and clients sending old field names will receive validation errors from the required `counterpartAccountId` field.

## Self-Check: PASSED

- [x] `packages/validators/src/__tests__/transactions.test.ts` exists
- [x] `packages/validators/src/transactions.ts` modified
- [x] `packages/validators/src/index.test.ts` modified
- [x] Commit `59caedb` exists (RED)
- [x] Commit `c4148f4` exists (GREEN)
- [x] `pnpm --filter @ploutizo/validators test` passes (70/70)
- [x] `pnpm --filter @ploutizo/validators typecheck` passes
