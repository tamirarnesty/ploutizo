# Phase 03.3.2: Implement Text Typography Component - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-17
**Phase:** 03.3.2-implement-text-typography-component-in-packages-ui
**Areas discussed:** Heading font family, Variant style map, Label variant semantics

---

## Heading Font Family

| Option | Description | Selected |
|--------|-------------|----------|
| font-heading / Noto Serif | Matches existing codebase pattern; font-heading text-xl font-semibold already used for section headings | ✓ |
| font-sans / Geist for all | Drop font-heading; all variants use default sans-serif | |

**User's choice:** font-heading (Noto Serif Variable) for h1/h2/h3 variants
**Notes:** Consistent with current usage pattern in the codebase.

---

## Variant Style Map

| Option | Description | Selected |
|--------|-------------|----------|
| Approved map | body=text-base leading-relaxed, body-sm=text-sm leading-relaxed, caption=text-xs text-muted-foreground leading-normal, label=text-sm font-medium leading-none | ✓ |
| Adjust body/body-sm | Change leading or size for body variants | |
| Adjust caption/label | Change caption or label styling | |

**User's choice:** Approved as-is.

---

## Label Variant Semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Typography only | text-sm font-medium leading-none, no peer-disabled pseudo-classes | ✓ |
| Mirror shadcn Label | Include peer-disabled:cursor-not-allowed peer-disabled:opacity-70 | |

**User's choice:** Typography only — no peer-disabled.
**Notes:** User clarified: `<Text variant="label">` is for "label for something that isn't explicitly an input." `<Label>` is reserved for actual form input pairings where peer-disabled makes sense.

---

## Claude's Discretion

- Polymorphic `as` prop implementation details (union type vs `React.ElementType`)
- Whether to use `cva` or plain `Record<>` for the variant map
- `ref` forwarding (React 19 — regular prop, no `forwardRef`)
