# Phase 03.4.6: Show invited member card in Household view — Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-05-04
**Phase:** 03.4.6 — Show invited member card in Household view (INSERTED)
**Areas discussed:** API delivery, Invited card UI, Revoke invite, Invitation data shape, Invite date on card, Empty/loading state, Spinner replacement

---

## API Delivery

| Option | Description | Selected |
|--------|-------------|----------|
| Separate GET /invitations | New endpoint, independent query key, /members contract unchanged | ✓ |
| Extend GET /members | Return `{ members, pendingInvitations }` from the same endpoint — simpler fetch but breaks existing return type | |

**User's choice:** Separate `GET /api/households/invitations` endpoint
**Notes:** Paired with `DELETE /api/households/invitations/:invitationId` for revoke. `useInviteMember` invalidates both `['org-members']` and `['org-invitations']` on settle.

---

## Invited Card UI

| Option | Description | Selected |
|--------|-------------|----------|
| New InvitedMemberRow component | Separate component — data shape (email + inviteId) too different from OrgMember to share | ✓ |
| Variant prop on MemberRow | `invited?: boolean` + `email?: string` added to MemberRowProps — conditional branching throughout | |

**User's choice:** New `InvitedMemberRow` component
**Notes:** Dimmed (`opacity-60`), email as display name, initials from email for UserAvatar.

---

## Revoke Invite

| Option | Description | Selected |
|--------|-------------|----------|
| In scope — add revoke | Trash button → AlertDialog → DELETE /invitations/:id | ✓ |
| Out of scope — display only | Card shows email + Invited badge, no action | |

**User's choice:** In scope — add revoke in this phase
**Notes:** AlertDialog matches the existing MemberRow remove pattern: "Revoke invite to {email}?" with Revoke / Cancel actions.

---

## Revoke Flow — Confirmation Dialog

| Option | Description | Selected |
|--------|-------------|----------|
| Confirmation dialog | AlertDialog before revoke — matches MemberRow remove pattern | ✓ |
| Direct revoke on click | Single click, no confirmation | |

**User's choice:** Confirmation dialog
**Notes:** Same AlertDialog component and pattern as member removal.

---

## Invitation Data Shape

| Option | Description | Selected |
|--------|-------------|----------|
| Minimal: id + email | Just what card needs to render and revoke | |
| id + email + createdAt | Include timestamp for relative time display | |
| Full Clerk fields | id, email, status, created_at, expires_at — maximum flexibility | |

**User's choice:** "depends on the other decisions" — resolved by the invite date decision below
**Notes:** Final shape: `{ id, email, createdAt, expiresAt, status }` (all five fields needed).

---

## Invite Date on Card

| Option | Description | Selected |
|--------|-------------|----------|
| No date — just Invited badge | Static badge only; minimal id + email from API | |
| Show relative time since sent | "Invited 3 days ago" from createdAt | |
| Show expiry countdown | "Expires in 4 days" from expiresAt | |
| Both — primary invited date + subtle expiry | Primary: invited date; secondary: expiry; warning as deadline approaches | ✓ |

**User's choice:** Both — show invited date as primary with subtle expiry
**Notes:** Clerk default expiry is 30 days; app REQUIREMENTS.md says 7-day. Researcher must verify actual expiry setting in `inviteMember` service. User prefers using Clerk's `status` field (not date calculation) to show "Expired". Warning threshold: ≤ 2 days remaining.

---

## Expiry Alert Threshold

| Option | Description | Selected |
|--------|-------------|----------|
| Last 2 days remaining | Concrete, predictable window | ✓ |
| Last 25% of invite duration | Proportional — adapts if Clerk expiry changes | |

**User's choice:** Last 2 days remaining

---

## Loading and Empty State

| Option | Description | Selected |
|--------|-------------|----------|
| Wait for both, single skeleton | isLoading = membersQuery.isLoading \|\| invitationsQuery.isLoading | ✓ |
| Progressive — show active members first | Staggered loading — complex | |

**User's choice:** Wait for both, single combined skeleton

| Option | Description | Selected |
|--------|-------------|----------|
| No separate case — invites render | Empty state only when both arrays are empty | ✓ |
| Custom text when only invites pending | e.g. "No active members yet — invitations pending." | |

**User's choice:** No separate empty-state case — invited cards render, empty state text only when both arrays are empty.

---

## Spinner/Loader Replacement

| Option | Description | Selected |
|--------|-------------|----------|
| Part of 03.4.6 — replace Spinner globally | Replace `packages/ui/src/components/spinner.tsx` via loading-ui shadcn registry | (considered) |
| Deferred — revisit later | Keep Loader2Icon for now | ✓ |

**User's choice:** Deferred — user didn't find a suitable component from loading-ui. Revisit later.
**Notes:** loading-ui available at https://github.com/turbostarter/loading-ui — shadcn registry model (copy-paste). Components tried: Ring, Spokes. Neither chosen.

---

## Claude's Discretion

- Ordering of active members vs. invited cards in the list (planner decides)
- Badge variant for "Invited" pill (secondary vs. outline — whichever reads as passive/pending)
- Exact relative time format ("3 days ago" / "3d ago" / etc.)

## Deferred Ideas

- Spinner/loader replacement — revisit later with a chosen library/component
- Bulk revoke all invitations — out of scope for this phase
