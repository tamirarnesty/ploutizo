---
phase: 03.4.6
plan: 03
type: execute
wave: 3
depends_on: ["03.4.6-02"]
files_modified:
  - packages/ui/src/components/item.tsx
  - packages/ui/src/components/empty.tsx
  - apps/web/src/components/settings/MemberRow.tsx
  - apps/web/src/components/settings/InvitedMemberRow.tsx
  - apps/web/src/components/settings/MembersSection.tsx
autonomous: false
requirements: []
tags:
  - react
  - ui
  - invitations
  - clerk

must_haves:
  truths:
    - "D-07: All list rows use shadcn Item (variant=outline) and ItemActions — never raw <li>; ItemGroup replaces <ul> in MembersSection"
    - "D-08: InvitedMemberRow is a new standalone component — not a variant of MemberRow"
    - "D-09: InvitedMemberRow has opacity-60, email as display name, initials-from-email UserAvatar"
    - "D-10: Time display shows 'Invited [relative]' from createdAt and 'Expires in N days' from expiresAt"
    - "D-11: Expiry warning when ≤ 2 days or status === 'expired'; expired state driven by Clerk status field, not date math"
    - "D-12: Trash icon opens AlertDialog 'Revoke invite to {email}?' — matches MemberRow remove pattern"
    - "D-13: MembersSection fires both queries at top level in parallel; combined isLoading = membersQuery.isLoading || invitationsQuery.isLoading"
    - "D-14: Empty state 'This household has no other members.' shows only when both members.length === 0 AND invitations.length === 0"
    - "InvitedMemberRow renders one card per pending or expired invitation, dimmed (opacity-60), with email + 'Invited' badge + revoke button"
    - "MembersSection fires useGetOrgMembers and useGetOrgInvitations in parallel — both at top level, no waterfall"
    - "Combined loading state (membersQuery.isLoading || invitationsQuery.isLoading) shows ONE skeleton block"
    - "Empty state shows ONLY when BOTH members.length === 0 AND invitations.length === 0"
    - "Revoke button opens AlertDialog with email-personalized text; confirming triggers useRevokeInvitation; card disappears on settle"
    - "Expiry label shows 'Expired' (text-destructive) when status === 'expired'; 'Expires in N days' (warning color when ≤ 2 days) otherwise"
    - "Sending a new invite causes the new card to appear in the list immediately (via useInviteMember invalidation from Plan 02)"
  artifacts:
    - path: "packages/ui/src/components/item.tsx"
      provides: "shadcn Item component (installed)"
      contains: "ItemGroup"
    - path: "packages/ui/src/components/empty.tsx"
      provides: "shadcn Empty component (installed)"
      contains: "EmptyContent"
    - path: "apps/web/src/components/settings/MemberRow.tsx"
      provides: "MemberRow updated to use Item + ItemActions"
      contains: "import { Item, ItemActions }"
    - path: "apps/web/src/components/settings/InvitedMemberRow.tsx"
      provides: "InvitedMemberRow component"
      contains: "export const InvitedMemberRow"
    - path: "apps/web/src/components/settings/MembersSection.tsx"
      provides: "Updated MembersSection with parallel queries and invited rows"
      contains: "useGetOrgInvitations"
  key_links:
    - from: "apps/web/src/components/settings/MembersSection.tsx"
      to: "apps/web/src/components/settings/InvitedMemberRow.tsx"
      via: "import { InvitedMemberRow }"
      pattern: "import \\{ InvitedMemberRow \\} from './InvitedMemberRow'"
    - from: "apps/web/src/components/settings/MembersSection.tsx"
      to: "useGetOrgInvitations"
      via: "TanStack Query hook"
      pattern: "useGetOrgInvitations\\(\\)"
    - from: "apps/web/src/components/settings/InvitedMemberRow.tsx"
      to: "useRevokeInvitation"
      via: "called via onRevoke prop from parent"
      pattern: "onRevoke\\(invitation\\.id\\)"
---

<objective>
Render pending Clerk invitations as dimmed "Invited" cards in the Members list,
matching the layout of the existing `MemberRow` but visually distinct. Wire up
the revoke flow with an AlertDialog. Rewire `MembersSection` to fire both
queries in parallel with combined loading and empty states.

Purpose: Close the UX gap between sending an invite and seeing the result —
admins now have immediate visual confirmation and can revoke before acceptance.

Output: New `InvitedMemberRow` component + updated `MembersSection`.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/phases/03.4.6-show-invited-member-card-in-household-view-inserted/03.4.6-CONTEXT.md
@.planning/phases/03.4.6-show-invited-member-card-in-household-view-inserted/03.4.6-RESEARCH.md
@.planning/phases/03.4.6-show-invited-member-card-in-household-view-inserted/03.4.6-PATTERNS.md
@.planning/phases/03.4.6-show-invited-member-card-in-household-view-inserted/03.4.6-UI-SPEC.md
@apps/web/src/components/settings/MemberRow.tsx
@apps/web/src/components/settings/MembersSection.tsx
@apps/web/src/components/members/UserAvatar.tsx
@apps/web/src/lib/data-access/org/index.ts

<interfaces>
MemberRow.tsx (analog for the new InvitedMemberRow — copy <li> structure):

  <li className="flex items-center justify-between rounded-md border border-border px-4 py-3 text-sm">
    <div className="flex min-w-0 items-center gap-2">
      <UserAvatar name={member.displayName} imageUrl={member.imageUrl} className="shrink-0" />
      <Text as="span" variant="body-sm" className="min-w-0 truncate font-semibold">
        {member.displayName}
      </Text>
      {isCurrentUser ? <Badge variant="secondary">You</Badge> : null}
    </div>
    <div className="ml-4 flex shrink-0 items-center gap-2">
      <Text as="span" variant="caption" className="capitalize">{member.role}</Text>
      {!isCurrentUser ? (
        <AlertDialog>
          <AlertDialogTrigger
            className="inline-flex size-9 items-center justify-center rounded-md text-muted-foreground hover:bg-destructive/10 hover:text-destructive focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none"
            aria-label={`Remove ${member.displayName}`}>
            <Trash2 className="size-4" />
          </AlertDialogTrigger>
          ...
        </AlertDialog>
      ) : null}
    </div>
  </li>

UserAvatar (reuse — accepts `name` for initials and optional `imageUrl`):

  <UserAvatar name={email} className="shrink-0" />
  // No imageUrl for invitations — falls back to AvatarFallback with getInitials(email)

PendingInvitation (from @ploutizo/types via Plan 01):

  export type PendingInvitationStatus = 'pending' | 'accepted' | 'revoked' | 'expired';
  export interface PendingInvitation {
    id: string;
    email: string;
    status: PendingInvitationStatus;
    createdAt: string;        // ISO string
    expiresAt: string | null; // ISO string or null
  }

Plan 02 hooks (consumed here):

  const membersQuery = useGetOrgMembers();         // existing
  const invitationsQuery = useGetOrgInvitations(); // new in Plan 02
  const revokeMutation = useRevokeInvitation();    // new in Plan 02
  // useInviteMember already invalidates ['org-invitations'] (Plan 02)

date-fns formatDistanceToNow (already a dep — apps/web/package.json line 30):

  import { formatDistanceToNow } from 'date-fns';
  formatDistanceToNow(new Date(invitation.createdAt), { addSuffix: true })
  // → "3 days ago"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 0: Install shadcn Item component</name>
  <files>packages/ui/src/components/item.tsx</files>
  <read_first>
    - packages/ui/components.json (confirm style: base-nova and aliases)
  </read_first>
  <action>
    Install the shadcn Item and Empty components into packages/ui. Run from the packages/ui directory:

    ```
    cd packages/ui && pnpm dlx shadcn@latest add item empty
    ```

    This installs:
    - `packages/ui/src/components/item.tsx` — exports `Item`, `ItemGroup`, `ItemActions`, `ItemContent`, `ItemTitle`, `ItemDescription`, `ItemMedia`, `ItemHeader`, `ItemFooter`, `ItemSeparator`.
    - `packages/ui/src/components/empty.tsx` — exports `Empty`, `EmptyHeader`, `EmptyMedia`, `EmptyTitle`, `EmptyDescription`, `EmptyContent`.

    Both are shadcn-generated — do NOT modify after install. Import paths for consumers: `@ploutizo/ui/components/item`, `@ploutizo/ui/components/empty`.
  </action>
  <verify>
    <automated>test -f packages/ui/src/components/item.tsx && test -f packages/ui/src/components/empty.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `packages/ui/src/components/item.tsx` exists and contains `ItemGroup`
    - `packages/ui/src/components/empty.tsx` exists and contains `EmptyContent`
  </acceptance_criteria>
  <done>item.tsx and empty.tsx installed in packages/ui.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 0.5: Update MemberRow to use Item + ItemActions</name>
  <files>apps/web/src/components/settings/MemberRow.tsx</files>
  <read_first>
    - apps/web/src/components/settings/MemberRow.tsx (current — full rewrite)
    - packages/ui/src/components/item.tsx (just installed — check Item and ItemActions props)
  </read_first>
  <action>
    Replace raw `<li>` with `<Item variant="outline">` and `<div className="ml-4...">` with `<ItemActions>`.
    Item base classes include `rounded-lg border` — override to `rounded-md` via className to match existing design token.
    Item does not set explicit padding — add `px-4 py-3` via className.

    Replace the ENTIRE contents of `apps/web/src/components/settings/MemberRow.tsx` with:

    ```
    import { Trash2 } from 'lucide-react';
    import {
      AlertDialog,
      AlertDialogAction,
      AlertDialogCancel,
      AlertDialogContent,
      AlertDialogDescription,
      AlertDialogFooter,
      AlertDialogHeader,
      AlertDialogTitle,
      AlertDialogTrigger,
    } from '@ploutizo/ui/components/alert-dialog';
    import { Badge } from '@ploutizo/ui/components/badge';
    import { Item, ItemActions } from '@ploutizo/ui/components/item';
    import { Text } from '@ploutizo/ui/components/text';
    import type { OrgMember } from '@ploutizo/types';
    import { UserAvatar } from '@/components/members/UserAvatar';

    interface MemberRowProps {
      member: OrgMember;
      isCurrentUser: boolean;
      onRemove: (memberId: string) => void;
    }

    export const MemberRow = ({
      member,
      isCurrentUser,
      onRemove,
    }: MemberRowProps) => (
      <Item variant="outline" className="rounded-md px-4 py-3">
        <div className="flex min-w-0 items-center gap-2">
          <UserAvatar
            name={member.displayName}
            imageUrl={member.imageUrl}
            className="shrink-0"
          />
          <Text
            as="span"
            variant="body-sm"
            className="min-w-0 truncate font-semibold"
          >
            {member.displayName}
          </Text>
          {isCurrentUser ? <Badge variant="secondary">You</Badge> : null}
        </div>
        <ItemActions>
          <Text as="span" variant="caption" className="capitalize">
            {member.role}
          </Text>
          {!isCurrentUser ? (
            <AlertDialog>
              <AlertDialogTrigger
                className="inline-flex size-9 items-center justify-center rounded-md text-muted-foreground hover:bg-destructive/10 hover:text-destructive focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none"
                aria-label={`Remove ${member.displayName}`}
              >
                <Trash2 className="size-4" />
              </AlertDialogTrigger>
              <AlertDialogContent className="w-[calc(100%-2rem)] max-w-md">
                <AlertDialogHeader>
                  <AlertDialogTitle>
                    Remove {member.firstName ?? member.displayName}?
                  </AlertDialogTitle>
                  <AlertDialogDescription>
                    Remove {member.displayName} from this household? They will lose
                    access immediately.
                  </AlertDialogDescription>
                </AlertDialogHeader>
                <AlertDialogFooter>
                  <AlertDialogCancel>Keep member</AlertDialogCancel>
                  <AlertDialogAction
                    variant="destructive"
                    onClick={() => onRemove(member.id)}
                  >
                    Remove
                  </AlertDialogAction>
                </AlertDialogFooter>
              </AlertDialogContent>
            </AlertDialog>
          ) : null}
        </ItemActions>
      </Item>
    );
    ```
  </action>
  <verify>
    <automated>grep -q "Item" apps/web/src/components/settings/MemberRow.tsx && grep -q "ItemActions" apps/web/src/components/settings/MemberRow.tsx && pnpm turbo typecheck --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - No `<li` in file: `grep -c "<li" apps/web/src/components/settings/MemberRow.tsx` outputs `0`
    - File contains `import { Item, ItemActions } from '@ploutizo/ui/components/item'`
    - File contains `<Item variant="outline"`
    - File contains `<ItemActions>`
    - `pnpm turbo typecheck --filter=web` exits 0
  </acceptance_criteria>
  <done>MemberRow uses Item + ItemActions; no raw &lt;li&gt; remains; typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 1: Create InvitedMemberRow component</name>
  <files>apps/web/src/components/settings/InvitedMemberRow.tsx</files>
  <read_first>
    - apps/web/src/components/settings/MemberRow.tsx (analog — copy li/AlertDialog structure)
    - apps/web/src/components/members/UserAvatar.tsx (confirm props: `name`, `imageUrl`, `className`)
    - .planning/phases/03.4.6-show-invited-member-card-in-household-view-inserted/03.4.6-UI-SPEC.md (visual contract)
    - packages/ui/src/components/text.tsx (confirm `<Text as="span" variant="body-sm">` is supported)
  </read_first>
  <action>
    Implements D-08 (new InvitedMemberRow component — NOT a variant of MemberRow), D-09 (opacity-60 dim, email-as-name, initials avatar), D-10 ("Invited X ago" + "Expires in N days" labels), D-11 (status === 'expired' authoritative — destructive color; warning color when ≤ 2 days remaining), D-12 (AlertDialog revoke flow).

    Create `apps/web/src/components/settings/InvitedMemberRow.tsx` with this EXACT content:

    ```
    import { Trash2 } from 'lucide-react';
    import { formatDistanceToNow } from 'date-fns';
    import {
      AlertDialog,
      AlertDialogAction,
      AlertDialogCancel,
      AlertDialogContent,
      AlertDialogDescription,
      AlertDialogFooter,
      AlertDialogHeader,
      AlertDialogTitle,
      AlertDialogTrigger,
    } from '@ploutizo/ui/components/alert-dialog';
    import { Badge } from '@ploutizo/ui/components/badge';
    import { Item, ItemActions } from '@ploutizo/ui/components/item';
    import { Text } from '@ploutizo/ui/components/text';
    import type { PendingInvitation } from '@ploutizo/types';
    import { UserAvatar } from '@/components/members/UserAvatar';

    interface InvitedMemberRowProps {
      invitation: PendingInvitation;
      onRevoke: (invitationId: string) => void;
    }

    // Per D-11: Clerk's `status` field is authoritative for the 'Expired' state.
    // Days remaining is computed from expiresAt for label text only — never to detect expiry.
    function getExpiryInfo(
      expiresAt: string | null,
      status: PendingInvitation['status']
    ): { label: string; className: string } | null {
      if (status === 'expired') {
        return { label: 'Expired', className: 'text-destructive' };
      }
      if (!expiresAt) return null;
      const daysLeft = Math.ceil(
        (new Date(expiresAt).getTime() - Date.now()) / (1000 * 60 * 60 * 24)
      );
      const label = `Expires in ${daysLeft} day${daysLeft === 1 ? '' : 's'}`;
      const className =
        daysLeft <= 2 ? 'text-warning-foreground' : 'text-muted-foreground';
      return { label, className };
    }

    export const InvitedMemberRow = ({
      invitation,
      onRevoke,
    }: InvitedMemberRowProps) => {
      const invitedAgo = formatDistanceToNow(new Date(invitation.createdAt), {
        addSuffix: true,
      });
      const expiryInfo = getExpiryInfo(invitation.expiresAt, invitation.status);

      return (
        <Item variant="outline" className="rounded-md px-4 py-3 opacity-60">
          <div className="flex min-w-0 items-center gap-2">
            <UserAvatar
              name={invitation.email}
              imageUrl={null}
              className="shrink-0"
            />
            <div className="flex min-w-0 flex-col">
              <Text
                as="span"
                variant="body-sm"
                className="min-w-0 truncate font-semibold"
              >
                {invitation.email}
              </Text>
              <div className="flex items-center gap-1">
                <Text
                  as="span"
                  variant="caption"
                  className="text-muted-foreground"
                >
                  Invited {invitedAgo}
                </Text>
                {expiryInfo ? (
                  <Text as="span" variant="caption" className={expiryInfo.className}>
                    · {expiryInfo.label}
                  </Text>
                ) : null}
              </div>
            </div>
            <Badge variant="secondary">Invited</Badge>
          </div>
          <ItemActions>
            <AlertDialog>
              <AlertDialogTrigger
                className="inline-flex size-9 items-center justify-center rounded-md text-muted-foreground hover:bg-destructive/10 hover:text-destructive focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none"
                aria-label={`Revoke invite to ${invitation.email}`}
              >
                <Trash2 className="size-4" />
              </AlertDialogTrigger>
              <AlertDialogContent className="w-[calc(100%-2rem)] max-w-md">
                <AlertDialogHeader>
                  <AlertDialogTitle>
                    Revoke invite to {invitation.email}?
                  </AlertDialogTitle>
                  <AlertDialogDescription>
                    {invitation.email} will no longer be able to join this household using this invite.
                  </AlertDialogDescription>
                </AlertDialogHeader>
                <AlertDialogFooter>
                  <AlertDialogCancel>Keep invite</AlertDialogCancel>
                  <AlertDialogAction
                    variant="destructive"
                    onClick={() => onRevoke(invitation.id)}
                  >
                    Revoke
                  </AlertDialogAction>
                </AlertDialogFooter>
              </AlertDialogContent>
            </AlertDialog>
          </ItemActions>
        </Item>
      );
    };
    ```

    Notes (do NOT skip):
    - `opacity-60` on `<Item>` (D-09) — visually distinguishes from active members.
    - `imageUrl={null}` on UserAvatar — invitations have no profile picture; falls back to initials. `getInitials(email)` produces a single character (Pitfall 5 in RESEARCH.md) — this is expected.
    - `aria-label={`Revoke invite to ${invitation.email}`}` (CLAUDE.md icon-only-button rule).
    - Copy strings exactly as in UI-SPEC.md "Copywriting Contract" — `Keep invite`, `Revoke`, `Invited`, `Expired`, etc.
    - NO `forwardRef` — React 19 project (CLAUDE.md `react19-no-forwardref`).
    - All text uses `<Text>` — never raw `<span>`/`<p>`/`<div>` for text content.
    - `<Badge variant="secondary">` matches UI-SPEC + reuses the existing "You" badge variant.
    - Days-left computation: `Math.ceil` (NOT floor) so an invitation 1.3 days away shows "Expires in 2 days" (matches user expectation).
    - Uses `<Item variant="outline">` + `<ItemActions>` (D-07) — NOT raw `<li>`. Import from `@ploutizo/ui/components/item`.
    - `className="rounded-md px-4 py-3 opacity-60"` on Item — overrides Item's default `rounded-lg` to match design system token; adds explicit padding.
  </action>
  <verify>
    <automated>test -f apps/web/src/components/settings/InvitedMemberRow.tsx && pnpm turbo typecheck --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - File `apps/web/src/components/settings/InvitedMemberRow.tsx` exists
    - `grep -c "export const InvitedMemberRow" apps/web/src/components/settings/InvitedMemberRow.tsx` outputs `1`
    - File contains `import { Item, ItemActions } from '@ploutizo/ui/components/item'`
    - No raw `<li` in file: `grep -c "<li" apps/web/src/components/settings/InvitedMemberRow.tsx` outputs `0`
    - File contains `<Item variant="outline"` with `opacity-60` in className (D-09 dimmed visual)
    - File contains `<ItemActions>` wrapping the AlertDialog
    - File contains literal `aria-label={\`Revoke invite to ${invitation.email}\`}` (icon-only button accessibility)
    - File contains literal `import type { PendingInvitation } from '@ploutizo/types'`
    - File contains literal `formatDistanceToNow` (date-fns import)
    - File contains the exact strings: `'Revoke invite to '`, `'Keep invite'`, `'Revoke'`, `'Invited'` (Badge text), `'Expired'`, `'Expires in '`
    - File contains literal `text-destructive` (used for expired state per D-11)
    - File contains literal `text-warning-foreground` (used for ≤ 2 days warning per D-11)
    - File contains literal `status === 'expired'` (D-11 expired branch)
    - No `forwardRef` in file: `grep -c "forwardRef" apps/web/src/components/settings/InvitedMemberRow.tsx` outputs `0`
    - No raw `<span>` text content (use Text): `grep -E "<span[^>]*>[A-Za-z]" apps/web/src/components/settings/InvitedMemberRow.tsx | wc -l` outputs `0`
    - `pnpm turbo typecheck --filter=web` exits 0
  </acceptance_criteria>
  <done>InvitedMemberRow component file exists with Item/ItemActions, correct structure, accessibility, status-based expiry logic, and typecheck passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update MembersSection to fire parallel queries and render invited rows</name>
  <files>apps/web/src/components/settings/MembersSection.tsx</files>
  <read_first>
    - apps/web/src/components/settings/MembersSection.tsx (current — full rewrite needed)
    - apps/web/src/components/settings/InvitedMemberRow.tsx (just created in Task 1)
    - apps/web/src/lib/data-access/org/index.ts (confirm useGetOrgInvitations + useRevokeInvitation are exported)
  </read_first>
  <action>
    Implements D-13 (parallel queries, combined isLoading) and D-14 (combined empty state — only when both arrays empty).

    Replace the ENTIRE contents of `apps/web/src/components/settings/MembersSection.tsx` with:

    ```
    import { useCallback } from 'react';
    import { useUser } from '@clerk/tanstack-react-start';
    import { UserPlus } from 'lucide-react';
    import { Button } from '@ploutizo/ui/components/button';
    import {
      Empty,
      EmptyContent,
      EmptyDescription,
      EmptyHeader,
      EmptyMedia,
      EmptyTitle,
    } from '@ploutizo/ui/components/empty';
    import { ItemGroup } from '@ploutizo/ui/components/item';
    import { Skeleton } from '@ploutizo/ui/components/skeleton';
    import { Text } from '@ploutizo/ui/components/text';
    import { InviteMemberForm } from './InviteMemberForm';
    import { InvitedMemberRow } from './InvitedMemberRow';
    import { MemberRow } from './MemberRow';
    import {
      useGetOrgInvitations,
      useGetOrgMembers,
      useRemoveMember,
      useRevokeInvitation,
    } from '@/lib/data-access/org';

    export const MembersSection = () => {
      // Per D-13: BOTH queries called unconditionally at top level — no waterfall.
      // Never gate one query behind the other's isLoading.
      const membersQuery = useGetOrgMembers();
      const invitationsQuery = useGetOrgInvitations();
      const { user } = useUser();
      const removeMutation = useRemoveMember();
      const revokeMutation = useRevokeInvitation();

      const members = membersQuery.data ?? [];
      const invitations = invitationsQuery.data ?? [];
      // Combined loading: single skeleton block until BOTH queries settle.
      const isLoading = membersQuery.isLoading || invitationsQuery.isLoading;

      const handleRemove = useCallback(
        (memberId: string) => removeMutation.mutate(memberId),
        [removeMutation]
      );
      const handleRevoke = useCallback(
        (invitationId: string) => revokeMutation.mutate(invitationId),
        [revokeMutation]
      );
      // InviteMemberForm's email input has id="invite-email" — focus it directly.
      const handleFocusInviteInput = useCallback(() => {
        (document.getElementById('invite-email') as HTMLInputElement | null)?.focus();
      }, []);

      // Per D-14: empty state ONLY when both lists are empty.
      const isEmpty = members.length === 0 && invitations.length === 0;

      return (
        <section className="flex flex-col gap-4">
          <Text as="h2" variant="h3">
            Members
          </Text>

          {isLoading ? (
            <div className="space-y-2">
              {Array.from({ length: 3 }).map((_, i) => (
                <Skeleton key={i} className="h-12 rounded-md" />
              ))}
            </div>
          ) : isEmpty ? (
            <Empty>
              <EmptyHeader>
                <EmptyMedia variant="icon">
                  <UserPlus className="size-6" />
                </EmptyMedia>
                <EmptyTitle>No members yet</EmptyTitle>
                <EmptyDescription>
                  This household has no other members. Invite someone to get started.
                </EmptyDescription>
              </EmptyHeader>
              <EmptyContent>
                <Button variant="outline" size="sm" onClick={handleFocusInviteInput}>
                  Send an invite
                </Button>
              </EmptyContent>
            </Empty>
          ) : (
            <ItemGroup className="gap-2">
              {members.map((member) => (
                <MemberRow
                  key={member.id}
                  member={member}
                  isCurrentUser={member.externalId === user?.id}
                  onRemove={handleRemove}
                />
              ))}
              {invitations.map((invitation) => (
                <InvitedMemberRow
                  key={invitation.id}
                  invitation={invitation}
                  onRevoke={handleRevoke}
                />
              ))}
            </ItemGroup>
          )}

          <InviteMemberForm />
        </section>
      );
    };
    ```

    Critical changes from the previous version (do NOT skip):
    - Both `useGetOrgMembers()` and `useGetOrgInvitations()` called UNCONDITIONALLY at the top of the component body. Never `if (membersQuery.isLoading) return ...` before calling the second hook.
    - `members.data ?? []` and `invitations.data ?? []` — defensive defaults so the render path never crashes on undefined.
    - Combined `isLoading` uses `||` (true if EITHER query is still loading).
    - `isEmpty` requires BOTH lists empty (D-14). Critical: if only invitations exist (zero active members), the list still renders the invitation cards — empty-state text is suppressed.
    - Render order: active members FIRST, then invited rows. Both inside the same `<ItemGroup>` for visual continuity.
    - `<ItemGroup className="gap-2">` — overrides ItemGroup's default `gap-4` to match current `space-y-2` spacing.
    - No raw `<ul>` or `<li>` — use `ItemGroup` (D-07).
    - Empty state: `<Empty>` with `<EmptyMedia variant="icon"><UserPlus /></EmptyMedia>`, title, description, and a `<Button onClick={handleFocusInviteInput}>Send an invite</Button>` in `<EmptyContent>`.
    - `handleFocusInviteInput` uses `document.getElementById('invite-email')` — the email input in `InviteMemberForm` already has this stable id. No ref threading, no changes to `InviteMemberForm`.
    - Both mutation handlers use `useCallback` (stable refs for memoized children).
    - No new prop threading on `MemberRow` — its API is unchanged.
  </action>
  <verify>
    <automated>grep -q "useGetOrgInvitations" apps/web/src/components/settings/MembersSection.tsx && grep -q "InvitedMemberRow" apps/web/src/components/settings/MembersSection.tsx && pnpm turbo typecheck --filter=web</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "const membersQuery = useGetOrgMembers()" apps/web/src/components/settings/MembersSection.tsx` outputs `1`
    - `grep -c "const invitationsQuery = useGetOrgInvitations()" apps/web/src/components/settings/MembersSection.tsx` outputs `1`
    - File contains literal `membersQuery.isLoading || invitationsQuery.isLoading` (combined loading per D-13)
    - File contains literal `members.length === 0 && invitations.length === 0` (combined empty per D-14)
    - File contains both `<MemberRow` and `<InvitedMemberRow` JSX
    - File contains `useRevokeInvitation` import
    - File contains `import { ItemGroup } from '@ploutizo/ui/components/item'`
    - File contains `import { Empty` (empty state component)
    - File contains `<UserPlus` (empty state icon)
    - File contains `id="invite-email"` reference in `handleFocusInviteInput`
    - File contains `<ItemGroup` — no raw `<ul`: `grep -c "<ul" apps/web/src/components/settings/MembersSection.tsx` outputs `0`
    - Anti-waterfall check: NO occurrence of `if (membersQuery.isLoading) return` or any early-return between the two `useQuery` calls. Run: `grep -B0 -A20 "useGetOrgMembers" apps/web/src/components/settings/MembersSection.tsx | head -25` — visually confirm both useQuery hooks are adjacent with no early-return between them.
    - `useCallback` count: `grep -c "useCallback" apps/web/src/components/settings/MembersSection.tsx` outputs at least `3` (remove, revoke, focusInviteInput)
    - `pnpm turbo typecheck --filter=web` exits 0
  </acceptance_criteria>
  <done>MembersSection fires both queries in parallel, renders MemberRow + InvitedMemberRow inside ItemGroup, shows Empty with UserPlus icon + invite action when both lists empty. Typecheck passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — invited member card visual + revoke + invite flow</name>
  <what-built>
    - `InvitedMemberRow` component with dimmed avatar, email, time labels, "Invited" badge, and trash-icon revoke button
    - `MembersSection` updated to fire both queries in parallel, render invited cards after member cards, combined loading + empty states, and integrated revoke flow
    - `useInviteMember` (from Plan 02) now invalidates `['org-invitations']` so newly sent invites appear immediately
  </what-built>
  <how-to-verify>
    Run the dev stack (in the project root): `pnpm dev`. Sign in as an admin of a test household. Navigate to Settings → Household → Members section.

    1. **Initial load:** Confirm a SINGLE skeleton block (3 rows) flashes briefly, then the populated list renders. There should NOT be a flash where members appear, then the page re-flashes when invitations load (waterfall would show this).
    2. **Existing data:**
       - Active members render as before (unchanged `MemberRow`).
       - Any pending invitations render BELOW active members as dimmed cards (`opacity-60`):
         - Email shown as the bold name
         - Initials avatar (single character — expected for emails)
         - "Invited X ago · Expires in N days"
         - "Invited" badge
         - Trash icon (revoke) on the right
    3. **Send a new invite:** Use the "Invite member" form below the list. Enter an email and submit. Confirm:
       - Toast: "Invite sent to {email}."
       - **Within ~1 second**, a new invited card appears in the list (no manual reload needed).
    4. **Expiry label color:**
       - For an invitation > 2 days from `expiresAt`: label color is muted (`text-muted-foreground`).
       - For an invitation ≤ 2 days: label color is the warning yellow (`text-warning-foreground`).
       - For a Clerk-expired invitation (`status === 'expired'`): label reads "Expired" in destructive red (`text-destructive`). To force this state for testing: in the Clerk dashboard, create an invitation with a 1-day expiry and wait, OR temporarily change the test invitation's expiry in Clerk admin to expose the expired state.
    5. **Revoke flow:**
       - Click the trash icon on a pending invited card.
       - AlertDialog opens with title "Revoke invite to {email}?" and description ending in "...will no longer be able to join this household using this invite."
       - Buttons: "Keep invite" (cancel), "Revoke" (destructive variant).
       - Click "Revoke". Confirm:
         - Dialog closes.
         - Card disappears from the list within ~1 second.
         - Refresh the page — card stays gone.
    6. **Empty state:**
       - In a household with no active OTHER members and no pending invitations (a brand new org), the list shows "This household has no other members."
       - In a household where the only entries are pending invitations (no active others), the empty-state text is HIDDEN and only the invited cards render.
    7. **Accessibility:**
       - Tab through the list using keyboard. Each revoke button receives a visible focus ring.
       - Each revoke button announces "Revoke invite to {email}" to a screen reader (or inspect DOM: `aria-label="Revoke invite to {email}"`).
    8. **Network/anti-waterfall sanity check:**
       - Open browser devtools → Network tab. Reload Settings → Household.
       - Confirm `/api/households/members` and `/api/households/invitations` start within ~50ms of each other (parallel — NOT sequential where the second only fires after the first resolves).
  </how-to-verify>
  <resume-signal>Type "approved" if all eight verifications pass. If any fail, describe the failure and which step.</resume-signal>
  <files>apps/web/src/components/settings/InvitedMemberRow.tsx, apps/web/src/components/settings/MembersSection.tsx</files>
  <action>Human verification only — no automation in this task. Follow the steps in <how-to-verify> and report results via <resume-signal>.</action>
  <verify><manual>Steps 1–8 in <how-to-verify> pass</manual></verify>
  <done>User has typed "approved" after running through all eight verification scenarios.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Web client → Hono API | UI invokes apiFetch which crosses the API edge |
| Render layer → DOM | React renders user-controlled content (invitation email) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.4.6-03-01 | XSS via invitation email field | InvitedMemberRow render | mitigate | React escapes all string children by default. Email is rendered via `{invitation.email}` (text node) and `aria-label={\`Revoke invite to ${invitation.email}\`}` — both auto-escaped by React. Never use `dangerouslySetInnerHTML`. |
| T-03.4.6-03-02 | Click-jacking on revoke action | AlertDialog | mitigate | Radix `AlertDialog` requires explicit user click on "Revoke" inside a focus-trapped dialog. Single-click revoke is gated behind dialog confirmation per D-12. |
| T-03.4.6-03-03 | Missing aria-label on icon-only revoke button | InvitedMemberRow trigger | mitigate | `aria-label={\`Revoke invite to ${invitation.email}\`}` set on `AlertDialogTrigger`. Acceptance criteria includes a grep for this literal pattern. |
| T-03.4.6-03-04 | Tampering — UI displaying expired invitations as active | Expiry label logic | mitigate | Per D-11, "Expired" text + destructive color is driven by `status === 'expired'` (Clerk's authoritative field), NOT by date math. Date math is used only to compute a "days remaining" label for non-expired invitations. Acceptance criteria includes a grep for `status === 'expired'`. |
| T-03.4.6-03-05 | Information Disclosure — email leakage to non-admins | Members section visibility | accept | This page is already gated behind admin-org context (existing posture for `MembersSection`). No new gate added; same posture as showing active members' emails today. |

## Security Posture Notes

- React's default escaping handles all dynamic strings (email, status, date labels). No raw HTML.
- All HTTP via `apiFetch` — bearer token already attached.
- No new dependencies introduced (`date-fns`, `lucide-react`, shadcn primitives all already in tree).
</threat_model>

<verification>
- `pnpm turbo typecheck --filter=web` passes after both tasks.
- Human checkpoint validates the eight visual/interaction scenarios listed in Task 3.
- Network tab confirms parallel queries (anti-waterfall).
- Revoke flow: card disappears on settle, persists across reload.
- Send-invite flow: new card appears in list without reload (Plan 02 invalidation).
</verification>

<success_criteria>
- [ ] shadcn `Item` and `Empty` components installed to `packages/ui/src/components/`
- [ ] `MemberRow` uses `Item variant="outline"` + `ItemActions` — no raw `<li>`
- [ ] `InvitedMemberRow` component renders with `opacity-60`, email name, initials avatar, time/expiry labels, "Invited" badge, and trash revoke button — using `Item` + `ItemActions`
- [ ] `MembersSection` uses `ItemGroup` — no raw `<ul>`
- [ ] Empty state shows `UserPlus` icon, "No members yet" title, description, and "Send an invite" button that focuses the invite form input
- [ ] Revoke button has `aria-label="Revoke invite to {email}"`
- [ ] Expiry label uses `status === 'expired'` (NOT date math) for the "Expired" red state
- [ ] Expiry label uses `text-warning-foreground` when ≤ 2 days remaining
- [ ] `MembersSection` fires `useGetOrgMembers` and `useGetOrgInvitations` in parallel at the top of the component
- [ ] Combined loading state: `membersQuery.isLoading || invitationsQuery.isLoading`
- [ ] Empty state ONLY when `members.length === 0 && invitations.length === 0`
- [ ] Active members render before invited rows; both inside the same `<ul>`
- [ ] Sending a new invite via `InviteMemberForm` causes the new card to appear without reload
- [ ] Revoking a card removes it from the list without reload
- [ ] `pnpm turbo typecheck --filter=web` exits 0
- [ ] Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/03.4.6-show-invited-member-card-in-household-view-inserted/03.4.6-03-SUMMARY.md` documenting:
- Confirmation of the eight checkpoint scenarios.
- Any visual deviations from UI-SPEC.md with rationale.
- Any rough edges noted during human verification (e.g. avatar single-letter initials, days-remaining edge cases).
</output>
