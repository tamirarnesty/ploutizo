---
phase: "03.4.6"
plan: "02"
subsystem: "data-access/org"
tags:
  - tanstack-query
  - data-access
  - invitations
dependency_graph:
  requires:
    - "03.4.6-01: PendingInvitation type + API endpoints"
  provides:
    - "useGetOrgInvitations hook (query key: ['org-invitations'])"
    - "useRevokeInvitation mutation hook"
    - "useInviteMember invalidates ['org-invitations'] on settle"
  affects:
    - "apps/web/src/lib/data-access/org barrel"
tech_stack:
  added: []
  patterns:
    - "TanStack Query query/mutation hook pair mirroring useGetOrgMembers/useRemoveMember"
key_files:
  created:
    - "apps/web/src/lib/data-access/org/useGetOrgInvitations.ts"
    - "apps/web/src/lib/data-access/org/useRevokeInvitation.ts"
  modified:
    - "apps/web/src/lib/data-access/org/useInviteMember.ts"
    - "apps/web/src/lib/data-access/org/index.ts"
decisions:
  - "onSettled used (not onSuccess) for cache invalidation — project convention ensures invalidation on both success and error"
  - "apiFetch used exclusively (no raw fetch()) per CLAUDE.md and threat mitigation T-03.4.6-02-03"
metrics:
  duration: "~5 minutes"
  completed: "2026-05-05"
---

# Phase 03.4.6 Plan 02: TanStack Query hooks for org invitations Summary

TanStack Query hooks bridging the Plan 01 invitation API to the React UI: `useGetOrgInvitations` (GET), `useRevokeInvitation` (DELETE), and updated `useInviteMember` (dual cache invalidation).

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Create useGetOrgInvitations + useRevokeInvitation + barrel update | a411daa |
| 2 | Update useInviteMember to invalidate ['org-invitations'] on settle | 4f93d18 |

## Deviations from Plan

**1. [Rule 3 - Blocking] Prettier formatting auto-fixed before commit**
- **Found during:** Task 1 commit attempt
- **Issue:** Pre-commit hook ran `prettier --check` and failed on the two new files — Prettier wrapped the long `apiFetch(...)` call in `fetchOrgInvitations` and the `onSettled` arrow in `useRevokeInvitation`
- **Fix:** Ran `pnpm --filter=web exec prettier --write` on both files before re-staging
- **Files modified:** `useGetOrgInvitations.ts`, `useRevokeInvitation.ts`
- **Commit:** Included in a411daa (re-staged after format fix)

No other deviations — hook content, query keys, and invalidation logic match plan exactly.

## Verification

- `pnpm turbo typecheck --filter=web` exits 0 (confirmed by pre-commit hook on both commits)
- All four files compile against `@ploutizo/types` exporting `PendingInvitation`
- `useInviteMember` invalidates both `['org-members']` and `['org-invitations']` on settle
- No raw `fetch(` in either new hook file (only `apiFetch`)
- queryKey `['org-invitations']` present in both `useGetOrgInvitations` and `useRevokeInvitation`

## Ready for Plan 03

Plan 03 UI work can now consume:
- `useGetOrgInvitations` — fetch pending invitations list
- `useRevokeInvitation` — revoke a specific invitation by ID
- `useInviteMember` (updated) — newly sent invite triggers immediate pending card appearance via `['org-invitations']` invalidation

## Self-Check: PASSED

- `apps/web/src/lib/data-access/org/useGetOrgInvitations.ts` — exists
- `apps/web/src/lib/data-access/org/useRevokeInvitation.ts` — exists
- Commit a411daa — confirmed in git log
- Commit 4f93d18 — confirmed in git log
