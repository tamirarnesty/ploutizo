---
phase: 03.4.6-show-invited-member-card-in-household-view-inserted
verified: 2026-05-05T00:00:00Z
status: passed
score: 14/14
overrides_applied: 0
---

# Phase 03.4.6: Show Invited Member Card in Household View — Verification Report

**Phase Goal:** Show invited member card in Household view — pending Clerk invitations rendered as dimmed "Invited" cards in the Members list, with revoke flow.
**Verified:** 2026-05-05
**Status:** passed
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | API returns pending Clerk invitations alongside active members | VERIFIED | `GET /invitations` route exists, calls `listInvitations(orgId)`, returns `{ data: PendingInvitation[] }` |
| 2 | Members list renders an "Invited" card per pending invite | VERIFIED | `InvitedMemberRow` renders in `MembersSection.tsx` via `invitations.map(...)` |
| 3 | Invited cards are visually distinct (dimmed, opacity-60) | VERIFIED | `InvitedMemberRow` has `opacity-60` on `<Item>` |
| 4 | GET /api/households/invitations exists, separate from GET /api/households/members | VERIFIED | `householdsRouter.get('/invitations', ...)` at line 84 of routes.ts |
| 5 | DELETE /api/households/invitations/:invitationId exists and revokes via Clerk | VERIFIED | `householdsRouter.delete('/invitations/:invitationId', ...)` at line 102; calls Clerk POST `.../revoke` |
| 6 | PendingInvitation shape { id, email, createdAt, expiresAt, status } | VERIFIED | Interface in `packages/types/src/index.ts` lines 56–62 |
| 7 | Both routes scoped to orgId from JWT (never client-supplied) | VERIFIED | Both routes use `c.get('orgId')` — 9 occurrences of this pattern across routes.ts |
| 8 | useGetOrgInvitations hook with queryKey ['org-invitations'] | VERIFIED | `queryKey: ['org-invitations']` present in `useGetOrgInvitations.ts` |
| 9 | useRevokeInvitation mutation calls DELETE and invalidates ['org-invitations'] | VERIFIED | `method: 'DELETE'` + `invalidateQueries({ queryKey: ['org-invitations'] })` in `useRevokeInvitation.ts` |
| 10 | useInviteMember invalidates BOTH ['org-members'] AND ['org-invitations'] on settle | VERIFIED | Both invalidation calls present in `useInviteMember.ts` lines 13–15 |
| 11 | InvitedMemberRow is a standalone component (not a variant of MemberRow) | VERIFIED | Separate file `InvitedMemberRow.tsx`; `InvitedMemberRowProps` takes `PendingInvitation`, not `OrgMember` |
| 12 | ConfirmDialog wraps AlertDialog; both row components use it, no inline AlertDialog | VERIFIED | `grep -c "AlertDialog" MemberRow.tsx` = 0; `grep -c "AlertDialog" InvitedMemberRow.tsx` = 0; both import `ConfirmDialog` |
| 13 | Combined loading state — skeleton only on cold load; empty only when both lists empty | VERIFIED | `isLoading = !membersQuery.data && !invitationsQuery.data`; `isEmpty = members.length === 0 && invitations.length === 0` |
| 14 | Human verification 8/8 scenarios passed | VERIFIED | Stated in task prompt: "Human verification was completed and approved (8/8 scenarios passed after fixes)" |

**Score:** 14/14 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `packages/types/src/index.ts` | PendingInvitation type + PendingInvitationStatus union | VERIFIED | Both exports present at lines 54–62 |
| `apps/api/src/services/households.ts` | listInvitations() + revokeInvitation() service functions | VERIFIED | Both `export async function` present; no x1000 multiplier (correct for ms timestamps) |
| `apps/api/src/routes/households.ts` | GET /invitations + DELETE /invitations/:invitationId | VERIFIED | Both routes registered; both use `c.get('orgId')` |
| `apps/api/src/__tests__/households.test.ts` | Test coverage for new invitations routes | VERIFIED | `describe('GET /api/households/invitations'` + `describe('DELETE /api/households/invitations/:invitationId'` both present |
| `apps/web/src/lib/data-access/org/useGetOrgInvitations.ts` | queryKey ['org-invitations'] | VERIFIED | Exact key present; uses `apiFetch`; imports `PendingInvitation` from `@ploutizo/types` |
| `apps/web/src/lib/data-access/org/useRevokeInvitation.ts` | DELETE mutation | VERIFIED | `method: 'DELETE'`; `invalidateQueries({ queryKey: ['org-invitations'] })` on settle |
| `apps/web/src/lib/data-access/org/useInviteMember.ts` | Invalidates both ['org-members'] and ['org-invitations'] | VERIFIED | Both invalidation calls in `onSettled` block |
| `apps/web/src/lib/data-access/org/index.ts` | Barrel re-exports both new hooks | VERIFIED | Lines 4–5 export both new hooks |
| `apps/web/src/components/settings/invitationUtils.ts` | getExpiryInfo, status === 'expired' check | VERIFIED | `export function getExpiryInfo`; `status === 'expired'` guard; no React import |
| `apps/web/src/components/settings/ConfirmDialog.tsx` | Shared dialog, tooltip prop | VERIFIED | `export function ConfirmDialog`; `tooltip` prop; controlled `AlertDialog`; no `forwardRef` |
| `apps/web/src/components/settings/MembersEmptyState.tsx` | Standalone empty state | VERIFIED | `export function MembersEmptyState`; uses `Empty` from shadcn; `onInviteClick` prop |
| `apps/web/src/components/settings/MemberRow.tsx` | Item + ItemActions + ConfirmDialog, no inline AlertDialog | VERIFIED | `import { ConfirmDialog }`; `<Item variant="outline">`; `<ItemActions>`; 0 AlertDialog references |
| `apps/web/src/components/settings/InvitedMemberRow.tsx` | opacity-60, email display, getExpiryInfo | VERIFIED | `opacity-60` on Item; email as display name; `getExpiryInfo` imported from `invitationUtils` |
| `apps/web/src/components/settings/MembersSection.tsx` | Parallel queries, ItemGroup, combined isLoading/isEmpty | VERIFIED | Both queries at top level (lines 19–20); `ItemGroup`; combined loading/empty logic |
| `packages/ui/src/components/item.tsx` | ItemGroup installed | VERIFIED | `grep -c "ItemGroup"` = 2 |
| `packages/ui/src/components/empty.tsx` | EmptyContent installed | VERIFIED | `grep -c "EmptyContent"` = 2 |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `apps/api/src/routes/households.ts` | `apps/api/src/services/households.ts` | `import { listInvitations, revokeInvitation }` | WIRED | Both functions imported and called in route handlers |
| `apps/api/src/services/households.ts` | Clerk REST API | `fetch` to `https://api.clerk.com/v1/organizations/${orgId}/invitations` | WIRED | URL with `?status=pending&status=expired&limit=100` present |
| `apps/api/src/services/households.ts` | `@ploutizo/types` | `import type { PendingInvitation }` | WIRED | Present at line 11 |
| `apps/web/src/lib/data-access/org/useGetOrgInvitations.ts` | `/api/households/invitations` | `apiFetch GET` | WIRED | `apiFetch<{ data: PendingInvitation[] }>('/api/households/invitations')` |
| `apps/web/src/lib/data-access/org/useRevokeInvitation.ts` | `/api/households/invitations/:id` | `apiFetch DELETE` | WIRED | `apiFetch('/api/households/invitations/${invitationId}', { method: 'DELETE' })` |
| `apps/web/src/lib/data-access/org/useInviteMember.ts` | `queryClient.invalidateQueries(['org-invitations'])` | `onSettled` | WIRED | Both `['org-members']` and `['org-invitations']` invalidated |
| `apps/web/src/components/settings/MembersSection.tsx` | `InvitedMemberRow.tsx` | `import { InvitedMemberRow }` | WIRED | Import present; rendered via `invitations.map(...)` |
| `apps/web/src/components/settings/MembersSection.tsx` | `MembersEmptyState.tsx` | `import { MembersEmptyState }` | WIRED | Import present; rendered when `isEmpty` |
| `apps/web/src/components/settings/MemberRow.tsx` | `ConfirmDialog.tsx` | `import { ConfirmDialog }` | WIRED | Import present; `<ConfirmDialog>` rendered for non-self members |
| `apps/web/src/components/settings/InvitedMemberRow.tsx` | `invitationUtils.ts` | `import { getExpiryInfo }` | WIRED | Import present; `getExpiryInfo(invitation.expiresAt, invitation.status)` called |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `MembersSection.tsx` | `invitations` | `useGetOrgInvitations()` → `apiFetch('/api/households/invitations')` → `listInvitations(orgId)` → Clerk REST API | Yes — Clerk REST returns real org invitations | FLOWING |
| `InvitedMemberRow.tsx` | `invitation` prop | Passed from `MembersSection` via `invitations.map(...)` | Yes — flows from Clerk API data | FLOWING |
| `MemberRow.tsx` | `member` prop | Passed from `MembersSection` via `members.map(...)` | Yes — flows from `useGetOrgMembers()` existing data path | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED — full stack requires running dev server + Clerk credentials. Human verification of 8 scenarios serves as the behavioral check (approved).

---

### Requirements Coverage

No explicit requirement IDs (REQ-*) are declared in any plan's frontmatter for this phase. Phase 03.4.6 implements the feature described in the ROADMAP "Delivers" bullets, all of which are verified above.

---

### Anti-Patterns Found

| File | Pattern | Severity | Impact |
|------|---------|----------|--------|
| `apps/api/src/services/households.ts` | `new Date(inv.created_at)` without x1000 | Info | Intentional fix: Clerk timestamps are ms, not seconds. Confirmed during human verification. Prior plan spec said multiply by 1000, which was wrong. |
| `apps/web/src/components/settings/MembersSection.tsx` | `isLoading = !membersQuery.data && !invitationsQuery.data` | Info | Intentional fix: gates skeleton on absent data (not `isLoading`) to prevent flash on stale-cache revalidation. Documented deviation from plan spec. |

Neither anti-pattern is a code smell — both are correct, documented deviations from the original plan spec that were discovered and fixed during human verification.

---

### Human Verification Required

Human verification was completed and approved prior to this verification run. All 8 scenarios from Plan 03 Task 7 passed:

1. Single skeleton block on cold load — no waterfall flash
2. Active members render unchanged
3. Pending invitations render as dimmed cards with email, "Invited X ago", expiry, "Invited" badge, trash icon
4. New invite appears within ~1 second without reload
5. Revoke flow: AlertDialog opens, card disappears on confirm
6. Expiry label colors: muted (normal), warning (≤2 days), destructive (expired status)
7. Empty state shows only when both members and invitations are empty
8. Network tab confirms parallel queries

---

### Gaps Summary

No gaps. All 14 must-haves verified. All artifacts exist, are substantive, are wired, and have real data flowing through them. Human verification (8/8 scenarios) was approved.

The two documented deviations from plan spec (timestamp unit correction, isLoading gate change) are correct improvements confirmed during human verification — not defects.

---

_Verified: 2026-05-05T00:00:00Z_
_Verifier: Claude (gsd-verifier)_
