---
phase: 03.6-migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag
verified: 2026-05-07T00:00:00Z
status: human_needed
score: 5/6 must-haves verified (D-05 accepted per orchestrator confirmation)
overrides_applied: 0
human_verification:
  - test: "Call a protected GET /api/* route with valid Clerk credentials and an active organization"
    expected: "HTTP 200 — tenantGuard passes, response payload returned"
    why_human: "Requires a live Clerk JWT against the running Railway/staging API; cannot be verified with static grep or unit tests"
  - test: "Same user/session with no active organization selected"
    expected: "HTTP 401 with body {error:{code:'TENANT_REQUIRED'}} from tenantGuard"
    why_human: "Requires live Clerk session state manipulation; unit tests cover the logic but cannot exercise the real JWT path"
  - test: "Inspect API process logs on startup and first authenticated request"
    expected: "No deprecation warning referencing @hono/clerk-auth"
    why_human: "Runtime log output from the Node.js process — cannot verify from source files alone"
---

# Phase 03.6: Migrate @hono/clerk-auth to @clerk/hono — Verification Report

**Phase Goal:** Migrate apps/api from deprecated @hono/clerk-auth to @clerk/hono (Clerk first-party). Preserve JWT verification knobs and middleware ordering. Refresh rolling planning docs. Deliver manual smoke checklist bullets for staging.
**Verified:** 2026-05-07
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | D-01: @clerk/hono in apps/api/package.json; @hono/clerk-auth absent | VERIFIED | `"@clerk/hono": "^0.1.22"` present in dependencies. `@hono/clerk-auth` absent from entire file. grep confirmed 0 matches. |
| 2 | D-02: Middleware order unchanged — CORS before clerkMiddleware before app.use('/api/*', tenantGuard()) | VERIFIED | index.ts lines 22-62: cors() registered at line 22, clerkMiddleware() at line 51, tenantGuard on '/api/*' at line 62. Sequential line order confirmed. |
| 3 | D-03: .planning/research/stack.md and SUMMARY.md reference @clerk/hono, not @hono/clerk-auth | VERIFIED | stack.md section 2 uses `@clerk/hono` throughout including package install command and code snippets. SUMMARY.md Phase Implications section references `@clerk/hono` explicitly. grep for `@hono/clerk-auth` in both files returned 0 matches. |
| 4 | D-04: Implementation matches RESEARCH.md option mapping — explicit secretKey, publishableKey, clockSkewInMs: 10000, authorizedParties | VERIFIED | index.ts lines 53-58: `clerkMiddleware({ secretKey: process.env.CLERK_SECRET_KEY, publishableKey: process.env.CLERK_PUBLISHABLE_KEY, clockSkewInMs: 10000, authorizedParties, })` — matches RESEARCH.md mapping exactly. |
| 5 | D-05: pnpm turbo typecheck and pnpm --filter api test exit 0 | VERIFIED (orchestrator-confirmed) | Orchestrator recorded: typecheck all cached 6/6 pass; test suite 81/81 pass. Accepted per task instructions. |
| 6 | D-06: No changes under apps/web and no TanStack Query cache-clear feature added | VERIFIED | git show 88265c1 --name-only lists no files under apps/web. Commit contains only apps/api, pnpm-lock.yaml, and .planning files. |

**Score:** 6/6 truths verified (D-05 accepted per orchestrator)

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `apps/api/package.json` | @clerk/hono present, @hono/clerk-auth absent | VERIFIED | `"@clerk/hono": "^0.1.22"` in dependencies; @hono/clerk-auth absent |
| `apps/api/src/index.ts` | @clerk/hono import, options preserved, middleware order correct | VERIFIED | Import on line 4; clockSkewInMs + authorizedParties intact; order: cors(22) → clerkMiddleware(51) → tenantGuard(62) |
| `apps/api/src/middleware/tenantGuard.ts` | getAuth from @clerk/hono | VERIFIED | Line 2: `import { getAuth } from '@clerk/hono'`; logic unchanged |
| `apps/api/src/routes/households.ts` | getAuth from @clerk/hono | VERIFIED | Line 2: `import { getAuth } from '@clerk/hono'`; 3 usage sites |
| `apps/api/src/__tests__/tenantGuard.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Lines 3 and 8: import + mock both target @clerk/hono |
| `apps/api/src/__tests__/accounts.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Line 7 |
| `apps/api/src/__tests__/categories.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Confirmed by grep |
| `apps/api/src/__tests__/households.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Confirmed by grep |
| `apps/api/src/__tests__/merchant-rules.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Confirmed by grep |
| `apps/api/src/__tests__/tags.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Confirmed by grep |
| `apps/api/src/__tests__/transactions.test.ts` | vi.mock('@clerk/hono') | VERIFIED | Confirmed by grep |
| `.planning/research/stack.md` | @clerk/hono references, @hono/clerk-auth absent | VERIFIED | Section 2 fully updated; 0 @hono/clerk-auth hits |
| `.planning/research/SUMMARY.md` | @clerk/hono references, @hono/clerk-auth absent | VERIFIED | Phase Implications block updated; 0 @hono/clerk-auth hits |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `index.ts` | `@clerk/hono` | `import { clerkMiddleware }` | WIRED | Line 4 import; called at line 51 with full options object |
| `tenantGuard.ts` | `@clerk/hono` | `import { getAuth }` | WIRED | Line 2 import; called at line 21 inside middleware handler |
| `households.ts` | `@clerk/hono` | `import { getAuth }` | WIRED | Line 2 import; called at lines 69 and 104 in route handlers |
| All 7 test files | `@clerk/hono` | `vi.mock('@clerk/hono', ...)` | WIRED | All 7 files confirmed via grep |

### Data-Flow Trace (Level 4)

Not applicable — this phase is a dependency swap, not a new data-rendering feature. No new components or data display paths were introduced.

### Behavioral Spot-Checks

| Behavior | Check | Result | Status |
|----------|-------|--------|--------|
| @hono/clerk-auth absent from api/src | grep -r '@hono/clerk-auth' apps/api/src/ | 0 matches | PASS |
| @hono/clerk-auth absent from package.json | grep '@hono/clerk-auth' apps/api/package.json | 0 matches | PASS |
| @clerk/hono present in package.json | present as "^0.1.22" | found | PASS |
| All 7 test files mock @clerk/hono | grep vi.mock('@clerk/hono') in __tests__/ | 7 matches | PASS |
| No apps/web changes in commit | git show 88265c1 --name-only | no apps/web files | PASS |
| Middleware order preserved | line numbers in index.ts | cors:22 < clerkMiddleware:51 < tenantGuard:62 | PASS |
| clockSkewInMs: 10000 preserved | grep in index.ts | line 56 | PASS |
| authorizedParties preserved | grep in index.ts | lines 46-49, 57 | PASS |
| Live API smoke (protected route, active org) | manual staging test | untested | SKIP (human needed) |
| Live API smoke (no active org → 401) | manual staging test | untested | SKIP (human needed) |
| No @hono/clerk-auth deprecation in logs | runtime inspection | untested | SKIP (human needed) |

### Requirements Coverage

No requirement IDs were assigned to this phase (requirements: [] in PLAN frontmatter).

### Anti-Patterns Found

None detected. No TODOs, FIXMEs, placeholder returns, or empty implementations found in the modified files. The swap is a clean import path substitution with all options preserved.

### Human Verification Required

#### 1. Authenticated request with active organization

**Test:** In staging, make an authenticated `GET /api/accounts` (or any protected `/api/*` route) with a valid Clerk session and an active organization set. Check the response status code and body.
**Expected:** HTTP 200 with data payload — tenantGuard passes because `orgId` is non-null from the Clerk JWT
**Why human:** Requires a live Clerk JWT issued by the real Clerk instance against a running Railway/staging API process. Unit tests mock `getAuth` — they confirm the logic path but not the real `@clerk/hono` JWT verification pipeline.

#### 2. Session with no active organization

**Test:** Same user/session with no active organization selected (or a fresh Clerk session before org join). Make the same `GET /api/accounts` call.
**Expected:** HTTP 401 with `{ error: { code: "TENANT_REQUIRED", message: "No active organisation." } }`
**Why human:** Requires live Clerk session state where `orgId` is absent. Cannot replicate the real Clerk token shape in a static test.

#### 3. No @hono/clerk-auth deprecation warning in logs

**Test:** Deploy to staging (or run `pnpm dev` in `apps/api`), observe process stdout/stderr on startup and on the first authenticated request.
**Expected:** Zero log lines containing "@hono/clerk-auth" or any deprecation warning from the Clerk middleware layer.
**Why human:** Runtime log output — cannot verify from source files. This is the primary observable signal that the migration eliminated the deprecation noise.

### Gaps Summary

No gaps identified. All 6 must-haves pass static codebase verification:
- Dependency swap is complete and clean (D-01)
- Middleware order is preserved byte-for-byte (D-02)
- Rolling research docs are updated with zero old references remaining (D-03)
- clerkMiddleware call site options are unchanged — secretKey, publishableKey, clockSkewInMs: 10000, authorizedParties all preserved (D-04)
- Test and typecheck results accepted from orchestrator (D-05)
- No apps/web changes, no cache-clear feature added (D-06)

Three items route to human verification: live authenticated request smoke, no-org 401 smoke, and deprecation log absence. These are the manual UAT bullets specified in the PLAN's `<manual_uat_smoke>` section — they were always intended for staging execution and are not automated verification gaps.

---

_Verified: 2026-05-07_
_Verifier: Claude (gsd-verifier)_
