# Phase 03.6: migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag - Context

**Gathered:** 2026-05-04
**Status:** Ready for planning

<domain>
## Phase Boundary

Replace deprecated `@hono/clerk-auth` with first-party `@clerk/hono` across **apps/api** so JWT verification and `clerkMiddleware` / `getAuth` behavior stay equivalent. Eliminate runtime deprecation warnings. No new endpoints, no schema changes, no **apps/web** work in this phase.

**Out of scope:** TanStack Query cache clear on sign-out / user switch (explicitly deferred until after 03.6 per `.planning/ROADMAP.md`).

</domain>

<decisions>
## Implementation Decisions

### Package and code changes
- **D-01:** Add `@clerk/hono` to `apps/api/package.json`, remove `@hono/clerk-auth`, run `pnpm install`, and update **every** import and `vi.mock` path from `@hono/clerk-auth` to `@clerk/hono` in `apps/api/src/**` (production + tests). Update `apps/api/.env.example` comment to name `@clerk/hono` instead of `@hono/clerk-auth`.
- **D-02:** Preserve middleware order and behavior from `apps/api/src/index.ts`: **CORS → `clerkMiddleware` → `tenantGuard` on `/api/*`**. Preserve the current option **names** (`secretKey`, `publishableKey`, `clockSkewInMs: 10000`, `authorizedParties` as `string[]`) unless `03.6-RESEARCH.md` proves an official rename — if Clerk renamed an option, document the mapping in RESEARCH and apply it in code.

### Documentation (same PR as code)
- **D-03:** Update rolling planning docs that describe the **current** stack (not archived phase artifacts): at minimum `.planning/research/stack.md` and `.planning/research/SUMMARY.md` so all reader-facing references say `@clerk/hono` and point to Clerk’s Hono integration (remove/stale the `honojs/middleware` clerk-auth narrative). Optional: adjust `.planning/STATE.md` bullets that name `@hono/clerk-auth` if they read as “current truth” rather than historical.

### Research gate
- **D-04:** Phase **RESEARCH** (`03.6-RESEARCH.md`) MUST cite current **official** `@clerk/hono` documentation for `clerkMiddleware` (or equivalent export) and list each middleware option the app passes, confirming types and semantics match our current `index.ts` configuration. Note whether `authorizedParties` remains `string[]`-only (historical constraint from Phase 01 still assumed until disproven).

### Verification
- **D-05:** **Automated:** `pnpm --filter apps/api test` and `pnpm turbo typecheck` (or project-standard equivalents) must pass; CI must remain green. **Manual:** Add a short smoke checklist to **PLAN** / **UAT** (3 bullets max): e.g. authenticated request with active org succeeds on a protected route; same user with **no** active org gets **401** from tenant guard; confirm deprecation warning no longer appears in API logs on startup/request.

### Explicitly deferred
- **D-06:** Do **not** implement “clear TanStack Query cache on logout” in 03.6. Track `.planning/todos/pending/2026-04-21-clear-query-cache-on-logout-prevent-stale-data-cross-account.md` and ROADMAP **Post-phase todo** for a follow-up after 03.6 ships.

### Claude's Discretion
- Exact semver range for `@clerk/hono` and alignment with existing `@clerk/backend` / Clerk JS majors in the monorepo.
- Whether additional non-rolling `.planning/` files get touched if grep finds misleading “current install” statements (default: touch only `stack.md` + `SUMMARY.md` unless a file is clearly the canonical stack doc).

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Phase intent and backlog
- `.planning/ROADMAP.md` — Phase **03.6** (goal, dependency on 03.5, post-phase logout-cache note)
- `.planning/todos/completed/2026-04-21-migrate-hono-clerk-auth-to-clerk-hono.md` — prior write-up of files and deprecation warning

### API implementation (must match after migration)
- `apps/api/src/index.ts` — `clerkMiddleware` options, CORS, route mounting, `onError`
- `apps/api/src/middleware/tenantGuard.ts` — `getAuth(c)` usage, org upsert / `seenOrgs`
- `apps/api/src/routes/households.ts` — remaining `getAuth` import site (if still present after refactors)
- `apps/api/package.json` — dependency swap
- `apps/api/src/__tests__/*.test.ts` — `vi.mock('@clerk/hono', …)` must match new module path

### Prior architectural locks (behavior must not regress)
- `.planning/phases/03.3.1-api-layering-and-structural-refactor-inserted/03.3.1-CONTEXT.md` — `tenantGuard` sets `c.set('orgId', …)`; middleware order

### External (required for RESEARCH step)
- Official Clerk documentation for **Hono** integration (`@clerk/hono`) — URLs to be recorded in `03.6-RESEARCH.md` with option-by-option notes (no substitute for formal RESEARCH per D-04).

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable assets
- `isAllowedParty` + static `authorizedParties: string[]` in `apps/api/src/index.ts` — keep pattern; only the middleware **package** changes unless RESEARCH mandates option shape changes.

### Established patterns
- All route tests mock the Clerk module to stub `getAuth`; mocks must target `@clerk/hono` after the swap or interception will fail.
- `tenantGuard` uses `!orgId` for unauthorized-no-org behavior — unchanged.

### Integration points
- Single mount of `clerkMiddleware` on `*` in `index.ts`; webhook and health routes remain outside `tenantGuard`.

</code_context>

<specifics>
## Specific Ideas

- Historical phase plans under `.planning/phases/**` may still mention `@hono/clerk-auth` as archival text; **D-03** targets rolling docs (`stack.md`, `SUMMARY.md`), not a repo-wide rewrite of old plans.

</specifics>

<deferred>
## Deferred Ideas

- **Query cache on logout / account switch** — after 03.6 per roadmap; see `.planning/todos/pending/2026-04-21-clear-query-cache-on-logout-prevent-stale-data-cross-account.md`.

</deferred>

---

*Phase: 03.6-migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag*
*Context gathered: 2026-05-04*
