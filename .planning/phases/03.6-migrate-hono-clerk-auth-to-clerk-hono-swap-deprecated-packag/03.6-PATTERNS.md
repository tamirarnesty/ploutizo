# Phase 03.6 — Pattern map

Analogs inside the codebase for executor orientation (no structural change — package swap only).

| Target (after migrate) | Role | Existing analog |
|------------------------|------|----------------|
| `apps/api/src/index.ts` | Clerk middleware registration + options | Same file — only import path changes; `isAllowedParty` + static `authorizedParties` pattern stays |
| `apps/api/src/middleware/tenantGuard.ts` | `getAuth(c)` consumer | Phase 03.3.1 layering — middleware order invariant in CONTEXT |
| `apps/api/src/routes/households.ts` | `getAuth(c)` consumer | Mirrors other route handlers importing auth from same package |
| `apps/api/src/__tests__/*.test.ts` | `vi.mock` auth module | Matches `tenantGuard.test.ts` pattern (`getAuth`, optional `clerkMiddleware` shim) |

**Data flow (unchanged):** Request → CORS → `clerkMiddleware` → (routes) `/api/*` → `tenantGuard` → handlers using `getAuth(c)`.

## PATTERN MAPPING COMPLETE
