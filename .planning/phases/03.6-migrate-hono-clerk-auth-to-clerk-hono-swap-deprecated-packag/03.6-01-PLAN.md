---
plan: 03.6-01
wave: 1
title: "Swap @hono/clerk-auth for @clerk/hono across apps/api"
objective: "Replace deprecated `@hono/clerk-auth` with Clerk first-party `@clerk/hono` for `clerkMiddleware` and `getAuth`, preserving middleware order/options and updating tests and rolling research docs. No schema or apps/web changes."
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/src/index.ts
  - apps/api/src/middleware/tenantGuard.ts
  - apps/api/src/routes/households.ts
  - apps/api/src/__tests__/accounts.test.ts
  - apps/api/src/__tests__/categories.test.ts
  - apps/api/src/__tests__/households.test.ts
  - apps/api/src/__tests__/merchant-rules.test.ts
  - apps/api/src/__tests__/tags.test.ts
  - apps/api/src/__tests__/tenantGuard.test.ts
  - apps/api/src/__tests__/transactions.test.ts
  - apps/api/.env.example
  - .planning/research/stack.md
  - .planning/research/SUMMARY.md
depends_on: []
autonomous: true
requirements: []
threat_model:
  assets:
    - "JWT session tokens verified by Clerk middleware"
    - "CLERK_SECRET_KEY / CLERK_PUBLISHABLE_KEY handling in apps/api"
  threats:
    - id: T-03.6-01
      category: Spoofing
      component: "authorizedParties JWT azp verification"
      disposition: mitigate
      rationale: "`authorizedParties: string[]` + `isAllowedParty` layer remain unchanged — continue allowlisting production and localhost origins per RESEARCH alignment with Clerk middleware options doc."
      mitigations:
        - "Preserve exact `authorizedParties` array literals and `isAllowedParty` regex in apps/api/src/index.ts during import swap."
    - id: T-03.6-02
      category: Tampering
      component: "Import path / mock drift in Vitest"
      disposition: mitigate
      rationale: "Wrong module mock leaves tests passing while prod imports break or vice versa."
      mitigations:
        - "Grep confirms zero `@hono/clerk-auth` references under apps/api/src after edits."
        - "`pnpm --filter apps/api test` must exit 0."
    - id: T-03.6-03
      category: Elevation_of_privilege
      component: "tenantGuard organization context"
      disposition: mitigate
      rationale: "`getAuth(c)` must still supply `orgId` semantics used by tenantGuard."
      mitigations:
        - "Preserve `tenantGuard.ts` authorization logic exactly — only change import origin."
        - "`tenantGuard.test.ts` exits green."
  residual_risk: low
must_haves:
  - "CONTEXT decision D-01 — `@clerk/hono` in apps/api/package.json dependencies; `@hono/clerk-auth` absent."
  - "CONTEXT decision D-02 — Middleware order unchanged: CORS middleware block before `clerkMiddleware` before `app.use('/api/*', tenantGuard())`."
  - "CONTEXT decision D-03 — `.planning/research/stack.md` and `.planning/research/SUMMARY.md` reference `@clerk/hono`, not `@hono/clerk-auth`, for middleware guidance."
  - "CONTEXT decision D-04 — Implementation matches `03.6-RESEARCH.md` option mapping (explicit `secretKey`, `publishableKey`, `clockSkewInMs: 10000`, `authorizedParties`)."
  - "CONTEXT decision D-05 — `pnpm turbo typecheck` and `pnpm --filter apps/api test` exit 0."
  - "CONTEXT decision D-06 — No changes under `apps/web` and no TanStack Query cache-clear feature added."
---

<objective>
Migrate `apps/api` from `@hono/clerk-auth` to **`@clerk/hono`** (Clerk-maintained SDK). Preserve JWT verification knobs and middleware ordering documented in CONTEXT and `03.6-RESEARCH.md`. Refresh rolling planning docs (`stack.md`, `SUMMARY.md`). Deliver manual smoke checklist bullets for staging.

Purpose: Eliminate deprecation warnings tied to `@hono/clerk-auth` and adopt supported Clerk+Hono middleware.

Output: Updated dependencies, unified `@clerk/hono` imports/mocks throughout `apps/api/src/**`, refreshed research docs.

</objective>

<execution_context>
@/Users/tamirarnesty/Developer/ploutizo/.cursor/get-shit-done/workflows/execute-plan.md
@/Users/tamirarnesty/Developer/ploutizo/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.6-migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag/03.6-CONTEXT.md
@.planning/phases/03.6-migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag/03.6-RESEARCH.md
</context>

<manual_uat_smoke>
1. Call a protected `GET /api/...` route with valid Clerk credentials and an **active organization** selected — response is **not** HTTP 401 from tenantGuard.
2. Same user/session with **no** active organization — tenantGuard responds **401** on `/api/*` routes.
3. API process logs contain **no** deprecation warning referencing `@hono/clerk-auth` on startup / first authenticated request pattern used in QA.
</manual_uat_smoke>

<tasks>

<task type="execute">
  <name>Task 1: Dependencies — add @clerk/hono and remove @hono/clerk-auth</name>
  <files>apps/api/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json — current `dependencies` and `devDependencies` blocks (note `@clerk/backend` existing version)
    - .planning/phases/03.6-migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag/03.6-RESEARCH.md — versioning notes
  </read_first>
  <action>
From the repo root, run **`pnpm add @clerk/hono@latest --filter apps/api`** then **`pnpm remove @hono/clerk-auth --filter apps/api`** (pnpm filter syntax as used elsewhere in the monorepo — equivalent: edit `apps/api/package.json` to add `"@clerk/hono": "^0.1.22"` or the latest matching caret resolved by install, delete the `"@hono/clerk-auth"` line entirely, then `pnpm install` at root).

The resulting `apps/api/package.json` `dependencies` must include **`@clerk/hono`** and must **not** include **`@hono/clerk-auth`**.

Do **not** add `@hono/clerk-auth` elsewhere in the workspace for this phase.
  </action>
  <acceptance_criteria>
    - Shell: `grep -q '"@clerk/hono"' apps/api/package.json` exits **0**.
    - Shell: `grep '@hono/clerk-auth' apps/api/package.json` exits **non-zero**.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Production imports — index.ts, tenantGuard.ts, households.ts</name>
  <files>apps/api/src/index.ts, apps/api/src/middleware/tenantGuard.ts, apps/api/src/routes/households.ts</files>
  <read_first>
    - apps/api/src/index.ts — full file for clerkMiddleware option object and middleware order comments
    - apps/api/src/middleware/tenantGuard.ts
    - apps/api/src/routes/households.ts
  </read_first>
  <action>
Replace every import from **`@hono/clerk-auth`** with **`@clerk/hono`**:

- `index.ts`: `import { clerkMiddleware } from '@clerk/hono'`
- `tenantGuard.ts` and `households.ts`: `import { getAuth } from '@clerk/hono'`

Keep the **`clerkMiddleware({ secretKey: process.env.CLERK_SECRET_KEY, publishableKey: process.env.CLERK_PUBLISHABLE_KEY, clockSkewInMs: 10000, authorizedParties })`** call **byte-for-byte** except update **comments** that name `@hono/clerk-auth` to say **`@clerk/hono`** where they describe middleware behavior (especially the `authorizedParties` note near `isAllowedParty`).

Do **not** reorder middleware: keep `cors` block first, `clerkMiddleware` second on `'*'`, then `tenantGuard` on **`/api/*`** only — exactly as existing `index.ts`.

Do **not** change `tenantGuard` logic beyond the import source.
  </action>
  <acceptance_criteria>
    - `rg 'from '\''@hono/clerk-auth' apps/api/src` returns **empty** stdout (exit **1** is OK for ripgrep when no matches).
    - `rg "from '@clerk/hono'" apps/api/src/index.ts apps/api/src/middleware/tenantGuard.ts apps/api/src/routes/households.ts` shows **exactly three** matching import lines (one `clerkMiddleware`, two `getAuth`).
    - File `apps/api/src/index.ts` still contains substring **`clockSkewInMs: 10000`** and **`authorizedParties,`** passed into `clerkMiddleware(` on adjacent lines inside the option object (same literals as before the edit).
    - File `apps/api/src/index.ts` still registers middleware in sequence: substring **`cors(`** occurs **before** **`clerkMiddleware(`** occurs **before** **`app.use('/api/*'`** (order check via sequential line numbers acceptable if both blocks present in file top-to-bottom in that order).
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Vitest mocks — rewrite @hono/clerk-auth mocks to @clerk/hono</name>
  <files>apps/api/src/__tests__/*.test.ts</files>
  <read_first>
    - apps/api/src/__tests__/tenantGuard.test.ts — full hoisted mocks pattern
    - apps/api/src/__tests__/accounts.test.ts — shortest mock barrel for route tests
    - apps/api/package.json — ensure Task 1 already applied `@clerk/hono`
  </read_first>
  <action>
In **every** file under **`apps/api/src/__tests__/`** that references **`@hono/clerk-auth`**:

- Replace `import { … } from '@hono/clerk-auth'` with the same identifiers from **`'@clerk/hono'`** (maintain **`getAuth`**, **`clerkMiddleware`** where used).
- Replace **`vi.mock('@hono/clerk-auth',`** with **`vi.mock('@clerk/hono',`**.

Preserve mock **factory behavior** (`getAuth: vi.fn(...)`, **`clerkMiddleware` no-op shim** in `tenantGuard.test.ts`) — only module string and import paths change.

Files known to contain references today (confirm with grep — do **not** skip any leftover): `accounts.test.ts`, `categories.test.ts`, `households.test.ts`, `merchant-rules.test.ts`, `tags.test.ts`, `transactions.test.ts`, `tenantGuard.test.ts`.
  </action>
  <acceptance_criteria>
    - `rg '@hono/clerk-auth' apps/api/src/__tests__` returns **zero** matches.
    - **`apps/api/src/__tests__/`** lists **≥1** file containing **`vi.mock('@clerk/hono'`** (`rg \"vi\\.mock\\('@clerk/hono'\" apps/api/src/__tests__/ -l`).
    - Shell: `pnpm --filter apps/api test` exits **0**.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Environment example comment</name>
  <files>apps/api/.env.example</files>
  <read_first>
    - apps/api/.env.example
  </read_first>
  <action>
Replace the Clerk comment line that cites **`@hono/clerk-auth`** with wording that cites **`@clerk/hono`** (keys **CLERK_SECRET_KEY** / **CLERK_PUBLISHABLE_KEY** unchanged).
  </action>
  <acceptance_criteria>
    - `grep '@clerk/hono' apps/api/.env.example` exits **0**.
    - `grep '@hono/clerk-auth' apps/api/.env.example` exits **non-zero**.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 5: Rolling research docs — stack.md & SUMMARY.md</name>
  <files>.planning/research/stack.md, .planning/research/SUMMARY.md</files>
  <read_first>
    - .planning/research/stack.md — Clerk / Hono section (~lines referencing `@hono/clerk-auth`)
    - .planning/research/SUMMARY.md — bullet list occurrences
  </read_first>
  <action>
Update reader-facing Clerk+Hono narrative to **`@clerk/hono`** and **`https://www.npmjs.com/package/@clerk/hono`** (or Clerk docs URL cited in RESEARCH):

- **`stack.md`:** Replace prose that `@hono/clerk-auth` is the official Clerk path with **`@clerk/hono`** (Clerk first-party). Replace `pnpm add @hono/clerk-auth` sample with **`pnpm add @clerk/hono`** (scoped to **`apps/api`** if the doc shows filtered install command). Replace code snippet import line with `import { clerkMiddleware, getAuth } from '@clerk/hono'`.
- **`SUMMARY.md`:** Replace every **`@hono/clerk-auth`** mention in middleware context with **`@clerk/hono`**.

Do **not** rewrite archived `.planning/phases/**` plans (out of CONTEXT scope except these two rolling files).

After edits, **`rg '@hono/clerk-auth' .planning/research/stack.md .planning/research/SUMMARY.md`** must return **zero** hits.
  </action>
  <acceptance_criteria>
    - `rg '@hono/clerk-auth' .planning/research/stack.md .planning/research/SUMMARY.md 2>/dev/null` produces **zero** matching lines (exit status **1** OK).
    - File `.planning/research/stack.md` contains substring **`@clerk/hono`** at least once.
    - File `.planning/research/SUMMARY.md` contains substring **`@clerk/hono`** at least once.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 6: Monorepo typecheck sweep</name>
  <files>(none — command only)</files>
  <read_first>
    - CLAUDE.md — project standard commands
  </read_first>
  <action>
From repo root run **`pnpm turbo typecheck`**. Resolve any TypeScript breakages purely from Clerk type surface changes (prefer adjusting call sites minimally — middleware options already match research).

Fix **within `apps/api` only** for this phase unless Turbo reports breakage in workspace packages importing API types (unexpected — document if encountered).
  </action>
  <acceptance_criteria>
    - Shell: `pnpm turbo typecheck` from repo root exits **0**.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 7: Final grep sentinel under apps/api</name>
  <files>(verification)</files>
  <read_first>
    - `.planning/phases/03.6-migrate-hono-clerk-auth-to-clerk-hono-swap-deprecated-packag/03.6-CONTEXT.md` — scope boundaries
  </read_first>
  <action>
Run **`rg '@hono/clerk-auth' apps/api`** — must return **no** hits.

If stray hits occur in **`node_modules`** (should be absent from ripgrep default if gitignored — if not ignored, restrict path to **`apps/api/src apps/api/package.json apps/api/.env.example`** explicitly).
  </action>
  <acceptance_criteria>
    - `rg '@hono/clerk-auth' apps/api/src apps/api/package.json apps/api/.env.example` exits **1** with **zero** matches (exit **1** = no matches in ripgrep).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Bearer/session tokens verified by Clerk; `authorizedParties` limits azp |
| Repo → CI | Source-controlled mocks and middleware config reviewed on PR |

## STRIDE register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|----------------|
| T-03.6-01 | Spoofing | JWT `authorizedParties` / `azp` | mitigate | Preserve `authorizedParties` + `clockSkewInMs` literals from `03.6-RESEARCH.md` alignment |
| T-03.6-02 | Tampering | Test mock drift vs production import | mitigate | Repo-wide grep + Vitest suite |
| T-03.6-03 | Elevation | Missing org isolation | mitigate | Untouched `tenantGuard` logic besides import + green `tenantGuard.test.ts` |

## Residual risk

**Low.** No new routes; dependency surface moves to Clerk-supported package with equivalent `authenticateRequest` wiring per upstream `clerkMiddleware` implementation.

</threat_model>

<verification>

- **Automated:** `pnpm turbo typecheck` → exit 0; `pnpm --filter apps/api test` → exit 0.
- **Grep sentinel:** `@hono/clerk-auth` absent under `apps/api/src`, `apps/api/package.json`, `apps/api/.env.example`.
- **Manual smoke:** Execute `<manual_uat_smoke>` in staging against real Clerk and record pass/fail in execute-phase VERIFY.md / UAT.

</verification>
