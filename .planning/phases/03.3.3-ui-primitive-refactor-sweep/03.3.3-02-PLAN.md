---
phase: 03.3.3
plan: 02
type: execute
wave: 2
depends_on:
  - 03.3.3-01-PLAN.md
files_modified:
  - apps/web/src/components/settings/HouseholdOverviewSection.tsx
  - apps/web/src/components/settings/MembersSection.tsx
  - apps/web/src/components/settings/CategoriesSettings.tsx
  - apps/web/src/components/settings/MerchantRulesSettings.tsx
  - apps/web/src/components/dashboard/Dashboard.tsx
  - apps/web/src/components/transactions/Transactions.tsx
  - apps/web/src/components/accounts/Accounts.tsx
  - apps/web/src/components/onboarding/Onboarding.tsx
  - apps/web/src/routes/_layout.settings/route.tsx
autonomous: true
requirements:
  - D-03
  - D-04
  - D-05

must_haves:
  truths:
    - "No raw <h1>–<h6> elements remain in the 9 listed files (except the dev scaffold in index.tsx which is out of scope per D-04)"
    - "All headings that had font-heading text-xl font-semibold now use variant=\"h3\""
    - "All files that changed import Text from @ploutizo/ui/components/text"
    - "pnpm --filter @ploutizo/web tsc --noEmit exits 0"
  artifacts:
    - path: "apps/web/src/components/settings/HouseholdOverviewSection.tsx"
      provides: "Household heading via Text"
      contains: "<Text as=\"h2\" variant=\"h3\">"
    - path: "apps/web/src/components/settings/CategoriesSettings.tsx"
      provides: "Categories/Tags headings via Text"
      contains: "<Text as=\"h1\" variant=\"h3\">"
    - path: "apps/web/src/routes/_layout.settings/route.tsx"
      provides: "Settings page heading via Text"
      contains: "<Text as=\"h1\" variant=\"h3\">"
  key_links:
    - from: "all 9 files"
      to: "@ploutizo/ui/components/text"
      via: "named import Text"
      pattern: "import.*Text.*from.*@ploutizo/ui/components/text"
---

<objective>
Migrate all raw heading elements (`<h1>`–`<h6>`) in 9 apps/web component files to `<Text as="hN" variant="...">`. This is a pure markup refactor — no logic, no style, no layout changes.

Purpose: Establish the Text component as the single heading primitive across the app.
Output: 9 modified files with `<h1>`–`<h6>` replaced by `<Text>` equivalents.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/PROJECT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-CONTEXT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-RESEARCH.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-PATTERNS.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-01-SUMMARY.md

<interfaces>
<!-- Text component API (from Plan 01 output) -->
```tsx
import { Text } from "@ploutizo/ui/components/text"
// Props: as (h1–h6 | p | span | div | label), variant (h1|h2|h3|body|body-sm|caption|label|error), className
// h3 variant = "font-heading text-xl font-semibold leading-snug"
// h1 variant = "font-heading text-3xl font-semibold tracking-tight leading-tight"
// label variant = "text-sm font-medium leading-none"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate headings in settings components (5 files)</name>
  <files>
    apps/web/src/components/settings/HouseholdOverviewSection.tsx
    apps/web/src/components/settings/MembersSection.tsx
    apps/web/src/components/settings/CategoriesSettings.tsx
    apps/web/src/components/settings/MerchantRulesSettings.tsx
    apps/web/src/routes/_layout.settings/route.tsx
  </files>
  <read_first>
    - apps/web/src/components/settings/HouseholdOverviewSection.tsx
    - apps/web/src/components/settings/MembersSection.tsx
    - apps/web/src/components/settings/CategoriesSettings.tsx
    - apps/web/src/components/settings/MerchantRulesSettings.tsx
    - apps/web/src/routes/_layout.settings/route.tsx
  </read_first>
  <action>
    For each file: (1) add the Text import if not already present, (2) apply the heading replacements listed below.

    **Import to add to each file (or append Text to existing @ploutizo/ui imports):**
    ```tsx
    import { Text } from "@ploutizo/ui/components/text"
    ```

    **HouseholdOverviewSection.tsx — line 10:**
    ```tsx
    // Before
    <h2 className="font-heading text-xl font-semibold">Household</h2>
    // After
    <Text as="h2" variant="h3">Household</Text>
    ```
    (The span on line 21 is handled in Plan 04 — do NOT touch it here.)

    **MembersSection.tsx — line 16:**
    ```tsx
    // Before
    <h2 className="font-heading text-xl font-semibold">Members</h2>
    // After
    <Text as="h2" variant="h3">Members</Text>
    ```
    (The paragraph on lines 25–27 is handled in Plan 03 — do NOT touch it here.)

    **CategoriesSettings.tsx — 3 headings:**
    ```tsx
    // Line 91 — page heading
    // Before: <h1 className="font-heading text-xl font-semibold">Categories & Tags</h1>
    // After:
    <Text as="h1" variant="h3">Categories & Tags</Text>

    // Line 98 — sub-section heading (no font-heading, text-sm)
    // Before: <h2 className="text-sm font-semibold">Categories</h2>
    // After:
    <Text as="h2" variant="label" className="font-semibold">Categories</Text>

    // Line 199 — sub-section heading (no font-heading, text-sm)
    // Before: <h2 className="text-sm font-semibold">Tags</h2>
    // After:
    <Text as="h2" variant="label" className="font-semibold">Tags</Text>
    ```

    **MerchantRulesSettings.tsx — line 33:**
    ```tsx
    // Before: <h1 className="font-heading text-xl font-semibold">Merchant Rules</h1>
    // After:
    <Text as="h1" variant="h3">Merchant Rules</Text>
    ```

    **route.tsx (_layout.settings) — line 40 (D-05):**
    ```tsx
    // Before: <h1 className="text-xl font-semibold">Settings</h1>
    // After (h3 variant adds font-heading — intentional per D-05):
    <Text as="h1" variant="h3">Settings</Text>
    ```
  </action>
  <verify>
    <automated>grep -rn '<h[1-6]' /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/settings/ /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/routes/_layout.settings/route.tsx 2>/dev/null || echo "No raw headings found"</automated>
  </verify>
  <acceptance_criteria>
    - HouseholdOverviewSection.tsx contains `<Text as="h2" variant="h3">` and does NOT contain `<h2`
    - MembersSection.tsx contains `<Text as="h2" variant="h3">` and does NOT contain `<h2`
    - CategoriesSettings.tsx contains `<Text as="h1" variant="h3">` and `<Text as="h2" variant="label" className="font-semibold">` (twice)
    - CategoriesSettings.tsx does NOT contain `<h1` or `<h2`
    - MerchantRulesSettings.tsx contains `<Text as="h1" variant="h3">` and does NOT contain `<h1`
    - route.tsx contains `<Text as="h1" variant="h3">` and does NOT contain `<h1`
    - All 5 files import Text from "@ploutizo/ui/components/text"
  </acceptance_criteria>
  <done>5 settings/route files have zero raw heading elements. All headings use Text with the correct variant.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate headings in dashboard, transactions, accounts, onboarding (4 files)</name>
  <files>
    apps/web/src/components/dashboard/Dashboard.tsx
    apps/web/src/components/transactions/Transactions.tsx
    apps/web/src/components/accounts/Accounts.tsx
    apps/web/src/components/onboarding/Onboarding.tsx
  </files>
  <read_first>
    - apps/web/src/components/dashboard/Dashboard.tsx
    - apps/web/src/components/transactions/Transactions.tsx
    - apps/web/src/components/accounts/Accounts.tsx
    - apps/web/src/components/onboarding/Onboarding.tsx
  </read_first>
  <action>
    For each file: add the Text import, apply heading replacements.

    **Import to add to each file:**
    ```tsx
    import { Text } from "@ploutizo/ui/components/text"
    ```

    **Dashboard.tsx — line 4:**
    ```tsx
    // Before: <h1 className="mb-4 font-heading text-xl font-semibold">Dashboard</h1>
    // After (preserve mb-4 layout class via className):
    <Text as="h1" variant="h3" className="mb-4">Dashboard</Text>
    ```
    (The paragraph on lines 7–9 is handled in Plan 03 — do NOT touch it here.)

    **Transactions.tsx — line 223 (approximate):**
    ```tsx
    // Before: <h1 className="min-w-0 truncate font-heading text-xl font-semibold">Transactions</h1>
    // After (preserve layout classes):
    <Text as="h1" variant="h3" className="min-w-0 truncate">Transactions</Text>
    ```

    **Accounts.tsx — line 29 (approximate):**
    ```tsx
    // Before: <h1 className="min-w-0 truncate font-heading text-xl font-semibold">Accounts</h1>
    // After (preserve layout classes):
    <Text as="h1" variant="h3" className="min-w-0 truncate">Accounts</Text>
    ```

    **Onboarding.tsx — line 8:**
    ```tsx
    // Before: <h1 className="font-heading text-3xl font-semibold">Welcome to Ploutizo</h1>
    // After (maps to h1 variant which is text-3xl + font-heading + font-semibold):
    <Text as="h1" variant="h1">Welcome to Ploutizo</Text>
    ```
    (The paragraph on lines 11–13 is handled in Plan 03 — do NOT touch it here.)
  </action>
  <verify>
    <automated>grep -rn '<h[1-6]' /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/dashboard/ /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/Transactions.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/accounts/Accounts.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/onboarding/ 2>/dev/null || echo "No raw headings found"</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx contains `<Text as="h1" variant="h3" className="mb-4">` and does NOT contain `<h1`
    - Transactions.tsx contains `<Text as="h1" variant="h3" className="min-w-0 truncate">` and does NOT contain `<h1`
    - Accounts.tsx contains `<Text as="h1" variant="h3" className="min-w-0 truncate">` and does NOT contain `<h1`
    - Onboarding.tsx contains `<Text as="h1" variant="h1">` and does NOT contain `<h1`
    - All 4 files import Text from "@ploutizo/ui/components/text"
    - `pnpm --filter @ploutizo/web tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>4 page-level component files have zero raw heading elements. Onboarding uses h1 variant (text-3xl). All others use h3 variant with layout className overrides where needed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | Pure markup refactor — no auth, data, or input changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.3.3-03 | Tampering | Heading markup | accept | No semantic change — only the HTML element wrapper changes from raw h1/h2 to Text component rendering the same element |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn '<h[1-6]' apps/web/src/components/settings/ apps/web/src/components/dashboard/ apps/web/src/components/transactions/Transactions.tsx apps/web/src/components/accounts/Accounts.tsx apps/web/src/components/onboarding/ apps/web/src/routes/_layout.settings/route.tsx` — should return nothing (or only false positives inside strings/comments)
2. `pnpm --filter @ploutizo/web tsc --noEmit` exits 0
</verification>

<success_criteria>
- All 9 files have zero raw `<h1>`–`<h6>` elements in JSX
- All use `<Text as="hN" variant="...">` with correct variant mapping
- Layout-only classes (mb-4, min-w-0, truncate) preserved via className prop
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-02-SUMMARY.md`
</output>
