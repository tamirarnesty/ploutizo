---
phase: 03.3.3
plan: 03
type: execute
wave: 2
depends_on:
  - 03.3.3-01-PLAN.md
files_modified:
  - apps/web/src/components/settings/HouseholdSettingsForm.tsx
  - apps/web/src/components/settings/MembersSection.tsx
  - apps/web/src/components/settings/HouseholdSettings.tsx
  - apps/web/src/components/settings/CategoryForm.tsx
  - apps/web/src/components/settings/InviteMemberForm.tsx
  - apps/web/src/components/settings/CategoriesSettings.tsx
  - apps/web/src/components/settings/MerchantRulesSettings.tsx
  - apps/web/src/components/settings/RuleForm.tsx
  - apps/web/src/components/settings/MerchantRuleRow.tsx
  - apps/web/src/components/dashboard/Dashboard.tsx
  - apps/web/src/components/transactions/TransactionTableEmpty.tsx
  - apps/web/src/components/transactions/TransactionTableEmptyFiltered.tsx
  - apps/web/src/components/accounts/AccountForm.tsx
  - apps/web/src/components/accounts/AccountsTable.tsx
  - apps/web/src/components/onboarding/Onboarding.tsx
  - apps/web/src/components/categories/LucideIconPicker.tsx
autonomous: true
requirements:
  - D-06
  - D-07
  - D-09

must_haves:
  truths:
    - "No raw <p> elements remain in the 16 listed files"
    - "Error paragraphs (text-xs text-destructive) use variant=\"error\""
    - "Muted body paragraphs (text-sm text-muted-foreground) use variant=\"body-sm\" className=\"text-muted-foreground\""
    - "Caption paragraphs (text-xs text-muted-foreground) use variant=\"caption\""
    - "pnpm --filter @ploutizo/web tsc --noEmit exits 0"
  artifacts:
    - path: "apps/web/src/components/settings/HouseholdSettingsForm.tsx"
      provides: "Error text via Text variant=error"
      contains: "variant=\"error\""
    - path: "apps/web/src/components/settings/CategoryForm.tsx"
      provides: "Error text via Text variant=error"
      contains: "variant=\"error\""
    - path: "apps/web/src/components/settings/MerchantRuleRow.tsx"
      provides: "Mixed paragraph styles via Text"
      contains: "variant=\"body-sm\""
  key_links:
    - from: "all 16 files"
      to: "@ploutizo/ui/components/text"
      via: "named import Text"
      pattern: "import.*Text.*from.*@ploutizo/ui/components/text"
---

<objective>
Migrate all raw `<p>` elements across 16 apps/web component files to `<Text>` with appropriate variants. Covers three patterns: muted body-sm (D-06), error (D-07), caption, and mixed-class overrides (D-09).

Purpose: Eliminate raw paragraph elements from the component layer; all text uses the Text primitive.
Output: 16 modified files with `<p>` replaced by `<Text>` equivalents.
</objective>

<execution_context>
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tarnesty/Developer/personal/ploutizo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tarnesty/Developer/personal/ploutizo/.planning/PROJECT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-CONTEXT.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-RESEARCH.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-PATTERNS.md
@/Users/tarnesty/Developer/personal/ploutizo/.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-01-SUMMARY.md

<interfaces>
<!-- Text variant reference -->
```tsx
import { Text } from "@ploutizo/ui/components/text"
// Variants and their class maps:
// body-sm  → "text-sm leading-relaxed"
// caption  → "text-xs text-muted-foreground leading-normal"
// error    → "text-xs text-destructive leading-normal"  (added in Plan 01)
// Default as="p" when no as prop given
```

<!-- Pattern quick-reference -->
// D-06 muted body:    <Text variant="body-sm" className="text-muted-foreground">
// D-07 error xs:      <Text variant="error">
// caption xs muted:   <Text variant="caption">  (no className needed — variant includes text-muted-foreground)
// mixed body-sm:      <Text variant="body-sm" className="...extra classes...">
// text-sm destructive (not xs): <Text variant="body-sm" className="text-destructive">  (NOT error variant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate paragraphs in settings components (8 files)</name>
  <files>
    apps/web/src/components/settings/HouseholdSettingsForm.tsx
    apps/web/src/components/settings/MembersSection.tsx
    apps/web/src/components/settings/HouseholdSettings.tsx
    apps/web/src/components/settings/CategoryForm.tsx
    apps/web/src/components/settings/InviteMemberForm.tsx
    apps/web/src/components/settings/CategoriesSettings.tsx
    apps/web/src/components/settings/MerchantRulesSettings.tsx
    apps/web/src/components/settings/RuleForm.tsx
  </files>
  <read_first>
    - apps/web/src/components/settings/HouseholdSettingsForm.tsx
    - apps/web/src/components/settings/MembersSection.tsx
    - apps/web/src/components/settings/HouseholdSettings.tsx
    - apps/web/src/components/settings/CategoryForm.tsx
    - apps/web/src/components/settings/InviteMemberForm.tsx
    - apps/web/src/components/settings/CategoriesSettings.tsx
    - apps/web/src/components/settings/MerchantRulesSettings.tsx
    - apps/web/src/components/settings/RuleForm.tsx
  </read_first>
  <action>
    For each file: add `import { Text } from "@ploutizo/ui/components/text"` (or append Text to existing @ploutizo/ui import). Then apply replacements:

    **HouseholdSettingsForm.tsx:**
    ```tsx
    // Line 62 — error text (D-07):
    // Before: <p className="text-xs text-destructive">{...error message...}</p>
    // After:
    <Text variant="error">{...error message...}</Text>

    // Line 77 — error text with margin (D-07):
    // Before: <p className="text-xs text-destructive mt-2">{...error message...}</p>
    // After:
    <Text variant="error" className="mt-2">{...error message...}</Text>
    ```

    **MembersSection.tsx:**
    ```tsx
    // Lines 25–27 — muted body (D-06):
    // Before: <p className="text-sm text-muted-foreground">This household has no other members.</p>
    // After:
    <Text variant="body-sm" className="text-muted-foreground">This household has no other members.</Text>
    ```
    (The heading on line 16 is handled by Plan 02 — do NOT touch it here.)

    **HouseholdSettings.tsx:**
    ```tsx
    // Line 27 — caption:
    // Before: <p className="text-xs text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="caption">{...text...}</Text>
    ```

    **CategoryForm.tsx:**
    ```tsx
    // Line 122 — error text (D-07):
    // Before: <p className="text-xs text-destructive">{...error...}</p>
    // After:
    <Text variant="error">{...error...}</Text>
    ```

    **InviteMemberForm.tsx:**
    ```tsx
    // Line 74 — error text (D-07):
    // Before: <p className="text-xs text-destructive">{...error...}</p>
    // After:
    <Text variant="error">{...error...}</Text>
    ```

    **CategoriesSettings.tsx:**
    ```tsx
    // Line 115 — muted body (D-06):
    // Before: <p className="text-sm text-muted-foreground">{...description...}</p>
    // After:
    <Text variant="body-sm" className="text-muted-foreground">{...description...}</Text>

    // Lines 118–120 — caption:
    // Before: <p className="text-xs text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="caption">{...text...}</Text>
    ```
    (Headings in this file are handled by Plan 02 — do NOT touch them here.)

    **MerchantRulesSettings.tsx:**
    ```tsx
    // Line 46 — muted body (D-06):
    // Before: <p className="text-sm text-muted-foreground">{...description...}</p>
    // After:
    <Text variant="body-sm" className="text-muted-foreground">{...description...}</Text>

    // Line 50 — caption with font-medium:
    // Before: <p className="text-xs font-medium text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="caption" className="font-medium">{...text...}</Text>

    // Line 53 — caption:
    // Before: <p className="text-xs text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="caption">{...text...}</Text>
    ```

    **RuleForm.tsx:**
    ```tsx
    // Lines 217–222 — error text block (D-07). May be multiple error <p> elements or conditional:
    // Before: <p className="text-xs text-destructive">{...error...}</p>  (for each error paragraph)
    // After:
    <Text variant="error">{...error...}</Text>
    ```
    NOTE: The `<span>(optional)</span>` elements inside `<FieldLabel>` on lines 163 and 180 are EXEMPT per D-13 — do NOT change them.
  </action>
  <verify>
    <automated>grep -rn '^\s*<p ' /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/settings/ 2>/dev/null || echo "No raw paragraphs found"</automated>
  </verify>
  <acceptance_criteria>
    - HouseholdSettingsForm.tsx contains `variant="error"` (twice — lines 62 and 77 targets) and does NOT contain `<p className="text-xs text-destructive`
    - HouseholdSettingsForm.tsx line targeting mt-2 error: `<Text variant="error" className="mt-2">`
    - CategoryForm.tsx contains `variant="error"` and does NOT contain `<p className="text-xs text-destructive`
    - InviteMemberForm.tsx contains `variant="error"` and does NOT contain `<p className="text-xs text-destructive`
    - RuleForm.tsx contains `variant="error"` and does NOT contain `<p className="text-xs text-destructive`
    - RuleForm.tsx still contains the raw `<span>` elements on the FieldLabel optional suffix lines (D-13 exemption unchanged)
    - CategoriesSettings.tsx contains `variant="body-sm"` and `variant="caption"` for paragraph targets; no raw `<p` elements
    - MerchantRulesSettings.tsx contains no raw `<p` elements; has `variant="caption" className="font-medium"` entry
    - All 8 files import Text from "@ploutizo/ui/components/text"
  </acceptance_criteria>
  <done>8 settings component files have zero raw paragraph elements. Error paragraphs use variant="error". Muted descriptions use body-sm. Caption-sized muted text uses caption.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate paragraphs in dashboard, transactions, accounts, onboarding, categories (8 files)</name>
  <files>
    apps/web/src/components/dashboard/Dashboard.tsx
    apps/web/src/components/transactions/TransactionTableEmpty.tsx
    apps/web/src/components/transactions/TransactionTableEmptyFiltered.tsx
    apps/web/src/components/accounts/AccountForm.tsx
    apps/web/src/components/accounts/AccountsTable.tsx
    apps/web/src/components/onboarding/Onboarding.tsx
    apps/web/src/components/categories/LucideIconPicker.tsx
    apps/web/src/components/settings/MerchantRuleRow.tsx
  </files>
  <read_first>
    - apps/web/src/components/dashboard/Dashboard.tsx
    - apps/web/src/components/transactions/TransactionTableEmpty.tsx
    - apps/web/src/components/transactions/TransactionTableEmptyFiltered.tsx
    - apps/web/src/components/accounts/AccountForm.tsx
    - apps/web/src/components/accounts/AccountsTable.tsx
    - apps/web/src/components/onboarding/Onboarding.tsx
    - apps/web/src/components/categories/LucideIconPicker.tsx
    - apps/web/src/components/settings/MerchantRuleRow.tsx
  </read_first>
  <action>
    For each file: add `import { Text } from "@ploutizo/ui/components/text"` (or append Text to existing @ploutizo/ui import). Then apply replacements:

    **Dashboard.tsx:**
    ```tsx
    // Lines 7–9 — muted body (D-06):
    // Before: <p className="text-sm text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="body-sm" className="text-muted-foreground">{...text...}</Text>
    ```
    (The heading on line 4 is handled by Plan 02 — do NOT touch it here.)

    **TransactionTableEmpty.tsx:**
    ```tsx
    // Line 5 — label-ish body:
    // Before: <p className="text-sm font-medium">{...text...}</p>
    // After:
    <Text variant="body-sm" className="font-medium">{...text...}</Text>

    // Lines 6–8 — muted body (D-06):
    // Before: <p className="max-w-xs text-sm text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="body-sm" className="max-w-xs text-muted-foreground">{...text...}</Text>
    ```

    **TransactionTableEmptyFiltered.tsx:**
    ```tsx
    // Line 9 — label-ish body:
    // Before: <p className="text-sm font-medium">{...text...}</p>
    // After:
    <Text variant="body-sm" className="font-medium">{...text...}</Text>
    ```

    **AccountForm.tsx:**
    ```tsx
    // Lines 319–322 — muted body in shared section (D-06):
    // Before: <p className="text-sm text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="body-sm" className="text-muted-foreground">{...text...}</Text>

    // Line 389 — caption:
    // Before: <p className="text-xs text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="caption">{...text...}</Text>

    // Lines 407–412 — submit error (text-sm, NOT text-xs, so NOT error variant per RESEARCH.md):
    // Before: <p className="text-sm text-destructive">{...error...}</p>
    // After (body-sm + className override — NOT variant="error" which is text-xs only):
    <Text variant="body-sm" className="text-destructive">{...error...}</Text>
    ```
    NOTE: The `<span>(optional)</span>` elements inside `<FieldLabel>` on lines 263 and 285 are EXEMPT per D-13 — do NOT change them.
    NOTE: The `<span />` empty spacer on line 451 is handled by Plan 04 — do NOT touch it here.

    **AccountsTable.tsx:**
    ```tsx
    // Lines 148–149 — label-ish body:
    // Before: <p className="text-sm font-medium">{...text...}</p>
    // After:
    <Text variant="body-sm" className="font-medium">{...text...}</Text>

    // Lines 150–152 — muted body (D-06):
    // Before: <p className="max-w-xs text-sm text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="body-sm" className="max-w-xs text-muted-foreground">{...text...}</Text>
    ```

    **Onboarding.tsx:**
    ```tsx
    // Lines 11–13 — muted body (D-06):
    // Before: <p className="text-sm text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="body-sm" className="text-muted-foreground">{...text...}</Text>
    ```
    (The heading on line 8 is handled by Plan 02 — do NOT touch it here.)

    **LucideIconPicker.tsx:**
    ```tsx
    // Line 174 — caption with layout classes:
    // Before: <p className="py-2 text-center text-xs text-muted-foreground">{...text...}</p>
    // After (caption = text-xs text-muted-foreground; preserve layout via className):
    <Text variant="caption" className="py-2 text-center">{...text...}</Text>
    ```

    **MerchantRuleRow.tsx:**
    ```tsx
    // Line 51 — mixed classes (D-09):
    // Before: <p className="truncate font-mono text-sm">{...text...}</p>
    // After (body-sm = text-sm; preserve font-mono and truncate via className):
    <Text variant="body-sm" className="truncate font-mono">{...text...}</Text>

    // Line 53 — caption (conditional render):
    // Before: <p className="text-xs text-muted-foreground">{...text...}</p>
    // After:
    <Text variant="caption">{...text...}</Text>
    ```
    (The span on lines 48–50 is handled by Plan 04 — do NOT touch it here.)
  </action>
  <verify>
    <automated>grep -rn '^\s*<p ' /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/dashboard/ /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/TransactionTableEmpty.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/transactions/TransactionTableEmptyFiltered.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/accounts/AccountForm.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/accounts/AccountsTable.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/onboarding/ /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/categories/LucideIconPicker.tsx /Users/tarnesty/Developer/personal/ploutizo/apps/web/src/components/settings/MerchantRuleRow.tsx 2>/dev/null || echo "No raw paragraphs found"</automated>
  </verify>
  <acceptance_criteria>
    - AccountForm.tsx contains `variant="body-sm" className="text-destructive"` for the submit error (NOT `variant="error"`)
    - AccountForm.tsx does NOT contain `<p className="text-sm text-destructive`
    - AccountForm.tsx still contains the raw `<span>` elements on the FieldLabel optional suffix lines (D-13 exemption)
    - LucideIconPicker.tsx contains `variant="caption" className="py-2 text-center"`
    - MerchantRuleRow.tsx contains `variant="body-sm" className="truncate font-mono"` and `variant="caption"`; no raw `<p` elements
    - Dashboard.tsx, TransactionTableEmpty.tsx, TransactionTableEmptyFiltered.tsx, AccountsTable.tsx, Onboarding.tsx — no raw `<p` elements
    - All 8 files import Text from "@ploutizo/ui/components/text"
    - `pnpm --filter @ploutizo/web tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All 16 files (across both tasks) have zero raw paragraph elements. Error text uses correct variant. Mixed classes preserved via className prop. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| No new boundaries | Pure markup refactor — no auth, data, or input changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03.3.3-04 | Tampering | Paragraph markup | accept | Semantic output unchanged — Text renders a <p> by default; no content or behavior change |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn '^\s*<p ' apps/web/src/components/` — should return nothing (or only false positives inside strings/comments)
2. `pnpm --filter @ploutizo/web tsc --noEmit` exits 0
3. `grep -rn 'variant="error"' apps/web/src/components/` — should return entries in HouseholdSettingsForm, CategoryForm, InviteMemberForm, RuleForm
</verification>

<success_criteria>
- All 16 files have zero raw `<p>` elements
- Error pattern (text-xs text-destructive) uses `variant="error"` — NOT body-sm + className
- Submit error in AccountForm (text-sm text-destructive) correctly uses body-sm + className="text-destructive"
- Caption pattern uses `variant="caption"` without redundant className="text-muted-foreground"
- TypeScript clean
</success_criteria>

<output>
After completion, create `.planning/phases/03.3.3-ui-primitive-refactor-sweep/03.3.3-03-SUMMARY.md`
</output>
