---
phase: 03.3.3-ui-primitive-refactor-sweep
plan: "01"
subsystem: ui
tags: [react, typescript, tailwind, text, typography, accessibility, aria]

requires:
  - phase: 03.3.2-implement-text-typography-component-in-packages-ui
    provides: Text component with TextVariant union and variantClasses map in packages/ui

provides:
  - Text component error variant ("text-xs text-destructive leading-normal") in packages/ui
  - SwatchButton accessible radio primitive co-located with ColourPicker in apps/web
  - ColourPicker refactored to use SwatchButton (no inline raw button in map)

affects:
  - 03.3.3-02
  - 03.3.3-03
  - 03.3.3-04

tech-stack:
  added: []
  patterns:
    - "SwatchButton pattern: raw <button> wrapper for swatch-specific a11y (role=radio, aria-checked, aria-label) that is incompatible with shadcn Button base styles"
    - "error variant pattern: <Text variant='error'> for text-xs text-destructive error messages"

key-files:
  created:
    - apps/web/src/components/categories/SwatchButton.tsx
  modified:
    - packages/ui/src/components/text.tsx
    - apps/web/src/components/categories/ColourPicker.tsx

key-decisions:
  - "SwatchButton co-located in apps/web/src/components/categories/ (not packages/ui) — ColourPicker is the only consumer; lower friction than exporting from package"
  - "SwatchButton wraps raw <button>, not shadcn Button — swatch solid-color styling and role=radio are incompatible with Button base styles (D-15/D-16)"
  - "error variant mirrors caption but uses text-destructive instead of text-muted-foreground (D-08)"

patterns-established:
  - "error variant: use <Text variant='error'> for text-xs text-destructive error messages (requires packages/ui error variant)"
  - "swatch primitive: extract accessibility-critical button variants into named components rather than inline in map bodies"

requirements-completed:
  - D-07
  - D-08
  - D-15
  - D-16
  - D-17
  - D-18

duration: 2min
completed: "2026-04-17"
---

# Phase 03.3.3 Plan 01: Add error variant to Text + extract SwatchButton Summary

**Text `error` variant added to packages/ui and ColourPicker refactored to use an accessible `SwatchButton` primitive, unblocking all Wave 2 `variant="error"` usages**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-17T00:00:00Z
- **Completed:** 2026-04-17T00:02:00Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments

- Added `"error"` to `TextVariant` union and `error: "text-xs text-destructive leading-normal"` to `variantClasses` in `packages/ui/src/components/text.tsx`
- Created `SwatchButton.tsx` with `role="radio"`, `aria-checked`, `aria-label`, `type="button"` — wraps raw `<button>` (not shadcn Button, incompatible styling)
- Replaced inline `<button>` block in `ColourPicker`'s `SWATCHES.map()` with `<SwatchButton>` — zero raw `<button>` elements remain in the map body

## Task Commits

1. **Task 1: Add error variant to text.tsx** - `37eb062` (feat)
2. **Task 2: Create SwatchButton and update ColourPicker** - `c3048b4` (feat)

## Files Created/Modified

- `packages/ui/src/components/text.tsx` — Added `"error"` to TextVariant union and `error: "text-xs text-destructive leading-normal"` to variantClasses
- `apps/web/src/components/categories/SwatchButton.tsx` — New accessible swatch radio button primitive
- `apps/web/src/components/categories/ColourPicker.tsx` — Import and use SwatchButton; removed inline raw `<button>` from SWATCHES.map()

## Decisions Made

- SwatchButton placed in `apps/web/src/components/categories/` (co-located) rather than `packages/ui` — sole consumer is ColourPicker, no cross-app reuse anticipated
- Preserved all original accessibility attributes (`role="radio"`, `aria-checked`, `aria-label`, `type="button"`) verbatim in SwatchButton

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered

Pre-existing TypeScript errors in `pnpm --filter web exec tsc --noEmit` (reui path aliases, clsx resolution, RuleForm type) are unrelated to this plan's changes. Zero new errors introduced by SwatchButton or text.tsx edits — confirmed by grep for SwatchButton/ColourPicker in tsc output.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

- `variant="error"` is now a valid TextVariant — Wave 2 plans (03.3.3-02, 03.3.3-03, 03.3.3-04) can use `<Text variant="error">` without TypeScript errors
- SwatchButton is importable by ColourPicker — no further extraction needed for Wave 2
- Both changes are committed and on the phase branch

---
*Phase: 03.3.3-ui-primitive-refactor-sweep*
*Completed: 2026-04-17*
